# Build output (excluded per PLAN_EN.md §18.1 rule 1)
src-tauri/target/
src-tauri/gen/
node_modules/
dist/

# Vite / TS
.vite/
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# IDE
.vscode/
.idea/
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.local
.env.*.local
# Signing/notarisation credentials — never committed, never pushed
docs/ID.txt

# Local-only files
CLAUDE.md
.claude/

# Download metrics: the CSV is the record and is committed; the dashboard is
# regenerated from it on every run, so committing it would be daily churn.
docs/metrics/dashboard.html
docs/metrics/snapshot.log

# 形态 C sidecar 的产物（scripts/build-sidecar.sh 每次构建重新生成）
src-tauri/sidecar/target/
src-tauri/binaries/
