# 交接 —— 2026-08-19（v0.6.0 发布收口）

> **这份是当前的开工面。** 情人节支线已经并进 `main` 并作为 **v0.6.0 发布**，所以这份从
> 「情人节支线交接」改写成了「现在还欠什么」。
>
> - 做了什么、为什么这么做 → `docs/DESIGN_VALENTINE_EDITION.md`（设计与取证，含休息提醒两轮）
> - 更早那批主线欠账的原始上下文 → `docs/BACKLOG-2026-08-15.md`、`docs/HANDOFF-2026-08-15.md`
> - **还欠什么 → 就是这份的 §2。** 我把散在四份文档里的 ⏳ 逐条核过一遍，
>   **已经不成立的删掉了**，剩下的都在下面。

---

## 0. 现在的状态（2026-08-19 实测）

| 项 | 值 |
|---|---|
| `main` / `windows-port` | 同一个提交；v0.6.0 之后又叠了发布说明、台账、官网三笔 |
| tag | **`v0.6.0`** → `c8beb04` |
| 发布 | **已发布**，四个资产（两个带版本号 + 两个固定名），固定名 URL 实测 **200** |
| 公证 | `.app` `d3b2c6b8…` · `.dmg` `fa58021b…`，**两个都 Accepted 并 staple**；`spctl` = `Notarized Developer ID` |
| 版本号 | **0.6.0**，四处同步（`package.json` / `tauri.conf.json` / `Cargo.toml` / `Cargo.lock` 里 spool 那条） |
| 装机 | `/Applications/Spool.app` = **0.6.0**，发布证书，2026-08-19 12:28 换装 |
| 真库 | **12 工作区 / 39 项目 / 186 块**，`user_version` **23**，装前装后一字未变；**没跑过迁移** |
| 授权 | 启动日志 `[double-tap] installed at HID/active` —— **两个 TCC 授权都在**（今早那两条 NOT granted 已恢复） |
| 基线 | commits **473** · Vitest **456**（42 文件）· cargo **99** · tsc clean · i18n `(none missing)` · schema **v23** · MCP 工具面 **19** |
| 官网 | 已部署，含新增的「休息提醒」一段（中英文）+ Ocean 重拍的 S1 |
| 回滚 | app：`~/Desktop/Spool-old-20260819-pre060.app`；库：`~/Desktop/spool.before-060-20260819-122803.db`（integrity 核过） |

---

## 1. 本窗做完的（不用再看，只留索引）

- **休息提醒第二轮**：两个外观都有 / 设置里可关 / 30·60·120 / 到点整屏锁 5 分钟 /
  边栏表盘与捕捉数交替 / 设置里给出论文出处。设计与取证：`DESIGN_VALENTINE_EDITION.md`
  **§0.1、§5.2–5.4、§8.7**。
- **情人节支线并入 `main`**，v0.6.0 发布，官网加了一段休息提醒。
- **S1 重拍并入**（Ocean 自己拍的），顺手修掉 2 号放大框还钉在旧位置那个 bug。

---

## 2. ⏳ 还没做的 —— 全部在这里

### 2.1 只等 Ocean 动手，代做不了（**这一类是现在的大头**）

这几件都不是「没写代码」，是**没有人用真手指在真机上走过一遍**。我做不了：合成事件在
授权齐全的机器上驱动不了捕捉手势（见 `isolated-verify-workflow` §14），面板好不好看
也不是我能替他判断的。

| # | 要他做什么 | 判据写在哪 | 为什么还欠着 |
|---|---|---|---|
| 1 | **跑 v0.6.0 的休息提醒验收** | `DESIGN_VALENTINE_EDITION.md` **§8.7**（分 a/b/c/d 四段） | 这一版全新，**一条都还没打勾**。想快就先把时长调成 30 分钟 |
| 2 | **看一眼跟进面板长什么样** | `BACKLOG-2026-08-15.md` §3.2 #5 | 行清单、切标记、收起/重开、待审逐条卡——代码跑通了，**没有人看过它长什么样** |
| 3 | **跑捕捉焦点那三步** | `DESIGN_CAPTURE_FOCUS.md` **§7** | 装机之后一直没跑 |
| 4 | **真手指双击 ⌥ 复核新浮窗位置** | `HANDOFF-2026-08-15.md` §9.5 | 浮窗改成按 `work_area` 定位之后，只有截图证据，没有手指证据 |
| 5 | **在 ChatGPT 里跑 §7.1 那五句** | `DESIGN_MCP_INTENT_ROUTING.md` **§7.1** | 判据是 `mcp_tool_call` 事件里的工具名，不是模型说了什么。**六件里唯一没兑现的一格** |
| 6 | **验 Windows 的 0.6.0 安装包** | 包在 CI run `32213830941` | 他 08-19 说要验，还没回结果 |

### 2.2 可以直接做（已批，无阻塞）

1. **`AXFrontmost` 被拒那条路做实测**（`DESIGN_CAPTURE_FOCUS.md` §9.6）。
   `on_overlay_shown` 里补注册全局 ⌘Z 那一支**是照推理写的，没有实测证据**。
   要一个从没授过权的新 identifier（`.fr4` 起步，`.fr1`–`.fr3` 都用掉了）。
2. **多显示器 / 全屏 Space 下的 `frontmost_window_owner_pid()`**（同上）。
   按 layer 0 取最前窗口在这些情形下**应当**仍成立，但没量过。
3. **环境卫生**（都不影响功能，但会在下次排障时骗人）：
   - `~/Library/Application Support/com.oceanjin.spool/codex-marketplace/`（44K）——
     codex 插件那条路 08-15 就拆了，**没有任何东西指向这个目录**，可删。
   - `~/Desktop/Spool/Spool-Demo/Spool-old-040.app` —— 换成 0.6.0 演示包时挪开的旧版，可删。
   - `~/Desktop/Spool-old-20260819-pre060.app` —— 这是**当前唯一的 app 回滚路径**，
     等 Ocean 验完 0.6.0 再删。
   - ⚠️ **`~/Desktop/spool.before-060-20260819-122803.db` 不要删**，那是换装前的库备份。

### 2.3 要先决策（我不动手）

1. **Windows 代码签名** —— 首版故意不签（2026-08-15 决策 5）。买不买证书是独立一件事。
2. **官网要不要把论文写出来** —— 现在官网只写「默认 60 分钟，这个数字的来处设置里写着」，
   **故意没有引那篇 BJSM**。⚠️ 理由要说清楚：**那篇文献是 Ocean 给的，我核实不了它存不存在**
   （我的知识到 2026-05，查不到这一篇）。放进 app 里当引文是一回事；官网以产品自己的口径
   断言一个研究结论是另一回事。要写，说一声我加。
3. **演示环境要不要把 `spool-demo` 那个 MCP 条目加回 Claude Desktop** ——
   现在配置里只有 `spool`（真库）和 `spool_lab`（实验库），所以 `mcp-*.png` 那三张**拍不了**
   （直接问会读到真库）。备份 `claude_desktop_config.json.bak-demo` 还在。
4. **面板交替要不要兜底定时器** —— 现在**只在窗口失焦那一刻换**，所以一小时没切过窗口的人
   会一直看到捕捉数。这是 Ocean「不能让用户看到 ui 切换的过程」那条的直接后果：
   加兜底就等于加一条会当面换的路。要换取舍，说一声。

### 2.4 已关闭，**不要再当欠账**

- **S1 重拍** —— 2026-08-19 做完（`BACKLOG-2026-08-15.md` §5.4 #1）。
- **119 处死类名** —— 已修并合并。
- **打包成文件夹 / 库导出导入 / 工作区嵌套** —— 早就提交了。老文档里那些「⏳ 未提交」
  **全是过期标记**，别照着再做一遍。
- **免费引擎、M4 定时调度器、演示视频、案例研究四至六期、LICENSE** ——
  见 `BACKLOG-2026-08-15.md` §3.4。

---

## 3. 不要重做 / 不要推翻的

**外观与主题**

- **经典版 = 发布版本身，不是「长得像旧版的主题」。** `tokens.css` 的裸 `:root` 不许动，
  情人节的一切都在 `[data-theme='valentine']` 里，默认 `classic`。
- **`--font-ui` 两个主题都是 Geist**（Ocean：「正常文字不变」）。只有标题字体换。
- **`--accent` 不是 `#e66d98`**，是它压暗降饱和的版本——原色对纸只有 2.84 : 1，撑不住小字。
  爱心用的才是原色。别「修正」回去。
- **搁置/紧急两个状态色故意没改**，它们必须和玫瑰色的 accent 一眼分得开。
- **Gwen 彩蛋故意不持久化**，关掉重开变回 Spool，理由在 `Wordmark.tsx`。
  ⚠️ **Gwen 仍然只在情人节版** —— 被推翻的是休息提醒那半条，不是这半条。

**休息提醒**

- **休息 5 分钟是写死的，工作时长（30/60/120）才是设置。** 论文三组的休息都是 5 分钟：
  5 是剂量，间隔才是排班。别把它也做成选项。
- **边栏面板只在窗口失焦那一刻换面**，且 ≥30 秒一次。不要加当面换的兜底（见 §2.3 #4）。
- **锁屏一上来就把 DOM 焦点收走。** 不收的话人会继续往看不见的笔记里打字——
  这不是防御性代码，是这个交互本身要求的。

**发布与装机**

- **换装前先看现装的是什么签名**（`codesign -dvv /Applications/Spool.app | grep Authority`），
  看到什么就用什么。本机是**发布证书**，所以要
  `APPLE_SIGNING_IDENTITY="Developer ID Application: Hanze JIN (Q5Y5JRXZ58)"`，
  **且不改 `tauri.conf.json`**。装自签的那份会把 TCC 授权弄没。
- **`--mcp` 子进程一个都不要杀**（本机常年二十来个，别的 AI 客户端起的）。
  停 GUI **按 pid 停**，绝不用模糊 `pkill -f spool`。
- ⚠️ **公证凭据要两样都有**：钥匙串里的 `spool-notary` profile 只够 `xcrun notarytool` 那步用；
  **Tauri 2.11 读不了钥匙串**，它只认 `APPLE_ID` / `APPLE_PASSWORD` / `APPLE_TEAM_ID`
  （或 API key 三件套）。`~/.spool-release-env` 留着别删。
- **构建日志要重定向到文件，不要接 `| tail`** —— `.app` 的公证 id 只在那一行 inline 输出里
  出现一次，接管道还会把退出码一起骗了。

**官网**

- ⚠️ **换了截图，压在图上面的东西要重新量**（`.zoom-box-*` 是图片高度的百分比）。
  框歪了页面看着还挺像回事，这才是容易发出去的原因。
- ⚠️ **换了截图，文件名没变，回头客看到的是旧图**（GitHub Pages `max-age=600`）。
  报「没生效」之前，先用 cache-buster 验一次服务器上到底是什么。
- 这两条都写进 `scripts/build-site-shots.sh` 的头注释了。

---

## 4. 其它文档现在怎么用

| 文档 | 还看它做什么 |
|---|---|
| `DESIGN_VALENTINE_EDITION.md` | 外观 + 休息提醒的**全部设计与取证**，验收清单在 §8 |
| `BACKLOG-2026-08-15.md` | 主线欠账的**原始上下文**。⚠️ 里面的 ⏳ 有过期的，**以本文档 §2 为准** |
| `HANDOFF-2026-08-15.md` | 08-15 至 08-18 那几窗的落地记录（拖放、打包、库迁移、发布） |
| `HANDOFF.md` | 更早的长历史。**不是开工面**，当档案查 |
| `RELEASE.md` | 发布手册。§2 的步骤这次一步没跳，可信 |
| `CASE_STUDY_LEDGER.md` | 每次发布追一行；v0.6.0 那行**两个公证回执都在** |
| `isolated-verify-workflow`（记忆，不在仓库里） | 实机验证 / 截图 / 换装规程，§31–32 是本窗新加的 |
