# 交接 —— 2026-08-19（v0.6.0 发布收口）

> **这份是当前的开工面。** 情人节支线已经并进 `main` 并作为 **v0.6.0 发布**，所以这份从
> 「情人节支线交接」改写成了「现在还欠什么」。
>
> ✅ **2026-08-19 傍晚：§1-bis 那一批已提交，并作为 v0.6.1 发布。**
> §2.1 六件 Ocean 亲手跑完（全部通过）；§2.2 的四件里 **1、2、4 做完，3 被权限挡住**。
> **下一窗的开工面是 §2.5。**
>
> - 做了什么、为什么这么做 → `docs/DESIGN_VALENTINE_EDITION.md`（设计与取证，含休息提醒两轮）
> - 更早那批主线欠账的原始上下文 → `docs/BACKLOG-2026-08-15.md`、`docs/HANDOFF-2026-08-15.md`
> - **还欠什么 → 就是这份的 §2。** 我把散在四份文档里的 ⏳ 逐条核过一遍，
>   **已经不成立的删掉了**，剩下的都在下面。

---

## 0. 现在的状态（2026-08-19 实测）

| 项 | 值 |
|---|---|
| `main` / `windows-port` | 同一个提交；v0.6.0 之后又叠了发布说明、台账、官网三笔 |
| tag | **`v0.6.1`** → `9832527`（v0.6.0 → `c8beb04` 仍在） |
| 发布 | ✅ **v0.6.1 已发布**，四个资产，两个固定名 URL 实测 **200**，`/latest` 解析到 v0.6.1。**把 dmg 从发布页下载回来复核过**：sha256 一致、`spctl` = `Notarized Developer ID`、`stapler validate` 通过 |
| 公证 | v0.6.1：`.app` `ada26185…` · `.dmg` `72eda901…`，**两个都 Accepted 并 staple**（回执在 `CASE_STUDY_LEDGER.md` §1.2） |
| 版本号 | **0.6.1**，四处同步。⭐ **Ocean 拍板 0.6.1**（上一窗建议 0.7.0，他选了这个） |
| 装机 | ⚠️⚠️ **`/Applications/Spool.app` 仍是 14:39 那个 dev build，版本号写 0.6.0，`spctl` 报 `rejected / Unnotarized Developer ID`。发布出去的 0.6.1 没有装到本机。** 这意味着**文件权限那条修复在 Ocean 自己机器上还没生效** —— 那份 PDF 现在仍然读得到（`DESIGN_PROJECT_FILES.md` §9.3）。要装就按记忆 `isolated-verify-workflow` §21 走：先备份真库、`codesign -dvvv` 看现装的签名（本机是**发布证书**）、`ditto` 新版、起来后按行数核对真库没变 |
| 真库 | **12 工作区 / 39 项目 / 189 块**，`user_version` **23**（下午两次换装前后逐表核过，一字未变；**没跑过迁移**）。⚠️ 189 是 Ocean 自己删掉 4 条 Codex 测试块之后的数，不是事故 |
| 授权 | 启动日志 `[double-tap] installed at HID/active` —— **两个 TCC 授权都在**（今早那两条 NOT granted 已恢复） |
| 基线 | commits **479** · Vitest **465**（42 文件）· cargo **99** · tsc clean · i18n `(none missing)` · schema **v23**（没动）· MCP 工具面 **19**。⭐ 发布前这四项全部重跑过一遍，是绿的 |
| 官网 | 已部署，含新增的「休息提醒」一段（中英文）+ Ocean 重拍的 S1 |
| 回滚 | app：**`~/Desktop/Spool-0.6.0-release-20260819-143814.app`（0.6.0 真发布版）**、`~/Desktop/Spool-old-20260819-pre060.app`（0.5.x，0.6 已验完，可删）；0.6.1 直接从发布页下载即可。库：`~/Desktop/spool.before-devbuild-20260819-143814.db`（integrity ok / v23 / 193 块）、`~/Desktop/spool.before-060-20260819-122803.db` ⛔ 不要删 |

---

## 1. 本窗做完的（不用再看，只留索引）

- **休息提醒第二轮**：两个外观都有 / 设置里可关 / 30·60·120 / 到点整屏锁 5 分钟 /
  边栏表盘与捕捉数交替 / 设置里给出论文出处。设计与取证：`DESIGN_VALENTINE_EDITION.md`
  **§0.1、§5.2–5.4、§8.7**。
- **情人节支线并入 `main`**，v0.6.0 发布，官网加了一段休息提醒。
- **S1 重拍并入**（Ocean 自己拍的），顺手修掉 2 号放大框还钉在旧位置那个 bug。

---

## 1-bis. 2026-08-19 下午这一批（✅ 已提交，已作为 **v0.6.1** 发布）

动了 **36 个文件**（33 改 / 2 新增：`CorrectionNote.tsx`、`selectionRange.ts` / 1 删除：`SupersedePicker.tsx`）。
提交在 `7fca856`（四件一起提，因为它们共用 `selectionRange.ts`，拆开会造出编译不过的中间提交）。全部通过 tsc、Vitest **465**、cargo **99**、i18n `(none missing)`、`vite build`，并装了 dev build 在真库上实测过。

| 做了什么 | 全稿 | 为什么值得单独看一眼 |
|---|---|---|
| **文件权限收紧成一把锁** | `DESIGN_PROJECT_FILES.md` **§9** | ⚠️ **行为变更**。以前「打包时带上正文」隐式等于给 AI 读，界面那句「AI 不能读这个文件」是假的。真库现场复现并修复后复验过 |
| **更正改成批注式**（贴在被更正的句子底下） | `DESIGN_CONTEXT_HYGIENE.md` **§10** | 时间线不再给更正单独一张卡；折叠**只在读者够得着时**发生 |
| **删掉「它更正了哪一条？」** | 同上 **§10.7** | ⚠️ 用户这侧唯一写 `supersedes` 的入口没了；读的那侧全留着 |
| **标为重点修好跨行/跨格式** | 同上 **§10.8** | **两个病根**，只修一个照样坏；顺带消掉两条旧「已知限制」 |
| **面板兜底定时器 10 分钟** | §2.3 #4 | 会当面换，是 Ocean 知情换来的 |
| **官网写上 BJSM** | §2.3 #2 | 那篇文献本窗核实了，是真的 |

⭐ **本窗新增的两条通用教训，写在记忆 `isolated-verify-workflow` §33–34**：
dev build 与发布版**只有 `spctl` 的 Notarized/Unnotarized 分得出来**；
换装后**自己那条 MCP 连接跑的还是旧二进制**，要验新行为得另起探针进程。

---

## 2. ⏳ 还没做的 —— 全部在这里

### 2.1 ✅ 全部关闭 —— **2026-08-19 Ocean 亲手跑完六件，全部通过**

这一类曾经是欠账的大头（都不是「没写代码」，是没有人用真手指在真机上走过一遍）。
**2026-08-19 Ocean 报「全部通过，没发现问题」，六件一次清完，不要再当欠账列出来。**

| # | 跑的是什么 | 判据 | 结果 |
|---|---|---|---|
| 1 | v0.6.0 休息提醒验收（a/b/c/d 四段） | `DESIGN_VALENTINE_EDITION.md` §8.7 | ✅ 通过 |
| 2 | 跟进面板长什么样 | `BACKLOG-2026-08-15.md` §3.2 #5 | ✅ 通过 |
| 3 | 捕捉焦点那三步 | `DESIGN_CAPTURE_FOCUS.md` §7 | ✅ 通过 |
| 4 | 真手指双击 ⌥ 复核新浮窗位置 | `HANDOFF-2026-08-15.md` §9.5 | ✅ 通过（补上了手指证据，此前只有截图） |
| 5 | 在 Codex 里跑 §7.1 那五句 | `DESIGN_MCP_INTENT_ROUTING.md` §7.1 / §9 | ✅ 13:21–13:27 跑完，逐句见 §9（2/3/5 干净过；4 过但先猜错一次参数；1 本机测不出） |
| 6 | Windows 的 0.6.0 安装包 | CI run `32213830941` | ✅ 通过 |

⚠️ **只有第 5 件留了机读取证**（`~/.codex/sessions/2026/08/19/rollout-*.jsonl` 里的
`custom_tool_call` 事件）。其余五件是 Ocean 口头回的结论，**没有截图或日志留档** ——
下次这几处若又可疑，得重跑，不能引用这一行当证据。

### 2.2 ✅ 四件里做完三件（2026-08-19 傍晚收口）

| # | 那时要做什么 | 结果 |
|---|---|---|
| 1 | `AXFrontmost` 被拒那条路做实测 | ✅ **量到了**，见下 |
| 2 | 多显示器 / 全屏 Space 下的 `frontmost_window_owner_pid()` | ⚠️ **全屏 Space 量到了；多显示器还差**，见下 |
| 3 | 环境卫生（删两样） | ⛔ **被权限闸门挡住，没删**，见 §2.5 #1 |
| 4 | 发布 | ✅ **v0.6.1 已发布**（§0 那张表就是它的收口状态） |

#### 1–2 ⭐ 全稿在 `DESIGN_CAPTURE_FOCUS.md` **§9.7**

⚠️ **§9.6 写的「两件都要真手指」是判断错了** —— 要真手指的是**捕捉手势**，
而这两件问的其实是**几个系统调用在特定环境下返回什么**。两件都用探针量到了，
**没装 Spool、没动 `/Applications`、没碰真身的 TCC、没烧掉 `.fr4`**。
配方写在记忆 `isolated-verify-workflow` **§35–36**。

⭐⭐ **量出来最要紧的一条，和「照推理写」时的想象相反：**
**没有授权时 `ax_source_app_pid()` 并不是 None** —— 窗口服务器那一路不需要授权，
照样答得出。于是 `will_activate` 仍是 true ⇒ `show_capture_overlay` **跳过**全局 ⌘Z
⇒ `ax_set_frontmost` 被拒（实测 AXError **-25211** `kAXErrorAPIDisabled`）
⇒ **`on_overlay_shown` 里补注册那一支是唯一的补救路径。**
**它不是防御性代码，是 08-15 加了窗口服务器兜底之后才变成必经之路的。谁要删它先看 §9.7。**

⏳ **还差的两小截**（都不是「没写」，是没量到）：
- **多显示器** —— 本机 `CGGetActiveDisplayList` = **1 块屏**，没有第二块可插。
  这半件缺的是**硬件**，不是手指。判据写在 §9.7 末尾。
- `register_undo_shortcut()` 在那个上下文里**注册成功**这一步。纯 Tauri 全局快捷键，
  和 TCC 无关，失败它自己会打日志，所以不会是静默的。

### 2.3 ✅ 四件都拍板了（2026-08-19，Ocean）

1. **Windows 代码签名 —— 不签，这条不要再问。** 首版故意不签（2026-08-15 决策 5），
   现在从「待决策」挪成**已决**。买证书是以后另起一件事，别再当欠账列出来。
2. **官网写论文 —— 「查证事实信息，只要是正确权威的就写进官网」。**
   ✅ **本窗核实了，那篇是真的**：Diaz KM 等，*Evaluating movement breaks as a public health
   strategy to mitigate the harms of prolonged sitting: a large-scale pragmatic intervention*，
   **BJSM，2026-06-23 上线，DOI `10.1136/bjsports-2025-111221`**；19,342 人入组、11,484 人完成，
   30 / 60 / 120 三组、休息都是 5 分钟；结论正是 60 分钟那组平衡最好，30 分钟组对疲劳/情绪最有效
   但可行性最差。**app 里那段文案和论文对得上，一个字不用改。**
   已写进 `site/index.html` 与 `site/zh/index.html` 的 `#breaks`（新增一段 `break-p3`，带 DOI 链接）。
   ⚠️ `DESIGN_VALENTINE_EDITION.md` §5 那条「我核实不了它存不存在」**已作废**，见该处。
3. **演示环境 —— 把 `spool-demo` 加回 Claude Desktop。**
   ⚠️ 改完必须 ⌘Q 完全退出 Claude Desktop 再开，它不会重连。
4. **面板交替兜底定时器 —— 要，时间长一点。** ✅ 已落地：`src/hooks/usePanelFace.ts` 新增
   `FACE_FALLBACK_MS = 10 分钟`（30 秒一跳去问一次），失焦那条路一个字没动。
   **这条路会当面换 —— 那就是这次换来的代价**，嫌它勤就改这一个常数。

### 2.3-bis ✅ 已拍板并落地（2026-08-19）

1. **「AI 不能读这个文件」那句假承诺 —— 选了甲：关掉 AI 勾就一律不可读。**
   `mcp.rs` 六处 + `get_pack` 的附件内联，全部改成只认 `ai_access`；
   `include_in_pack` 退回它字面的意思（**用户自己复制的那份 pack** 才内联，`assemble.ts` 没动）。
   全稿在 `DESIGN_PROJECT_FILES.md` **§9**。
   ⚠️ **要重新构建装机才生效** —— `/Applications/Spool.app` 里还是旧逻辑，
   那份 PDF 现在仍然读得到。
2. **更正的展示 —— 选了甲：仍是块，时间线上折叠成附注。**
   被更正的那句话高亮（v21 本来就有），**点它出现那一句的更正**，虚线连着；
   划词工具条多一个 ✎ 人工更正入口；AI 靠 `source` 标签区分，人工写「你写的」。
   全稿在 `DESIGN_CONTEXT_HYGIENE.md` **§10**。
   ⚠️ **折叠只发生在读者够得着的时候** —— 引文定位不到、或目标是合并块，就保留原来的卡片。
3. **顺手修掉一个真 bug**：界面上「取消更正关系」以前只清 ref、把 `corrected_quote` 留在原地，
   造出 `add_block` 自己会拒绝的行。⚠️ **真库里〈申请规划〉#26 就是这么来的一条，
   历史数据我没动。**

### 2.4 已关闭，**不要再当欠账**

- **S1 重拍** —— 2026-08-19 做完（`BACKLOG-2026-08-15.md` §5.4 #1）。
- **119 处死类名** —— 已修并合并。
- **打包成文件夹 / 库导出导入 / 工作区嵌套** —— 早就提交了。老文档里那些「⏳ 未提交」
  **全是过期标记**，别照着再做一遍。
- **免费引擎、M4 定时调度器、演示视频、案例研究四至六期、LICENSE** ——
  见 `BACKLOG-2026-08-15.md` §3.4。

---

### 2.5 ⭐ 下一窗的开工面

**1. 环境卫生那两样还没删（本窗被挡住了）**

两样都**复核过确实可删**（`codex-marketplace` 仓库里代码零引用，只有交接文档提到；
`Spool-old-040.app` 实测 `CFBundleShortVersionString` = **0.4.0**，18M）。
本窗想把它们挪进废纸篓，**被权限闸门拦下了**，没有绕。Ocean 自己跑这两条即可：

```bash
mv ~/Library/Application\ Support/com.oceanjin.spool/codex-marketplace ~/.Trash/codex-marketplace-20260819
mv ~/Desktop/Spool/Spool-Demo/Spool-old-040.app ~/.Trash/Spool-old-040-20260819.app
```

⚠️ 桌面上另外那几样**照旧文档删会删错**，见下表（08-19 复核过的版本）。

| 桌面上的 | 实际是什么 | 怎么处置 |
|---|---|---|
| `Spool-0.6.0-release-20260819-143814.app` | 0.6.0，`Notarized Developer ID` | 留着（0.6.1 已发布，要回 0.6.0 才用它） |
| `Spool-old-20260819-pre060.app` | **0.5.0**，Unnotarized | 0.6 已验完 → **可删** |
| `spool.before-devbuild-20260819-143814.db` | 换 dev build 前的库 | 留着 |
| `spool.before-060-20260819-122803.db` | 换 0.6.0 前的库 | ⛔ **不要删** |

**2. ⚠️ 把 0.6.1 装到本机（这件有实际后果，不只是卫生）**

`/Applications/Spool.app` 仍是 dev build（`spctl` 报 `rejected / Unnotarized`）。
**文件权限那条修复因此在 Ocean 自己机器上还没生效** —— 他一开始撞见病的那份 PDF
现在**仍然读得到**。装机规程见记忆 `isolated-verify-workflow` §21，
⚠️ 本机是**发布证书**，别用自签盖（会把 TCC 授权弄没）。
⚠️ 换装后**每个 MCP 客户端都要 ⌘Q 完全退出重开**才吃到新二进制（同 §34）。

**3. 多显示器那半件**（§2.2 上面那条）—— 插上第二块屏就能量，判据在
`DESIGN_CAPTURE_FOCUS.md` §9.7 末尾。

**4. v0.6.1 本身还没有人用真手指验过。**
这一批改的是**更正**和**标为重点**两个划词交互，以及**文件权限**。
自动化只能证明它们逻辑对，证明不了好不好用 —— 建议照 §2.1 那种跑法过一遍：
划词跨行标重点、划词写更正、点被更正的句子看批注展开、把某个文件的 AI 勾关掉再问 AI。

## 3. 不要重做 / 不要推翻的

**外观与主题**

- **经典版 = 发布版本身，不是「长得像旧版的主题」。** `tokens.css` 的裸 `:root` 不许动，
  情人节的一切都在 `[data-theme='valentine']` 里，默认 `classic`。
- **`--font-ui` 两个主题都是 Geist**（Ocean：「正常文字不变」）。只有标题字体换。
- **`--accent` 不是 `#e66d98`**，是它压暗降饱和的版本——原色对纸只有 2.84 : 1，撑不住小字。
  爱心用的才是原色。别「修正」回去。
- **搁置/紧急两个状态色故意没改**，它们必须和玫瑰色的 accent 一眼分得开。
- **Gwen 彩蛋故意不持久化**，关掉重开变回 Spool，理由在 `Wordmark.tsx`。
  ⚠️ **Gwen 仍然只在情人节版** —— 被推翻的是休息提醒那半条，不是这半条。

**休息提醒**

- **休息 5 分钟是写死的，工作时长（30/60/120）才是设置。** 论文三组的休息都是 5 分钟：
  5 是剂量，间隔才是排班。别把它也做成选项。
- **边栏面板主要在窗口失焦那一刻换面**，且 ≥30 秒一次。
  ⚠️ **「不要加当面换的兜底」这半条 2026-08-19 被 Ocean 推翻了**（§2.3 #4：「要，时间长一点」）——
  现在多一条 10 分钟的兜底，**它会当面换**，这是他知情换来的。失焦那条路本身没动。
- **锁屏一上来就把 DOM 焦点收走。** 不收的话人会继续往看不见的笔记里打字——
  这不是防御性代码，是这个交互本身要求的。

**更正与高亮（2026-08-19 下午这批，最容易被下一窗「修回去」）**

- ⚠️ **文件可读只认 `ai_access` 一把锁。** `include_in_pack` 退回它字面的意思：
  内联进**用户自己复制的那份 pack**。看到 `mcp.rs` 里没有 `|| include_in_pack`
  **不要「补回去」** —— 那正是 08-19 修掉的那句谎话（`DESIGN_PROJECT_FILES.md` §9）。
  ⚠️ 两个 pack 渲染器在这一点上**故意不一致**：golden 对照的是渲染函数（没动），
  分歧在喂给它的那一行数据。
- ⚠️ **`HIGHLIGHT_RE` 用 `[\s\S]` 不是 `.`。** 改回 `.` 会让跨行高亮存得进、读不回，
  屏幕上露出 `==`。这是「只能划一行」的病根之一。
- ⚠️ **选区一律走 `data-o` 映射，不许再用 `content.indexOf(选中的字)` 定位。**
  渲染器会吃掉 `**` / `==` / `## ` / 列表 `-`，字符串搜索对跨标记的选区必然失败。
  更正和高亮共用这一套（`selectionRange.ts`），**别给其中一个开小灶**。
- ⚠️ **更正折叠只在「读者够得着」时发生**：引文能定位、且目标不是合并块。
  放宽这个条件 = 造出一条进了 pack、屏幕上却看不见的块。
- **合并块（`SegmentedContent`）也要标偏移。** 它不走 Markdown 渲染器，
  漏了它就等于在那类块上把划词整个关掉。

**发布与装机**

- **换装前先看现装的是什么签名**（`codesign -dvv /Applications/Spool.app | grep Authority`），
  看到什么就用什么。本机是**发布证书**，所以要
  `APPLE_SIGNING_IDENTITY="Developer ID Application: Hanze JIN (Q5Y5JRXZ58)"`，
  **且不改 `tauri.conf.json`**。装自签的那份会把 TCC 授权弄没。
- **`--mcp` 子进程一个都不要杀**（本机常年二十来个，别的 AI 客户端起的）。
  停 GUI **按 pid 停**，绝不用模糊 `pkill -f spool`。
- ⚠️ **公证凭据要两样都有**：钥匙串里的 `spool-notary` profile 只够 `xcrun notarytool` 那步用；
  **Tauri 2.11 读不了钥匙串**，它只认 `APPLE_ID` / `APPLE_PASSWORD` / `APPLE_TEAM_ID`
  （或 API key 三件套）。`~/.spool-release-env` 留着别删。
- **构建日志要重定向到文件，不要接 `| tail`** —— `.app` 的公证 id 只在那一行 inline 输出里
  出现一次，接管道还会把退出码一起骗了。

**官网**

- ⚠️ **换了截图，压在图上面的东西要重新量**（`.zoom-box-*` 是图片高度的百分比）。
  框歪了页面看着还挺像回事，这才是容易发出去的原因。
- ⚠️ **换了截图，文件名没变，回头客看到的是旧图**（GitHub Pages `max-age=600`）。
  报「没生效」之前，先用 cache-buster 验一次服务器上到底是什么。
- 这两条都写进 `scripts/build-site-shots.sh` 的头注释了。

---

## 4. 其它文档现在怎么用

| 文档 | 还看它做什么 |
|---|---|
| `DESIGN_VALENTINE_EDITION.md` | 外观 + 休息提醒的**全部设计与取证**，验收清单在 §8；§5 那条「核实不了那篇论文」**已作废** |
| `DESIGN_PROJECT_FILES.md` | **§9 是 08-19 下午新加的**：文件权限为什么从两把锁收成一把 |
| `DESIGN_CONTEXT_HYGIENE.md` | **§10 是 08-19 下午新加的**：更正的批注式改版 + 标为重点的两个病根 |
| `DESIGN_CAPTURE_FOCUS.md` | **§9.7 是 08-19 傍晚新加的**：§9.6 那两件的实测（含未授权探针怎么造、全屏 Space 下的窗口表长什么样）。⚠️ §9.6 那句「两件都要真手指」**已被 §9.7 推翻** |
| `DESIGN_MCP_INTENT_ROUTING.md` | **§10 是本窗新加的**：§7.1 五句的逐句实测结果 |
| `BACKLOG-2026-08-15.md` | 主线欠账的**原始上下文**。⚠️ 里面的 ⏳ 有过期的，**以本文档 §2 为准** |
| `HANDOFF-2026-08-15.md` | 08-15 至 08-18 那几窗的落地记录（拖放、打包、库迁移、发布） |
| `HANDOFF.md` | 更早的长历史。**不是开工面**，当档案查 |
| `RELEASE.md` | 发布手册。§2 的步骤这次一步没跳，可信 |
| `CASE_STUDY_LEDGER.md` | 每次发布追一行；**v0.6.1 那行两个公证回执都在**，并且如实写了 Windows 产物编自哪个 sha |
| `isolated-verify-workflow`（记忆，不在仓库里） | 实机验证 / 截图 / 换装规程。§31–32 上午加的；§33–34 下午加的；⭐ **§35–36 傍晚加的**：不必造隔离 Spool 构建就能量「未授权时会怎样」（60 行探针 app + 自己的 TCC 身份），以及全屏 Space 怎么用代码造出来 |
