# 下一窗交接（2026-08-24 傍晚重写；夜里又补了 §8）

> **一句话**：**v0.6.2 发出去了**，B 批整批空了。
> 🆕 **2026-08-25：`S` 批 + `V` 批做完，傍晚 Ocean 用出六条（§10），
> 夜里读真库 Flux 又提五条（§11）—— 十一条全修完、全提交、已经换装。**
> **下一窗第一件事：照 §10.2 和 §11.2 逐条真手指验**（⛔ 十一条一条都没上手验过），
> 验完做 `Q 批`（引用理由 / 补摘要工具 / 摘要页签，存法已拍：加列不开新表），再轮到渠道。
> ⭐ **开工提示词照抄 §12**，⛔ 不是 §9.9 那一份（过期了）。
> ⚠️ **但夜里又插进来一批 `S`，排在渠道前面** —— Ocean 打开真库说「我看不懂现在的
> 替代关系和修正关系」，当场查出三代选校名单同时活着，还有一条更正被当天的压缩打断了。
> **下一件是 `S 批`，不是渠道。全部细节在 §8。**

---

## 1. 发布回执（⛔ 别再重发，也别重打包）

| 项 | 值 |
|---|---|
| tag | `v0.6.2` → `58ec01f`（已推，`origin/main` 已同步） |
| 产物 | `Spool_0.6.2_aarch64.dmg`，**10,390,759 字节** |
| sha256 | `fc74abee8f3c1084d70a885ccdd79520cb200a266b349536c3ad3fb12d992899` |
| 签名 | `Developer ID Application: Hanze JIN (Q5Y5JRXZ58)`，hardened runtime，带时间戳 |
| 公证 | **两个产物都 `accepted` · `source=Notarized Developer ID`** |
| `.dmg` submission | `64c13142-db18-43fc-b208-0a2df177d309` · Accepted |
| 固定名 URL | `releases/latest/download/Spool-macOS-arm64.dmg` 实测 **200**，**下载回来那一份 sha256 相同、staple 还在** |
| 库版本 | **v23 → v24**（`blocks` 加两个可空列，加法式迁移） |
| Windows | ⛔ **这一版没发**（Ocean 08-24 明说只发 macOS） |

全稿在 `CASE_STUDY_LEDGER.md` §1.2，发布说明在 `docs/RELEASE_NOTES_v0.6.2.md`。

---

## 2. ⛔ 发布留下的三件尾巴（都很小，但别忘）

| # | 是什么 | 怎么做 |
|---|---|---|
| **①** | **`.app` 那次的 submission id 没截住** —— 打包跑在终端里，滚屏没留。台账 §1.2 里那一格现在是**标记，不是数字** | `xcrun notarytool history --apple-id … --password … --team-id …`，Apple 还留着就能捞回来。⚠️ 捞到直接填进 §1.2，别编 |
| **②** | **`RELEASE.md` §3 那张验收单没走完** —— 「全新机器装 dmg 首启建库」「双击 ⌥ 授权」那几条 | ⛔ 只有真手指做得了。⚠️ 已经发出去了，所以这是**发后复核**，撞到问题就是 0.6.3 |
| **③** | ✅ **已经换装了**（2026-08-24 16:12，见 §2-bis） | —— |

---

## 2-bis. ✅ `/Applications` 已经换成 v0.6.2 正式版（2026-08-24 16:12）

⚠️ **上一版交接里「装的是 `Spool Dev` 签的 dev build」那句是错的** —— 实测
`codesign -dvv /Applications/Spool.app` 报的一直是 `Developer ID`。**签名身份从头到尾没变**，
所以这次换装 **TCC 授权一条都没掉**（启动 stderr 打的是
`[double-tap] installed at HID/active`，按 §6-bis 这一行就等于两个授权都在）。

| 核了什么 | 结果 |
|---|---|
| identifier | `com.oceanjin.spool`（⛔ 不是 `.verify`，§21 ① 那个坑） |
| 版本 | `0.6.2` |
| 签名 | `Developer ID Application: Hanze JIN (Q5Y5JRXZ58)`，`--verify --deep --strict` 通过 |
| `spctl` | ⭐ **`source=Notarized Developer ID`** —— 这是 §33 里唯一分得出「dev build / 真发布版」的那一条，现在是发布版 |
| **真库** | ⭐ **一个字没动**：`user_version 24`、`integrity_check ok`、五张表逐项一致（21/52/250/1/11）、**250 块正文哈希相同**（`7ddb5b19d0609ed1`）。⛔ 没有新的迁移快照 —— v24→v24 本来就不该跑迁移 |
| 新二进制 | 探针进程验过（§34）：`serverInfo` 报 `0.6.2`，**20 个工具**，`get_block_original` 在，**没有一个缺 `annotations`** |
| `--mcp` 子进程 | **32 个一个没杀**（换 bundle 不打断在跑的进程）。⚠️ 但它们跑的**仍然是旧二进制** —— 每个 AI 客户端要吃到新的都得各自 ⌘Q 重开 |

**留下的两份退路**：库备份 `spool.before-v062-install-20260824-161251.db`；
旧包 `/Applications/Spool.app.pre-v062-20260824-161251` 和
`~/Desktop/Spool-old-20260824-161251-pre-v062-devbuild.app`。⚠️ 确认几天没问题再删。

⚠️ **一个新发现**：从 dmg `ditto` 出来的 bundle 权限是 **owner-only**（`drwx------`），
和原来装的 `drwxr-xr-x` 不一样。已经 `chmod -R go+rX` 归正，**签名没被破坏**
（`--verify --deep --strict` 复跑通过）。⛔ 下次换装记得再看一眼。

---

## 3. 这一窗改的产品代码（就一处）

`d312bef` —— 睡前排队那一格的三条，Ocean 真手指验收之后提的：

1. **时间看不出来能改** → 「几点跑 [扁框]」换成「几点开始 [下拉]:[下拉] 24 小时制」
2. ⛔ **点 01:55 存成 13:55** —— `<input type="time">` 显示 12 还是 24 小时制**由系统区域决定，
   网页这边关不掉**。英文区域下框里写「01:55 PM」，`.value` 给的是 `13:55`。
   ⭐ **差别是钱**：01:55 在 DeepSeek 闲时那一档（北京 00:30–08:30 半价），13:55 是原价。
   ⛔ **别改回 `type="time"`**，理由写在 `CompressBoard.tsx` 的 `HOURS` 常量上面
3. **「今晚一起压」→「今晚自动压缩」**，底下补一行「为什么值得等到夜里」。
   ⚠️ 半价那句后面**必须跟着「换别家就看那家的价目表」** —— 端点是可以改的，
   少了后半句，界面上就多一句假话

---

## 4. ⛔ 第六轮挖出来、**仍然没修**的三条（发版前有意排在后面的）

⭐ 现在发版过去了，动 Rust 的顾虑（要重新公证）也过去了 —— **这三条可以排进去了**。

| # | 是什么 | 证据 |
|---|---|---|
| **①** | **`split_cuts` 之后没人看一眼正文空不空** —— 撞到一次 `ok=true` 配 **0 字节**压缩稿 | §8.15。`worthRetrying` 接住了，**但信封是假的** |
| **②** | **失败信封不带用量** —— `Envelope::Err` 只有 `ok`/`kind`/`message`/`status`。「已经跑出去的那一段照样算钱」那句护栏**至今拿不到证据** | §8.7。那一夜 8 次失败全记 ¥0，所以 ¥2.92 是**下限** |
| **③** | **实测台重编一次就要重新授权一次**，而授权框**锁屏状态下弹不出来** | §8.16。那一夜因此丢了八小时 |

①② 各一处判断 / 一个结构体加字段。③ 的做法写在 §5。

---

## 5. ⛔ 下次做连夜实测之前必读

**钥匙串 ACL 按二进制认，`cargo test` 重编一次就要重新授权一次。**
而**授权框在锁屏状态下弹不出来**，`SecItemCopyMatching` 会**无限期阻塞**。

| 空计划耗时 | 什么意思 |
|---|---|
| **~0.8s** | 已授权过，正常 |
| **~10–75s，屏幕上没有框** | 刚重编过，系统在重新验签。⭐ **正常，等着就好** |
| **几百秒过不去** | ⛔ **屏幕锁了** |

```bash
ioreg -n Root -d1 -r | grep -o 'CGSSessionScreenIsLocked[^,]*'   # = Yes 就是它
```

⭐ **做法**：跑之前一次性把代码改完编完，当着人的面跑一次空计划把授权拿到，
之后整夜一行 Rust 都不要再改。（全稿在记忆 `isolated-verify-workflow` §35。）

---

## 6. 顺序上下一件是什么

⚠️ **这一节 08-24 夜里被 §8 改过一次，08-25 又往后走了两格。现序是：**

`A` → `D` → `T` → `B 发版` ✅ → `S 批` ✅ → `V 批` ✅ → **§10 六条 + §11 五条（做完了，
⛔ 没验）** → `渠道` → `E` → `F`

⛔ **下一件是「装上去，把 §10.2 那六步验掉」，不是渠道。** 下面这段留着是因为渠道那部分的判断没变。

`A` → `D` → `T` → `B 发版` ✅ → `渠道` → `E` → `F`

**渠道**那一批（`WORKPLAN §渠道`）的前提是「A 批 + B 批全做完」—— **现在成立了**。
✅ **官网也部署过了**（`Deploy site` 2026-08-24 08:05Z，跟着这次推送触发的，success）。
实测复核：`No key` / `nowhere in Spool to put one` / `never sends data anywhere on its own` /
`zero built-in AI` **在线上一条都搜不到了**，新说法 `subprocess, with a key you supplied` 在。
⭐ **B0 那批假话彻底下线了 —— 渠道可以往官网指了。**

**零成本随时插的**：`C 批`五条（`C-a` 重扫 `scope_limited` · `C-b` Safety 纸面复核 ·
`C-c` 零提示那条路 · `C-d` 文档五处 · `C-e` 主窗尺寸验收②）。

**`F 批`（事件层）动之前仍然卡在一件要你拍的事上**：**event 存哪儿**。
三个选项连好处/代价写在 `WORKPLAN §2.F3`。⚠️ 原来那句「⛔ 不建 `events` 表」是在
**错的前提**下写的（`C1` 的数据模型 08-24 被推翻了），要你明说推翻才算数。
⭐ **不受影响的**：`C3` · `C4` · `C5` · `C6`，以及 `C7` 的第 2 条（事件进 pack）。

---

## 8. 🆕 08-24 夜里这一窗：调研 + `S 批`（**下一件就是它**）

### 8.1 起因

Ocean 问「能不能用 RAG 增强 Spool 的上下文管理」。调研全稿
**`docs/RESEARCH_RAG_CONTEXT-2026-08-24.md`**（⛔ 开工前读它，尤其 §0-bis）。

**结论**：不上 RAG。真库量出来 **39 项目 / 195 块**，最大的一份 pack 36,685 字符、
预算 50,000 —— **没有一个项目超预算**，库内检索根本不是瓶颈。
⭐ 但同一次测量跳出来两件更重要的事，S 批就是从这里来的。

### 8.2 那两件事

**一，人不建关系，AI 建。** 全库用户亲手 pin 的只有 **3/195（1.5%）**；
而 Codex 写回的块里 5/6 带引用、4/6 带保质期、2/6 带逐句更正。
⇒ 工作计划 `F2` 那句「建立引用太贵、图是空的」**过期了**。

**二，而 AI 唯一能用的关系（`corrects`）按定义不让旧块退休。**
⇒〈申请规划〉现在**三代选校名单同时活着**：seq 21（14 所）· seq 23（15 所）·
seq 26（16 项基准），`stale_at` 全空。**这是事实，不是预测 —— 24 条已经批准入库了。**

### 8.3 Ocean 当晚拍的（九条）

| # | 拍了什么 |
|---|---|
| 1 | ⛔ **推翻红线**：`DESIGN_CONTEXT_HYGIENE` §5「AI 不许提整块取代」作废。**AI 可以提，仍然只能提案** |
| 2 | 名字统一成 **「整条取代」**，⭐ 两边都用（E3 那个按钮也改） |
| 3 | 新提案**并进 E3 那张审阅卡**，不新开 |
| 4 | 压缩撞上带更正的块 → **走乙**（拿原文重定位，定不到就退回只报块号 + 界面说一句） |
| 5 | 一块上多条更正 → **走丙**（不再一次只开一条，每条跟在它划的那句底下） |
| 6 | 引文划太宽 → **走乙**（审阅面上引文和更正正文并排摆） |
| 7 | `解除` → **`取消更正`**；`什么都不动` → **`不动`**（字太多） |
| 8 | 🆕 **一句话标签进搜索**（`S8`）—— 唯一从 RAG 里活下来的那条 |
| 9 | 🆕 **把那 2 块污染的正文清掉** —— ✅ 当晚做完了，见 §8.6 |

### 8.4 ⛔ 开工前必看的三条

**① 按钮名字别照文档抄。** U9（08-23）已经改过一次：现在屏幕上是
**合并 / 新的取代旧的 / 什么都不动**，⛔ **不是**「只退旧的」。
这次要改成 **合并 / 整条取代 / 不动**。
⚠️ 那两个中文串是 **i18n 表的键**，改键就等于改调用点 ——
`i18n/index.ts` 和 `Compress/StaleReview.tsx` **必须同一次改完**，漏一处运行时掉回中文原文。

**② `S3` 是唯一非做不可的一条。** 其余六条要是想先开渠道，都可以等。
理由：**它已经在真库里造成损害了** ——〈申请帮助〉#11 那条更正的引文，
被 **2026-08-24 11:20:51 的一次压缩**打断，屏幕上什么都不划、也不报错。
⚠️ `quoteFold.ts` 的头注释 08-23 就预言过这条路；T4 修的是标点那一半
（真库 seq 23 / seq 25 就是靠它救回来的 —— Codex 把半角标点写成了全角），
**措辞被改写这一半没修**，折叠救不了。

**③ `S6` 不许比 `S3` 晚。** `S3` 让更多引文对得上，
**而引文一旦对得上，划太宽的害处才真正显出来**（seq 25 那条会一次划掉四个主张，
其中三个没人更正过）。

### 8.5 施工细节在哪

`WORKPLAN-2026-08-22.md` 的 **`S 批`**（表在 §1，逐条在 **§2.S1 – §2.S8**）。
⛔ 别在交接里再排一遍。

### 8.6 顺手记两件已经在库里的脏东西（都进 `S7`）

- **脚手架回流** —— ✅ **那 2 块 21:48 已经清掉了**（seq 21 558→413、seq 24 270→124；
  备份 `spool.before-scaffold-cleanup-20260824-214837.db`；核过全库 0 残留、FTS 同步、
  `integrity_check ok`、四条更正定位状态不变）。**闸还没做，在 `S7`。**
  ⛔⛔ **诊断改过一次**：不是 MCP 写回抄的，是**压缩那一步**写进去的 ——
  两块的 `original_content` 都干净，只有压缩稿 `content` 带着。
  ⇒ **闸要加在压缩的写回路径上**，和 `S3` 同一段代码，合起来做。
  ⚠️ 只扫了三串（这条 2 处、`SPOOL CONTEXT PACK` 0、`Full Record (chronological)` 0），
  `↩ cites:` / `📌` / `🗜` / `dropped for budget` **还没扫**。
- **一段转述挂着「用户原文」的牌子**：08-24 那批的 `source_text` 980 字，
  通篇「用户要求…」，是 Codex 的第三人称转述，**已经入库**在〈申请帮助〉。
  ⏸ 这一块**没有清**（要不要留是内容判断，不是缺陷）。

### 8.7 ⭐ 下一窗开工提示词（照抄）

```
读 docs/HANDOFF-NEXT-2026-08-24.md §8 和 docs/WORKPLAN-2026-08-22.md 的 S 批
（表在 §1，细节在 §2.S1–§2.S8）。背景稿是 docs/RESEARCH_RAG_CONTEXT-2026-08-24.md，
先看它的 §0-bis。

这一窗做 S 批。开工顺序按依赖排，不按编号排：

1. S3 + S7①  —— 同一段代码（压缩的写回收尾）：压完重定位 corrected_quote，
   定不到就退回只报块号并在界面上说一句；同一步把 pack 骨架行从压缩稿里剔掉。
2. S6        —— 审阅面上引文和更正正文并排摆。⛔ 不许比 S3 晚。
3. S1        —— 统一成「整条取代」，连带 解除→取消更正、什么都不动→不动。
4. S4 · S5   —— 关系行读得懂（cites 给动词 + 跨项目显项目名）、多条更正配得上对。
5. S2        —— AI 提「整条取代」提案，并进 E3 那张审阅卡。
6. S8        —— 一句话标签进搜索。

开工前先自己核三件（别照文档抄）：
- i18n/index.ts 和 Compress/StaleReview.tsx 里那三个按钮现在的实际字串；
- src/lib/blocks/quoteFold.ts 的折叠表，和 src-tauri/src/api_engine.rs::fold_char 是否仍逐项一致；
- 动 MCP 工具的话，OPENERS / INSTRUCTION_BODY 和 annotations 两条硬规矩。

红线提醒：S2 的提案权是 2026-08-24 Ocean 明说推翻旧红线才有的，AI 仍然只能提案、
永不直接写；界面上不许出现「作废」两个字；引文逐字闸的输入一个字符都不许动。
```

---

---

## 9. 08-25 这一窗：`V 批` 四条全做完了（**✅ 已验，见 §9.5**）

> **起因**：Ocean 装上 `S` 批那一版、**在用的过程中**提的四条。
> ⭐ 其中 `V2` 直接说明 `S` 批白做了一半：**块默认折叠，所以 `S5`/`S6` 的更正标记他根本看不见。**

**开工顺序按依赖排，不按编号**：`V4`（独立常数）→ `V2`（改完每块高度全变）→ `V1`（要用 V2 的锚点）→ `V3`。

### 9.1 逐条去处

| 条 | 做了什么 | 落在哪 |
|---|---|---|
| **V4** | 双击 ⌥ 窗口 `500 → 250` | `double_tap.rs:163`、`double_tap_win.rs:75`。⚠️ **两处都改了，两处的注释也都改了** —— 原来两边都写着「匹配系统默认」，现在写清是 Ocean 拍的、为什么（正常用 ⌥ 被误判），以及**退路是 300，⛔ 不是退回 500** |
| **V2 ①** | 块默认展开 | `BlockItem.tsx` `useState(false)` |
| **V2 ②** | ↓ / ↑ 跳上下一块 | `BlockFeed.tsx` 的 window keydown；落点高亮走 `setActive`，和单击一块一样 |
| **V2 ③** | 右边缘刻度条 | 新文件 `ScaleRail.tsx`，挂在 `LogView` 的滚动容器上 |
| **V2 ④** | 首装教学 | `BlockFeed.tsx` 顶上一行字 + 新设置位 `blockNavHintPending`（`App.tsx` 首启时点亮） |
| **V1** | 浏览位置按项目记住，存 30 天 | 新表 `read_positions`（**库 v26 → v27**）+ 新文件 `readPositions.ts`；落点判断在 `LogView.tsx` |
| **V3** | 编辑区放大 + 背景压暗 | ⚠️ **这一行已经被 §9.4 第一轮整条推翻** —— 压暗删了、`editingBlockStore.ts` 删了，编辑器变成铺满线程面的 portal 面板。以 §9.4 为准 |

**新文件**：`ScaleRail.tsx`、`viewportAnchor.ts`(+test)、`readPositions.ts`(+test)。
⚠️ `editingBlockStore.ts` 建过又删了（压暗被否之后它就是死代码）。
**验证**（两轮验收改完之后）：`tsc` 干净、`npm run build` 过、`vitest` **670 个全绿**、
`cargo test` **130 个全绿**（⚠️ 不是 `cargo check` —— schema 那条守卫只在 test 里跑）。

### 9.2 ⚠️ 三件开工前核过的事（结论和文档里写的不一样，记下来）

1. **`BlockItem.tsx:440` 那段「点外面自动折回去」——⛔ 不能原样留着。**
   它是「折叠是默认」那年写的：那时候「展开着」只可能是用户自己点开的，所以点外面折回去
   = **回到默认**。现在每块都默认展开，不改的话**第一次点任何地方（另一块、左栏、空白）
   整个 feed 会当场全折上** —— V2 被第一次点击撤销，而且看不见是怎么发生的。
   ⇒ **没删**（工作计划写着别顺手删），改成**只对「用户自己点开的」那种展开生效**
   （`manuallyExpanded` 这个 ref）。这样它原来的活儿一点没少：你把一块收起来、又点开读、
   点到别处、它回到你留下的样子。

2. **首装教学 ⛔ 没走播种教程块那条路**（虽然核下来教学确实是播种块，不是浮层）。
   理由是**给老库的代价**：`retranslateTutorial` 是**逐字**把库里的行和种子配对的，
   配不齐就整条跳过。往 gesture 教程里加第 7 块，**所有已经装了的库**（Ocean 自己那个在内）
   只有 6 行，配到第 7 条断掉 ⇒ **那条教程从此不再跟着语言切换走，而且一声不吭**。
   ⇒ 改成 feed 顶上一行字，和「现在够打一个包了」那行**同一个形状、同一个首启闸门**
   （`blockNavHintPending`，老库从来没写过这个键，所以只有新装的人看得到）。
   ⛔ 不是悬浮窗 —— Ocean 那条红线还在。

3. **「视口顶上是哪一块」以前确实没有** —— 现在有了，`lib/blocks/viewportAnchor.ts`。
   ⚠️ `BlockFeed.tsx` 里那两个 anchor（`selectionAnchor` / `marqueeOriginContent`）
   **是框选拖拽用的，不是这个**，别去接。V1 的落点、V2 的 ↓ 起点、刻度条的亮条，三处都读它。

### 9.3 ⭐ V1 多加的那条规矩（Ocean 没提，我加的）

**离开之后这个项目有新块进来 → 还是回到底部，不回原位。**
理由：不然新东西他永远看不见 —— 而「有新的」正是他打开这个项目的原因。
⚠️ 界面上**没有**任何一句话说这件事：落到底部本来就是「有新的」的样子。
实现在 `readPositions.ts` 的 `resolveLanding`，有测试。

### 9.4 🆕 验收两轮：Ocean 当场提的,已经改完并换装

**第一轮（刻度条挡住 + V3 三条）**

| 报的 | 真正的原因 | 改法 |
|---|---|---|
| 「右侧本身有一个默认滑轨,刚好遮住了 block 的刻度」 | feed 的原生滚动条和刻度条在同一个右边缘 | ⭐ **feed 藏掉原生滚动条,刻度条就是滚动条**（`global.css` 的 `.no-native-scrollbar`,⛔ 只有 feed 用）。往左挪 16px 不成立：macOS 覆盖式滚动条平时不占位、**一滚就浮出来盖住** |
| 「底部的批注无法在同一页面编辑」 | 批注编辑器长在块底部 | 正文 + 批注合成**一个面板**，四个入口（双击正文/双击批注/✎/✑）都开它，只差光标落哪儿 |
| 「编辑窗口不是固定的,需要下滑才能找到」 | 编辑器在块原位长大 | 面板挂在**整个线程面**上，`LogView` 的 `[data-block-editor-host]`，`BlockItem` portal 进去 |
| 「背景不要变暗,让编辑窗口填满整个背景」 | —— | ⛔ **压暗整条删了**（`bg-ink/25`），面板不透明铺满。⚠️ 连带 `editingBlockStore` 变成死代码，已删 |
| 「AI 批注后人为修改仍然是 AI 批注,需要做区分」 | `updateBlockAnnotation` **无条件**写 `annotation_by='user'` —— 改个错字，AI 那句就拿到了用户权限（pack 的 💭 Personal + W7 标题位） | ⭐ 三态：`user` / `ai` / **`ai-edited`**。见 §9.6 |

⚠️ **合成面板必须连带改的一条**：两个输入框**不再 blur 就保存**。原来 blur 即保存并关闭，
在双字段面板里就是陷阱 —— 光标从正文挪到批注会把面板关掉。现在只有 **完成 / Esc / 点面板外面** 收尾。
⚠️ 另一条：`==重点==` 按钮在悬停工具条上，工具条长在块上、被面板盖住了 ⇒ **补了一个到面板抬头**。

**第二轮（刻度条三条）**

| 报的 | 真正的原因 | 改法 |
|---|---|---|
| 「刻度条根本调不出来,只有划到最顶部才能看到」 | ⭐⭐ **滚动容器里的 `absolute inset-y-0` 是相对被滚动的内容定位的** —— 它跟着内容滚走了 | 挪到一个**不滚动的 `relative` 盒子**上，和滚动容器平级。⛔ `ScaleRail.tsx` 和 `LogView.tsx` 都钉了「别挪回去」 |
| 「动效卡顿,不丝滑」 | 每个滚动帧对所有挂载块跑 `getBoundingClientRect()` = **每帧 200 次强制重排**；且每帧都 setState | ① 偏移**只量一次**存起来，滚动只比数字，零 DOM 读；② 只在亮的那根真换了才 setState；③ `transition-all` → 只过渡 `width, opacity` |
| 「当前块刻度最长间隔最大,然后依次递减」 | —— | 高斯透镜（σ≈3.2 块）。⭐ **"间隔"和"长度"用同一个权重**：当前块拿到高度里最大的一份，所以所在位置那一段是撑开的，不只是线变长。另加每格 26px 上限（不加的话五块的项目会把五根发丝摊满整面） |

⚠️ **这坑踩了两次**：压暗层第一版也是挂在滚动容器里，上一轮修了压暗、**漏了刻度条**。

### 9.5 ✅ 已验（2026-08-25 傍晚，Ocean 逐条回的）

> **他的原话**：第 7 条那两处（编辑面板盖住搜索导航条、切项目时先保存）—— **「通过」**。
> 其余几条他没单独挑毛病，挑出来的六条毛病另开一节（§10），⛔ 别把那六条读成这七条的重复。

下面这张单子留档，验的方法没变（真手指、按顺序）：

**必须真手指验的**：
1. ⭐ **`V4` 只能真手指验** —— 授权齐全时 tap 在 HID 层，**合成事件看不见**
   （记忆 `isolated-verify-workflow` §14）。验点：正常用 ⌥ 还会不会被误判；双击还灵不灵。
   ⚠️ **要是他说「不灵了」，下一个数是 300，⛔ 别推翻回 500。**
2. **V2 ①** 打开一条长块的项目 —— 更正标记（`S5`/`S6`）现在看得见了吗？**这是整批的起点。**
   然后**点一下别处**，⛔ 块不许全折上。
3. **V2 ②** 在 feed 里按 ↓ / ↑；⚠️ 再到正文编辑、批注框、搜索框、捕捉框里各按一次 ↓，
   **必须是移光标，不许跳块**。
4. **V2 ③ 刻度条**：⚠️ **长项目**滚起来顺不顺、透镜跟不跟得上；**短项目**（三五块）是不是
   居中一小簇而不是摊满整面；顺手确认右边缘 16px 以外的框选、选字没被挡住。
5. **V1** 切走再切回来 —— 停在原位；⚠️ 再让 AI 写一块进去、切走切回来，**这次要落到底部**。
6. **V3 面板**：双击正文/双击批注光标落对没有；⚠️ **光标从正文挪到批注，面板不许关**；
   Esc 放弃、完成保存、点左栏（面板外）保存并关。
7. ⚠️ **两处我只能从代码上判断是对的，没法自动验**：
   ① 面板盖住了顶上的搜索导航条（⌘⇧F 落点那个）—— 编辑时要不要还能看见它？
   ② 点左栏切项目时是**先保存再切**。

### 9.6 ✅ 已拍板：`ai-edited` **算 AI 的**（2026-08-25，Ocean）

> **他的原话**：「ai-edited：算AI的」。
> ⇒ **和已经装机的那一版一致，一行代码都不用动**（`annotationAuthor.ts` 的 `annotationIsAi`
> 已经把 `ai-edited` 算进 AI 里，Rust 的 `annotation_is_ai` 同）。这条**不再挡发版**。

当时问他的原文留在下面。

**`ai-edited` 在 pack 里算谁的？** 实现：**算 AI 的**，拿不到 💭 Personal。

依据是他那句「**仍然是 AI 批注**，需要做区分」。⚠️ 但这是我按那半句话定的，
另一种读法是「改过之后就该算你自己的」。**改回去是一行**（`annotationAuthor.ts`
的 `annotationIsAi` 里把 `ai-edited` 挪出去）。⛔ 发版前必须问清楚 ——
它决定 AI 写的句子会不会以用户的权限进到下一个模型的上下文里。

「不能人为新增」是靠**界面上根本没有写出 `ai` 的路径**保证的：作者由 `updateBlockAnnotation`
写入时算出来，⛔ 调用方没有参数可以覆盖它。Rust 那边 `annotation_is_ai` 也认了 `ai-edited`。

### 9.7 ✅ 已收（2026-08-25 傍晚）—— 但**收的是 §10 修完之后那一版**

Ocean 的答复：「commit 之前我还有下面一些问题，把这些修复了再提交」。
⇒ 六条修完之后**一次性提交**（`S` 批 + `V` 批 + §10 那六条）。

⚠️ **为什么不拆成两个 commit**（一个对着装机那一版、一个是今天的修）：拆不干净。
今天这六条改的就是 `V` 批那几个函数本身（比如删掉的那两条聚焦 effect 是 `V3` 自己写的），
按文件切的话，中间那个 commit **编译不过** —— 一个 bisect 时会骗人的 commit，
比一个大 commit 更贵。

原文留档：

`git log -1` 曾停在 `37b9c99`。**`S` 批和 `V` 批（含两轮验收修改）全部躺在工作区**，
40 多个文件。⚠️ 装机的那一版就是工作区这一版，**没有任何 commit 对得上它**。

⚠️ 提交时记住 `CLAUDE.md §5`：⛔ 不许写 `Co-Authored-By`、不许写 🤖 footer、
不许在 message 里出现 Claude/Anthropic。自查那条 grep 会命中产品内容里的 claude
（记忆 `attribution-grep-false-positive`），用窄版复核，⛔ 绝不为了让它变绿改代码。

### 9.8 库版本与装机状态

- **库 v26 → v27**（新表 `read_positions`，⛔ 不碰 `blocks`）。退回一句
  `DROP TABLE read_positions` 就回 v26，不丢任何 v26 就有的数据。
- ⭐ **`mcp.rs` 的 `EXPECTED_SCHEMA_VERSION` 必须跟着 `client.ts` 走** —— 这次差点漏：
  加了 v27 迁移没改它，装上去的话**所有 AI 客户端的写入工具会当场全部拒绝请求**。
  仓库里有守卫测试专防这个（`schema_version_locked_to_gui`），⚠️ **但它是 Rust 测试，
  `cargo check` 不跑，必须 `cargo test`**。
- 装的是 **Developer ID 签名的 dev build**（`spctl` 报 `Unnotarized Developer ID`，§33）。
  两个授权都在（`[double-tap] installed at HID/active`）。真库 296 块、
  哈希 `c4a4e300716e4a61`，三次换装一字未动。
- 退路在 `~/Desktop/spool-snapshot-20260825-*.db` 和
  `~/Desktop/Spool/spool-pre-install/Spool-old-20260825-*.app`。
- ⚠️ 67 个 `--mcp` 子进程跑的**还是旧二进制**，各 AI 客户端要 ⌘Q 重开才吃到新的（§34）。

### 9.9 下一窗开工提示词 —— ⛔ **过期了，用 §12 那一份**

（下面这一份是 08-25 白天写的，留档。）

```
读 docs/HANDOFF-NEXT-2026-08-24.md §10 和 §11（两整节）。S 批 + V 批 + Ocean 08-25
提的十一条已经全部做完、提交、并且换装到 /Applications 了，⛔ 但十一条一条都没上手验过。

这一窗第一件事：照 §10.2 和 §11.2 逐条问 Ocean，他说哪条不对就改哪条。
⛔ 别自己先动手重构。⚠️ 他要 ⌘Q 重开 Spool 才吃得到新版本；各 AI 客户端也要各自重开（§34）。

验完之后就是 Q 批（WORKPLAN §1 · Q 批，逐条在 §2.Q1–§2.Q4）——
⛔ 开工前必须先问他一句：Q1 要加的 blocks.ref_note 撞上他自己定的「别往 blocks 表加列」，
对照表在 §2.Q1。

两件要主动提的：
1. 上面那条红线（Q1 的存法：加一列，还是另开一张表）。
2. §10.2 第 5 条（休息不抢捕捉弹窗）要攒够一小时专注才验得到，问他要不要专门排一次
   （记忆 isolated-verify-workflow §32）。



开工前先自己核三件（别照文档抄）：
- ScaleRail.tsx 每一根刻度的高度现在是谁算的、总高凭什么不会超出刻度条；
- CaptureOverlay.tsx 里休息卡被攒下来之后，是哪几条出口把它放出来的（少一条 = 休息丢了）；
- client.ts 的 SCHEMA_VERSION 和 mcp.rs 的 EXPECTED_SCHEMA_VERSION 是不是都是 27
  （⚠️ 守卫测试只在 cargo test 里跑，cargo check 看不出来）。

红线提醒：⛔ 不做悬浮窗；⛔ 浏览位置不许按 scrollTop 存；⛔ 别往 blocks 表加列；
⛔ 提交不许带任何署名（CLAUDE.md §5，自查 grep 会误报，见记忆
attribution-grep-false-positive）；⛔ 用户不能看到测试环境的 Spool。
```

---

## 10. 🆕 08-25 傍晚：Ocean 用出来的六条（**下一窗第一件事是验它们**）

> **起因**：§9.5 的验收单他走完了（§9.5、§9.6 已收口），然后接着用，用出六条。
> ⛔ 六条**全部修完了**，但**一条都没上手验过** —— 只过了 `tsc`、`vitest`（676 全绿）、
> `npm run build`。⚠️ 而且 `/Applications` 里那一版**还是修之前的**，要重新装才能验。

| 条 | 他的原话 | 真正的原因 | 改在哪 |
|---|---|---|---|
| **1** | 「周回顾没法暂停」 | 停止键**只长在右边栏的 `LiveRun` 卡片上**，而钉住的视图（项目管理 / 周回顾）**根本不挂右边栏**（`App.tsx`：`pinnedView ? null : …`）。从那一屏点「回顾这一周」的人，看着它转，没有任何地方能停 | `ReviewBoard/index.tsx` 引擎条上加一个「停下」，走同一个 `engineStore.cancel`。⚠️ 是**停**不是暂停：停了不能续跑，再点是从头再来（按钮的 title 就是这么写的） |
| **2** | 「双击批注和点击工具栏里的批注都无法焦距在批注框，输入仍然在正文里面」 | ⭐⭐ `BlockItem` 里还留着**合成面板之前**那两条聚焦 effect。`openEditor` 现在一次把两个字段都打开 ⇒ 盯 `editingContent` 那条**每次都成立**，而它是 passive effect，跑在决定落点的那条 layout effect **之后** —— 刚落到批注上的光标又被抢回正文 | 两条一起删（`BlockItem.tsx`，原处留了一段「⛔ 别加回来」）。落点从此只由 `focusField` 那一条 layout effect 说了算 |
| **3** | 「批注无法高亮和更正」 | 划词那条 effect 只认**正文**：显示态只映射 `measureRef` 里的选区，编辑态只读正文那个 textarea。光标一进批注框，「标为重点」当场灰掉 | 选区现在**带着它划在哪一格**（`FieldRange`）：批注两个 view 都上了 `withOffsets` + 同一个 `onSurfaceMouseUp`，`runHighlight` 按字段写回 `annotationDraft` / `setAnnotation`。⚠️ **更正仍然只对正文开着** —— 见下面那条 ⛔ |
| **4** | 「两个不同来源的 block 合并之后，来源的文字特别大，和正文混在一起，太突兀」 | 合并时贴在非幸存段前面的 `[from chatgpt] ` 是**存在正文里的普通文字**，所以按正文画：15px、和上下文一样黑。块头上那个来源徽章才 10px —— 差了一半还多 | ⭐ **存的字节一个都没动**（pack 里那一份还带着它，收件 AI 靠它分辨谁说的；而且所有划词/更正都按 `content` 下标定位）。只在**画**的时候摘出来当一枚小灰标签：`contentRuns.tsx` 多了一个 `'source'` 记号，`SegmentedContent.tsx` 走同一个 `splitSegmentSource` |
| **5** | 「休息弹窗会强制关闭我正在写批注的捕捉弹窗」 | 休息卡直接 `setContent` 顶掉捕捉卡 —— 正在打的半句话连同这次捕捉一起没 | ⭐ 休息**等**：捕捉卡在屏幕上时先攒着（`pendingBreakRef`），等这张卡自己走完（写完 / Esc / 点外面 / 超时）再顶上来。⛔ 不是丢掉。⚠️ Rust 那边发休息卡时已经按休息卡的高度缩过窗口了，所以攒的那一下要顺手把高度量回来 |
| **6** | 「侧边的刻度只能显示 4 到 (n-4) 个 block，边缘没了，确保每一个刻度代表一个 block」+「悬浮在每一个刻度上可以预览 block」 | ⭐⭐ 刻度高度是交给 `flexGrow` 分的，配 `maxHeight: 26px`。CSS 伸缩算法在「一批顶到 max、剩下的分不够」时，会把没顶到的**冻结在 1px 基准上**；总高一超，`justify-center` 就把超出的部分从**两头**挤出去 —— 头尾各几根当场被裁。他数出来的正是这个 | 高度改成**算死的像素**（一个统一缩放系数，同时受刻度条高度和 26px 上限约束 ⇒ 总高恒 ≤ 刻度条）；`py-3` 也去了（那 12px 上下留白本身就吃掉头尾几根的位置）。点击/拖动从「按比例算」换成 `indexAtOffset`（⛔ 透镜之下按比例算会点到隔壁块）。悬浮预览：有批注用批注，没批注用正文（`plainText` 去掉标记），150 字封顶、四行截断 |

**新增/改动的文件**：`ScaleRail.tsx`（重写）、`viewportAnchor.ts`（`ratioToBlockIndex` → `indexAtOffset`，测试跟着换）、`BlockItem.tsx`、`ReviewBoard/index.tsx`、`CaptureOverlay.tsx`、`contentRuns.tsx`、`SegmentedContent.tsx`、`segments.ts`、`LogView.tsx`、`i18n/index.ts`（两条新英文）。

### 10.1 ⛔ 一个还没问 Ocean 的（不挡验收）

**批注要不要能「更正」？** 现在：**不能**，划在批注上的选区只出「标为重点」，不出「更正这里？」。

理由是数据模型：一条更正存的是 `corrected_quote`，pack、`check_quote_occurs`、
`correctedSpans` 三处都拿它去**正文**里找。拿批注里的句子存进去 = 一条谁也定位不到的更正
（`correctionsBySource` 会把 quote 置空，卡片退回时间线上那种「指过去」的形态）。
⇒ 要做的话是另一件事：得先决定「批注上的更正卡画在哪儿」，⛔ 不是在 `startCorrection`
里加一行。

### 10.2 验它们的顺序（照着点，一条一分钟）

1. **周回顾**：左栏进周回顾 → 点「回顾这一周」→ **旁边应该有个「停下」** → 点它，
   toast 说「已停止 周回顾」。
2. **批注落点**：双击一条**带批注**的块的批注 → 光标应该在**批注框**里（下面那个小框），
   直接打字进批注；⌘Z 之前先确认打的字没进正文。再用工具条上那个 ✑ 试一次。
3. **批注划词**：读的时候在批注上划一段 → 应该出「标为重点?」（⛔ **不出**「更正这里?」，
   见 §10.1）；点它 → 批注上出现黄底。再进编辑面板，在批注框里划一段 → 抬头那个
   「标为重点」**不该是灰的**。
4. **合并**：选两条**来源不同**的块（一条 chatgpt 一条自己写的）→ 合并 →
   第二段前面应该是**一枚小灰标签**，不是一行和正文一样大的字。
5. **休息不抢**：（要等够一小时的专注，见记忆 `isolated-verify-workflow` §32）
   捕捉弹窗开着、正在写批注时休息到点 → **弹窗不许被顶掉**；写完/Esc 之后休息卡才出来。
6. **刻度条**：开一条 30 块以上的项目 → **第一根和最后一根都要在**（贴着上下边缘），
   数一遍根数 = 块数；鼠标停在任意一根上 → 左边出一张小卡，有批注的显示批注；
   点最上面那一根 → 跳到**第一块**，不是第四块。

---

## 11. 🆕 08-25 夜里：读真库 Flux 提的五条（**已修，⛔ 未验**）

> **起因**：Ocean 打开真库的 Flux 项目，逐块读 `#7`、`#13`、`#16`。
> ⚠️ 和 §10 那六条不同，这五条**全部是他在真实数据上看出来的**，所以每一条底下都记了
> 那一块当时长什么样 —— 下一窗要复现，直接照着找。

### 11.1 逐条

| 条 | 他的原话 | 真库现场 / 真正的原因 | 改在哪 |
|---|---|---|---|
| **1a** | 「这个更正是否是历史残留，而不是当前的更正？」 | ⭐ **两者都不是。** `#13`（Codex · MCP 写的）更正 `#7`，关系是**活的**，但 `corrected_quote` 是**空的** —— 而**旧的写入闸门允许不给准星**，所以它不是老 schema 的遗物，今天的 AI 照样能写出这种 | 见 1c |
| **1b** | 「这个 ⚠️ 符号不要有，删掉」 | 「其中一处已被更正：」本来就是一行灰色小字，一个警告表情把它读成了「出事了」 | `CorrectedByLine.tsx` 去掉 |
| **1c** | 「AI 更正并不能落实到单独一个词上……按道理应该能更准确」 | 没有准星的更正，界面上只能说「其中一处」+ 一个块号 —— 读的人要把两块对着读 | ⭐ `mcp.rs` **两条写入路径都要求** `corrects` 带 `corrected_quote`（`add_block` / `propose_blocks`）。⚠️ 拒绝时必须说出退路：「整块都不作数用 `propose_supersede`」，⛔ 不然模型只学到一个「不行」 |
| **2** | 「AI 的批注 UI 应该和用户批注一样，放在 block 顶上，只是加了 AI 批注的标签」 | AI 批注被压在正文下面、12px 灰字（v14 §9.3 拍板乙） | `BlockItem.tsx`：一个视图，永远在顶上，AI 的多一枚标签。⚠️⚠️ **只动了画在哪儿，⛔ 没动权限** —— pack 里 AI 批注仍是 `ai note`（🧩 Synthesis），拿不到 💭 Personal |
| **3** | 「被引用的 block 正文文字再也不显示……我需要 AI 给出引用的理由」 | `#16` 引 `#9`，`#9` 是一封原文邮件，引用行截出来的是「Dear Hanze, I have great news for you — …」 | `CitationLine.tsx`：正文**整条不再出现**，换成一句关于那一块的话 —— `gist` → 批注 → 都没有就只剩编号和时间。⛔ 那句「理由」还差一半，见 §11.3 |
| **4** | 「批注不能被更正」 | 一句原话只往**正文**里找 | 正文找不到就再问批注（`assemble.ts` + `mcp.rs` 同一条规则），记号和卡片画在批注那一格。**正文优先**：同一句话两格都有时，更正说的多半是正文 |
| **5** | 「取消更正后，更正的内容会变成一个 block，修复，直接消失」 | 以前只**解开关系**，那一块留在时间线上成了孤块 —— 它的全部内容是「这句话应该是……」，离开它划的那一句没人读得懂 | 整块删掉，走普通删除那条路 ⇒ **进撤销栈，⌘Z 拿得回来**（提示里写了） |

### 11.2 验它们的顺序（真库 Flux，一条一分钟）

1. 打开 Flux `#7` —— 那行「其中一处已被更正：」**不许有 ⚠️**。
2. 同一块：AI 批注「v3 里说「MIT 12/22」…」应该在**块的最上面**，和用户批注一个字号，
   前面一枚「AI 批注」标签；正文降级成灰色引文（和有用户批注的块一样）。
3. 在那条 AI 批注上**划一段字** → 应该同时出「标为重点?」和「更正这里?」；
   点「更正这里?」写一句 → 卡片挂在**批注底下**。
4. 那张卡片上点「取消更正」→ **整条消失**（不是变成一个块留在时间线上）；⌘Z 拿回来。
5. `#16` 的引用行 —— **不许再有那段邮件原文**。⚠️ `#9` 既没有 gist 也没有批注，
   所以现在那一行只剩「↩ 引用了 #9 00:35」，⛔ 这是**预期**，不是没做完（见 §11.3）。
6. 让某个 AI 客户端写一条更正、故意不给 `corrected_quote` → 应该被**拒绝**，
   而且拒绝里要提到 `propose_supersede`。⚠️ 客户端要 ⌘Q 重开才吃到新二进制（§34）。

### 11.3 ✅ 已拍板（2026-08-25 当晚）：**乙 + 丙都做**，写成了 `Q 批`

> **他的原话**：「三条路我认为加上乙：给引用加一个理由字段。并且加上丙：加一个『给已有块
> 补摘要』的工具。加丙的理由是被更正的 block 摘要无法被更正，读取会被误导，更正的信息也
> 可以出现在摘要里面。」
> 追加一条：「**摘要也需要可视化，可以修改，放在内容，压缩，过期检测同级的新区域显示。**」

⭐ **他给丙的那条理由比我提的那条硬**：我说的是「老块补不上摘要」，他说的是
**「摘要会过期而且改不了」** —— `gist` 只能在 `add_block` 那一刻写一次，而更正是后来落下的，
所以**一块被更正之后，它的摘要必然停在更正之前那个说法上**，而搜索命中给 AI 看的正是它。

⇒ 施工计划全部写进 `WORKPLAN-2026-08-22.md`：批次卡在 `§1 · Q 批`，逐条在
`§2.Q1`（引用理由字段）、`§2.Q2`（`set_block_gist`）、`§2.Q3`（搜索命中提示更正）、
`§2.Q4`（「摘要」页签）。

✅ **存法当晚也拍了**：「认可：加一列，不开新表」⇒ `blocks.ref_note` / `blocks.gist_by` /
`proposals.ref_note`，库 v27 → v28。⚠️ 那条「别往 blocks 表加列」不是废了，是**有了例外**
（理由和边界写在 `§2.Q1`）—— ⛔ 下次再有人想往 blocks 上加列，仍然要重新问一次。

下面是当时给他看的三条路，留档。

### 11.3-bis 原文：**「引用的理由」还差一半**

他要的是「AI 给出引用的理由，结合被引用的 block 中的关键信息」。现在做到的是**后半句**：
引用行会显示被引块的 `gist`（AI 写的「这一块整体是什么」，v26 §2.S8 就有）或批注。

⚠️ **真库里现在一条 gist 都没有**（`select count(*) … gist is not null` = 0），
`#9` 也没有批注 —— 所以他今晚看到的那一行是空的。

**前半句（「为什么我引用它」这句话本身）没有地方存**，三条路，好处/代价：

| 路 | 好处 | 代价 |
|---|---|---|
| **甲：就用 `gist`**（现在这样） | 零改动，字段已经在，`add_block` 就能写；搜索命中那一行也一起变好 | 说的是「那一块是什么」，⛔ 不是「我为什么引它」；而且**老块补不了 gist** —— 没有任何工具能改已有块的 gist |
| **乙：给引用加一个理由字段** | 正好是他要的那句话，写在引用这一头，AI 一次 `add_block` 就能带上 | 要动库（v27 → v28）+ MCP 契约 + pack 那一行；⚠️ **老引用一条都没有**；⛔ 撞「别往 blocks 表加列」那条红线（可以另开一张表绕开，但那是多一张表） |
| **丙：补一个「给已有块写 gist」的工具** | 老块也能被补上；一处改动，三个地方（引用行 / 搜索 / pack）一起受益 | 多一个写入工具 = 多一道要防的口子；而且写的仍然是「那一块是什么」 |

---

## 12. ⭐⭐ 下一窗开工提示词（**照抄这一份**，⛔ 不是 §9.9 那一份）

```
读 docs/HANDOFF-NEXT-2026-08-24.md §10、§11、§12（三节），
以及 docs/WORKPLAN-2026-08-22.md 的「§1 · Q 批」和 §2.Q1–§2.Q4。

现状一句话：S 批 + V 批 + Ocean 08-25 提的十一条已经全部做完、提交、并且换装到
/Applications（三个 commit：9cd2fe0 / cc7ec76 / 40f208e，都没推）。
⛔ 但那十一条一条都没上手验过。

这一窗按这个顺序走，⛔ 别自己先动手重构：

一、先验，不写代码。照 §10.2（六条）和 §11.2（五条）逐条问 Ocean，他说哪条不对就改哪条。
    ⚠️ 先确认他已经 ⌘Q 重开过 Spool —— 换装不打断在跑的进程，他看到的可能还是旧的那份；
    各 AI 客户端也要各自重开一次才吃得到新二进制（记忆 isolated-verify-workflow §34）。
    ⚠️ §10.2 第 5 条（休息弹窗不许顶掉正在写的捕捉弹窗）要攒够一小时专注才验得到，
    问他要不要专门排一次（同一条记忆 §32）。

二、验完做 Q 批（四条，WORKPLAN §2.Q1–§2.Q4）。⭐ 存法他已经拍了：
    「加一列，不开新表」⇒ blocks.ref_note + blocks.gist_by + proposals.ref_note，
    库 v27 → v28。⛔ 别再提新表那条路，也别把这一条当成「以后都能往 blocks 加列」。
    开工顺序按依赖排：Q1（库 + MCP 契约 + pack 两个渲染器）→ Q2（set_block_gist）
    → Q4（「摘要」页签）→ Q3（搜索命中提示更正，最小的一条，我加的，Ocean 可以划掉）。

    Q 批里三件最容易漏、漏了没有症状的：
    - client.ts 的 SCHEMA_VERSION 和 mcp.rs 的 EXPECTED_SCHEMA_VERSION 要一起改成 28。
      ⚠️ 守卫测试只在 cargo test 里跑，cargo check 看不出来；漏了的话装上去之后
      所有 AI 客户端的写入工具会当场全部拒绝请求。
    - 新 MCP 工具必须带 annotations（记忆 mcp-tool-annotations-required），
      而且必须同时改 OPENERS / INSTRUCTION_HEADER / INSTRUCTION_BODY 的路由文案
      （记忆 mcp-tool-routing-required）—— 只改 schema 不改路由 = 第三方客户端永远不调它。
    - pack 的两个渲染器（assemble.ts 和 mcp.rs 的 render_block）必须逐字节一致，
      pack 字节一变，assemble.test.ts 里那几条 golden 跟着改。

三、Q 批做完之后才是渠道（WORKPLAN §1 现序）。

开工前先自己核三件（⛔ 别照文档抄，自己去代码里看）：
- correctionsBySource 现在怎么判断一句原话落在正文还是批注，正文和批注同时命中时谁赢；
- CaptureOverlay.tsx 里被攒下来的那张休息卡，是哪几条出口把它放出来的（少一条 = 休息丢了）；
- gist 现在一共在几个地方被读（搜索命中 / 引用行 / pack），Q2 改完之后哪几处会跟着变。

红线提醒：⛔ 不做悬浮窗；⛔ 浏览位置不许按 scrollTop 存；⛔ 用户不能看到测试环境的 Spool；
⛔ 提交不许带任何署名（CLAUDE.md §5，自查 grep 会误报，见记忆
attribution-grep-false-positive）；⚠️「别往 blocks 表加列」现在有一个明确的例外，
就是 Q 批那三列 —— 别扩大到别的地方。
```

---

## 7. 归档在哪

`~/Library/Application Support/spool-compress-sweep/2026-08-24-round6/`
⛔ **不进仓库**（里面是真实的选校名单、成绩、个人材料）。清单见上一版交接 §6，没变。

---

## 13. 🆕 08-25 深夜：`Q 批` 四条全做完了（**✅ 已提交，⛔ 一条都没验**）

> **这一窗做了什么**：Ocean 说 §10 / §11 那十一条他已经重开验过、没问题（§10.2 第 5 条
> 那个「攒够一小时」的他明说不用专门排），所以直接进了 `Q 批`。

### 13.1 三个提交

| 提交 | 内容 |
|---|---|
| `16f7f20` | **Q1** —— `blocks.ref_note` / `blocks.gist_by` / `proposals.ref_note`，库 v27 → v28 |
| `cf45a1e` | **Q2 + Q3** —— `set_block_gist`；搜索命中里 `corrected` / `gist_predates_the_correction` |
| `74b6d28` | **Q4** —— 「块摘要」页签 |
| `a24d050` | 顺手修的：周回顾那一页印出整条 NDJSON（见 §13.4） |

⛔ 四个都没推。TS 681 / Rust 134 全绿，`npm run build` 通过。
⛔ **`/Applications` 里那一版还是 08-24 那份** —— 要重新装才验得到。

### 13.2 ⛔ 交接单里有一句是错的（§12 那份提示词）

> 「gist 现在一共在几个地方被读（搜索命中 / 引用行 / pack）」

**pack 根本不读 `gist`。** 只有两处：搜索命中（`query.ts` + `mcp.rs`）和引用行
（`CitationLine.tsx`）。pack 的 `↩ cites:` 走的是 `block_label`（批注 → 正文前 40 字
那把梯子），`gist` 的 schema 描述里也明写着 "It never appears in packs"。
⇒ **`Q1` 是往 pack 里放这类字段的第一次**，而 `Q2` 改完 `gist` 不会波及 pack。

### 13.3 三件当时定了、Ocean 应该知道的

1. **引用的理由是「必须给」的**（他选的推荐项）：`ref_block_id` 且不是 `corrects` ⇒
   不给 `ref_note` 直接拒。⚠️ 老客户端下次写引用会先吃一个拒绝 —— 那是有意的，
   和 08-25「更正必须给准星」同一形状，拒绝话术里写清了该写什么。
2. **`Q3` 做了**（他选的推荐项）。⭐ 那个「摘要是不是停在更正之前」的判断**不用新列**：
   `gist_by IS NULL` 就等于「这一句是 `add_block` 那一刻写的」，而更正必然在那之后。
3. ⚠️ **页签叫「块摘要」，不是「摘要」。** 他说的是「摘要」，但那两个字在**同一屏上**
   已经被占了两次（`ThreadHeader` 的「摘要 / 全记录」、项目头上的「＋ 写一句话摘要」），
   英文那边更硬 —— `'摘要'` 这个 i18n 键已经是 `'Digest'` 了。**他可以改回去，我这边改一行。**

### 13.4 🆕 周回顾那一页印出整条 NDJSON —— 已修（`a24d050`）

他贴回来的是从 `{"type":"system",…}` 一路到 `{"type":"result",…}` 的整条事件流。

病根一行：`engine.rs` 非零退出那个 `match` 里 `EngineKind::Claude => None` ——
claude 失败时一个字都不解析，掉进兜底「stderr 空就把整个 stdout 端上去」，
而 claude 的失败**全写在 stdout 的事件流里**、stderr 是空的。那一整条流成了 `detail`，
存进 `engine_runs.detail`，再被 `ReviewBoard` 一字不差地印出来。

修法不是新写解析器：`parse_claude_stream` 早就读得懂那一行，只是退出码为零时才被调用。
⚠️ **这只修「看到的是什么」。** 403 本身是他那台机器上 `claude` CLI 的认证问题
（`apiKeySource: none`），不是 Spool 的事。

### 13.5 ✅ DeepSeek 跑周回顾 —— **做完了（`d9fb8c3`），⛔ 未验**

> **他的原话**：「考虑到 CLI 只支持 codex 和 claude，这两个模型太贵了（gemini 的能力有限，
> 且额度少）加入 deepseek 的周总结，总结的 model 可以让用户自行选择。」

**先摸出来的现状 —— 一大半东西已经在了**：

| 已经有的 | 在哪 |
|---|---|
| sidecar 会说 OpenAI 兼容协议，默认就是 DeepSeek | `src-tauri/sidecar/src/main.rs` |
| 「模型」是**用户已经能自己填**的一个设置框 | `ApiEngineConfig.tsx:120`，默认 `deepseek-v4-flash` |
| 形态 C 已经跑着两件事：压缩、过期检测 | `api_engine.rs`（`compress_pack_via_api` / `stale_scan_via_api`） |

⇒ **真正缺的只有一件：周回顾没有形态 C 那条路。** 它今天只走形态 B（用户自己装的
`claude` / `codex` / `gemini` CLI）。

⚠️ **动手前有一个必须先问 Ocean 的**：两条路拿材料的方式根本不一样。
形态 B 是把 MCP 交给 CLI，让它**自己一轮轮去翻**（`get_digest` → `list_threads` → `get_pack`）；
形态 C **只有一发** —— 一个 prompt 进去，一段文字出来，没有 MCP。
所以「这一周的材料」得由 Spool 自己在本地拼好、一次性发出去，而**拼多少**是要拍板的：

- **甲**：只发 `get_digest` 那一份（跨项目、按周截断）。便宜、稳，⛔ 但深度不如 CLI 那条路。
- **乙**：digest + 本周有动静的项目各一份 pack。接近 CLI 的效果，⚠️ 但一份 pack 就上万字，
  几个项目叠起来会撞上下文上限，而且**按字数计费**。
- **丙**：分两步（先 digest 挑出项目，再每个项目一发，最后合稿）。最接近 CLI，
  ⚠️ 但那是好几发，钱和时间都乘上去。

⭐ 另外一条**不用问就该做**的：`ReviewBoard` 那根引擎条现在只认形态 B，
要能选「用 CLI」还是「用 API」—— 压缩那一页早就是两条路并行的，照抄它的形态。

---

#### ✅ 他拍的是第四条（比上面三条都好）

> 「digest 加上本周有动静的项目的**新加入的所有 block**（不是整个 pack），
> digest 当历史，新 block 为新动作。」

⭐ **量跟着这一周真实的活动走，⛔ 不跟着库的大小走。** 一个攒了两年的项目这一周没动，
就一个字都不发；而「整份 pack」那条路是历史越厚发得越多，钱和上下文一起爆。

#### 做了什么（`d9fb8c3`）

| 在哪 | 什么 |
|---|---|
| `mcp.rs` | `WEEKLY_API_SYSTEM` 新提示词 + `weekly_messages_for_api` + `weekly_fresh_blocks_text` + `weekly_material_for_api` |
| `api_engine.rs` | `weekly_review_via_api`（形态 C，⛔ 一个字都不往库里写） |
| `compress.ts` | `weeklyReviewViaApi` |
| `ReviewBoard` | 第二个按钮；那根条子的闸从「只认 CLI」改成「随便哪条准备好了」 |

⛔ **没有复用 `weekly_review_prompt_text`。** 那份的第 1 条写着「要展开某个项目，先用
`get_pack` / `get_blocks` 补读」—— 这条路上**没有 MCP**，那是一句做不到的话，
而一个被要求去调不存在工具的模型**会开始编**它本该去读的东西。
新提示词第 4 条明说「你手上只有这两段材料，没有任何可以调用的工具」。

⭐ 「模型自行选择」**没有做第二套设置** —— 设置里那个「模型」框（`apiModel`，
默认 `deepseek-v4-flash`）本来就是用户自己填的，按钮上把它的名字说出来就够了。

#### ⛔ 验它的时候看这几条

1. 设置里填好端点 + key，**不装任何 CLI 也应该看得见按钮**（以前看不见）。
2. 两个按钮并排时，各自写着跑的是谁（`回顾这一周（用 claude）` / `（用 deepseek-v4-flash）`）。
3. 这一周没动过的项目，回顾里应该写「这一周没动」，⛔ 而不是拿历史编一段本周动态。
4. 跑完那一条和 CLI 跑的躺在同一张列表里，`engine` 那一栏是**端点回报的真模型名**。

---

## 14. 🆕 08-26 换装之后 Ocean 提的两件（`W 批`，**下一窗做**）

> ✅ **换装已完成**（2026-08-26 01:14）。`/Applications/Spool.app` 是含 Q 批 + 周回顾两件的
> 那一版，签名 `Developer ID Application: Hanze JIN`（所以输入监听/辅助功能**没掉**），
> `spctl` 报 `Unnotarized Developer ID` = 本机 dev build（§33）。
> 备份两份在桌面：`spool-snapshot-20260826-011338-pre-v28-migration.db`（真库 v27 / 54 脉络 / 335 块）
> 和 `Spool-old-20260826-011338-pre-qbatch.app`。
> 探针验过新二进制：22 个工具、`set_block_gist` 带 annotations、`ref_note` 进了两个 schema、路由文案也在。

### 14.1 他的原话

> 1. 每周自动回顾无法选模型，还有周回顾为什么没有 codex？
> 2. 周回顾每周的新进展应该组合在一起，以周为单位呈现，现在周回顾是线性的。

### 14.2 ⛔ 第一件的根子有**四条**，不是一条（已在代码里逐条核过）

| # | 事实 | 在哪 |
|---|---|---|
| ① | 模型选择器 `showModels = status?.selected === 'claude'` —— **只对 claude 出**。他设置里选的是 **codex** ⇒ 那一行根本不画 | `RightRail/EngineBar.tsx:127` |
| ② | ⭐⭐ 那个选择器住在**右边栏**，而**钉住的视图（周回顾 / 项目管理）根本不挂右边栏** ⇒ 从周回顾那一屏走过去，**没有任何地方能选模型**。**和 08-25「周回顾没法暂停」是同一个根子** | `App.tsx:375`（`pinnedView ? null : …`） |
| ③ | 自动那条路调的是 `enqueue('', '', 'weekly_review', timeoutSecs)` —— **签名里根本没有模型这一项**；模型在 store 里被写死成「gemini 用 `aiModelGemini`，其余**一律**用 `aiModelClaude`」。⇒ 选 codex 时发出去的是 **claude 的模型名**（Rust 按 codex 名单过滤掉，无害，但结果是 codex 永远跑账号默认） | `useAutoMaintain.ts:71` / `engineStore.ts:283-287` |
| ④ | 自动那条路的闸是 `canShowEngineActions({cliAvailable,…})` ⇒ **自动回顾只会走 CLI，永远用不上 08-25 刚接的 API / DeepSeek 那条路** | `useAutoMaintain.ts:48` |

⇒ **「无法选模型」不是一个 bug，是四条叠在一起。** ⛔ 只修①（把选择器对 codex 也打开）
解决不了他的问题：他站在周回顾那一屏上，而那一屏**看不见右边栏**。

### 14.3 「周回顾为什么没有 codex」—— 答案是**有意留空**，但界面没说

`ENGINE_MODELS.codex = []`，注释写着这是**测量结论**（`EngineBar.tsx:32`）：
codex 的模型名来自**服务端目录、本地不校验**，写在这里的任何名字都会在 API 那头才失败，
而不是在点击那一刻失败。

⇒ 不是漏了。但**今天的表现是「一个选择器都不出」，用户读到的就是「坏了」**。
⚠️ **这一条要 Ocean 拍**（14.6 第 1 问）。

### 14.4 第二件：周回顾要按周分组

现在是一条 `listRunsForAction('weekly_review')` 拉 50 条、平铺成一个 `<ul>`
（`ReviewBoard/index.tsx:65` + 底下那个 `ReviewItem`）。

⚠️ **一周里会有好几条**：自动跑一次 + 手动点几次 + 失败重试，都落在同一张
`engine_runs` 表里。所以「一周」那一格里要能装多条，⛔ 不是「一周一条」。
⚠️ 失败的那几条现在也一起平铺着（`outcome !== 'ok'` 渲染成「这次什么也没回来」+ CLI 原话）——
分组之后它们该怎么摆，是 14.6 第 2 问。

### 14.5 ⛔ 开工前先自己核三件（别照这份文档抄，去代码里看）

1. `weeklyReviewDue()` 判「这一周跑过没有」用的是**哪个时间基准** —— 是「距上次满 7 天」
   还是「本周内跑过」？⭐ 这一条直接决定 14.4 的分组该按什么切周（自然周 / 滚动 7 天），
   两边**必须是同一把尺子**，否则界面上「这一周」那一格和自动跑的「该跑了」对不上。
2. `engine_runs` 里 `weekly_review` 那些行的 `thread_id` 是不是真的都为 NULL
   （§3.4 说是，去库里数一遍）—— 分组要按时间切，一条挂错项目的行会掉进错的格子。
3. `ReviewBoard` 那根引擎条现在有几个按钮、各自的闸是什么
   （08-25 刚加了 API 那条，`engineReady || apiOn`）—— 模型选择器要塞进这根条子里，
   ⛔ 别新开一排。

### 14.6 ⚠️ 要 Ocean 先拍的两件（⛔ 没答案之前别写那部分代码）

1. **codex 的模型怎么选？** 甲：给一个**自由填写**的框（像 API 那条路的「模型」框那样），
   填错了在 API 那头失败，界面把原话报出来；乙：继续不给选，但**在界面上说一句为什么**
   （「codex 的模型名它自己不校验，Spool 给不出可靠的单子」）。
2. **按周分组之后，失败的那几条摆哪儿？** 甲：和成功的混在同一格里，按时间排；
   乙：折起来，那一格上写「另有 2 次没跑成」，点开才看。

---

## 15. ⭐⭐ 下一窗开工提示词（**照抄这一份**，⛔ 不是 §12）

```
读 docs/HANDOFF-NEXT-2026-08-24.md §13、§14（两节），以及 docs/WORKPLAN-2026-08-22.md
的「§1 · W 批」。

现状一句话：Q 批四条 + 周回顾那两件（NDJSON 病根、DeepSeek 走 API）已经全部做完、提交、
并且换装到 /Applications（七个 commit：16f7f20 / cf45a1e / 74b6d28 / a24d050 / 6961b42 /
d9fb8c3 / 34911b8，都没推）。
⛔ 但那些一条都没上手验过。

这一窗按这个顺序走，⛔ 别自己先动手重构：

一、先验，不写代码。⚠️ 先确认他已经 ⌘Q 重开过 Spool —— 换装不打断在跑的进程
    （记忆 isolated-verify-workflow §34），而且**库的 v27 → v28 迁移是在他重开那一刻才跑的**。
    重开之后先问一句：库还在不在（54 条脉络 / 335 块）。
    ⚠️ 各 AI 客户端也要各自重开一次，否则它们还跑着 v27 的二进制，写入工具会被拒绝
    ——那是设计好的护栏，不是坏了。
    逐条问他这七条：
      1. 项目里多了个「块摘要」页签（⚠️ 名字是「块摘要」不是「摘要」，理由见 §13.3 第 3 条，
         他可以要求改回去，改一行）；点进去一行一块，没有摘要的块也在列表里，就地能改。
      2. 让某个 AI 客户端写一条**引用**、故意不给理由 → 应该被拒，拒绝里要说清该写什么。
      3. 引用行上，AI 写的理由前面有个 `∵`；老引用（v28 之前的）照旧显示 gist 或批注。
      4. 让 AI 更正一块带摘要的块 → 再搜那一块 → 命中里应该带 corrected 和
         gist_predates_the_correction；然后让它调 set_block_gist 改掉，旗子应该落下来。
      5. 周回顾那一屏：CLI 那个按钮 + API 那个按钮，各自写着跑的是谁。
         ⚠️ 只填了 API key、没装 CLI 的用户也该看得见按钮（以前看不见）。
      6. 用 API 跑一次周回顾：这一周没动过的项目应该写「这一周没动」，
         ⛔ 而不是拿历史编一段本周动态。
      7. 故意让 claude CLI 认证失败 → 周回顾那一页上应该只有一句
         「Failed to authenticate…」，⛔ 不是整条 NDJSON。

二、验完做 W 批（两条，见 §14）。
    ⛔ **先问 §14.6 那两件，没答案之前别写那部分代码。**
    ⚠️ W1「无法选模型」的根子有四条（§14.2），⛔ 只修「把选择器对 codex 打开」
    解决不了他的问题 —— 他站在周回顾那一屏上，而那一屏**看不见右边栏**。
    ⭐ 落点多半是「把模型选择器搬进 ReviewBoard 那根引擎条」，和 08-25 那个「停下」按钮
    同一个理由、同一个位置。
    ⚠️ 自动那条路（useAutoMaintain）还要多做一件：它今天的闸只认 CLI，
    ⇒ 「自动回顾用哪条路 / 哪个模型」是一个**新设置**，不是把现有的挪一下。

    W 批里三件最容易漏、漏了没有症状的：
    - engineStore 那行「gemini 用 aiModelGemini，其余一律用 aiModelClaude」
      （engineStore.ts:283-287）——加了 codex 的模型之后这里必须跟着改，
      否则用户填的值根本发不出去，而界面上看着是填好了的。
    - ENGINE_MODELS 的名字必须和 engine.rs 的 CLAUDE_MODELS / GEMINI_MODELS 一致
      （那边才是真正校验的地方，只加在前端 = 静默失效，EngineBar.tsx:36 写着这条）。
    - 分组切周的尺子必须和 weeklyReviewDue() 是同一把（§14.5 第 1 条），
      否则界面上「这一周」那一格和自动跑的「该跑了」对不上。

三、W 批做完之后才是渠道（WORKPLAN §1 现序）。

开工前先自己核三件（⛔ 别照文档抄，自己去代码里看）：§14.5 那三条。

红线提醒：⛔ 不做悬浮窗；⛔ 浏览位置不许按 scrollTop 存；⛔ 用户不能看到测试环境的 Spool；
⛔ 提交不许带任何署名（CLAUDE.md §5，自查 grep 会误报，见记忆
attribution-grep-false-positive）；⚠️「别往 blocks 表加列」的例外只有 Q 批那三列，
⛔ 别扩大；⛔ 换装必须带 APPLE_SIGNING_IDENTITY，否则 TCC 授权当场作废
（记忆 isolated-verify-workflow §6-bis）。
```

---

## 16. 🆕 08-26 这一窗：`W 批` + `X 批` 做完了（`3e6b8a5` / `5894dbc`，✅ 已换装，⛔ 未验、未推）

> **这一窗的顺序**：先做了 §14.5 那三条自查（结论和文档有出入，见 16.1），
> 顺手从库里结掉了七条验收里的两条（16.2），拿到 Ocean 三个拍板（16.3），
> 然后一口气做完 `W1`–`W4` 加两条顺带修的（16.4）。

### 16.1 §14.5 那三条自查 —— 两条和文档写的不一样

| # | 文档说 | 代码里实际是 |
|---|---|---|
| 1 | 「是『距上次满 7 天』还是『本周内跑过』？」 | **距上一次成功满 7 天**（`engineRuns.ts` `MAX(finished_at) WHERE outcome='ok'`）。⭐ 它是一根**滚动**的线，⛔ **划不出固定的周格子** —— 两把尺子对不上是必然的，不是没对齐 |
| 2 | `weekly_review` 的 `thread_id` 都是 NULL | ✅ 对。真库 8 条（3 ok / 3 failed / 2 cancelled），一条都没挂项目 |
| 3 | 引擎条上有几个按钮 | CLI 按钮（闸 `engineReady`）+ API 按钮（闸 `apiOn`）+「停下」+ 自动开关；整根条子闸 `engineReady \|\| apiOn`。**没有模型选择器** |

⛔ **§14.2 ① 那一条是错的。** `showModels = status?.selected === 'claude'` 只管**思考力度**
那一行（而它本来就 `EFFORT_PICKER_ENABLED = false`）；模型选择器的真闸是
`models.length > 0`，所以 **gemini 一直是有选择器的**。codex 没有，是因为
`ENGINE_MODELS.codex = []` —— 那才是唯一的原因。

### 16.2 七条验收里，两条从库里就结掉了

⭐ **他 08-26 01:20:48 已经用 DeepSeek 跑成过一次周回顾**（`engine` 栏记着
`deepseek-v4-flash`，44753 进 / 11313 出，99 秒）。那条只有新二进制跑得出来，
⇒ 换装成功、`weekly_review_via_api` 那条路通了，两件同时确认。

- **第 6 条（不许拿历史编本周）✅ 过了**：那份回顾结尾是「其余项目基本这周没动，
  像 Georgia Tech MS CSE 还停在一条置顶锚点上」。
- **第 5 条**：代码这侧确认闸已经是「随便哪条准备好了」，两个按钮各带引擎名。

⚠️ **第 7 条（NDJSON）验不了旧数据**：库里那条整条事件流是 `hr1KNB…`（08-25 21:32），
跑在**老二进制**上，它不会自己变短。要重新让 claude 失败一次才看得到。

⭐ **另外挖到一条**：`hr1KNB…` 那条列表上写着 `codex`，但 `detail` 里那条流是 **claude** 的
（工具名一看就是 Claude Code）。病根在 `engineStore.ts:260` —— 跑失败时记进库的引擎名是
**设置里选的那个**，不是真跑起来的那个（`run_action` 会重新 `detect()`，偏好指着没装的引擎
会回退）。⇒ **「周回顾为什么没有 codex」有几次其实压根没在跑 codex。** 已修。

### 16.3 Ocean 08-26 拍的三件

| 问的 | 他答的 |
|---|---|
| codex 的模型怎么选 | **乙** —— 继续不给选，但界面上说清楚为什么 |
| 按周分组后失败的摆哪儿 | ⭐ **两个选项都没选**：「失败的可以删除记录，另外跑成功的」⇒ 折起来**并且**给删除按钮 |
| 切周的尺子 | **甲** —— 界面按自然周，自动那条（滚动 7 天）不动，界面自己把话说明白 |

⭐ 他同时提了一件新的：**API 剩余额度**（「摩擦还是比较大，用户需要反复查看余额，
但是尽可能保住不出网叙事」）—— 已写成 `X 批` 进 `WORKPLAN`，⛔ **没排进顺序**。

### 16.4 逐条去处（提交 `3e6b8a5`）

| 序 | 在哪 | 做了什么 |
|---|---|---|
| `W1` | `lib/engine/models.ts`（新）· `ReviewBoard` · `EngineBar` | `ENGINE_MODELS` 搬出来给两处共用，选择器进了周回顾那根引擎条。⚠️ `modelKeyFor` 把「哪个引擎写哪个 key」收成一处 |
| `W2` | `settingsStore` · `useAutoMaintain` · `lib/engine/gate.ts` | 新设置 `autoReviewRoute`（默认 `cli`）。⭐ **模型没开第三份设置** —— 走 CLI 用 CLI 那份，走 API 用 `apiModel` |
| `W3` | `models.ts` `CODEX_NO_MODELS` | 两处界面各说一句为什么 codex 没单子 |
| `W4` | `lib/weeks.ts`（新）· `ReviewBoard` | 自然周分组；没跑成的折起来 + 可删（`deleteRun`，⛔ 闸在 SQL 那侧：`AND outcome <> 'ok'`） |

**两条顺带修的**：

1. `engineStore.ts` —— 失败时记的引擎名改读**探针的答案**（`status.selected`，和
   `run_action` 里那次 `detect()` 同源），⛔ 不再读设置。
2. `useAutoMaintain` —— **失败刹车**（`RETRY_MS = 1h`）。⚠️ 病根：`weeklyReviewDue`
   只认跑成的，所以一次失败之后它**一直**是 true，而这个循环每十分钟看一次。
   CLI 那边只是白转；**API 那边是每十分钟烧一次钱**，而用户什么都看不见。

⭐ **`engineStore.ts:283-287` 那行『其余一律用 aiModelClaude』最后没改** —— 因为
Ocean 选了乙，codex 仍然没有模型，那行发出去的值仍然会被 Rust 按名单丢掉。
⚠️ **codex 哪天有了单子，这行必须跟着改**，`models.ts` 的 `modelKeyFor` 上写着这条。

### 16.5 🆕 `X 批`（API 余额）—— Ocean 同一天拍的，**也做完了**（`5894dbc`）

> **他的原话**：「能不能拿到用户的 api 剩余额度，目前使用 api 摩擦还是比较大，
> 用户需要反复查看余额，但是尽可能保住不出网叙事。」
> **他选的刷新口径**：点一下查一次 + 跑完 API 顺带刷。⛔ **不定时轮询。**

⭐ **不出网叙事一个字没改**：走的还是那个 `spool-ai` 子进程、还是用户自己填的端点。
那句话现在的形状是「只有 spool-ai 会出去，而且只在你要它出去的时候」。
⛔⛔ **哪天有人想给它加个定时器，那才是真的推翻，要先问 Ocean。**

| 在哪 | 什么 |
|---|---|
| `sidecar/src/main.rs` | 第二种请求 `kind:"balance"` → `GET {base}/user/balance`；`Envelope::Balance` |
| `api_engine.rs` | `api_balance` + `parse_balance` + `BalanceOutcome`。⛔ **不占 `RUNNING`、不挂 `CHILD`**（理由写在函数注释里，两条都会造成真事故） |
| `stores/balanceStore.ts` | 四种状态。⭐ 「这家不报余额」是**独立一种**，⛔ 不是失败、更不是 0 |
| `components/ApiBalance.tsx` | 那一行，两处共用（设置里 key 底下 / 周回顾引擎条上） |

⭐ **地址实测过了**：拿一个**假 key** 打 `api.deepseek.com/user/balance` 回 **401 而不是 404**
⇒ 这个地址是真的存在。⛔ 全程没动过任何真 key。
⚠️ 别家端点大多没有这个接口 —— 那时候界面写「这家不报余额」。

⚠️ **金额从头到尾是字符串**，⛔ 任何一层都别折算成 number：`"25.50"` 过一次二进制浮点
就可能变成 `25.499999`。

### 16.5-bis ✅ 换装完成（2026-08-26 11:47）

- `/Applications/Spool.app` 已是 `5894dbc` 那一版；签名 `Developer ID Application: Hanze JIN`
  （⇒ **输入监听 / 辅助功能没掉**），`spctl` 报 `Unnotarized Developer ID` = 本机 dev build（§33）。
- 备份两份在桌面：`spool-snapshot-20260826-114634-pre-wx.db` 和 `Spool-old-20260826-114634-pre-wx.app`。
- 换装后真库**一条没少**：v28 / 54 脉络 / 335 块 / 22 个工作区，和装前一模一样。
- 探针（`SPOOL_DATA_DIR` 指向临时库）验过新二进制：22 个工具、`set_block_gist` 在。
- ⚠️ **旧 bundle 还挪在 `/Applications/Spool.app.replaced-20260826-114634`** ——
  正在跑的那几个进程还执行着它里面的 inode，⛔ 他重开之前别删。重开之后删掉就行。

⭐⭐ **这一次和上一次不一样：AI 客户端不用重开。**
`W`/`X` 两批**没有动 schema、没有加 MCP 工具**，库还是 v28 —— 上一次要各客户端重开，
是因为 v27 → v28 那次迁移。这一次只有 **Spool 自己要 ⌘Q 重开**（换装不打断在跑的进程，§34）。

### 16.6 验收单（换装之后，一条一分钟）

1. 周回顾那一屏，CLI 按钮旁边多了个**模型选择器**（选 claude 时是 sonnet / haiku）。
2. 设置里把引擎切成 codex → 选择器不出，但下面多一句话说为什么。
3. 打开「每周自动回顾一次」→ 旁边出现「自动那次用 …」，下面一行写着走谁、还有几天。
4. 列表按周分成一格一格，本周那格标着「这一周」。
5. 没跑成的那几条折在「另有 N 次没跑成」里，点开每条右边有「删掉」，删完不再出现。
6. ⛔ **跑成功的那三条没有删除按钮**（SQL 那侧也拦着）。
7. 让 claude 失败一次 → 列表里那条的引擎名应该是 **Claude Code**，⛔ 不是他设置里的 codex。
8. 设置 → API 那一节，key 底下多了一行「查一下余额」；点它应该出现 `¥…`。
9. 周回顾那一屏，DeepSeek 那个按钮旁边也有同一行；跑完一次之后它应该**自己刷新**。
10. 把接口地址改成一个不报余额的端点 → 应该写「这家不报余额」，⛔ 不是 0、也不是一句报错。

