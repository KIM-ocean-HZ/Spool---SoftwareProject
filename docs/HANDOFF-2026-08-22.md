# 交接 —— 2026-08-22

> **每次开工先读这一份，只读这一份。**
> 要知道某一批具体做什么 → `WORKPLAN-2026-08-22.md`。
> 要知道某件事当初为什么这么定 → 那份的 §6「已经做完的事去哪查」。
> ⛔ **不要去翻 `archive/` 里的东西来开工。**

---

## 1. 现在的确切状态

| 项 | 值 |
|---|---|
| 当前分支 | `main` |
| HEAD | `5f7e668`（第二轮第 2/3/6 条）之后还有一笔文档。⚠️ **本地领先 `origin/main` 十五个提交，还没推** |
| 工作区 | ✅ **干净**。第三窗做掉六条，见 §3 |
| 版本号 | `0.6.1`（`package.json` + `tauri.conf.json`）—— 发版那天改 `0.6.2` |
| 已发布的 | **v0.6.1（2026-08-19）** —— ⛔ **压缩这一整块（含今天三窗的全部改动）一样都不在里面** |
| 装机的 | ⚠️ **19:51 那份，比代码旧了两笔** —— 第二轮的三条改动（按字对 / 指到句子 / 结论排表）还没换装。之前那次是 ✅ **2026-08-22 19:51 换的**（`0.6.1` dev build，`Unnotarized Developer ID`，和换之前同一个签名身份，两个 TCC 授权没掉）。已核：identifier `com.oceanjin.spool`、`codesign --verify --deep --strict` 通过、真库仍是 **13 工作区 / 45 项目 / 218 块 / user_version 23**、`--mcp` 子进程 **25 个一个没杀**、认领同一个 bundle id 的仍是 **16 个**（换下来那份放在本窗的临时目录，没进 Desktop）。装前备库：`~/Desktop/spool.before-d7d6-20260822-194950.db` |
| 官网 | spoolapp.org **已部署最新**（run 32552301537），这一窗没动官网 |
| 真实外部用户 | **0**（看板上那 32 次全是 Ocean 自测） |

---

## 2. 这一窗做什么

**R 批**（压缩第二轮，Ocean 用过第一轮之后写的八条）。⛔ **它排在 E3 前面** ——
E3 的界面本来就要进 R4 造出来的那个页签。全文在 `COMPRESS-UX-R2-2026-08-22.md`，排期在工作计划 §1.R。

| 序 | 做什么 | 一句话 |
|---|---|---|
| **R4** | 整理工作区：项目内「内容 / 整理」页签 | ⭐ 先做这个。他每次用都撞到：关掉找不回来、进去就看不了别的项目 |
| **R5** | 批注根本不发给 AI | ⭐ 把「求模型守规矩」换成「结构上做不到」。六类警告里有五类根源都是「那些字被送出去了」 |
| **R1** | 压缩稿写回库 + 原文跟着块走 | ⛔ 他明说解了「库里一个字都不动」那条锁。⚠️ 数字硬闸门不动 |
| **E3** | 作废检测接产品 | ⛔ 最后。前置仍然是 key 挪进钥匙串 |

⚠️ **两件他已经拍了，别再问**：整理工作区走**项目内切页签**（不是另开一个总台）；
原库备份走**原文跟着块走**（不是整库快照，快照做不到一一对应）。

⚠️ **挡在发版前面的 B0 还在**：官网和 README 有**十处**「Spool 不需要 key / 永不联网」的说法。

## 3. 上一窗（2026-08-22 第三窗）做了什么 —— 六条

**五笔提交，全在本地 `main` 上，⚠️ 还没推。**

| 提交 | 是什么 | 里面最要紧的一条 |
|---|---|---|
| `06c07bf` | **D8 的另一半** —— 划线和底色直接铺在两栏正文上 | 底下那个「压掉 X 句 · 它自己写了 Y 句」的折叠**整块删掉**。⚠️ 比的是 `body` 不是 `raw`：拿 `raw` 比，头行的差异会变成正文第一段的划线，指的却是屏幕上没有的东西 |
| `2e8ba70` | **D7 加回去 + D-b 数字硬闸门** | 补回去的是**整行**，落脚点是「它前面最后一个原样留下来的行」。⛔ 补不回去的必须报出来 —— 假装全补上了，就是核对界面自己在撒谎 |
| `b4150a0` | **D-a 重复度探针** | ⚠️ 走 Rust 那套 `find_similar_blocks`（三元组 Jaccard ≥ 0.6），**没有在 TS 里另写一个判断** —— 界面上的数和别的 AI 通过 MCP 看到的必须是同一个 |
| `97d0735` | **D-c 坏结果自动重跑一次** | ⚠️ **两次的账加起来报**。只报第二次那笔，「这一次花了多少」当场变成假话 |
| `c454f0f` | **D6 四带改名 + 界面撤 emoji** | ⚠️ **pack 表头里的记号一个没动** —— 那是给收件 AI 读的格式契约。所以 golden 不用重跑，Rust 一个字没改 |

**六条逐条**：

| | 做了什么 |
|---|---|
| **D8-b** | Ocean 原话「我还是想直接在原文上去划线，直接在压缩稿显示新加的内容」。左栏原文里没进压缩稿的那几段当场划掉，右栏它新写的那几段当场上底色，中间那个折叠没了。新增 `diffChunks`：把行级 diff 折成一侧的段落，段尾空行剪掉换成下一段头上的 gap。⛔ 拼不回原样就返回 `null`，退回不带标记的正文 |
| **D7** | 每一处丢掉的数字/日期后面一个「从原文加回去」。⭐ **纯本地**：那几行在原文里都还在，不问模型、不出网、不花钱。补过的那一份走 `session.patched`，⚠️ **报账那一行照旧读 `outcome`**（补一行字不花钱），⛔ 补过的那份**不进实测台账** |
| **D-b** | `numbersGateOpen`：丢了数字/日期就是不够格进库，⛔ **写入解锁之后这条也不放宽**，而且不许「用户点了确认」就放行。页脚把这条写在用户看得见的地方 |
| **D-a** | 新 Tauri 命令 `compress_duplicate_probe`。开桌子时顺手算，⚠️ **不挡开桌子**；⛔ 算不出来就什么都不显示，不编一个数。报的是「**能并掉几块**」不是组数。没有重复就直说「这一次大概压不短多少，钱可以省下来」 |
| **D-c** | 四条结构性判据（切不出块 / 重复编号 / 块数对不上 / 剩 >95%）命中就**自动重跑一次**，⛔ 只一次。⛔ **丢数字不在判据里** —— 那一类有「加回去」补，不该再花第二笔钱。夜里那一批走同一条路 |
| **D6** | 四个名字一起换：你自己写的 / 来源材料 / **AI 写的整理稿** / **和 AI 的对话**。卡片头上的 emoji 记号撤掉。顺带补上一个洞：这八条**从来没进过 EN 词表**，英文界面上显示的是中文 |

**同一窗晚些时候又做了第二轮的三条**（提交 `5f7e668`）：

| | 做了什么 |
|---|---|
| **R2-2** | **按字对，不是按段**。同一处的删行和加行先配对（= 同一句被改写），配上的按字对一遍，只标真正变的那几个字。⚠️ 被改写的那一行不走 markdown 渲染 —— 渲染器会吃掉 `**` 和 `==`，字和字就对不上了。⚠️ 超过 1500 字的行不按字对（LCS 是 O(n·m)，一屏二十几块会卡死） |
| **R2-3** | **指到句子上**：卡片上直接列出那句话的原文，后面跟它自己的「加回去」。**补回去的那一行在右栏当场标出来**（「你加回去的」）—— ⚠️ 不特意标的话 LCS 会把它当成「没变过」，用户看不见自己刚才那一下 |
| **R2-6** | **结论排成表**：块数 / 长度 / 用时 / 花费 一个数一格；模型交代删了什么改成一条一行；「压多狠?」改成「压缩强度」 |

**每一笔提交后都跑了署名自查，六笔全干净**（窄版：只查作者/trailer）。

**做完之后全量验了一遍**：**578 个 TS 测试 + 113 个 Rust 测试全绿**（TS 多 26 个，Rust 多 2 个），
`tsc -b --force` 干净，`cargo check` 干净，`cargo clippy` **没有新警告**（前后都是 14 条），
`npm run build` 出得来。

✅ **19:51 换装做完了**，`/Applications` 那份是**前六条**（详见 §1 那一行）。
⚠️ **第二轮那三条（按字对 / 指到句子 / 结论排表）还没换装，也没真手指验过** —— 见 §4 第一条。

**一件顺手记下的**：工作计划把 D6 估成「最贵的一条，四个名字在界面 + pack 表头各一份，
golden 要重跑」。**实际上表头那份是英文**（Reference / Synthesis / Process / Personal），
Ocean 说的是中文界面那一份，只在 `band.ts` 里，而且只有核对卡片一个地方用它。

---

## 4. 等 Ocean 的

### ⛔ 还没换装，也没真手指验过

`/Applications` 那份停在 19:51，**第二轮那三条不在里面**。要看见得再换一次装
（照记忆 `isolated-verify-workflow` §21，同一个签名身份，⛔ 换装前先备库）。

换完之后挑五条看：

1. 压一次，**被改写的那一行是不是只有变了的那几个字带记号**（不再整段红 + 整段黄）；
2. 顶上四个数是不是排成了一行小表（块数 / 长度 / 用时 / 花费），AI 说自己删了什么是不是一条一行；
3. 报丢数字的时候，卡片上是不是**直接把那句话原文列出来**了，后面跟着「加回去」；
4. 点一下「加回去」，右栏那一行有没有当场出现、并且写着「你加回去的」；
5. 档位那一行的标题是不是「压缩强度」。

### 要拍的一件

**R 批的先后认不认**：`R4（工作区）→ R5（不发批注）→ R1（写回库）→ E3`。
理由写在 `COMPRESS-UX-R2-2026-08-22.md` 末尾。他原话是「接下来做 E3」，
⚠️ 但那句话是在**工作区方案定下来之前**说的 —— 而 E3 的界面本来就要进 R4 那个页签。

### 知会一件（不是问题，是代价）

**D-c 之后，一次「压缩」最多会真的发出两次请求。** 判据是结构性的（十次里有三四次命中），
命中就自动重跑一次，两次的钱都算在你账上，界面上写明白了。
⛔ 这是照工作计划 D-c 那条做的；不想要的话，那一条得由你撤。

### 不挡路的两条小选择题

在工作计划 §3.2：① 一句话摘要要不要升级（建议不升级）② 机器上那些旧 app 删哪几个。

### ✅ 已经答完的（别再问）

不急着发版（`A → D → B → 渠道 → E → F`）· E1/E2 不提前 · D1 撤销 ·
E3 算发版前的交互（留在 D 批，排最后）· 版本号 0.6.2 认可 · D-k 紧急加 ·
**D-k 的三条真手指验收 08-22 做完了，没问题** ·
A4 休息浮窗四步全成立、功能留下 · 官网问卷升成主按钮 ·
那条「提交按钮不显示」是我排错了，已撤回。

---

## 5. 动手之前

1. **产品红线** → 工作计划 §4.1。⛔ 只能由 Ocean **明说**推翻，不能作为做别的事的副作用被顺手推倒。
2. **工程护栏** → 工作计划 §4.2（这一窗新加了六条）。**每一条都曾经被写反过一次，而反过来之后测试照样是绿的。**
3. **要在本机做实机验证** → 工作计划 §4.3 先跑那个 `find`（⛔ **不是 `mdfind`**）。
   **16 个** app 认领着同一批 bundle id，`open -a <绝对路径>` 会被 LaunchServices 劫持到别的实例上，**而且不报错**。
4. **文案** → 大白话；**少用 emoji**（2026-08-22 Ocean 再次提醒）；一屏最多一个 ⚠️。

---

## 6. 下一窗结束时，这份要更新哪几处

⛔ **别在这份里堆任务** —— 任务的家在工作计划。这份只更新五处：

1. §1 那张状态表（分支 / HEAD / 工作区干不干净 / 装机的是哪一份 / 发布了什么）
2. §2「这一窗做什么」换成新的一批
3. §3「上一窗做了什么」覆盖掉
4. §4 拿到答案的划掉，新冒出来的加上
5. 做完的任务**从工作计划里删掉**（不是打勾），完成记录留在 §3
