# 交接 —— 2026-08-22

> **每次开工先读这一份，只读这一份。**
> 要知道某一批具体做什么 → `WORKPLAN-2026-08-22.md`。
> 要知道某件事当初为什么这么定 → 那份的 §6「已经做完的事去哪查」。
> ⛔ **不要去翻 `archive/` 里的东西来开工。**

---

## 1. 现在的确切状态

| 项 | 值 |
|---|---|
| 当前分支 | `main` |
| HEAD | T 批那一笔（`584d8d1`）。⚠️ **本地领先 `origin/main` 三十个提交，还没推** |
| 工作区 | ✅ **干净**。这一窗做掉 T1–T10，见 §3 |
| 版本号 | `0.6.1`（`package.json` + `tauri.conf.json`）—— 发版那天改 `0.6.2` |
| 已发布的 | **v0.6.1（2026-08-19）** —— ⛔ **压缩这一整块（含四窗的全部改动）一样都不在里面** |
| **库版本** | **v24**，真库和装机的都已经在 v24 上（08-23 迁移走完，见下一行）。⚠️ T 批**一行 schema 都没动** |
| 装机的 | ⚠️⚠️ **是 01:09 那一份，T 批的六条改动一样都不在里面** —— 真手指验收之前要再换一次装。下面这段是 01:09 那次的记录：✅ **2026-08-23 01:09 换成了第四窗的全部改动**（`0.6.1` dev build，`Developer ID Application: Hanze JIN (Q5Y5JRXZ58)`，和换之前**同一个签名身份**，授权没掉）。已核：identifier `com.oceanjin.spool`、`codesign --verify --deep --strict` 通过、前端 01:01 构建 / 二进制 01:09 链接、探针进程确认 `get_block_original` **在 tools/list 里而且路由文本点名了它**、`--mcp` 子进程 **29 个一个没杀**。装前备库：`~/Desktop/spool.before-e3-20260823-010045.db`；换下来的旧包：`~/Desktop/Spool/spool-pre-install/Spool-old-20260823-010437-pre-e3.app` |
| **真库迁移** | ✅ **v23 → v24 走完了，没事**。行数逐项一致（**13 / 45 / 218**），**218 块正文内容哈希前后相同**（`594dde407625cbf1`），`integrity_check ok`，两个新列**全空**（迁移一个字的用户数据都没写）。自动备份：`spool.pre-migration-v23-2026-08-22T17-05-09-895Z.db` |
| 官网 | spoolapp.org **已部署最新**（run 32552301537），这一窗没动官网 |
| 真实外部用户 | **0**（看板上那 32 次全是 Ocean 自测） |

---

## 2. 这一窗做什么

⭐⭐ **第五轮实测跑完了**（217 次、实付 ¥12.17），结论在
`Deepseek-API-compress-test.md` §7。**这一窗做两件事**：

| | |
|---|---|
| ① **把实测挖出来的全部要改的记进任务库** | ✅ 做完了 —— 工作计划新开一批 **`T`**（T1–T11），插在 `D` 和 `B` 之间。⛔ **发版前做完** |
| ② **按 T 批改代码** | 见 §3 |

⛔⛔ **任务不许留在这一份里。** 新冒出来的十一条**一条都没写进交接**，
全部在 `WORKPLAN-2026-08-22.md` 的 **T 批**里 —— 这一份只说「现在什么状态、这一窗做哪几件」。
（Ocean 2026-08-22：「不允许任务无限堆积在一个文件夹，这会导致上下文过长，执行不准确」。）

⭐ **今晚还有一轮**：`OVERNIGHT-TEST-PLAN-2026-08-24.md`（第六轮，183 次、约 ¥2.6、约 2.5 小时）。
**它验的就是 T 批** —— 每一格都和第五轮同格并排，⛔ 只报新数字等于没答。
⚠️ **跑之前四件里最要紧的一件**：T 批改过 Rust，`cargo test` 出来的是**另一个二进制**，
第五轮点的「始终允许」多半不认它 —— ⛔ **必须有人在场先手动起一次把钥匙串的框点掉**，
无人值守的时候弹窗就是卡死（第五轮就这么死过一次）。

⚠️ **发版仍然挡着两件**：**真手指验收**（Ocean 自己做，⚠️ 现在多了 T 批要看）和 **B0**
（官网 README 十处「不需要 key / 永不联网」）。

**三条有意留着的尾巴**（⛔ 别顺手做，理由都在工作计划里）：
**R5b**（长 pack 切批，排进 E 批一起量）· **E3 的 `scope_limited`**（等 C-a）·
**T11**（查过期不稳，在拿到第二对真值之前改哪边都是下注）。

## 3. 这一窗做了什么 —— 读完第五轮实测，排出 T 批，当天做掉十条

**一笔提交（`584d8d1`），⚠️ 还没推。** 逐条的证据在 `Deepseek-API-compress-test.md` §7。

| | 是什么 | 里面最要紧的一条 |
|---|---|---|
| **T1** | **自动重跑的判据和默认档打架** | ⭐ 判据里有一条「压完还剩 95% 以上就重跑」，而**默认档在这个库最大的项目上压完就是剩 98%** —— 十次里 **7–8 次**会自动再发一次，**两次的钱都算用户的，压完还是 98%**。⚠️ 病根不在重跑，在判据：「只删重复」这一档压完剩 98% 是**正确答案**。而且重跑用的是**一模一样的请求**，凭什么第二次就会短 —— 实测中位数说不会。长度这一条从此只对「压到最短」成立 |
| **T2** | **查过期会毁掉一条已经有的关系** | ⛔ 两半都要命：① 5 次里最稳的那条提议（4/5）指的关系**库里本来就有**，用户被要求批准一件已经批准过的事；② `setBlockSupersession` 是**覆盖式**写入，在一条 `corrects` 上点「只退旧的」会把它**降级成 `supersedes`、把旧块整条退掉** —— 而 `corrects` 当初就是因为「旧块还成立」才选的。三条提议里 **2 条**是这种 |
| **T3** | **`ok=true` 不等于拿到了完整回答** | 流式那条路（产品走的就是它）**从头到尾没读过 `finish_reason`**，非流式只在正文为空时才读。⛔ `cut_off` 那里自己写着「半份稿子看起来和『删得很狠』一模一样」，而这条路上它连 `cut_off` 都不报 |
| **T4** | **引文两把尺子** | 闸门**折叠**全角/半角标点才放行，两个 pack 渲染器和 feed 用的是**不折叠**的 `includes`。「先接受一条更正、之后再压缩这个项目」会咬：压缩改写标点（那一份换掉 **46%**），存好的引文从此对不上，**那句高亮悄悄消失，界面不报任何错**。新增 `lib/blocks/quoteFold.ts`，和 `api_engine.rs::fold_char` 同一张表 |
| **T5** | **头行上的数字会把好稿子永久锁在库外** | 条目头行方括号里那个日期是渲染器从 `created_at` 印的，「加回去」按设计**拒绝**把头行粘回正文 —— 于是硬闸门关死、「用这一份」永远点不动，**而用户无论如何都解不开**。⭐ 更硬的理由：写回库的只有 `content`，头行是重新渲染的，它**不可能丢进库里**。⚠️ 只去掉方括号那一截，头行**后面**的正文照常核对（块的第一行正文就印在头行上） |
| **T6** | **「只退旧的」的代价说出来** | 退掉一个**短**块，pack 反而**多 92 个字符**。按渲染器**自己那几个函数**算（⛔ 不另算一遍），写成事实不写成建议 |
| **T7** | **估价不放回界面，钉进红线表** | 两轮独立判定：跑前写死 90% 落在 ±30% 内，实际 **38%** 和 **46%** |
| **T8** | ✅ **复核完毕，不改** | 设置里「思考力度」那句话已经和实测一致。⛔ 不把实测数字写进去 —— 那正是「用户不能看到测试环境的 Spool」点名禁掉的写法 |
| **T9** | ✅ **查清了，不用改** | 掉的 22 行批注，**21 处逐处打印，全部**是「那一块整个不见了」，⛔ 一处都不是 `unshieldPack` 的配对出洞 —— 它已经被 `worthRetrying` 接住了 |
| **T10** | ⛔ **把实测文档被覆盖掉的四条结论补回去**（`§7.15`） | `104cf47` 用「跑完了」那一版整段替换了 §7，**离线那半的四条一起没了**，而其中两条是这个功能最重要的两个数：**骨架占 43%（正文压到 0 也只能缩到 43%）** 和 **省下 8% 要用 77 次才回本**。⚠️ 下次整段重写一节之前先问一句「旧那版里有没有还成立的东西」 |

**做完之后全量验了一遍**：**614 个 TS 测试 + 126 个 Rust 测试全绿**（TS 多 6 个，Rust 多 2 个），
`tsc -b --force` 干净，`cargo clippy` **没有新警告**（前后都是 12 条），
`npm run build` 出得来，`i18n-check` 报 `(none missing)`。提交后署名自查干净。

⚠️ **T 批只做了改动，没验过真实端点。** T3 那一条尤其 —— 离线构造的响应体证明得了解析，
证明不了真实端点在**流式**里怎么写 `finish_reason`。**今晚第六轮的阶段 2 就是为这个留的。**

---
## 4. 等 Ocean 的

### ⛔ 真手指验收还没做，⚠️ **而且要先换装**

⛔ **`/Applications` 那份是 08-23 01:09 的，T 批六条改动一样都不在里面。**
验收之前先按工作计划 §4.3 那套换一次装（⛔ 先跑 `find`，不是 `mdfind`）。
⚠️ **这一次不动 schema**（还是 v24），但**先备份再开**是这个仓库的规矩，照做。

⭐ **T 批那几条真手指看什么**（都在压缩/整理那一面）：

1. **压一次默认档**，压完之后**不该**出现「第一次不合格，自动重压了一次」那句话 ——
   ⚠️ 之前十次里七八次会出现，而且钱是两次的；
2. 「整理」页最上面那段点「查一遍」之后，**已经处理过的那几条不再问一遍**，
   而是写一行「另有 N 条你已经处理过了」；
3. 「只退旧的」**悬停**在一个很短的块上，会多一句「这一块很短：退掉之后 pack 反而会长一点」；
4. ⚠️ **一条都别急着点** —— 这三条的判据都是离线验的，今晚第六轮才有真实端点的证据。

**下面这十条是上一窗（第四窗）留下来的，⛔ 一条都还没验过**，一起看：

1. 项目里 block 上面那个**排序图标没了**，feed 永远按时间；
2. 压一个项目的时候，**左栏那个项目名后面写着「整理中」**；
3. 核对面顶上：**没有「这一份别用」那句话**了，四个数在左、警告在右、排成一行；
   AI 说自己删了什么**默认折起来**；
4. 压一次，**大部分被改写的行是不是只有变了的那几个字带记号**（不再整段红 + 整段黄）——
   ⚠️ 这一条是这一窗的正题，真手指最该看的就是它；
5. 报丢数字的时候点「加回去」，**不会再冒出「这 0 个补不回去」**；
   真的插不回去的那种，现在会把原文接回它那一节；
6. 核对面右下角多了一个 **「用这一份」**。点它之前先看一眼左边那句话说的是
   「原文会留着」还是「关掉了留原文」；
7. 用过一次之后，**那几块的头行上有 `🗜`**，块的操作条上多一个**还原**按钮；
8. 再压同一个项目，会说 **「N 块已经压过了，这一次跳过它们」**；
9. 整理页**最上面**多了一段「可能已经过期的块」，点「查一遍」之后每一条给
   **合并 / 只退旧的 / 不动** 三个按钮 —— ⛔ 这一面上**不该出现「作废」两个字**；
10. ⚠️ **第一次开新装的 Spool 会弹一个「允许访问钥匙串」的框** —— 那是对的，
    换了签名身份系统就把它当成另一个程序来问。点允许。

### ✅ 已经答完的三件（2026-08-23）

**① key 挪进钥匙串加哪个依赖** → **`security-framework`**（已做完，见 §3）。
**② R1 先于 key 挪完就做了，认不认** → **「认，继续往下」**。
那条护栏的措辞从此**只管 E3**，不再管所有写入。
**③ E3 的「合并」做哪一种** → **A（只连线不动字）**。已做完。
⚠️ 代价记一下：**pack 不会因此变短**。想要真变短那一档（B：把旧块里仍然成立的
几句并进新块）没做，⛔ 理由是「哪几句仍然成立只有你知道」——要做就排 F 批。

### 知会两件（不是问题，是代价）

**① D-c 之后，一次「压缩」最多会真的发出两次请求**（自动重跑，两次的钱都算你的）。
⚠️⚠️ **2026-08-23 实测发现它比说好的凶得多**：判据里有一条「压完还剩 95% 以上」，
而默认档压完就是剩 98% —— **十次里七八次会重跑，钱翻倍，压完还是 98%**。
✅ **已经修了**（T1）：长度这一条从此只对「压到最短」成立，其余两档只挡「比原文还长」。
⛔ 另外三条判据（切不出块 / 重复编号 / 块数对不上）一个字没动。

**② 查过期这个功能不稳。** 同一个项目、同一份 pack 跑五次，给的不是同一批
（4/5 · 2/5 · 1/5）—— **你会看到「昨天没有今天有」**。
⛔ 现在**有意不修**：能修的两条路都要么再花一笔钱、要么压低召回，而它的**假阳性是 0**、
召回只有一对真值撑着。今晚第六轮会把这个数再量一遍，两轮摆一起才知道是模型的性质
还是那一次的运气。

### 不挡路的两条小选择题

在工作计划 §3.2：① 一句话摘要要不要升级（建议不升级）② 机器上那些旧 app 删哪几个。

### ✅ 已经答完的（别再问）

不急着发版（`A → D → B → 渠道 → E → F`）· E1/E2 不提前 · D1 撤销 ·
E3 算发版前的交互（留在 D 批，排最后）· 版本号 0.6.2 认可 · D-k 紧急加 ·
**D-k 的三条真手指验收 08-22 做完了，没问题** ·
A4 休息浮窗四步全成立、功能留下 · 官网问卷升成主按钮 ·
那条「提交按钮不显示」是我排错了，已撤回 ·
**R 批先后 `R5 → R1 → E3` 08-22 拍了**（R4 已完成）。

---

## 5. 动手之前

1. **产品红线** → 工作计划 §4.1。⛔ 只能由 Ocean **明说**推翻，不能作为做别的事的副作用被顺手推倒。
2. **工程护栏** → 工作计划 §4.2。**每一条都曾经被写反过一次，而反过来之后测试照样是绿的。**
3. **要在本机做实机验证** → 工作计划 §4.3 先跑那个 `find`（⛔ **不是 `mdfind`**）。
   **16 个** app 认领着同一批 bundle id，`open -a <绝对路径>` 会被 LaunchServices 劫持到别的实例上，**而且不报错**。
4. **文案** → 大白话；**少用 emoji**（2026-08-22 Ocean 再次提醒）；一屏最多一个 ⚠️。

---

## 6. 下一窗结束时，这份要更新哪几处

⛔ **别在这份里堆任务** —— 任务的家在工作计划。这份只更新五处：

1. §1 那张状态表（分支 / HEAD / 工作区干不干净 / 装机的是哪一份 / 发布了什么）
2. §2「这一窗做什么」换成新的一批
3. §3「上一窗做了什么」覆盖掉
4. §4 拿到答案的划掉，新冒出来的加上
5. 做完的任务**从工作计划里删掉**（不是打勾），完成记录留在 §3
