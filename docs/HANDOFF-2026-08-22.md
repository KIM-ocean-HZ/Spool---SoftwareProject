# 交接 —— 2026-08-22

> **每次开工先读这一份，只读这一份。**
> 要知道某一批具体做什么 → `WORKPLAN-2026-08-22.md`。
> 要知道某件事当初为什么这么定 → 那份的 §6「已经做完的事去哪查」。
> ⛔ **不要去翻 `archive/` 里的东西来开工。**

---

## 1. 现在的确切状态

| 项 | 值 |
|---|---|
| 当前分支 | `main` |
| HEAD | `7e7009a`（改写单列成第三类）。⚠️ **本地领先 `origin/main` 五个提交，还没推** |
| 工作区 | ✅ **干净**。D 批做掉九条，见 §3 |
| 版本号 | `0.6.1`（`package.json` + `tauri.conf.json`）—— 发版那天改 `0.6.2` |
| 已发布的 | **v0.6.1（2026-08-19）** —— ⛔ **压缩、主窗尺寸、表头、休息浮窗、这一窗的 D 批，一样都不在里面** |
| 装机的 | ✅ **2026-08-22 17:53 已经换成这一窗的构建**（`0.6.1` dev build，`Unnotarized Developer ID`，和换之前同一个签名身份，所以两个 TCC 授权没掉）。换下来的旧版留在 `~/Desktop/Spool-devbuild-061-20260822-175313.app`，⛔ 别删，那是回去的路 |
| 官网 | spoolapp.org **已部署最新**（run 32552301537），这一窗没动官网 |
| 真实外部用户 | **0**（看板上那 32 次全是 Ocean 自测） |

---

## 2. 这一窗做什么

**D 批还剩五条**：`D7+D-b` → `D-a` → `D-c` → `D6` → `E3`（细节在工作计划 §1.D）。
⛔ **还是不发版** —— B 批的前提是 D 批全部做完。

| | 做什么 | 细节在 |
|---|---|---|
| **D7 + D-b** | **丢掉的数字/日期给一个「加回去」** + **数字硬闸门**。⭐ 两件绑一起做 | 工作计划 §2.D7。⚠️ 难点是**插哪儿**（原句在压缩稿里可能已经被合并掉了），先想清楚再动手 |
| **D-a** | **本地重复度探针** —— 压之前先算这个项目有多少重复 | 工作计划 §1.D 那一行（里面写了现成的只有精确匹配那个，模糊那个前端够不着） |
| **D-c** | **坏结果自动重跑一次** | ⚠️ 重跑要把两次的 token 加起来报账 |
| **D6** | 四带译名 | ⚠️ 最贵的一条，和下一次动渲染器的活合并做 |
| **E3** | 作废检测接产品 | ⛔ 排最后，它是这批里唯一会写库的 |

⚠️ **先做一件不写代码的**：**D-k 的三条真手指验收还没做**（见 §4）。
✅ 换装已经做完了（17:53），**现在按下去就是新的那份**；缺的只有 Ocean 的手指。

⚠️ **挡在发版前面的 B0 还在**：官网和 README 有**十处**「Spool 不需要 key / 永不联网」的说法，
压缩 sidecar 一发出去就变成假话。⛔ 它挡的是 B 批，**不挡 D 批**，位置和改法在工作计划 §1.B。

---

## 3. 上一窗（2026-08-22 第二窗）做了什么 —— D 批九条

**四笔提交，全在本地 `main` 上，⚠️ 还没推。**

| 提交 | 是什么 | 里面最要紧的一条 |
|---|---|---|
| `bc71cc0` | **D-k · 一键暂停捕捉手势** | 一个开关同时按住双击 ⌥ 和用户自绑的全局捕捉键。⚠️ 暂停时 `on_event` **放行事件**（`return true`）—— 写成 `false` 是删事件，功能会变成它的反面，而且编译测试全绿。已进工程护栏 |
| `50bc1ec` | **D0 + D4-a + D5 + D8 + D10** —— 核对界面收口 | 「复制这次的数据」那个按钮删了：它存在的唯一理由是把数据贴进实测台账，是**采集口不是产品功能**。数据改走 console |
| `d33b2c2` | **D2 + D3** —— 右栏按项目分，有一份没核对完就不许再开跑 | 原来同一个项目可以同时存在两份互不知道对方的压缩稿，而屏幕上只有一张桌子 |
| `7e7009a` | **D4-b + D9** | 「少了 13 条批注」和「凭空写了 13 条」是**同一批东西**（改写）。⛔ 修法是单列第三类，**不是放松核对** —— 改写照样算损失 |

**九条逐条**（原十一条里的，D1 已撤销、D6/D7 未做）：

| | 做了什么 |
|---|---|
| **D-k** | 设置里一个开关 + 菜单栏 Spool 图标里一个 `CheckMenuItem`。⭐ 后者才是重点。关着的时候**菜单栏图标压到 40% alpha**（这开关会持久化，「关了忘了」必然发生）。状态进 `settings.json`，**Rust 启动时自己读**，不等 webview |
| **D0** | 八处全清：token/缓存那一行 → 一个数「这一次花了大约 ¥0.1646」；「复制这次的数据」删；「28,189 → 23,687 字符」→「比原来短了 16%」；「这个目标是提示词里写着的那一个」那条悬停提示删；思考力度那段「这几个值都试一次就知道」→ 说明 + 默认值 |
| **D4-a** | 顶上加一行结论：「这一份可以复制走 —— 比原来短了 16%」/「⚠️ 这一份别用 —— 丢了 3 个数字或日期」。⛔ 挑最重的那一条说，不并列。整条账**红不红跟着结论走**（原来只看「一字不改」那条线，整块不见了反而是正常颜色） |
| **D4-b** | 新增 `pairRewrites`：配得上对的从「丢了」和「编了」两边同时取出来。⚠️ 用「短的那条有多少落在长的那条里」，**不用 Dice** —— 那个真实例子的 Dice 只有 0.43，这个比值 0.83。六个测试盯着，其中一个专盯「不相干的两条绝不配对」 |
| **D5** | 那句「⚠️ 实测：压多少……不取决于你选哪一档」删了（它和上面三个按钮直接打架），意思并回档位说明。**D5-b：⚠️ 从一屏七八个减到一个**，只留给「这一份别用」 |
| **D8** | 前后对比折叠初值改 `true`。一行改动 |
| **D9** | 底部写清「这里没有『用这一份』」以及**为什么**。⚠️ 只做了这一半，那个入口本身的前提是 D7 + D-b |
| **D10** | 「目标 50–75% —— 没达标」整条撤（那个目标 §9.5 早写明是空话）；右栏睡前单子上的「约 ¥X」和「合计（估算）」撤，只留字数 |
| **D2 / D3** | 右栏分两段（这个项目的 / 别的项目的，后者点了先切过去）；已有一份没核对完时按钮改成「上一份还没核对完」并指回那一份 |

**每一笔提交后都跑了署名自查，四笔全干净**（窄版：只查作者/trailer）。

**做完之后全量验了一遍**：**552 个 TS 测试 + 111 个 Rust 测试全绿**（TS 比上一窗多 5 个，
是 D4-b 那一组），`tsc -b --force` 干净，`cargo check` 干净，`cargo clippy` **没有新警告**，
`npm run build` 出得来。

⚠️ **但这一窗一次都没有装机验证过** —— 见 §4 第一条。

**两件顺手记下的**：

1. 上一份写的 HEAD 是 `7709883`，实际当时已经是 `a3e4ba8`（验证版删干净那一笔）。
2. 我的改动让三样东西变成没人用的：`LEVEL_TARGET`、`estimateYuanForChars`（连同
   `TOK_PER_CHAR` / `OUT_PER_IN` 两个常数）、以及几条 i18n 词条。**按规矩删掉了**，
   删的地方留了整段注释写明为什么撤、代价是什么、要做回来去哪儿取数。

---

## 4. 等 Ocean 的

### ⛔ D-k：三条真手指验收，还没做

代码写完了，**这台机器上一次都没真的按过**。

✅ **换装已经做完（2026-08-22 17:53）**，照 §21 那套走的：现装的是发布证书签的，
所以用同一个身份（`APPLE_SIGNING_IDENTITY`）覆盖、`tauri.conf.json` 一个字没改 ——
换身份会把输入监听和辅助功能两个授权当场弄没。装完核过：identifier 是
`com.oceanjin.spool`、`codesign --verify --deep --strict` 通过、`spctl` 报
`Unnotarized Developer ID`（§33 说的那条"这是 dev build"的判据）、
真库仍然 **13 工作区 / 45 项目 / 218 块 / user_version 23**、
`--mcp` 子进程一个没杀。换之前又备了一份库：`~/Desktop/spool.before-dk-20260822-175106.db`。

⚠️ **认领同一个 bundle id 的从 15 涨到 16 个**（换下来那份留在桌面）——
下次做隔离验证前照工作计划 §4.3 那个 `find` 重数。

三条：

1. 关掉之后，在**别的 app 里**双击 ⌥ → Spool 没反应，**而且那个 app 自己的 ⌥ 快捷键正常工作**
   （⭐ 这条是这功能存在的理由）；
2. 再打开 → 双击 ⌥ 立刻恢复，**不用重启**；
3. 关着的状态下退出再进 → **仍然是关着的**，而且菜单栏图标看得出来（变淡）。

### 知会一件（不是问题，是代价）

**D10 之后，睡前勾项目的时候不再看得见「今晚大概多少钱」，只看得见字数。**
这是照他自己的规矩做的（「准确度没有保证……不然就不显示」），但代价要说明白：
**授权发生在花钱之前，而现在那个授权少了一个数。**
要把它做回来的路也写在代码里了：从四轮实测回归一条**把输出 token 算进去**的估算。
⛔ 在那之前不要凭感觉补一个数回去。

### 不挡路的两条小选择题

在工作计划 §3.2：① 一句话摘要要不要升级（建议不升级）② 机器上那些旧 app 删哪几个。

### ✅ 已经答完的（别再问）

不急着发版（`A → D → B → 渠道 → E → F`）· E1/E2 不提前 · D1 撤销 ·
E3 算发版前的交互（留在 D 批，排最后）· 版本号 0.6.2 认可 · D-k 紧急加 ·
A4 休息浮窗四步全成立、功能留下（隔离验证版已删干净，真库复核没被碰）·
官网问卷升成主按钮 · 那条「提交按钮不显示」是我排错了，已撤回。

---

## 5. 动手之前

1. **产品红线** → 工作计划 §4.1。⛔ 只能由 Ocean **明说**推翻，不能作为做别的事的副作用被顺手推倒。
2. **工程护栏** → 工作计划 §4.2。**每一条都曾经被写反过一次，而反过来之后测试照样是绿的。**
3. **要在本机做实机验证** → 工作计划 §4.3 先跑那个 `find`（⛔ **不是 `mdfind`**）。
   **15 个** app 认领着同一批 bundle id，`open -a <绝对路径>` 会被 LaunchServices 劫持到别的实例上，**而且不报错**。
4. **文案** → 大白话；**少用 emoji**（2026-08-22 Ocean 再次提醒）；一屏最多一个 ⚠️。

---

## 6. 下一窗结束时，这份要更新哪几处

⛔ **别在这份里堆任务** —— 任务的家在工作计划。这份只更新五处：

1. §1 那张状态表（分支 / HEAD / 工作区干不干净 / 发布了什么）
2. §2「这一窗做什么」换成新的一批
3. §3「上一窗做了什么」覆盖掉
4. §4 拿到答案的划掉，新冒出来的加上
5. 做完的任务**从工作计划里删掉**（不是打勾），完成记录留在 §3
