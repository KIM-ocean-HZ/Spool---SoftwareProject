# 交接 —— 2026-08-22

> **每次开工先读这一份，只读这一份。**
> 要知道某一批具体做什么 → `WORKPLAN-2026-08-22.md`。
> 要知道某件事当初为什么这么定 → 那份的 §6「已经做完的事去哪查」。
> ⛔ **不要去翻 `archive/` 里的东西来开工。**

---

## 1. 现在的确切状态

| 项 | 值 |
|---|---|
| 当前分支 | `main`。`feat/api-compress-sidecar` 已快进合入，两条指向同一个提交 |
| HEAD | `7709883`（开工面收成三份） |
| 工作区 | ✅ **干净**。两窗的活已经分成五笔提交推上去了，见 §3 |
| 版本号 | `0.6.1`（`package.json` + `tauri.conf.json`）—— 发版那天改 `0.6.2` |
| 已发布的 | **v0.6.1（2026-08-19）** —— ⛔ **压缩、主窗尺寸、表头、休息浮窗一样都不在里面。已推上 `main`，但没有发版** |
| 装机的 | `/Applications/Spool.app` 是 **0.6.1 的 dev build，`Unnotarized`** —— ⚠️ **在这台机器上做的任何"验发布版"都只验到了 dev build** |
| 官网 | spoolapp.org **已部署最新**（run 32552301537）。中英两份反馈问卷线上实测通过，见 §3 |
| 真实外部用户 | **0**（看板上那 32 次全是 Ocean 自测） |

---

## 2. 这一窗做什么

**A 批收口已经做完**（只剩 A4，在 Ocean 手上，见 §4）。按 2026-08-22 定的顺序
`A 收口` → **`D 看得见的交互`** → `B 发版` → `渠道` → `E 实验` → `F 功能`，
**这一窗做 D 批**。⛔ **不是发版。**

⛔ **D 批的验收标准只有一句：打开它的人看不出这里跑过一场实验。**

| | 做什么 | 细节在 |
|---|---|---|
| **D-k** | ⭐ **一键禁用捕捉快捷键**，设置里一个入口、菜单栏 Spool 图标里一个（**后者才是重点**） | 工作计划 §2.D-k。见下面那两条一错就把功能做反的 |
| **D0** | **测试环境痕迹清扫**，八处，名单已经扫好 | 工作计划 §2.D0 |
| **D8** | 前后对比**不许折叠** | 一行改动，收益最大 |
| 之后 | `D4 → D5+D10 → D2 → D3 → D-a → D-b → D7 → D9 → D6 → D4-b → D-c → E3` | 工作计划 §1.D |

**D-k 的原话**：「禁用当前快捷键，放在**设置**里和**系统顶部菜单栏的 Spool icon** 里
（方便在其他 app 里禁用快捷键）。这样能够防止使用其他带有 option 快捷键的 app 被 spool 抢占。」

⭐ **菜单栏那个入口是重点** —— 真实场景是人正在别的 app 里被抢，
让他先切回 Spool 主窗翻设置页，等于没做。

⚠️ **D-k 动手前必读工作计划 §2.D-k**，那里有两条一错就把功能做反的：
① 暂停时 `on_event` **必须放行事件（`return true`）**，返回 `false` 是删事件 —— 那正是要禁的行为；
② 别和已有的「系统把 tap 关了」那条自愈路径混在一起，否则自愈会去"修好"一个用户故意关掉的开关。

⚠️ **挡在发版前面的 B0 还在**：官网和 README 有**十处**「Spool 不需要 key / 永不联网」的说法，
压缩 sidecar 一发出去就变成假话。⛔ 它挡的是 B 批，**不挡 D 批**，位置和改法在工作计划 §1.B。

---

## 3. 上一窗（2026-08-22）做了什么 —— A 批收口

**两窗的活散在工作区的 31 个文件，现在分成五笔提交，全部推上 `main`。**

| 提交 | 是什么 | 里面最要紧的一条 |
|---|---|---|
| `4ddff8d` | **渲染器** —— 表头加「还适用吗」一节、pack 首尾边界、`typed→spoken`、MCP 路由文本、golden | 表头两份（`templates.ts` / `mcp.rs`）仍逐字节一致；边界行在表头**外面**，所以另加了一条同款测试盯它 |
| `6bf4ecc` | **专注判据 + 休息浮窗** | 判据改成「机器不空闲 AND（Spool 在前台 OR 15 分钟内有捕获）」。旧判据问的是 Spool 有没有被摸过，**和产品前提正好相反** |
| `65b5efd` | **官网表单**（英文 SurveyMars / 中文问卷星） | 链接和那句实话由测试盯成一对：「什么都不收集」在托管表单旁边当场变成假话，而两者在不同文件里 |
| `ce05bc8` | **文档审计** —— 拒绝清单逐条审 + `PLAN_EN` 源头 | 「结构恰好两层」这句是假的，而且假了很久（workspaces 早就能无限嵌套）。⛔ 审计只记录，不解除任何拒绝 |
| `7709883` | **文档重组** —— 开工面收成三份，十四份旧工作面转档 | `HANDOFF-2026-08-19` §2 那张工程护栏表整张搬进了工作计划 §4.2，没有丢 |

**每一笔提交后都跑了署名自查，五笔全干净**（用的是窄版：只查作者/trailer，
不查 `claude` 这个词 —— 产品里本来就有它，见记忆 `attribution-grep-false-positive`）。

**推之前和推之后各验了一遍**：

- 提交前后各跑一次全量测试：**547 个 TS 测试 + 111 个 Rust 测试全绿**，`tsc -b` 干净。
- 推之前复核了零出网那条架构红线：`cargo tree -e normal` 在主 crate 里
  **命中 0 个网络库**（`ureq|rustls|reqwest|hyper|openssl`）。
- 部署跑完之后，**中英两个表单链接是从线上页面里抓出来再打开的**，
  不是照着仓库里的字符串念的：
  `spoolapp.org` → `surveymars.com/q/eXk1aRDgW` → 页面标题 **Spool · How did it go?**；
  `spoolapp.org/zh/` → `v.wjx.cn/vm/hy3KvuB.aspx` → 页面标题 **Spool 反馈**。
  两份隐私页上那段第三方说明也在线上。
  ⚠️ **"链接印错了"这个风险已经堵掉了**；剩下的只有"表单本身好不好填"，那个要用眼睛看。

**三件顺手记下的**：

1. 上一份写的「领先 `main` 11 个提交」实际是 10 —— 现在已经合掉，不用管了。
2. `CHECK-DISPLAY-2026-08-21.md` 里有一个被误删的逗号，**已还原**，没跟着提交进去。
3. `SPOOL_OVERVIEW.md` 一个文件里同时有"文档审计"和"文档重组"两件事，
   **按段落分开提交了**，不是整个文件塞进一笔。

---

## 4. 等 Ocean 回答的

### ⛔ 一件要动手的：A4 · 休息浮窗的真手指实测（隔离版已经做好了）

**验证版在这儿**：`~/Desktop/Spool-休息验证-0822/Spool-休息验证.app`

它是**一个完全独立的 app**：bundle id `com.oceanjin.spool.brk`，
库在 `~/Library/Application Support/com.oceanjin.spool.brk/`，
⛔ **碰不到真库**。里面只有一处和正式版不同：**多了一个 1 分钟档**，
判定逻辑一个字没动（照 `isolated-verify-workflow` §32 的招）。
⚠️ 它触发的时间是**大约一分半**，不是一分钟整 —— 时钟 30 秒一跳，
第一跳只起表不计时，所以是第三跳才到。

**怎么测（一步一动作）**

1. 先把现在装着的 Spool 退出（托盘图标 → 退出）。两个都开着会有两张一样的窗，分不清谁是谁。
2. 双击打开 `Spool-休息验证.app`。它会自己建一个空库，跟真库无关。
3. 切到 Word（或任何别的软件），随便打点字。
4. 在 Word 里选一句话 → `⌘C` → `⌘⇧K`。捕捉框弹出来，存一条。
   ⚠️ 这一步不能跳：新判定要「最近 15 分钟里有过捕获」才算你在干活。
   （这个版本预置了 `⌘⇧K`，因为它不需要任何系统授权；双击 ⌥ 需要授权，会跟装着的那个抢。）
5. 回到 Word 继续打字，**大约一分半**。
6. 看这一串成不成立：
   ① 卡片浮在 Word 上面（**主窗没有跳到前面**）→ ② 点一下卡片 →
   ③ Spool 主窗上来 → ④ 卡片自己消失。

**哪一步不对，说明什么**

- ①里主窗自己跳到前面 = ⛔ 最严重的那种坏，「主窗永不跳前」被破了。
- ③点了没反应、或者过一会儿才上来 = **先别急着判功能死刑**：这个验证版是新 bundle id，
  系统还没给它「辅助功能」授权。去「系统设置 → 隐私与安全性 → 辅助功能」，
  把这个 app 加进去打开，然后**退出重开**再试一次。第二次还不行，才是功能的问题。

⛔ **不成立的话，正确动作是下架这个功能**，不是把判据放宽到永远为真。
这个项目已经有两次上线后回滚的先例（进度条、`next_step`）。

**测完之后**：整个 `~/Desktop/Spool-休息验证-0822/` 文件夹可以直接删，
连同 `~/Library/Application Support/com.oceanjin.spool.brk/`。
⚠️ 删之前顺手把「辅助功能」里那条也删掉，别在系统设置里留一条指向不存在的 app。
⚠️ 它让认领 `com.oceanjin.spool*` 的 app 从 14 个变成 15 个（见 §5 第 3 条）。

### ⛔ 撤回一条：英文问卷的提交按钮**没有**问题

上一版这里写着「提交按钮还是中文的，挡着英文渠道」。**那条是错的，已经撤掉。**
Ocean 2026-08-22 亲自测过：submit 正常显示。

**我是怎么错的**：`FEEDBACK-FORM-2026-08-22.md` §7 的标题就是
「✅ 英文表单的界面语言 —— 已修，已复核」，正文下面留着当时的原始记录。
**我读了下面那段历史，没读上面那行结论**，就把一件已经结案的事排成了待办。
⚠️ 存档件里「原始记录」和「现在的结论」挨着放的时候，先看结论那一行。

### 🆕 他真正说的那件事：问卷放得太隐蔽

**原话**：「但是放的有点隐蔽了，感觉不会有人去做。」

两处让它看不见：① 那一节的标题是「出问题的时候」，**用得好的人根本不会读这一节**，
而问卷问的恰恰是「用得怎么样」；② 链接在长长一段邮件表单**之后**、小字行里排**第四个**。

已排成工作计划的 `C-f`（C 批，随时插，不占顺序）。**改法要他点头**，
选择题写在工作计划 §3.2 第 4 条 —— 关键的一句：官网调性一直是安静的，
这一改那一节会比现在响一点，**要多响是他的判断**。
⚠️ 期限是渠道那一批：人进来之前弄好，否则把人引进来也是白引。

### 不挡路的三条小选择题

在工作计划 §3.2：① 一句话摘要要不要升级（建议不升级）
② 机器上 14 个旧 app 删哪几个 ③ D 批内部先后（建议按 §1.D 末尾那个序）。

### ✅ 已经答完的（别再问）

不急着发版（`A → D → B → 渠道 → E → F`）· E1/E2 不提前 · D1 撤销 ·
E3 算发版前的交互（留在 D 批，排最后）· 版本号 0.6.2 认可 · 🆕 紧急加 D-k。

---

## 5. 动手之前

1. **产品红线** → 工作计划 §4.1。⛔ 只能由 Ocean **明说**推翻，不能作为做别的事的副作用被顺手推倒。
2. **工程护栏** → 工作计划 §4.2。**每一条都曾经被写反过一次，而反过来之后测试照样是绿的。**
3. **要在本机做实机验证** → 工作计划 §4.3 先跑那个 `find`（⛔ **不是 `mdfind`**）。
   14 个 app 认领着同一批 bundle id，`open -a <绝对路径>` 会被 LaunchServices 劫持到别的实例上，**而且不报错**。
4. **文案** → 大白话；**少用 emoji**（2026-08-22 Ocean 再次提醒）；一屏最多一个 ⚠️。

---

## 6. 下一窗结束时，这份要更新哪几处

⛔ **别在这份里堆任务** —— 任务的家在工作计划。这份只更新五处：

1. §1 那张状态表（分支 / HEAD / 工作区干不干净 / 发布了什么）
2. §2「这一窗做什么」换成新的一批
3. §3「上一窗做了什么」覆盖掉
4. §4 拿到答案的划掉，新冒出来的加上
5. 做完的任务**从工作计划里删掉**（不是打勾），完成记录留在 §3
