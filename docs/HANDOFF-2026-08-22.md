# 交接 —— 2026-08-22

> **每次开工先读这一份，只读这一份。**
> 要知道某一批具体做什么 → `WORKPLAN-2026-08-22.md`。
> 要知道某件事当初为什么这么定 → 那份的 §6「已经做完的事去哪查」。
> ⛔ **不要去翻 `archive/` 里的东西来开工。**

---

## 1. 现在的确切状态

| 项 | 值 |
|---|---|
| 当前分支 | `main` |
| HEAD | 换装到 U 批第三轮那一笔（`4633f88`）。⚠️ **本地领先 `origin/main` 四十五个提交，还没推** |
| 工作区 | ⚠️ **有未提交的改动**：B0（十个文件，全是文档和官网，⛔ 一行产品代码都没动），见 §3 |
| 版本号 | `0.6.1`（`package.json` + `tauri.conf.json`）—— 发版那天改 `0.6.2` |
| 已发布的 | **v0.6.1（2026-08-19）** —— ⛔ **压缩这一整块（含四窗的全部改动）一样都不在里面** |
| **库版本** | **v24**，真库和装机的都已经在 v24 上（08-23 迁移走完，见下一行）。⚠️ T 批**一行 schema 都没动** |
| 装机的 | ✅ **2026-08-23 21:48 换成了 U 批第三轮那一份**（`0.6.1` dev build，`Developer ID Application: Hanze JIN (Q5Y5JRXZ58)`，**签名身份从头到尾没变，两个授权都没掉** —— 启动 stderr 打的是 `[double-tap] installed at HID/active`）。已核：identifier `com.oceanjin.spool`、`codesign --verify --deep --strict` 通过、二进制 21:47、**`--mcp` 子进程 30 个一个没杀**。装前备库 `spool.before-ubatch3-20260823-214709.db`，旧包 `Spool-old-20260823-214819-pre-ubatch3.app`。⚠️ 复核真库：`user_version 24`、`integrity_check ok`、**六张表逐项一致，226 块正文哈希相同**（`e0dd6c365bcd0cf2`）。⚠️ **今天换了三次装**，三份备库和三个旧包都留着 |
| **真库迁移** | ✅ **v23 → v24 走完了，没事**。行数逐项一致（**13 / 45 / 218**），**218 块正文内容哈希前后相同**（`594dde407625cbf1`），`integrity_check ok`，两个新列**全空**（迁移一个字的用户数据都没写）。自动备份：`spool.pre-migration-v23-2026-08-22T17-05-09-895Z.db` |
| 官网 | ⚠️ **本地改了、还没部署**：B0 动了 `site/` 六个文件（见 §3）。线上仍是 run 32552301537 那一份，**上面那批「不需要 key」的话还挂着** —— ⛔ 推上去才会变 |
| 真实外部用户 | **0**（看板上那 32 次全是 Ocean 自测） |

---

## 2. 下一窗做什么

⭐ **`A` / `D` 空了，`T` 只剩一条有意不做的，`B0` 也做完了。挡在发版前面的只剩两件，两件都不在我手上。**

| 挡着的 | 谁做 | 现在什么状态 |
|---|---|---|
| **真手指验收（U 批十六条 + T 批三条）** | ⛔ **Ocean** | 装机的已经是最新那份（§1），逐条验收单在 §4 |
| **今晚第六轮实测** | 半自动 | ⭐ **开跑就读 `OVERNIGHT-TEST-PLAN-2026-08-24.md` 最上面那段「开跑前先读这一段」** —— 状态、三条不许做的、东西在哪，都在那里，⛔ 不用再读这一份 |

**他验完之后，B 批剩下的三条**（B1 版本号 `0.6.2` · B2 打包公证 · B3 RELEASE_NOTES）就能连着做完。

**顺手能插的（零成本，不占顺序）**：工作计划 C 批五条 —— **C-a** 拿现成的 39 条标注重扫
`scope_limited`（⭐ 它决定过期检测那三个按钮要不要变四个）· C-b Safety 面纸面复核 ·
C-c `assemble({ instructions: false })` 那条零提示的路 · C-d 文档五处 · C-e 主窗尺寸验收②。

---

### ✅ 阶段 2b：Ocean 拍了「跑」，代码已经写完并核过

第六轮的计划里**新加了一格 `阶段 2b`** —— U14（按一下停下，产品叫用户去重装 Spool）
原来**只有离线证据**，而离线证明不了「真的把一个正在联网的子进程杀掉之后走到哪条分支」。

✅ **实测台加好了**（`compress_sweep.rs` 的 `Step` 多一个 `cancel_after_secs`，
到点从另一个线程调 `compress_cancel()`，⚠️ **那个线程 join 过**，免得迟到的取消飘到下一步）。
✅ **重编之后逐项核过**：二进制**还是同一个路径** `spool_lib-d0a4830bf0c10b04`（23:27），
比每一个 `.rs` 都新，记号齐；**128 个 Rust 测试全绿、`clippy` 仍然 11 条**；
⭐ **空计划当场跑过：9.6 秒回来、零成本、⛔ 没弹钥匙串的框。**

⛔ **从现在起到开跑，别再 `cargo build` / `cargo test`** —— 这一份是核过的那一份。

### ⚠️⚠️ 今晚第六轮跑之前，最要紧的一件（⛔ 别无人值守地开跑）

**实测台是 `cargo test` 出来的二进制，和 `/Applications` 里那个不是同一个。**
而 key 现在住在钥匙串里，**钥匙串是按二进制认的** —— 换一个二进制就重新弹一次
「允许访问钥匙串」，而**无人值守的时候那个框就是卡死**（第五轮就这么死过一次）。

⚠️ **这一窗又改了 `api_engine.rs`（U 批），所以那个二进制又变了一次。**
✅ 已经**预先编好**放在那儿了，跑的时候不会再花时间编译：

```
src-tauri/target/debug/deps/spool_lib-d0a4830bf0c10b04   （2026-08-23 21:45，adhoc 签名）
```

✅ **2026-08-23 晚逐项核过**：它比 `src-tauri/src` 下**每一个** `.rs` 都新，
`strings` 里 `stopped by the user`（U14）· `same_block` · `no_block`（U8）都在。
sidecar（`sidecar/target/debug/spool-ai`，14:19）**U 批一行没碰**，T3 的三个分类还在。
⇒ **两个都不用重编。**

⛔⛔ **所以开跑之前不要顺手 `cargo build` / `cargo test`** —— 重编就换掉这个已经核过的二进制，
换来的正好是这一夜最怕的东西：**半夜弹一个没人点的框**。
⚠️ **23:27 为了阶段 2b 计划内重编过一次**，上面那些数（23:27 / 128 绿 / 空计划 9.6 秒）
说的就是重编之后那一份 —— **它才是今晚要用的**。

⛔ **要有人在场先手动起一次空计划，再开整夜那一轮。**

---

## 3. 这一窗做了什么 —— B0（官网/README 那批假话）

⚠️ **还没提交。** 十个文件，**全是文档和官网，⛔ 一行产品代码都没动**：
`README.md` · `docs/PRIVACY.md` · `site/{index,privacy,story}.html` ·
`scripts/site-zh-{strings.mjs,privacy.html}` · `site/zh/*.html`（三份是生成的）。

**为什么这件事排在最前面**：压缩这一块一发出去，官网和 README 上「不需要 key」「永不联网」
那一批话**当场变成假话**。⭐ 而这个产品的招牌就是「测量记录」——
**发一版就让自己官网上四句话变成假话**，是这块招牌伤得最重的一种错法。

**改成什么**（形态 C 定案时给的写法，一字未动）：

> **Spool 主进程不发出任何网络请求。当你打开 API 引擎时，请求由 Spool 启动的本地子进程发出，
> 用你自己的 key 和额度，内容到达你选择的那家模型厂商。**

⭐ **零出网这条没有收回，收回的只有「不需要 key」和「永不联网」** ——
`cargo tree -e normal | grep -iE "ureq|rustls|reqwest|hyper|openssl"` 当场跑过，**是空的**。

| | 做了什么 |
|---|---|
| **原来数的十一处不全** | ⚠️ 真正动到的比那张表多一倍。漏掉的那几处不是边角：`README:130`「zero built-in AI」、官网 FAQ「**No key — there is nowhere in Spool to put one**」、`privacy.html`「Spool never sends data anywhere on its own」。⭐ **按关键词扫，别照名单改** |
| **README 多了一节** | 新增「The API engine (optional, off by default)」——⭐ 不加这一节，那些改过的句子就没有指向的东西（README 里从来没提过 API 引擎）。里面写清：你自己的 key、你自己的账、**中途取消也照样收费**、key 存哪、`cargo tree` 那条自己验的命令 |
| **隐私页多了一整行表格** | 那张表列的是「什么时候会有数据离开你的电脑」，**少列最新的那条就是最大的一句谎**。三种方式改成四种，新那一行写明：这是唯一一条 Spool 自己去跑的路 |
| **架构图加了第三条网络路径** | ⚠️ 原来那张 SVG 画的是两条，并写死「ONLY THE TWO ACCENT PATHS CROSS IT」——**图本身在说假话**。现在是三条，第三条是 `spool-ai`。⛔ 坐标是手摆的，改完是**渲染出来看过**的（`viewBox` 现在 `0 0 1160 706`） |
| **中文站是生成的** | `scripts/site-zh-strings.mjs` 改了 14 个键、`site-zh-privacy.html` 改了 5 处，跑了 `node scripts/build-site-zh.mjs`。`build-site-zh.test.mjs` 12 条全绿 |
| **两处日期跟着改** | `PRIVACY.md` 和 `site/privacy.html` 的「对应 Spool v0.4.0」→ **v0.6.2**。⚠️ 版本号要是变了，这两处跟着变 |

**验了一遍**：615 个 TS 测试全绿、`tsc -b --force` 干净、`i18n-check` `(none missing)`、
`build-site-zh.test.mjs` 全绿。⛔ **没跑 `cargo`** —— 理由见 §2 那条（重编会换掉今晚要用的二进制）。

**顺手做的两件**（都是文档对不上现状）：

- `OVERNIGHT-TEST-PLAN-2026-08-24.md`：⛔ **阶段 1 原来写的做法跑不通**（自动重跑那套在
  TypeScript 里，Rust 实测台调不到），改成两段跑；新增**阶段 2b**（U14 的真取消）和
  **0f/0g**（U 批的离线补算）；阶段 3 补上 `reason` 那一列；§4 补二进制复核和「别顺手重编」。
- **`compress_sweep.rs` 加了取消那条路**（唯一一处产品仓库里的代码改动，⛔ 只在
  `#[cfg(test)]` 的实测台里，**出货二进制一个字节都没变**）。逐项核过的结果在上面 §2。
- `WORKPLAN-2026-08-22.md`：B0 从任务库里删掉；U 批「九条/两轮」→「十六条/三轮」；
  机器上认领 bundle id 的 app **15 / 17 → 21**（两处原来自相矛盾）；MCP 工具 **19 → 20**；
  §4.2 补上 U8 那条**从来没写进去**的护栏。

---

## 3-bis. 上一窗做了什么 —— 三轮真手指验收，十六条全部修完

⛔ **这一节先别删** —— §4 那张验收单逐条指着它，**Ocean 验完才能覆盖掉**。

**六笔提交（三笔改动 + 三笔换装），⚠️ 还没推。** 编号跟着 Ocean 写的顺序走。
⚠️ **一天里换了三次装** —— 他每验一轮就当场再指几件，所以修一轮换一次。

| | 他说的 | 改了什么 |
|---|---|---|
| **U1** | 「工作区『只看我写的』筛选按钮去掉」 | `BlockFeed` 那个笔形图标整条撤掉（连同 `mineOnly` 状态和空态那句话）。和排序图标同一个理由：占位置，没被用过 |
| **U2 · U7** | 报「这几句话里的数字/日期，压缩稿里没有了」，而那几个数其实还在；点「加回去」弹「这几处已经在压缩稿里了」，**红字却一直不消失** | ⭐ **同一个根：两个分母。** 卡片按**这一块**数丢没丢，「加回去」按**整份**数。而压缩干的主要活就是跨块合并 —— 一个数从 #12 挪进了 #8，两边就永远和解不了。修法是把卡片那一侧的分母改成整份（`compareByEntry` 里过一道 `packMissing`）：**整份压缩稿里真的找不到了才算丢**。三条理由：屏幕上写的就是「压缩稿里没有了」；数字硬闸门本来就按整份判；护栏写死过「看到的那一行必须就是点下去会被插回去的那一行」。加了回归测试 |
| **U3** | 「整理面板：把两个功能拆出来，变成内容，压缩，和一个新的」 | 页签从两个变三个：**内容 / 压缩 / 查旧块**。查旧块（原作废检测）从压缩核对面**头上**搬走，自己一页、自己滚动 —— 它原来顶在最上面，把核对区挤到半屏以下 |
| **U4** | 「右侧边栏的压缩按钮不要了，压缩直接到面板去使用（overnight 的勾选也放里面，此外 overnight 压缩可以在项目管理的面板里面加入，方便多项目加入）」 | 右栏那一格掏空：按钮、排队勾选、几点跑、队列清单全搬进「压缩」页签；**这一页现在自己开桌子**（进去就把 pack 组好，不用先去点一个按钮）。多项目排队进了**项目管理**每一行的展开条。右栏只剩一件**跨项目**的事：别的项目夜里压好的那几份在哪儿认领 —— 没有东西认领时整格不出现 |
| **U5** | 「未压缩的 pack 默认保存，不留原文的开关放到设置里面……现在的按钮只能回退到原文，**不能再次回到压缩后文本**」 | 块工具条上那个 ↺ 从**一次性写库的还原**改成**摊开来看**：原文以只读区块摊在这一块底下，随时收起，来回不限，**一个字都不写库**。真要换回去是摊开之后里面那一行「用回这一份」。⚠️ **⛔ 没有把上面那段正文替换掉** —— 那段带着 `withOffsets`，划词/高亮/更正全按 `content` 的字符下标定位，塞另一份文本进去下一次划词就落到错的字上，而屏幕上看不出来。打包仍然只带一份，有压缩就带压缩的（`assemble` 读 `b.content`），原文要靠 MCP 的 `get_block_original` 去问 |
| **U6** | 「压缩面板的提示词不能实时更新 —— 我已经把所有 block 都回退到未压缩了，它还说『5 块已经压过了』」 | 新增 `refreshSession`：块一变就把这一份重组一遍。⚠️ **看得见的是那句话，真正危险的是它底下那份 pack** —— `source` 是开桌子那一刻组的，不重组的话按「开始压缩」发出去的仍然是**少了那 5 块**的旧 pack，钱照花，压的却不是屏幕上那个项目 |
| **U8** | 「另有 1 条被丢掉了 —— 它给的引文在块里对不上，Spool 不拿它给你看：这个回复看不懂，且提示不给我看，意思是我的项目有问题，但是 spool 不告诉我，**不允许这样的情况发生**」 | ⭐ 病根不在措辞：Rust 那侧**只回了一个数**，界面手里根本没有可给的东西。现在整条带回来（`StaleDropped`：两个编号、理由、两句引文、**没过闸的是哪一步**），界面把它们摆出来，并且把两件事同时说清 —— **是 AI 记错了、不是你的项目有问题**，以及**这几条没有按钮**（核不实的东西不能拿去改库） |
| **U9** | 「『只退旧的』的中文表述根本看不懂……用户只知道新的 block 把旧的取代了，或者是合并了，或者什么都没动」 | 三个按钮改成 **合并 / 新的取代旧的 / 什么都不动**，悬停说明里不再出现「退」和「pack」两个词（改说「打包只带 #N」）。跟着改的还有左栏那个徽标：「整理中」→「**压缩中**」，屏幕上只留一个词指这件事 |

### 同一天第二轮 —— 他点了新装的那份，又指了四件

| | 他说的 | 改了什么 |
|---|---|---|
| **U10** | 「点击查旧块会把运行信息写在压缩里面，显示『压缩（在跑）』」 | ⭐ 病根：两件事**共用一把 sidecar 的锁**，而界面只问了「在不在跑」，**没问跑的是哪一件**。加 `runningKind`。⚠️ 说谎的地方不止一处 —— 压缩页签、左栏项目名的「压缩中」、压缩面的「正在写压缩稿…」、按钮变成「停下」，**四处同时**。⛔ 顺手抓到的同源缺陷：查一遍失败之后错误被写进**压缩**那一页的错误位，而压缩页现在靠它判断要不要自己开桌子 —— 会**永远停在「正在读…」**。已分成 `staleErrors` |
| **U11** | 「如果查出来没问题也不会显示，只有一片空白」 | 「查完了，什么都没找到」**是一个结果，不是没有结果**。现在它占住中间那一格，并且把「另有 N 条你已经处理过了」和「库里一个字都没动」一起说清 |
| **U12** | 「压缩和查旧块的 UI 排列应该是一致的」 | 这一页改成和压缩页**同一个骨架**：顶上一句说明 · 一行数（找到/还没决定/用时/花费）· 中间一格结果 · **右下角一个动作按钮**。⛔ 动作按钮不再塞在顶部那一行。⚠️ 顺带补上这一页**从来没有过的失败显示**（原来查一遍失败，屏幕上什么都不变） |
| **U13** | 「『查旧块』名字改成『过期检测』，更加贴切」 | 页签改名。⛔ 仍然不许出现「作废」两个字 ——「过期」不是「作废」 |

**第三轮，他又指了三件**（同一晚，装完第二次之后）：

| | 他说的 | 改了什么 |
|---|---|---|
| **U14** | ⛔ **「压缩跑起来停下会报错：找不到负责联网的那个小程序（spool-ai）。重装一次 Spool 应该能修好。」**「但是这不是错误，是用户主动停下的」 | ⭐⭐ **这一条是这一批里最难看的**：他按一下停下，产品让他去**重装软件**。病根在 Rust —— 取消把子进程杀了，于是 stdout 是空的，而「stdout 空」那条路**只有一种解释**（子进程死在产出信封之前 = 装坏了）。取消走的是同一条路，就被诊断成了装坏了。加 `CANCELLED` 标志，⚠️ 而且**必须排在「stdout 是空的」前面**，排在后面永远走不到。⛔ 停下之后**不落 outcome**：桌子回到按之前的样子，⚠️ 连自动重跑那一次被停下时也不许把第一份端上桌（他按了停下却冒出一份稿子＝「停下没起作用」）。⛔ 那句提示**不说「没花钱」**：已经吐出来的那一段厂商照样算 |
| **U15** | 「过期检测跑起来无法取消」 | 这一页**根本没有停下**。⚠️ 它和压缩共用同一条 sidecar 的路，`cancel()` 本来就管得住它 —— 少的只是那个按钮。现在和压缩逐字一样：跑的时候变成「停下（Ns）」 |
| **U16** | 「左侧边栏看不到压缩中，和过期检测中」 | ⚠️ **这是我上一轮修出来的回归**：U10 把左栏那个徽标按 `runningKind === 'compress'` 挡住了说错的一半，**却没补上说对的一半** —— 于是过期检测跑的时候左栏整个哑了。现在两件事各说各的：「压缩中」/「过期检测中」 |

**做完之后全量验了一遍**：**615 个 TS 测试 + 128 个 Rust 测试全绿**（TS 多 1 个，Rust 多 2 个），
`tsc -b --force` 干净，`cargo clippy` **没有新警告**（12 → 11，少的那条是顺手修掉的
`unused_mut`），`npm run build` 出得来，`i18n-check` 报 `(none missing)`（新加 44 个英文键，
删掉 32 个因为这次改动而没人用的）。

⚠️ **一条差点写进去的错**：改 `gate_proposals` 的时候顺手给引文加了 `.trim()`，
那会把**引文逐字闸**放宽一点点，⛔ **而且放宽之后测试照样是绿的**。已经改回去了 ——
只有给人读的 `why` 才 trim。

⛔ **U 批和 T 批一样，改动做完了但没在真机上点过** —— ✅ **装机的已经换成 U 批第三轮那一份了**
（21:48，见 §1），所以现在缺的只有 Ocean 的手指，逐条验收单在 §4。

---
## 4. 等 Ocean 的

### ⛔ 真手指验收 —— ✅ **换装 21:48 做完了，打开就能验**

✅ `/Applications` 里那份现在是 **U 批第三轮**（含他 21:45 指的那三件，见 §1 和 §3）。
签名身份没变，两个 TCC 授权都还在，真库六张表和备份逐项一致、226 块正文哈希相同。
⭐ **不用再做任何准备动作。**

⭐ **换装之后是怎么确认「装的真是这一批」的**（⚠️ 我自己那条 MCP 连接跑的还是旧二进制，
问它没有意义）：拿**旧包和新包对着比 `strings`** —— `same_block` / `no_block` 这两个新的
`StaleDropped.reason` 值**只有新包里有**（旧包 0 处）。前端那半是压缩存进二进制的，
grep 不到，所以核的是**这次 build 重跑过 vite、`dist/` 和二进制同一分钟（20:55）、
`dist/` 里有「查旧块」「新的取代旧的」而没有「只看我写的」「只退旧的」**。

⭐ **U 批看什么**（三轮加起来十六条，下面按你写的顺序逐条对着看）：

1. 项目里 block 上面**没有那个笔形的「只看我写的」了**；
2. 项目里现在是**三个页签**：内容 / 压缩 / **过期检测**；
3. **点开「压缩」不用先去右栏点任何东西** —— 它自己把这个项目读进来；
   ⚠️ 右栏那一格现在大多数时候**整格不出现**（只有别的项目夜里压好了才冒出来）；
4. 「今晚一起压」的勾选、几点跑、现在就跑、队列清单，**都在「压缩」页签里**（还没压出稿子的时候）；
   **项目管理**里每一行展开之后也多了一个「排进『一起压』」；
5. ⭐⭐ **压到一半按「停下」** —— ⛔ **不该**再弹「找不到 spool-ai，重装一次 Spool 应该能修好」。
   应该是一句「已经停下了」，而且桌子回到你按之前的样子；
6. 压一次，如果报「这几句话里的数字/日期，压缩稿里没有了」，那几个数**应该是真的整份都找不到**了；
   点「加回去」之后**那行红字要消失**（⛔ 不该再出现「已经在压缩稿里了」却红字不走）；
6. 把几块**还原成压缩前的原文**，再回「压缩」页签 —— 「N 块已经压过了」**那个数要跟着变**；
7. 压过的块，工具条上那个 ↺ 现在是**摊开来看**：点一下摊开原文，再点一下收起，**来回都行**；
   ⚠️ 摊开之后里面才有「用回这一份」，那一个才真的改库；
8. 「过期检测」里点「查一遍」：
   - ⚠️ 跑的时候「**压缩**」那个页签**不该**写「（在跑）」，左栏项目名也**不该**写「压缩中」；
   - 查完什么都没找到，中间那一格要写「查完了，这个项目里没有旧块被后面的块整条取代」，
     ⛔ 不该是一片空白；
   - 如果有对不上的，会**把 AI 的原话摆出来**给你看，并写明「是 AI 记错了，不是你的项目有问题」；
   - 这一页的排版应该和「压缩」那一页一样：顶上一句、中间一格、**右下角一个按钮**；
   - ⭐ 跑起来那个按钮变成「**停下（Ns）**」，点得动；
   - ⭐ 左栏那个项目名后面写「**过期检测中**」（压缩跑的时候写「压缩中」）；
9. 三个按钮现在叫 **合并 / 新的取代旧的 / 什么都不动**；
   悬停在很短的块上那句话不再说「退」和「pack」，改说「打包出来反而会多几个字」。

**T 批那三条还是没验过，一起看**（都在「压缩」页签上）：

1. **压一次默认档**，压完之后**不该**出现「第一次不合格，自动重压了一次」那句话 ——
   ⚠️ 之前十次里七八次会出现，而且钱是两次的；
2. 「过期检测」点「查一遍」之后，**已经处理过的那几条不再问一遍**，
   而是写一行「另有 N 条你已经处理过了」；
3. ⚠️ **一条都别急着点** —— 这些判据都是离线验的，今晚第六轮才有真实端点的证据。

⚠️ **第一次开新装的 Spool 会弹一个「允许访问钥匙串」的框** —— 那是对的，点允许。

### ✅ 第四窗那十条，08-23 你已经验过了

⛔ 不用再看。你当场写下的问题就是上面的 U 批，已经全部修完。

### ✅ 已经答完的三件（2026-08-23）

**① key 挪进钥匙串加哪个依赖** → **`security-framework`**（已做完，见 §3）。
**② R1 先于 key 挪完就做了，认不认** → **「认，继续往下」**。
那条护栏的措辞从此**只管 E3**，不再管所有写入。
**③ E3 的「合并」做哪一种** → **A（只连线不动字）**。已做完。
⚠️ 代价记一下：**pack 不会因此变短**。想要真变短那一档（B：把旧块里仍然成立的
几句并进新块）没做，⛔ 理由是「哪几句仍然成立只有你知道」——要做就排 F 批。

### 知会两件（不是问题，是代价）

**① D-c 之后，一次「压缩」最多会真的发出两次请求**（自动重跑，两次的钱都算你的）。
⚠️⚠️ **2026-08-23 实测发现它比说好的凶得多**：判据里有一条「压完还剩 95% 以上」，
而默认档压完就是剩 98% —— **十次里七八次会重跑，钱翻倍，压完还是 98%**。
✅ **已经修了**（T1）：长度这一条从此只对「压到最短」成立，其余两档只挡「比原文还长」。
⛔ 另外三条判据（切不出块 / 重复编号 / 块数对不上）一个字没动。

**② 查过期这个功能不稳。** 同一个项目、同一份 pack 跑五次，给的不是同一批
（4/5 · 2/5 · 1/5）—— **你会看到「昨天没有今天有」**。
⛔ 现在**有意不修**：能修的两条路都要么再花一笔钱、要么压低召回，而它的**假阳性是 0**、
召回只有一对真值撑着。今晚第六轮会把这个数再量一遍，两轮摆一起才知道是模型的性质
还是那一次的运气。

### 不挡路的两条小选择题

在工作计划 §3.2：① 一句话摘要要不要升级（建议不升级）② 机器上那些旧 app 删哪几个。

### ✅ 已经答完的（别再问）

不急着发版（`A → D → B → 渠道 → E → F`）· E1/E2 不提前 · D1 撤销 ·
E3 算发版前的交互（留在 D 批，排最后）· 版本号 0.6.2 认可 · D-k 紧急加 ·
**D-k 的三条真手指验收 08-22 做完了，没问题** ·
A4 休息浮窗四步全成立、功能留下 · 官网问卷升成主按钮 ·
那条「提交按钮不显示」是我排错了，已撤回 ·
**R 批先后 `R5 → R1 → E3` 08-22 拍了**（R4 已完成）。

---

## 5. 动手之前

1. **产品红线** → 工作计划 §4.1。⛔ 只能由 Ocean **明说**推翻，不能作为做别的事的副作用被顺手推倒。
2. **工程护栏** → 工作计划 §4.2。**每一条都曾经被写反过一次，而反过来之后测试照样是绿的。**
3. **要在本机做实机验证** → 工作计划 §4.3 先跑那个 `find`（⛔ **不是 `mdfind`**）。
   **16 个** app 认领着同一批 bundle id，`open -a <绝对路径>` 会被 LaunchServices 劫持到别的实例上，**而且不报错**。
4. **文案** → 大白话；**少用 emoji**（2026-08-22 Ocean 再次提醒）；一屏最多一个 ⚠️。

---

## 6. 下一窗结束时，这份要更新哪几处

⛔ **别在这份里堆任务** —— 任务的家在工作计划。这份只更新五处：

1. §1 那张状态表（分支 / HEAD / 工作区干不干净 / 装机的是哪一份 / 发布了什么）
2. §2「这一窗做什么」换成新的一批
3. §3「上一窗做了什么」覆盖掉
4. §4 拿到答案的划掉，新冒出来的加上
5. 做完的任务**从工作计划里删掉**（不是打勾），完成记录留在 §3
