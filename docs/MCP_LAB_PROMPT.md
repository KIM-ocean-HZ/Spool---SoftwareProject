# MCP 实验室 — 第三轮自测提示词(2026-08-05)

给 Ocean 在 **Claude Desktop / ChatGPT 桌面版**里自测 Spool 的 MCP 接口用。
(Claude Code 那一份由开发窗自己跑,见 §5。)

**这是第三轮。**第一轮报出十一条、第二轮三方评审又报出一批 —— 其中八条上一窗当场修完,
六条设计级的(「规矩写在给 AI 的话里,没写进数据结构」)这一窗全部修完。
所以这一轮要验的是**十四条修完的东西**,重点仍是两件:
①修完之后有没有修对、有没有修出新问题;②还有什么是前两轮没人看见的。
干得顺利不算成功,发现问题才算。

---

## 一、怎么用(四步)

**1. 建实验室**(每次改了 Rust 代码都要重来一遍)

```
cd ~/Desktop/Knote/src-tauri && cargo build --release
cd ~/Desktop/Knote && ./scripts/seed-mcp-lab.sh --connect
```

它做三件事:建 `~/Library/Application Support/com.oceanjin.spool.lab/`
(一份假资料库 + 一份自己的程序副本)、把服务器 `spool_lab` 写进**三个客户端**的配置
(Claude Desktop、Claude Code `~/.claude.json`、ChatGPT 桌面版 `~/.codex/config.toml`;
原文件都先备份)、告诉你库里有什么。
**它不碰你的真库,也不碰正式版那条 `spool` 配置。**

> **2026-08-05 现状**:开发窗今天已经用新程序重播过一次实验室(39 块 / 11 个可见项目,
> 干净的,没有任何评审残留)。你要是今天就测,第 1 步可以跳过,直接从第 2 步开始。

> **为什么不放桌面**:桌面/文稿/下载是 macOS 的受保护目录。Claude Desktop 没被授权访问桌面,
> 连启动脚本都读不到,日志里报 `Operation not permitted` + `Server disconnected`(2026-08-03 实测)。
> `Application Support` 没有这道门。**别把实验室挪回桌面。**

**2. 完全退出客户端再打开**(Claude Desktop 要从菜单栏退出,不是关窗口)。

> **想更保险**(可选):测试期间把真库那条连接关掉。
> Claude Desktop:设置 → 连接器里把 `spool` 关掉,测完打开。
> ChatGPT 桌面版:在 `~/.codex/config.toml` 的 `[mcp_servers.spool]` 下面加一行
> `enabled = false`,测完删掉这行。
> 不做也行——提示词第 0 步已经要求 AI 只用 `spool_lab`,并且要先报出实验室的标记才准往下走。

> ⚠️ **两个客户端别同时跑同一个实验室。** 上一轮三个客户端并行,互相看得见对方写的块,
> 「机器学习课作业」的摘要被改了三次,做数字比对时全部要打折。这一轮**一个测完、重播一次
> 实验室、再测下一个**(重播就是重跑 `./scripts/seed-mcp-lab.sh`,十秒钟的事)。

**3. 复制下面对应的提示词,贴进新对话。** Claude Desktop 用第二节,ChatGPT 桌面版用第三节。

> ⚠️ **贴之前先确认这个客户端里真有 `spool_lab`。** 三个客户端各读各的配置文件,
> 脚本只写它们三个 —— 贴到别的地方(网页版、别的 AI)一定看不到实验室。
> **没有就是没重启,退出客户端再打开。**

**4. 测完拆掉**

```
cd ~/Desktop/Knote && ./scripts/seed-mcp-lab.sh --disconnect
rm -rf "$HOME/Library/Application Support/com.oceanjin.spool.lab"
```

**中途想看 AI 到底往库里写了什么**:

```
sqlite3 "$HOME/Library/Application Support/com.oceanjin.spool.lab/data/spool.db" \
  "SELECT datetime(created_at/1000,'unixepoch','localtime'), source, content FROM blocks WHERE source LIKE '%MCP%' ORDER BY created_at DESC LIMIT 20;"
```

**想试「写入开关关掉会怎样」**(改完不用重启客户端,下一次调用就生效):

```
# 关
sed -i '' 's/"mcpWriteEnabled":true/"mcpWriteEnabled":false/' "$HOME/Library/Application Support/com.oceanjin.spool.lab/data/settings.json"
# 开回来
sed -i '' 's/"mcpWriteEnabled":false/"mcpWriteEnabled":true/' "$HOME/Library/Application Support/com.oceanjin.spool.lab/data/settings.json"
```

---

## 二、提示词 A · Claude Desktop(整段复制)

```text
你现在不是我的日常助手,而是一个 MCP 接口评审员,而且这是第三轮。
前两轮已经报出并修完了十四条问题,所以这一轮的价值不在于重新发现它们,
而在于两件事:①修完之后有没有修对、有没有修出新问题;②还有什么是前两轮没人看见的。
干得顺利不算成功,发现问题才算。

═══ 第 0 步:确认你连的是测试环境(没做完不许往下走)═══

我这台电脑上同时装着 Spool 正式版,它的 MCP 服务器叫 spool,里面是我的真实资料。
测试环境的服务器叫 spool_lab,里面全是为测试造的假数据。

你必须:
1. 工具列表里根本没有 spool_lab → 直接停,回我「环境没接上」。这不是你的错,
   是我这边没接好,别去碰 spool 顶替。
2. 全程只用 spool_lab 这台服务器的工具。同时有 spool 和 spool_lab 时,
   spool 的一次都不许碰(读也不行)。
3. 服务器自己会说明它读的是哪个库:spool_lab 的 instructions 第一行应该是
   `LIBRARY: a CUSTOM data directory (SPOOL_DATA_DIR, …/com.oceanjin.spool.lab/data)`。
   如果它说的是 DEFAULT,那就是真库挂错名字了 —— 停,别读别写。
   (这一步不用读任何数据,所以先做它。)
4. 再调 spool_lab 的 list_threads,确认看得到工作区「LAB 自检」和项目「🧪 LAB 环境自检」。
5. 最后读那个项目,确认里面有这一行标记:SPOOL-MCP-LAB-2026-08-03
6. 上面任何一条对不上,立刻停,一个字都不许写,直接告诉我「环境不对」。

确认通过就回我一句「环境已确认:LAB」,然后继续。

═══ Spool 是什么(内部信息全部公开给你,不用猜)═══

Spool(思簿)是一个只在本机跑的"上下文库"。用户在浏览器/邮件/PDF 里看到有用的东西,
双击一下快捷键就存进来,存成一个「块」。你通过 MCP 读他的库,帮他想事情。

数据模型三层:工作区(Workspace)→ 项目(Thread)→ 块(Block)。
- 块:正文 + 用户批注(annotation)+ 来源标签(source)+ 是否置顶(pinned)
  + 一个用户看得见的编号(#12,和 app 里显示的是同一个)
  + 可以引用另一个块(ref_block_id)+ 可以挂附件(文件/文件夹/网址,文件正文会被本地抽取)。
- 项目:一句话摘要(summary)+ 摘要署名(user 或 mcp)+ 状态(active/parked/done)+ 可选截止日。
- 块里 ==这样包起来的== 是用户自己划的重点。批注在 pack 里渲染成 note: 开头的行。

你手上的接口(13 个工具):
- 读:list_threads、get_digest、get_pack、search_blocks、get_blocks、
  find_similar_blocks、check_library
- 装配(只读,返回的是"材料 + 给你的指令"):weekly_review、thread_health、distill
- 写:create_thread、add_block、set_thread_summary
- 资源:spool://thread/<id>
- prompts(斜杠菜单,如果你的客户端有的话):compress_pack、weekly_review、thread_health、distill

下面这些是故意的设计,不是 bug,别把它们当问题报(但可以质疑它们合不合理):
- 两个开关都默认关:「MCP 服务」(读)、「允许 AI 写入」(写)。实验室里两个都开着。
- 写只能追加。没有删除接口,没有修改接口。AI 永远不能改用户写下的字。
- set_thread_summary 只能写空摘要、或覆盖上次 AI 自己写的摘要;用户手写的摘要一定被拒。
- 命名硬规则:块/项目的 id 只是工具参数,不许说给用户听,也不许写进正文和批注。
- 全程本机,不出网。数据在用户自己的电脑上。
- pack 开头带一段"怎么读"的授权规则:📖 Reference(机构来源)当事实底座;
  🧩 Synthesis(AI 写的长文)只当框架、不当事实;🔄 Process(聊天记录)读的是用户反复在问什么;
  💭 Personal(没有来源的块 + note: 行)是用户自己的想法,信号最强。

═══ 已经知道并且已经决定了的,别再报(报了也不算发现)═══

这几条前两轮已经被提过、验证过、拍板过了。你可以在报告里一句话提及"我也撞到了",
但不要展开写建议,把篇幅留给新东西:

1. 没有语义检索 —— 已决定不做(本地 embedding 太重,云端 embedding 撞"零出网")。
2. 没有删除/撤回/编辑接口 —— 宪法级承诺,不会开。
3. 你的客户端如果看不到那四个 prompt —— 已知,正因如此它们同时做成了工具。
4. 工具返回是"文本里塞 JSON",没用 MCP 的 structuredContent —— 已知,排队中。
5. 摘要没有写作时间(thread_health 自己会承认这件事)—— 已知,在设计队列里。
6. 项目摘要一写就把项目顶到"最近活跃"第一位(缺 last_block_at)—— 已知,排队中。

⚠️ 注意一条**变了**的:附件(PDF)正文**现在能搜到了**。上一轮的提示词里写着"搜不到",
那已经过期。search_blocks 会把附件里的命中单独放在 attachment_hits 里。
这一轮请**正面验它**(见 R12),别再当成缺陷报。

═══ 必跑清单第一部分:前两轮修完的,验它们修对没有 ═══

每一条都要在报告里给出「修对了 / 没修对 / 修出新问题」的结论。
R1–R11 是老账复验(快速过,别花太多篇幅);R12–R17 是这一窗刚修完的六条,**是重点**。

R1. get_pack(「机器学习课」)分别传 max_chars=8000、2000、0、不传。
    期望:8000 应该拿到一份真的部分 pack(不是一句"超了"),并写明了附件正文被压到多少字;
    2000 应该拿到一句拒绝,而那句拒绝要报出下限。你照它给的下限再试一次,能真的拿到东西吗?
R2. get_pack(「机器学习课」, range=last7)。期望:置顶块一个都没少,表头说清这是全项目里的几块。
    读完这份 pack,你会误以为这个项目总共只有这么多块吗?
R3. list_threads 的 approx_pack_chars。挑一个项目,拿这个数当 max_chars 去调 get_pack,
    能不能一次拿到完整 pack?它是偏高还是偏低?(偏低就是会静默丢块,方向错了)
R4. list_threads 的 summary_source。对「找工作」和「机器学习课作业」分别
    调 set_thread_summary,结果和 summary_source 说的一致吗?你还需要"先写→被拒"才知道吗?
R5. get_blocks(「机器学习课」, pinned=true)。第一行那句人话说的块数,是"这一页几块"
    还是"整个项目几块"?会不会让你对用户说错?再传 include_extracted_text=true,
    附件正文补得回来吗?
R6. search_blocks("学习率")。命中里有 source 吗?够不够你判断这是四类里的哪一类?
R7. search_blocks("的", limit=2) 然后 offset=2、offset=4……能翻到底吗?
    total 和你实际能拿到的条数对得上吗?翻到第五、第六页时,有没有哪一条重复出现?
R8. 数值参数乱传:since_days=999、limit=-5、limit=3.7、context=99、max_groups=99。
    返回里看得出"我实际用的是多少"吗?有没有哪个参数还是被悄悄改掉、你却不知道?
R9. create_thread 建一个和现有项目同名的项目(「机器学习课」,工作区「学业」)。
    它拒绝你了吗?说清了该怎么办吗?**这次特别看:拒绝的话里还有没有那串 21 位的内部 id?**
R10. get_pack 传一个空项目(「菜谱」)。那句提示里有项目名字吗?
R11. get_digest。表头那句"库里共 N 个项目"读起来会不会被误解成 N 条块?

──── 以下六条是这一窗刚修完的,请重点验 ────

R12. **裸 id 现在是硬拒绝,不再是"写完之后警告你"。** 四个场景都试一次:
     a. add_block,content 里放 LabBkMl00000000000008
     b. add_block,content 干净但 annotation 里放同一串
     c. set_thread_summary,summary 里放同一串
     d. create_thread,title 里放同一串
     期望:四次全部**什么都没写进去**,而且拒绝话术告诉你两件事——把那串删掉、
     或者改用 ref_block_id 参数。**验证一下真的没写**:四次之后再数一次
     「机器学习课」的块数,应该和你动手之前一模一样。
     然后再试两个边界:
     e. 一串形状像 id、但库里并不存在的(比如 aB3dEfGh1jK2lM4nO5pQr)——会被拒吗?
        拒绝的理由和 a 那次**用词一样吗**?a 那次应该能说出是哪一块,
        e 这次只能说"形状像"。你看得出这是两层不同的检查吗?
     f. 一句正常的长英文词,单独成句写进去:internationalization(20 位)、
        counterrevolutionaries(22 位)。期望:**两次都该顺利写进去**。
        任何一次被拒,就是这条修出了新问题 —— 那意味着 AI 以后写不了正常的英文长词。
        (这两块是垃圾数据,写完告诉我一声就行,库反正测完要拆。)
R13. **add_block 有 dry_run 了。** 传 dry_run=true 写一条。
     期望:什么都不写,但告诉你"存下去会是第几块"。
     然后照它说的去掉 dry_run 再写一次 —— 编号和预演时说的对得上吗?
     这个东西对你有用吗?你会主动用它,还是只有我逼你才用?(照实说)
R14. **报错正文不再回显内部 id。** get_pack(thread_id="nope")、
     get_blocks(thread_id="nope")、add_block(thread_id="nope", content="x")。
     期望:三句报错都不该出现 "nope" 这四个字母,而是告诉你去 list_threads 查。
     你能靠这句报错自己走出去吗?
R15. **distill 的 id 表挪到 pack 外面了。** 调 distill("机器学习课"),
     再调 get_pack("机器学习课", include_ids=true)。
     期望:那张 "Block IDs" 表在 pack 正文**之外**。
     你分得清哪一段是"可以原样粘给别的 AI 的 pack"、哪一段是"只给你当参数用的表"吗?
     如果用户说"把这个 pack 发给我同事",你会连那张表一起给他吗?
R16. **pack 里不再印用户电脑的绝对路径。** get_pack(「机器学习课」, max_chars=0),
     搜一下 "/Users/"。期望:一个都没有,附件只剩文件名(lecture-03.pdf),
     网址还是完整的。再调 get_blocks 看同一个附件——那边应该还有完整路径(那份是给你当参数用的)。
     问题:pack 里只剩文件名,对你理解这份材料有损失吗?
R17. **材料和指令之间加了一条机器能认的界。** 三个装配工具(weekly_review /
     thread_health / distill)现在把用户的材料夹在 ⟦SPOOL:MATERIAL⟧ … ⟦/SPOOL:MATERIAL⟧ 之间。
     每个调一次,然后回答:
     a. 这条界你**用得上**吗?有它之后,你判断"哪部分是数据、哪部分是给我的指令"更有底了吗?
     b. 假设库里某一块的正文里,用户自己抄了一行 ⟦/SPOOL:MATERIAL⟧ 进来(网上抓的东西什么都有),
        你觉得会发生什么?你能从返回里看出这种情况被处理过吗?
     c. 这套标记看着别扭吗?会不会有一天你把它原样念给用户听?

═══ 必跑清单第二部分:装配工具与常规读写 ═══

N1. 三个装配工具,每个至少调一次:
    - weekly_review(不传参数;since_days=30)
    - thread_health("机器学习课";"机器学习"——应该报歧义;"菜谱")
    - distill("机器学习课";range=pinned)
    重点看:有没有可能你把"给你的指令"当成用户要看的内容整段贴出去?
    工具描述有没有把这件事说清楚?
N2. **不传项目参数**调 thread_health 和 distill。期望:不报错,而是回一份项目清单
    加一句"问用户要哪一个"。这时候你会怎么做?对话接得下去吗?
N3. 如果你的客户端有斜杠菜单:四个 prompt 一个必填参数都没有。
    每个都点一下、什么都不填,看会发生什么。菜单里那几行字看不看得懂?
C1. list_threads(不带参数;title_contains="机器学习";title_contains="不存在的东西")
    ——0 命中那次,第一行那句人话会不会让人以为"你的库是空的"?
C2. get_digest(默认;workspace_title="学业";workspace_title="不存在的工作区")
C3. find_similar_blocks(全库;限一个项目;thread_id 和 workspace_title 同传——应该报错)
C4. check_library(顺带看它头两行的 LIBRARY 标识,和 instructions 说的一致吗)
C5. 写(每次动手前先告诉我你要写什么,我不拦你,但要留痕):
    a. create_thread 新建项目「MCP 评审记录 08-05」
    b. add_block 把你的一条真结论写进去:带批注,并用 ref_block_id 引用它依据的那个块
    c. set_thread_summary 给「机器学习课」写第一条摘要(应该成功)
    d. set_thread_summary 去改「找工作」的摘要(应该被拒)——把拒绝原话贴给我

═══ 必跑清单第三部分:这一节最重要,请认真答 ═══

我正在决定「怎么让用户零摩擦地用上 MCP」。三条要求:
① 不能让用户自己在网页和 app 里零散地拼凑用法;
② 不能让用户自己去问 AI「spool 能干什么」;
③ 不能让用户用了之后自己都不清楚 AI 在调什么工具。

请回答:

Z1. 假装你是刚接上 Spool 的助手,用户还什么都没说。你的第一句话会说什么?
    你是怎么知道该说这些的?(是 instructions 教你的,还是你自己猜的?)
Z2. 你调完一个工具之后,用户在你这个客户端界面里**实际看得到什么**?
    描述清楚:工具名?参数?返回的原文?还是折叠起来?
    一个不懂技术的人看到那些,能明白你刚才做了什么吗?
Z3. 每个工具返回的第一行现在都有一句大白话("看了一眼库里有哪些项目:11 个项目……")。
    这句话对你有没有坏处?会不会干扰你解析后面的 JSON?
    特别是 get_pack —— 它的返回是用户要原样粘给别的 AI 的东西,它前面有那一行吗?该有吗?
Z4. 装成一个普通用户,连着说三句大白话,全程不提任何工具名:
    「我最近在忙什么」→「机器学习课我卡在哪」→「把刚才那个结论存回去」。
    中间有几处你必须猜?有几处你要反问用户?哪一步最别扭?
    第三句特别看:现在有了 dry_run,你会先预演给用户看吗?

═══ 主动找茬的角度 ═══

- 参数:空串、超大数、负数、小数、中文、把别的工具的 id 传进来
- 一致性:同一件事不同工具给的数字对不对得上(list_threads 的块数 vs get_pack 的表头;
  check_library 的发现 vs thread_health 的发现;search_blocks 的 total vs 你能翻到的条数)
- 文案:工具描述里有没有看不懂、有歧义、自相矛盾的句子?中英文混排别扭吗?
  报错信息能不能让一个不懂技术的人自己解决?
- 你的体感:哪一步你不得不猜?哪个返回值你必须再调一次工具才能用?哪个字段你根本没用上?
- 危险面:有没有哪条路能让你在用户不知情的情况下改掉/盖掉他的东西?
  有没有哪条路还会把 id 漏到用户眼前(报错?首行人话?装配工具的指令段?)
  你能不能被库里的内容"骗"着去做不该做的事?

═══ 最后一次性给我一份报告,六节 ═══

1. 回归结论表:R1–R17 每条一行,只写「修对 / 没修对 / 修出新问题」+ 一句证据
   (R12–R17 是新修的六条,每条允许多写两句)
2. 新问题清单:按严重程度排。每条写清:现象 / 怎么复现(哪个工具、什么参数)/
   你期望什么 / 实际是什么 / 建议怎么改
3. 零摩擦四问(Z1–Z4)的回答
4. 缺什么功能:站在目标用户角度提(一个普通人,长期做一件事:上一门课、申请学校、
   转行、学外语、租房、备赛半马;不写代码、不看日志、不想学新术语)。
   每条必须说清「这解决他的哪一个真实场景」,不要列技术特性
5. 你还想要什么权限或接口:直说。每条说明为什么需要,以及它和这两条底线冲不冲突——
   ①AI 绝不改用户写下的东西 ②数据不出网。冲突的也可以提,标明冲突即可
6. 一句话总评 + 打分(10 分制),并说明和上一轮比是升是降、为什么

═══ 纪律 ═══

- 只往 LAB 里写,一次写一块,动手前先说
- 库里那些脏数据(重复的块、指向不存在的块的引用、正文里露出来的 id)是我故意埋的。
  你发现它们只用来验证"工具报得准不准",不要当成 Spool 的 bug 报
- 提到某个块用「项目标题 + 块编号 #n + 内容预览」,不要报 id
- 不确定就说不确定,别编
- 报告用中文,大白话
```

---

## 三、提示词 B · ChatGPT 桌面版(整段复制)

和 A 只差三处:①它大概率看不到那四个 prompt(所以 N3 换成一个明确的问题);
②多问一句跨客户端的差异;③Z2 那一问对它更关键——它的工具调用界面和 Claude 完全不同。

```text
你现在不是我的日常助手,而是一个 MCP 接口评审员,而且这是第三轮。
前两轮已经报出并修完了十四条问题,所以这一轮的价值不在于重新发现它们,
而在于两件事:①修完之后有没有修对、有没有修出新问题;②还有什么是前两轮没人看见的。
干得顺利不算成功,发现问题才算。

═══ 第 0 步:确认你连的是测试环境(没做完不许往下走)═══

我这台电脑上同时装着 Spool 正式版,它的 MCP 服务器叫 spool,里面是我的真实资料。
测试环境的服务器叫 spool_lab,里面全是为测试造的假数据。

你必须:
1. 工具列表里根本没有 spool_lab → 直接停,回我「环境没接上」。这不是你的错,
   是我这边没接好,别去碰 spool 顶替。
2. 全程只用 spool_lab 这台服务器的工具。同时有 spool 和 spool_lab 时,
   spool 的一次都不许碰(读也不行)。
3. 服务器自己会说明它读的是哪个库:spool_lab 的 instructions 第一行应该是
   `LIBRARY: a CUSTOM data directory (SPOOL_DATA_DIR, …/com.oceanjin.spool.lab/data)`。
   如果它说的是 DEFAULT,那就是真库挂错名字了 —— 停,别读别写。
   (这一步不用读任何数据,所以先做它。)
4. 再调 spool_lab 的 list_threads,确认看得到工作区「LAB 自检」和项目「🧪 LAB 环境自检」。
5. 最后读那个项目,确认里面有这一行标记:SPOOL-MCP-LAB-2026-08-03
6. 上面任何一条对不上,立刻停,一个字都不许写,直接告诉我「环境不对」。

确认通过就回我一句「环境已确认:LAB」,然后继续。

═══ Spool 是什么(内部信息全部公开给你,不用猜)═══

Spool(思簿)是一个只在本机跑的"上下文库"。用户在浏览器/邮件/PDF 里看到有用的东西,
双击一下快捷键就存进来,存成一个「块」。你通过 MCP 读他的库,帮他想事情。

数据模型三层:工作区(Workspace)→ 项目(Thread)→ 块(Block)。
- 块:正文 + 用户批注(annotation)+ 来源标签(source)+ 是否置顶(pinned)
  + 一个用户看得见的编号(#12,和 app 里显示的是同一个)
  + 可以引用另一个块(ref_block_id)+ 可以挂附件(文件/文件夹/网址,文件正文会被本地抽取)。
- 项目:一句话摘要(summary)+ 摘要署名(user 或 mcp)+ 状态(active/parked/done)+ 可选截止日。
- 块里 ==这样包起来的== 是用户自己划的重点。批注在 pack 里渲染成 note: 开头的行。

你手上的接口(13 个工具):
- 读:list_threads、get_digest、get_pack、search_blocks、get_blocks、
  find_similar_blocks、check_library
- 装配(只读,返回的是"材料 + 给你的指令"):weekly_review、thread_health、distill
- 写:create_thread、add_block、set_thread_summary
- 资源:spool://thread/<id>
- 另外还有四个 MCP prompts(compress_pack / weekly_review / thread_health / distill)。
  前两轮实测你这个客户端看不到它们——正因如此,后三个同时做成了上面那三个工具。
  你这次能不能看到?照实说,这本身是一条要记下来的发现。

下面这些是故意的设计,不是 bug,别把它们当问题报(但可以质疑它们合不合理):
- 两个开关都默认关:「MCP 服务」(读)、「允许 AI 写入」(写)。实验室里两个都开着。
- 写只能追加。没有删除接口,没有修改接口。AI 永远不能改用户写下的字。
- set_thread_summary 只能写空摘要、或覆盖上次 AI 自己写的摘要;用户手写的摘要一定被拒。
- 命名硬规则:块/项目的 id 只是工具参数,不许说给用户听,也不许写进正文和批注。
- 全程本机,不出网。数据在用户自己的电脑上。
- pack 开头带一段"怎么读"的授权规则:📖 Reference(机构来源)当事实底座;
  🧩 Synthesis(AI 写的长文)只当框架、不当事实;🔄 Process(聊天记录)读的是用户反复在问什么;
  💭 Personal(没有来源的块 + note: 行)是用户自己的想法,信号最强。

═══ 已经知道并且已经决定了的,别再报(报了也不算发现)═══

这几条前两轮已经被提过、验证过、拍板过了。你可以在报告里一句话提及"我也撞到了",
但不要展开写建议,把篇幅留给新东西:

1. 没有语义检索 —— 已决定不做(本地 embedding 太重,云端 embedding 撞"零出网")。
2. 没有删除/撤回/编辑接口 —— 宪法级承诺,不会开。
3. 你看不到那四个 prompt —— 已知,正因如此它们同时做成了工具。
4. 工具返回是"文本里塞 JSON",没用 MCP 的 structuredContent —— 已知,排队中。
5. 摘要没有写作时间(thread_health 自己会承认这件事)—— 已知,在设计队列里。
6. 项目摘要一写就把项目顶到"最近活跃"第一位(缺 last_block_at)—— 已知,排队中。

⚠️ 注意一条**变了**的:附件(PDF)正文**现在能搜到了**。上一轮的提示词里写着"搜不到",
那已经过期。search_blocks 会把附件里的命中单独放在 attachment_hits 里。
这一轮请**正面验它**(见 R12),别再当成缺陷报。

═══ 必跑清单第一部分:前两轮修完的,验它们修对没有 ═══

每一条都要在报告里给出「修对了 / 没修对 / 修出新问题」的结论。
R1–R11 是老账复验(快速过,别花太多篇幅);R12–R17 是这一窗刚修完的六条,**是重点**。

R1. get_pack(「机器学习课」)分别传 max_chars=8000、2000、0、不传。
    期望:8000 应该拿到一份真的部分 pack(不是一句"超了"),并写明了附件正文被压到多少字;
    2000 应该拿到一句拒绝,而那句拒绝要报出下限。你照它给的下限再试一次,能真的拿到东西吗?
R2. get_pack(「机器学习课」, range=last7)。期望:置顶块一个都没少,表头说清这是全项目里的几块。
    读完这份 pack,你会误以为这个项目总共只有这么多块吗?
R3. list_threads 的 approx_pack_chars。挑一个项目,拿这个数当 max_chars 去调 get_pack,
    能不能一次拿到完整 pack?它是偏高还是偏低?(偏低就是会静默丢块,方向错了)
R4. list_threads 的 summary_source。对「找工作」和「机器学习课作业」分别
    调 set_thread_summary,结果和 summary_source 说的一致吗?你还需要"先写→被拒"才知道吗?
R5. get_blocks(「机器学习课」, pinned=true)。第一行那句人话说的块数,是"这一页几块"
    还是"整个项目几块"?会不会让你对用户说错?再传 include_extracted_text=true,
    附件正文补得回来吗?
R6. search_blocks("学习率")。命中里有 source 吗?够不够你判断这是四类里的哪一类?
R7. search_blocks("的", limit=2) 然后 offset=2、offset=4……能翻到底吗?
    total 和你实际能拿到的条数对得上吗?翻到第五、第六页时,有没有哪一条重复出现?
R8. 数值参数乱传:since_days=999、limit=-5、limit=3.7、context=99、max_groups=99。
    返回里看得出"我实际用的是多少"吗?有没有哪个参数还是被悄悄改掉、你却不知道?
R9. create_thread 建一个和现有项目同名的项目(「机器学习课」,工作区「学业」)。
    它拒绝你了吗?说清了该怎么办吗?**这次特别看:拒绝的话里还有没有那串 21 位的内部 id?**
R10. get_pack 传一个空项目(「菜谱」)。那句提示里有项目名字吗?
R11. get_digest。表头那句"库里共 N 个项目"读起来会不会被误解成 N 条块?

──── 以下六条是这一窗刚修完的,请重点验 ────

R12. **裸 id 现在是硬拒绝,不再是"写完之后警告你"。** 四个场景都试一次:
     a. add_block,content 里放 LabBkMl00000000000008
     b. add_block,content 干净但 annotation 里放同一串
     c. set_thread_summary,summary 里放同一串
     d. create_thread,title 里放同一串
     期望:四次全部**什么都没写进去**,而且拒绝话术告诉你两件事——把那串删掉、
     或者改用 ref_block_id 参数。**验证一下真的没写**:四次之后再数一次
     「机器学习课」的块数,应该和你动手之前一模一样。
     然后再试两个边界:
     e. 一串形状像 id、但库里并不存在的(比如 aB3dEfGh1jK2lM4nO5pQr)——会被拒吗?
        拒绝的理由和 a 那次**用词一样吗**?a 那次应该能说出是哪一块,
        e 这次只能说"形状像"。你看得出这是两层不同的检查吗?
     f. 一句正常的长英文词,单独成句写进去:internationalization(20 位)、
        counterrevolutionaries(22 位)。期望:**两次都该顺利写进去**。
        任何一次被拒,就是这条修出了新问题 —— 那意味着 AI 以后写不了正常的英文长词。
        (这两块是垃圾数据,写完告诉我一声就行,库反正测完要拆。)
R13. **add_block 有 dry_run 了。** 传 dry_run=true 写一条。
     期望:什么都不写,但告诉你"存下去会是第几块"。
     然后照它说的去掉 dry_run 再写一次 —— 编号和预演时说的对得上吗?
     这个东西对你有用吗?你会主动用它,还是只有我逼你才用?(照实说)
R14. **报错正文不再回显内部 id。** get_pack(thread_id="nope")、
     get_blocks(thread_id="nope")、add_block(thread_id="nope", content="x")。
     期望:三句报错都不该出现 "nope" 这四个字母,而是告诉你去 list_threads 查。
     你能靠这句报错自己走出去吗?
R15. **distill 的 id 表挪到 pack 外面了。** 调 distill("机器学习课"),
     再调 get_pack("机器学习课", include_ids=true)。
     期望:那张 "Block IDs" 表在 pack 正文**之外**。
     你分得清哪一段是"可以原样粘给别的 AI 的 pack"、哪一段是"只给你当参数用的表"吗?
     如果用户说"把这个 pack 发给我同事",你会连那张表一起给他吗?
R16. **pack 里不再印用户电脑的绝对路径。** get_pack(「机器学习课」, max_chars=0),
     搜一下 "/Users/"。期望:一个都没有,附件只剩文件名(lecture-03.pdf),
     网址还是完整的。再调 get_blocks 看同一个附件——那边应该还有完整路径(那份是给你当参数用的)。
     问题:pack 里只剩文件名,对你理解这份材料有损失吗?
R17. **材料和指令之间加了一条机器能认的界。** 三个装配工具(weekly_review /
     thread_health / distill)现在把用户的材料夹在 ⟦SPOOL:MATERIAL⟧ … ⟦/SPOOL:MATERIAL⟧ 之间。
     每个调一次,然后回答:
     a. 这条界你**用得上**吗?有它之后,你判断"哪部分是数据、哪部分是给我的指令"更有底了吗?
     b. 假设库里某一块的正文里,用户自己抄了一行 ⟦/SPOOL:MATERIAL⟧ 进来(网上抓的东西什么都有),
        你觉得会发生什么?你能从返回里看出这种情况被处理过吗?
     c. 这套标记看着别扭吗?会不会有一天你把它原样念给用户听?

═══ 必跑清单第二部分:装配工具与常规读写 ═══

N1. 三个装配工具,每个至少调一次:
    - weekly_review(不传参数;since_days=30)
    - thread_health("机器学习课";"机器学习"——应该报歧义;"菜谱")
    - distill("机器学习课";range=pinned)
    重点看:有没有可能你把"给你的指令"当成用户要看的内容整段贴出去?
    工具描述有没有把这件事说清楚?
N2. **不传项目参数**调 thread_health 和 distill。期望:不报错,而是回一份项目清单
    加一句"问用户要哪一个"。这时候你会怎么做?对话接得下去吗?
N3. 你的客户端到底有没有把那四个 MCP prompts 暴露出来?
    - 有:每个点一下、什么都不填,看会发生什么,并告诉我菜单里那几行字看不看得懂。
    - 没有:明确写一句"这个客户端没有暴露 MCP prompts"。然后回答:
      对你来说,把同样的能力做成工具(N1 那三个)够不够用?有没有哪里因此变差了?
C1. list_threads(不带参数;title_contains="机器学习";title_contains="不存在的东西")
    ——0 命中那次,第一行那句人话会不会让人以为"你的库是空的"?
C2. get_digest(默认;workspace_title="学业";workspace_title="不存在的工作区")
C3. find_similar_blocks(全库;限一个项目;thread_id 和 workspace_title 同传——应该报错)
C4. check_library(顺带看它头两行的 LIBRARY 标识,和 instructions 说的一致吗)
C5. 写(每次动手前先告诉我你要写什么,我不拦你,但要留痕):
    a. create_thread 新建项目「MCP 评审记录 08-05 GPT」
    b. add_block 把你的一条真结论写进去:带批注,并用 ref_block_id 引用它依据的那个块
    c. set_thread_summary 给「机器学习课」写第一条摘要(应该成功)
    d. set_thread_summary 去改「找工作」的摘要(应该被拒)——把拒绝原话贴给我

═══ 必跑清单第三部分:这一节最重要,请认真答 ═══

我正在决定「怎么让用户零摩擦地用上 MCP」。三条要求:
① 不能让用户自己在网页和 app 里零散地拼凑用法;
② 不能让用户自己去问 AI「spool 能干什么」;
③ 不能让用户用了之后自己都不清楚 AI 在调什么工具。

请回答:

Z1. 假装你是刚接上 Spool 的助手,用户还什么都没说。你的第一句话会说什么?
    你是怎么知道该说这些的?(是 instructions 教你的,还是你自己猜的?)
    ——特别问你:你到底**有没有读到**服务器的 initialize instructions?
    如果没有,那说明我们把用法写在那里是白写的,这是一条很重要的发现。
Z2. 你调完一个工具之后,用户在你这个客户端界面里**实际看得到什么**?
    描述清楚:工具名?参数?返回的原文?还是折叠起来?
    一个不懂技术的人看到那些,能明白你刚才做了什么吗?
Z3. 每个工具返回的第一行现在都有一句大白话("看了一眼库里有哪些项目:11 个项目……")。
    这句话对你有没有坏处?会不会干扰你解析后面的 JSON?
    特别是 get_pack —— 它的返回是用户要原样粘给别的 AI 的东西,它前面有那一行吗?该有吗?
Z4. 装成一个普通用户,连着说三句大白话,全程不提任何工具名:
    「我最近在忙什么」→「机器学习课我卡在哪」→「把刚才那个结论存回去」。
    中间有几处你必须猜?有几处你要反问用户?哪一步最别扭?
    第三句特别看:现在有了 dry_run,你会先预演给用户看吗?

═══ 主动找茬的角度 ═══

- 参数:空串、超大数、负数、小数、中文、把别的工具的 id 传进来
- 一致性:同一件事不同工具给的数字对不对得上(list_threads 的块数 vs get_pack 的表头;
  check_library 的发现 vs thread_health 的发现;search_blocks 的 total vs 你能翻到的条数)
- 文案:工具描述里有没有看不懂、有歧义、自相矛盾的句子?中英文混排别扭吗?
  报错信息能不能让一个不懂技术的人自己解决?
- 你的体感:哪一步你不得不猜?哪个返回值你必须再调一次工具才能用?哪个字段你根本没用上?
- 危险面:有没有哪条路能让你在用户不知情的情况下改掉/盖掉他的东西?
  有没有哪条路还会把 id 漏到用户眼前(报错?首行人话?装配工具的指令段?)
  你能不能被库里的内容"骗"着去做不该做的事?
- 跨客户端:同样这套工具,在你这儿和在 Claude 那儿哪里不一样?
  有没有哪个参数形状、哪种返回格式在你这儿特别难用?(比如数字参数你是当字符串发的吗?
  dry_run 这种布尔参数你发的是 true 还是 "true"?)

═══ 最后一次性给我一份报告,六节 ═══

1. 回归结论表:R1–R17 每条一行,只写「修对 / 没修对 / 修出新问题」+ 一句证据
   (R12–R17 是新修的六条,每条允许多写两句)
2. 新问题清单:按严重程度排。每条写清:现象 / 怎么复现(哪个工具、什么参数)/
   你期望什么 / 实际是什么 / 建议怎么改
3. 零摩擦四问(Z1–Z4)的回答
4. 缺什么功能:站在目标用户角度提(一个普通人,长期做一件事:上一门课、申请学校、
   转行、学外语、租房、备赛半马;不写代码、不看日志、不想学新术语)。
   每条必须说清「这解决他的哪一个真实场景」,不要列技术特性
5. 你还想要什么权限或接口:直说。每条说明为什么需要,以及它和这两条底线冲不冲突——
   ①AI 绝不改用户写下的东西 ②数据不出网。冲突的也可以提,标明冲突即可
6. 一句话总评 + 打分(10 分制),并说明和上一轮比是升是降、为什么

═══ 纪律 ═══

- 只往 LAB 里写,一次写一块,动手前先说
- 库里那些脏数据(重复的块、指向不存在的块的引用、正文里露出来的 id)是我故意埋的。
  你发现它们只用来验证"工具报得准不准",不要当成 Spool 的 bug 报
- 提到某个块用「项目标题 + 块编号 #n + 内容预览」,不要报 id
- 不确定就说不确定,别编
- 报告用中文,大白话
```

---

## 四、实验室里埋了什么(你自己心里有数就行,别告诉 AI)

`scripts/seed-mcp-lab.sh` 造的库:4 个工作区、12 个项目、39 个块、4 个附件。

> **39 和 37 都是对的,别搞混**:库里一共 39 块,其中 2 块躺在软删项目和孤儿项目里
> (每处 1 块),任何工具都不该看见它们。所以工具报的是 **37 块 / 11 个项目**。
> AI 要是报出 39,那就是软删过滤漏了 —— 这是本轮最严重级别的 bug。

故意埋的坑:

| 埋的东西 | 在哪 | 该被谁抓到 |
|---|---|---|
| 两条一模一样的块 + 一条改了标点的 | 机器学习课 | find_similar_blocks / thread_health |
| 跨项目的重复(同一句话) | 机器学习课 ↔ 机器学习课作业 | 全库 find_similar_blocks |
| 指向不存在的块的引用 | 机器学习课 | check_library / thread_health / pack 里降级成一行说明 |
| 正文里露出的 `spool://` 和 21 位裸 id | 机器学习课 | check_library / thread_health。两条都指向现存对象,报告里会写「→ 指向现存块/项目」 |
| 摘要里露出的裸 id | 租房 | check_library(项目摘要那一节)。这条**故意指向不存在的对象**,报告里写「→ 未指向现存对象」 |
| 用户手写的摘要 | 找工作 | set_thread_summary 必须拒绝;list_threads 的 summary_source 应报 "user" |
| AI 写的摘要 | 机器学习课作业、租房、简历改版 | set_thread_summary 可以覆盖;summary_source 应报 "mcp" |
| 没有摘要 | 机器学习课等 | set_thread_summary 写第一条;summary_source 应为 null |
| 空项目(0 块) | 菜谱 | get_pack / distill 的空库提示,提示里要带项目名 |
| 软删的项目 + 软删工作区里的活项目 | 已删掉的项目 / 孤儿项目 | **任何工具都不该看见**,看见就是 bug |
| 标题互相包含 | 机器学习课 / 机器学习课作业 | 项目名解析应该报歧义 |
| 沉寂 40 天、只剩置顶 | 作品集官网 | get_digest 的"置顶锚点"那一段 |
| 7800 字的附件正文(内联进 pack) | 机器学习课的 lecture-03.pdf | get_pack 的预算阶梯 + get_blocks 的附件暴露 + 附件正文搜索 |
| 抽取失败的附件、指向不存在路径的文件夹 | 找工作 | pack 里怎么渲染 |
| 日语、英语混排 | 日语练习、机器学习课 | 搜索与输出语言 |

**本轮的对答案速查**(2026-08-05 开发窗拿新程序实测,拿来比对报告真假):

| 项 | 正确答案 |
|---|---|
| 「机器学习课」块数 / 置顶 | 16 / 3 |
| 「机器学习课」pack 全文 | **13207** 字符(比上一轮少 87 —— 就是去掉的那三处绝对路径) |
| `approx_pack_chars` | **13432**(比真值**高** 1.7%,方向安全,当 max_chars 直接用能一次拿全) |
| `max_chars=8000` | 拿到 **7615** 字符的部分 pack,附件正文压到 2000 字 |
| `max_chars=2000` | 拒绝,报下限 **4060**,建议提到 4060 以上或走 get_blocks |
| `range=last7` 表头 | 「5 of 16 blocks in this project (range: last7 …)」 |
| `range=last7` 的置顶 | 3 块全在 |
| search「的」total | 23(能靠 offset 翻完),另有 1 处附件命中(只随第一页给) |
| 只在 PDF 里的词(如「谷底两侧」) | `total=0` + `attachment_total=1` —— 正文里搜不到、附件里搜得到 |
| find_similar 全库组数 | 1 |
| 库里项目数 | 11(不含软删) |
| check_library 扫描口径 | 37 块 / 11 项目 / 4 工作区;发现「裸 id 3 · 引用完整性 1」 |

**这一窗六条修复的对答案**(R12–R17 判真假就看这张表):

| 验什么 | 实测正确行为 |
|---|---|
| R12 a–d 硬拒绝 | 四个位置(content / annotation / summary / title)全部**一个字都不写**,块数留在 39。拒绝话术:「没有写入任何东西:X 里出现了〈机器学习课〉里 **#8** 那一块的内部 id……」—— 认得出是哪一块,而且用的是可见编号,不是 id |
| R12 e 形状像但不存在 | **也拒**,但话术不同:「有一串 21 位、大小写混排的字符,和 Spool 的内部 id 一个形状」。两种话术要能区分,这是 D-1 / D-2 两层 |
| R12 粘前缀 | `见block:LabBkMl00000000000008吧` **照样拒**(D-2 是子串比对,不靠分词) |
| R12 `spool://` URI | 也拒,话术说的是「项目〈机器学习课〉的内部 id」 |
| R12 f 误拒风险 | `internationalization`(20 位)、`counterrevolutionaries`(22 位)、纯数字 21 位、base64 一段 —— **全部放行**。任何一条被拒都是修出了新问题 |
| R13 dry_run | 「预演,还没有写进 Spool:这条存下去会是〈机器学习课〉的 **#17** 块」+ JSON 里 `written:false` / `would_be_seq:17`;去掉 dry_run 真写,编号确实是 17 |
| R14 报错不回显 | `thread_id="nope"` → 「没有这个 id 对应的项目 — 用 list_threads 查有效 id(项目标题也在里面)。」**整句不含 "nope"** |
| R15 id 表在 pack 外 | `include_ids=true` 时 pack 正文 13207 字符结束、`---` 之后才是 `## Block IDs`(总长 14577);distill 里 id 表在 ⟦/SPOOL:MATERIAL⟧ **之后** |
| R16 无绝对路径 | 全文 pack 里 `/Users/` 出现 **0 次**;附件行是 `↳ attached file: lecture-03.pdf (pdf)`,Related Files 是 `- lecture-03.pdf` 和 `- week6 homework — https://course.edu/week6/homework`(网址整条留着)。get_blocks 的 JSON 里**仍有**完整路径 |
| R17 材料界 | distill 总长 15313:⟦SPOOL:MATERIAL⟧ 在第 95 字符、⟦/SPOOL:MATERIAL⟧ 在 13320、id 表 13949。顺序永远是 开界 → 材料 → 闭界 → 指令 |
| R17 b 伪造闭界 | 用户正文里的 ⟦/SPOOL:MATERIAL⟧ 会被改写成 `(/SPOOL:MATERIAL)`(圆括号),所以**全文只会出现一次真正的闭界**。AI 说不出这一点属正常,说得出算加分 |

> **AI 若报"没修对"而与上表冲突,先信上表**,再自己复跑一次。上一轮 GPT 就误判过 R1
> (理由是"没说省略 0 块",与实测不符)。
>
> ⚠️ **📌 这类星区字符,Rust 数码点、JS 数 UTF-16 单元**,所以 AI 报的全文长度可能比
> 13207 大十几个 —— 这不是 bug,`max_chars` 的说明里本来就写了这件事。

## 五、Claude Code 那一份

Claude Code 是唯一暴露 prompts 的客户端,所以它不用来做"找茬评审",而是**开发窗自己的
回归自测台**:`/mcp__spool_lab__<名字>` 直接跑四个 prompt,`mcp__spool_lab__<名字>`
直接跑十三个工具。开发窗每一轮改完 Rust 都在这里先跑一遍再交给你。

⚠️ **改了 Rust 代码之后,Claude Code 里的 spool_lab 还连着旧的那份程序副本。**
必须 `cargo build --release` → `./scripts/seed-mcp-lab.sh` → **重启 Claude Code**
(在 VS Code 里就是重开窗口),新代码才生效。

> **开发窗还有一个更省事的办法**(不重启客户端、不碰共享实验室):直接用 stdio 喂
> JSON-RPC 给那份程序副本 —— `initialize` → `notifications/initialized` → `tools/call`,
> 三行就能问出任何一个工具的真实返回。上面第四节那两张对答案表就是这么测出来的。
> 别人的评审会话正在跑的时候,这是唯一安全的验证手法。

## 六、测完把什么给我

AI 的那份报告整段贴回来就行(每个客户端各一份)。处理顺序:
先看第 1 节回归表 —— **R12–R17 有任何一条判"没修对 / 修出新问题",立刻回头查**,
其中 R12 f(正常长英文词被误拒)最要紧,它会让 AI 写不进任何正常内容;
再看第 3 节零摩擦四问(零摩擦引导那份稿子已落地并按约定删除,见提交 `268b8ec`;
这四问现在是**回归用**的 —— 看那套引导在新版本里还成不成立),
最后才是第 2、4、5 节。

