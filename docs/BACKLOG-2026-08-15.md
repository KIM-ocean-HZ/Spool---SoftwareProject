# 当前任务与决策 —— 2026-08-15

> **这是当前唯一的开工面。** 三份 Codex 交接文档（`HANDOFF-CODEX.md`、
> `COLLABORATION-CODEX-2026-08-13.md`、`NEXT-CODEX-WINDOW-PROMPT.md`）记录的是 08-13
> 那几轮的实现史，它们的 git 状态段已在本轮更正；不要再从那里取开工面。

## 0. 基线（本轮实测）

| 项 | 状态 |
|---|---|
| `HEAD` = `origin/main` | `42f2c79`，零未推 |
| 官网 | **已上线**，两次 Pages 部署都成功；spoolapp.org 已是新版 |
| Vitest / tsc / Rust / 中文专项 / i18n | 361·clean·72·9/9·`(none missing)` |
| 生成器确定性 | 两个生成器重跑零漂移 |
| 视觉回归 | 独立重跑 15 / 15 PASS |
| `src/**`、`src-tauri/**` | 08-13 两个提交里零改动 |

---

## 1. Dogfood 发现（2026-08-15，Ocean）

### 1.1 库的提取与迁移

**Ocean 的问题：「现在的发布用户安装新版本之后，数据本身就可以自动迁移吗？」**

**答：同一台机器上原地升级，已经会自动迁移，不需要新功能。** 取证：

- 数据在 `~/Library/Application Support/com.oceanjin.spool/spool.db`，**在 app bundle 之外**。
  装新版 `.dmg` 只替换 `/Applications/Spool.app`，不碰数据目录。
- 启动时 `migrateSchema()`（`src/lib/db/client.ts`）比对 `PRAGMA user_version` 与
  `SCHEMA_VERSION`（当前 **21**），按 `MIGRATIONS` 注册表逐步前进，**每一步单独 checkpoint**，
  中断后下次启动可续。
- 迁移前用 `VACUUM INTO` 自动写一份 `spool.pre-migration-v<旧版本>-<时间戳>.db` 快照。
- 兜底重建分支**拒绝清空非空库**（2026-05-29 事故之后加的防线）。

**所以真正缺的是另一件事：换机器 / 换库时用户可见的入口。** 现在只有终端命令
（`DB_BACKUP_AND_RECOVERY.md` §3），app 内没有「导出库 / 导入库」。

**功能定义**：导出一个自包含的库文件；在新机器导入后照常走 `migrateSchema()` 自动升级到当前
schema。这样「提取出来之后无论 Spool 怎么更新都能迁移过去」这条就由现有迁移链天然保证，
导出格式不需要自己再发明版本兼容。

⚠️ 一条相关事实：**降级会被拒绝启动**（用旧构建打开新库会报错而不是清空）。这是防线不是 bug，
但导入功能的文案要说清楚，否则用户会以为导入失败。

---

### 1.2 官网 `01 · SAVE` 左图顶部缺失 —— 已定位

> ⚠️⚠️ **2026-08-16 更正：下面「推断」那一段是错的，别照它办。**
> 真因不是裁图，是**浮窗自己就压在菜单栏底下**。取证与新的修法见 §4.5。
> 「像素里就没有」这个观察是对的，其余结论请以 §4.5 为准。

**不是 CSS，不是裁图坐标，是源图 `docs/screenshots/S1.png` 的像素里就没有。**

取证：

- S1 第 **0 行**，x = **2895…3544** 已经是浮窗内部的奶油色 `(250, 247, 241)`；
- 浮窗自己的描边色 `(212, 205, 182)` **只在左右两边、从第 2 行才出现**；
- 也就是说**浮窗的上边框和两个圆角根本没被拍进去**。

**产品侧是好的**：`OVERLAY_SCREEN_MARGIN = 20`（`src-tauri/src/capture.rs:642`），
`target_y = m_top + OVERLAY_SCREEN_MARGIN`，浮窗本来离屏幕顶有 20 逻辑像素
（Retina 2x = 40 物理像素）。**不是浮窗贴着屏幕顶。**

**推断**：S1 拍完之后被从顶部裁过（很可能是为了去掉 macOS 菜单栏），这一刀切进了浮窗。

**修法**：重拍，或从没裁过的原图重新导出。
⚠️ **不能用 CSS 补一条边框** —— 那是往截图里画产品本来没有的东西，撞
「不得伪造 / 重绘 / 修改截图里的产品内容」这条硬红线。

**影响面**：左边总览 `capture-page` 和右边细节 #2 `capture-toast` 是**同一批像素**
（`capture-toast` 由 `--cropOffset 0 2880` 从 `capture-page` 裁出），两处一起修。
换掉 S1 之后要重跑 `bash scripts/build-site-shots.sh` → `node scripts/build-site-zh.mjs`，
并更新视觉回归基线。

---

### 1.3 侧边栏排序与文件夹管理

1. 左侧边栏项目名称**按首字母排序**；
2. **文件夹管理**：可以在每个工作区内再新建工作区（工作区 = 文件夹，允许嵌套）。

⚠️ 第 2 条不是纯 UI —— 工作区目前是**平的**，嵌套需要 schema 改动（父子关系）、迁移步骤、
以及所有按工作区聚合的地方（项目板、digest、Pack、MCP 的 `list_threads`）跟着改。
排期时按「schema + 迁移 + UI + MCP 输出」四块估，不要当成一个排序任务。

---

### 1.4 捕捉弹窗零摩擦打字

1. **弹窗可以直接打字**，不用每次先点一下；
2. **原平台优先**：目标 app 里已经有输入框时（比如 Word 文档）**不抢占**焦点；
3. 弹窗输入框**提示 ⌘Z 可以撤回**。

⚠️ **这条和既有铁律直接相关**：「主窗永不跳前 > 直接打字」。第 2 条正是这条铁律的延伸，
实现时要沿用既有的 AXFrontmost 判定，不要另起一套。

⚠️⚠️ **顺带发现一处对外不一致，要一起查**：官网 `01 · SAVE` 的正文现在写着
"The confirmation appears with the cursor already in a note box"（中文版同义），
而 dogfood 说**还要点一下才能打字**。两者对不上 —— 要么是 bug，要么这句是超前承诺。
**在查清之前不要把这句当成已兑现的事实。**

---

## 2. Ocean 2026-08-15 的决策

| # | 事项 | 决策 |
|---|---|---|
| 1 | GitHub repository description | **做** → ✅ **已经是这句了，无需改动**（本轮实测） |
| 2 | codex 插件留不留 | **不留**。连带把左侧边栏的快捷导航去掉，复制那些也都不要了 |
| 3 | M4 定时跟进 | **不做**，换方式 —— 见 §2.1 |
| 4 | Windows 双击手势 | **先不做**，另做一个新的、方便的手势 |
| 5 | Windows 签名分发 | **先不签名** |
| 6 | Windows 首版范围 | **接受**：能装能跑 + 快捷键捕捉 + 打包 + MCP 接得上 |
| 7 | 免费引擎 | **放弃这条路** |
| 8 | 演示视频 | **先不做** —— 担心 app 还有小修改 |
| 9 | `RELEASE.md §3` 清单 | **排期，准备做** |
| 10 | `spool://thread/<id>` 资源入口 | **排期，准备做** |
| 11 | `/zh/story.html` | **补中文**（已批，本窗未做） |

### 2.1 跟进（Follow Up）换方式 —— 取代 M4 调度器

**Ocean 的判断**：跟进**内容是什么**才是最重要的，不是定时。

目标工作流：

1. 用 MCP 对话后，会留下一些**还没明确的问题**或**还没发生的事情**；
2. 这时希望 **MCP 主动把它们记进跟进内容**；
3. 等捕捉更新了、进入新一轮对话，**MCP 发现这些跟进问题，主动向我汇报，并申请写入 Spool**。

因此 MCP 需要做到两件事：

- **能主动提出**把不确定的信息放进跟进面板；
- **有新的跟进内容时能主动搜索并汇报**；有结论就写入，并**修正或删除**对应的跟进条目。

**手动联网搜索保留，作为用户主动的开关。**

⚠️ 这条**取代**了 `archive/INVESTIGATION_M4_SCHEDULED_FOLLOW_UP_2026-08-13.md` 的调度器方案；
那份报告的 §7 十二个问题**不再需要回答**，它作为「为什么不做定时调度」的论证留档。
新方案的落点是 `DESIGN_FOLLOW_UP.md`，性质从「后台定时任务」变成「MCP 会话内的主动提议 +
待审写入」，**复用现有的待审闸和署名写回机制**，不需要 scheduler、不需要预算/静默条件那一整套。

---

## 3. 任务队列

> ✅ **GitHub repository description 本轮实测已经是这句，一个字都不用改：**
> `Local-first project memory for macOS — capture context once, carry it across AI tools, and keep every AI write attributed.`

### 3.1 可以直接做（已批，无阻塞）

1. ~~**补 `/zh/story.html`**~~ —— ✅ **2026-08-15 做完**，见 §4.3。
2. ~~**拆掉 codex 插件那条路**~~ —— ✅ **2026-08-15 做完**，见 §4.1。
3. ~~**重拍 S1 并重建截图链**（§1.2）~~ —— ✅ **2026-08-16 换图并重建完**，见 §4.5。
   ⏳ 产品侧 2026-08-16 已修（浮窗改按可用区域定位），**但线上那张仍是旧位置拍的**。
   **Ocean 2026-08-18：先放着。** 代拍不了的原因和真要拍时的注意事项见 §5.4 第 1 条。
4. ~~**查「弹窗要不要点一下才能打字」**~~ —— ✅ **2026-08-15 查完是 bug，并已修**，见 §4.2；
   设计与落地记录在 `docs/DESIGN_CAPTURE_FOCUS.md`。
   ⏳ **只差装机后 Ocean 亲手跑一遍那份设计稿 §7 的三步。**

### 3.2 要设计再动手

5. **跟进换方式**（§2.1）—— ✅ **设计稿写完并拍板，M5 也已落地**（2026-08-16，见
   `DESIGN_FOLLOW_UP.md` **§8.10**）。schema **v22** + 迁移 + 面板改行清单 + 待审逐条卡 +
   `suggest_follow_up_item`（换掉 `suggest_follow_up_brief`）都在里面；引擎跟进行为不变。
   ✅ **M6 也做完了**（2026-08-16 第三窗，见 `DESIGN_FOLLOW_UP.md` **§8.11**）：
   `close_follow_up_item` + 汇报那半的路由文本 + 引擎跟进也读一次性问题，三件一起落的。
   ✅ **已装机**：真库 2026-08-16 23:05 跑完 v21→v22 迁移，183 块 / 33 项目 / 1 附件零变化，
   9 行条目按预期迁出（Flux 4 · 申请规划 4 · Georgia Tech 1）。
   ⏳ **只剩一件要 Ocean 亲眼看**：跟进面板的行清单、切标记、收起/重开、待审逐条卡，
   代码跑通了但**没有人看过它长什么样**。
   ✅ **顺手修掉一个死规则**：`last_raised_at` 从来没有人写，防唠叨那条本来永远不生效；
   Ocean 当天拍板「读的时候盖章」，已落地（见 §8.11 末尾）。
   下面这段留着记决策来源：
   Ocean 当天定的三条（§8.9）：**跟进合成一份清单**（不是两份）、**AI 加条目要用户点一下**、
   **AI 关条目不用点但看得见能重开**。落点：schema **v22** 一张 `follow_up_items` 表 +
   把现有 brief 拆成行的迁移 + 面板从文本框改成行清单 + 去掉 `suggest_follow_up_brief`、
   换成 `suggest_follow_up_item` / `close_follow_up_item`，分 **M5 / M6** 两刀。
   ⚠️ 迁移**只做加法，绝不重建 `threads` 表**（§8.7，2026-05-29 清库事故的根子就是重建分支）。
   ✅ **最后一件也拍了**（2026-08-17）：`get_pack` **不挂**待答条目。M5/M6 真用过一阵之后
   Ocean 的回答是「get_pack 不挂」——待答条目只走 `list_threads` / `get_project_overview` /
   `get_follow_up_brief` 这三条 JSON 路，pack 保持原样，跨语言 golden fixture 那笔账不付。
   代价（以 `search_blocks → get_pack` 开场的对话看不见待答问题）**是已知并接受的**，
   见 `DESIGN_FOLLOW_UP.md` §8.5 / §8.9 第 4 条。**这一条从此不是欠账。**
   ⚠️ 新增/改动 MCP 工具时必须同时改 `OPENERS` / `INSTRUCTION_HEADER` 路由文本并写 `annotations`，
   否则第三方客户端永远不会调用它。
6. **侧边栏排序 + 工作区嵌套 + 打包整个文件夹**（§1.3）—— ⭐ **2026-08-17 Ocean 点名这三件
   一起做**，设计稿：`docs/DESIGN_WORKSPACE_PACK.md`。
   ✅ **前两件已落地并推送**（2026-08-17，提交 `4161cf4`，见设计稿 **§6** 与交接 **§10**）：
   schema **v23**（`workspaces.parent_id`）+ 左栏树形（**每层缩进一格**）+ 工作区按首字母排序
   + 「在里面新建工作区」+ 右键移进/移到最外层。**真库已装机跑完 v22→v23，零变化。**
   ✅ 同一窗顺带做了 **左栏多选**（Ocean 当天要的「和 vscode 逻辑一样」）：⌘ 加减、⇧ 选段，
   移动/删除整批生效，**删除键从悬停 🗑 挪进右键菜单**（项目行和工作区行都是）。
   ✅ **打包那一半也落地了**（2026-08-17 第二窗，见设计稿 **§7** 与交接 **§11**，⏳ 未提交）：
   工作区行悬停多一个打包图标 → 勾选清单（默认全勾）+ 范围那排 → **导出成一个真的文件夹**
   （`INDEX.md` + 每项目一个 `.md`，子工作区就是子目录）。三条当窗拍的：导出走**系统目录选择器**、
   范围**保留且一个范围套所有项目**、`INDEX.md` 的摘要**用项目 summary，没有就退回首块开头**。
   ⚠️ 最要紧的一条铁律照做了：**打包不许降级、不许丢字** —— 每份 `.md` 的正文和单项目打包
   一字不差，省上下文全靠「目录 + 按需打开」。
   ✅ 挡工那条已拍并落地：**每份 `.md` 顶上一句指令 + 指回 `INDEX.md`**，
   而 `INDEX.md` 是**规则文件**（Ocean：「类似于 claude.md 的作用，让 ai 按照规则执行」）——
   所以它**规则在前、目录在后**（设计稿 §2）。
7. **库导出 / 导入**（§1.1）—— 用户可见入口。✅ **2026-08-17 落地并已推送**（`c83b717`，已装机），
   设计与验证记录在 `docs/DESIGN_LIBRARY_TRANSFER.md`。入口在**设置 → 高级 →「换机器」**。
   ⭐ **Ocean 当天拍的三条**：导入是**合并**不是替换（他的理由：future 库之间互传要留后路）、
   附件路径断掉**导入完成时直说**、导出**只带库不带偏好设置**。
   ⚠️ 合并**只做加法**：撞 id 就跳过（所以重复导入是安全的），本机已有的行绝不被外来文件改写；
   `engine_runs` / `date_dismissals` / `file_access_requests` 三张表**故意不跟**。
   ⭐ 真库快照副本上演练过：12 工作区 / 39 项目 / 186 块 / 2 附件 / 10 条跟进**一条不差**，
   嵌套的 `parent_id` 和软删除的行都原样过去，全文检索靠触发器自己长出来。
   ✅ **已装机，Ocean 亲手验过**：导出一次 → 再导入同一个文件 → 报「已经都有了 / 跳过」。
   ⚠️ 装机第一次就炸了一个：`db.close()` 不传参数会关掉**每一个**连接池（连真库那个），
   已修成 `close(path)`。剩余缺口见 `DESIGN_LIBRARY_TRANSFER.md` §4-bis。
8. **Windows 首版**（§2 决策 4–6）—— 按
   `archive/INVESTIGATION_WINDOWS_PORT_2026-08-13.md` 的分阶段面走，但**跳过双击手势、跳过签名**。
   ⚠️ 两个编译阻断项仍在：`capture.rs:757` 的 `RESTORE_FOCUS_APP` 未 cfg 门控，
   `mcp.rs` 无条件调用 `localtime_r` / `gmtime_r` / `timegm` / `mktime`。

### 3.3 排期中

9. `RELEASE.md §3` 干净机验证清单 —— 需要真机。
10. ~~`spool://thread/<id>` 资源入口~~ —— ✅ **2026-08-17 关掉，但和原文写的不是一回事**。
   ⚠️ **原文（08-13 排的「给『复制使用提示』补资源入口」）已经作废**：08-15 决策 2 把
   「复制使用提示」连同「问 AI」、codex 插件一起删了（整条「帮用户去别的 AI 里点名 Spool」
   的路），`McpBar.tsx` 头注释写着 **Do not make it a way in again**。照原文补＝把砍掉的路装回来。
   ✅ **真正欠的那件做完了**：资源本身早就实现了（`mcp.rs` §20.13），但 `PLAN_EN.md` §20.13
   的「@-mention UX 从没在真客户端里验过」挂到现在。本窗**用真 MCP 客户端实测**：
   `resources/list` 27 条、`resources/read` 给完整 pack，协议侧没问题。
   ⚠️ 实测暴露的真缺陷：`description` 没有 summary 时退回**工作区名**，而 27 个活项目里
   只有 2 个有 summary —— @ 选择器里 24 行全是「学校」。**已改成退回「首块开头 40 字」**，
   复用 `head_anchor`（它本来就是 TS `headAnchor` 的孪生，同一个 40）——和 `INDEX.md`
   用的是同一条规则、同一个数（Ocean 08-17 那条「标题信息量足够了，加上 40 字辅助」）。
   ✅ Ocean 同时定了：**不在 app 里告诉用户「可以 @ 一个项目」** —— 那正是 08-15 砍掉的门类。

### 3.4 已关闭，不要再当欠账

- **免费引擎** —— 放弃（§2 决策 7）。`archive/INVESTIGATION_FREE_AI_ENGINES_2026-08-13.md` 转为留档。
- **M4 定时调度器** —— 不做（§2.1）。
- **演示视频** —— 先不做（§2 决策 8）。
- **案例研究第四至六期** —— 已完成并上线；ledger §7 是收口记录。
- LICENSE 不加；既存死 CSS、`updated_at` warning、`threads.auto_maintain` 遗留列有意保留。

---

## 4. 2026-08-15 本轮落地记录

### 4.1 拆掉 codex 插件那条路（§2 决策 2）——【已完成】

⚠️ **决策 2 原文写的是「左侧边栏的快捷导航」，但左侧边栏里一处都没有。**
实际长着「快捷导航 + 复制」的是右栏和项目管理。已当场跟 Ocean 核实并由他逐项圈选。

**机器上（仓库外，不进提交）**：

- `codex plugin remove spool@spool` + `codex plugin marketplace remove spool` →
  `~/.codex/config.toml` 只少了 `[marketplaces.spool]` 与 `[plugins."spool@spool"]` 共 7 行，
  diff 里**没有任何其他改动**；`[mcp_servers.spool]` / `[mcp_servers.spool_lab]` 都在，
  `codex mcp list` 里 `spool` 仍是 `enabled`。
- 删掉空目录 `~/.codex/plugins/cache/spool/`。
- 备份：`~/.codex/config.toml.before-plugin-removal-20260815-114500`。
- ⏳ **还留着的**：`~/Library/Application Support/com.oceanjin.spool/codex-marketplace/`
  —— 现在没有任何东西指向它了，删不删随时都行，本轮没动。

**app 里（Ocean 圈的三处）**：

| 拆掉的 | 位置 |
|---|---|
| 右栏顶上 MCP 行的**展开客户端菜单** | `RightRail/McpBar.tsx` |
| 项目管理的**「问 AI」** | `ProjectBoard/AskAiButton.tsx`（整个删除） |
| 设置里**「复制使用提示」** | `Settings/McpConfig.tsx` |

**保留**：右栏那行**读数**（08-11 Ocean 要的「MCP 放最顶上」，只是不再能点）、
右栏收起按钮、设置里的**「复制 MCP 配置片段」**（拆了就只剩一键接入一条路）。

连带清掉的孤立代码：`components/mcp/ClientMenu.tsx`、`clients.ts` 的
`HOW_TO_ADDRESS` / `askPrompt` / `askInClient` / `focusClient` / `readConnectedClients`、
Rust 的 `focus_mcp_client`、22 个因此没人用的翻译键（既存的 55 个死键**没动**）。

⚠️ **`HOW_TO_ADDRESS` 那三次实测结论没丢**，以散文形式留在 `clients.ts` 头部注释里，
并指回 `HANDOFF.md §0-now.3-ter…septies` 和 ledger §3.39–3.41 ——
**下次再想做点名功能，先读那些，别重新量一遍。**

验证：Vitest **32 文件 / 350**（原 361，少的 11 条正是删掉的两个 describe）、
tsc clean、cargo **72 / 72**（只有既存 `updated_at` warning）、
i18n `(none missing)`、`git diff --check` PASS。

### 4.2 「弹窗要不要点一下才能打字」——【是 bug，当窗已修】

**结论：产品实现了直达打字，但「在 Spool 自己窗口里捕捉」这条路是断的。**

根因是一个前提在 2026-08-01 中午失效、判断条件没跟着改：`a3cfba2`（02:01）加抢焦点时
浮窗还是主进程的窗口，`74b87f1`（12:20）把浮窗搬进独立进程后，
`capture.rs` `ax_source_app_pid()` 里 `pid == std::process::id()` 那一支的依据就不成立了
（激活按进程算）。完整取证与三条愿望的合并设计见 **`docs/DESIGN_CAPTURE_FOCUS.md`**。

**Ocean 当窗把三个问题都拍了，三条愿望已一起实现**（改动清单见设计稿 §9.2）：

| # | 决定 |
|---|---|
| ① 抢占范围 | 抢占所有 —— **§1.4 原来的第 2 条（Word 有输入框就不抢）作废**，不要再照它实现 |
| ② ⌘Z | **丙**：框里有字撤字，空框撤捕捉 |
| ③ 提示 | 挂脚注 ↩ 图标的 tooltip，placeholder 不动 |

**辅助功能对外少说的那一半也补了**，实际是**五处**不是三处（隐私页中英各有一份权限表）：
官网首页中英、隐私页中英、`README.md`、`docs/PRIVACY.md`。中文页由生成器重跑。

✅ **已装机实测通过**（Ocean 亲手跑 Word / 浏览器 / Spool 三条路，全通）。

⚠️⚠️ **真因比最初判断的多两个，值得记住**：一共修了**三个独立缺陷**，其中最要紧的
一个是装机之后靠日志才挖出来的 —— **`AXFocusedApplication` 在浏览器前台时返回 `None`**
（连 Spool 自己的 Accessory 浮窗进程也报不出来）。改用窗口服务器
（`CGWindowList` 最前面那个 layer 0 窗口的 owner pid）兜底才修好。
⭐ 通则：**`None` 和「没事可做」是同一个值 —— 会静默退化成 no-op 的查询，出错时看起来就像答对了。**

⚠️ 设计稿 §9.4 记了丙自带的代价（**连按两下 ⌘Z 会连捕捉一起撤掉**），是已知并接受的，不是 bug。

### 4.3 补 `/zh/story.html`（§3.1 #1）——【已完成】

**做法：走 `data-i18n` 键值法，不走隐私页那种 `data-zh-body` 整段替换。**
理由：故事页里有一张 90 行的架构 SVG 和三组 `srcset` 响应式图，整段替换等于把这些结构复制
第二份，以后改英文版中文版会悄悄漂移。键值法只换字，结构留一份。隐私页那条路是特例
（中文才是权威版本），生成器注释里本来就写着「只有隐私政策需要」。

改动：

| 文件 | 改了什么 |
|---|---|
| `site/story.html` | 加 130 个 `data-i18n` / `-alt` / `-aria` 标记、`中文` 切换链接、三条 `hreflang`、`lang.js`；**可见英文一个字未改**（脚本比对：唯一新增文本就是「中文」） |
| `scripts/site-zh-strings.mjs` | `HEAD['story.html']` + 126 条中文；顺带去掉 `nav-story`/`foot-story`/`maker-link`/`demo-noscript` 里的「（英文）」 |
| `scripts/build-site-zh.mjs` | 加 `TOGGLE['story.html']`；**删掉 `"story.html"` → `"../story.html"` 的改写**（现在 `/zh/` 里三个页面齐了，链接留在 `/zh/` 内部） |
| `scripts/build-site-zh.test.mjs` | 加一条：故事页的 alt 与标题已译、页面里不再有英文标题 |
| `site/assets/lang.js` | 注释里「story.html 没有译本」那句已过期，改掉 |
| `scripts/visual-regression.sh` | 加 `zh-story` 路由；两处写死的「15」改成按路由数算 |

保留英文的只有专有名词和命令：`Rust / Tauri core`、`SQLite`、`FTS5`、`spool --mcp`、
`Claude Desktop · Cursor`、`Case Study Ledger`、哈希与提交号。

验证：Vitest **32 文件 / 352**（原 350，多的两条是新页面的构建比对 + 新增断言）、
i18n `(none missing)`、`git diff --check` PASS、生成器无 unused key。

### 4.4 ⚠️ 顺带查出来的两件事（都不是本轮改坏的）

1. **视觉基线在 `1432033` 之后就没重跑过。** 那个提交改了首页与隐私页的权限文案
   （中英四个文件），但 `docs/qa/visual-baselines/` 停在 `42f2c79`。本轮一跑 `check`
   **15 张全红**，页面高度对得上新增的那两句文案，不是布局事故。已连同新页面一起
   `update`，现在 **18 张**（6 路由 × 3 宽度）。
   ⭐ 教训：**改了官网文案的提交，必须连基线一起提交**，否则下一个人接手时看到的是一片红。

2. **故事页那两个路由在默认 settle 下会翻面。** `story.html` @768 与 `/zh/story.html`
   反复出现同一组差异（`changed=3524`、`max_channel_diff=55`，每次数字**一模一样**），
   差异全落在项目截图那一张 figure 上，别处一个像素都不差。
   是 `srcset` 选中哪个候选图的竞态（920w / 1229w 缩到同一个框，重采样结果差几个色阶），
   **不是页面改动**：比例 0.00065 远低于 0.001 的门槛，只是 `max_channel_diff` 顶破了 32。
   `VISUAL_SETTLE_MS=5000` 试过，**并不能稳定**（第二次同样红）。
   ⚠️ **别为了让它变绿去改产品的 `srcset`/`sizes`，也别反复 `update` 去挑一张能过的基线。**
   要治就治判据（比如对 figure 区域放宽 `max_channel_diff`），那是单独一件事。

---

## 4.5 2026-08-16：换掉 S1（`new.png`）——【已完成，但顶边框没修好，见末尾】

### 做了什么

Ocean 补拍的 `docs/screenshots/new.png`（3600 × 2338）替换了 `S1.png`。
⚠️ `app-capture.png` 和 S1 **本来就是同一份字节**（故事页的 `story-capture` 用它），
所以两份一起换，否则首页修好了、故事页还是旧的那张。换完 `new.png` 已删（内容在那两份里）。

新图 = 旧图**上面多出 78 行**屏幕顶部黑边，其余几何完全对齐：
旧 S1 的浏览器窗口区域与新图 `+78` 行**逐像素相同**，所以所有裁切坐标只是 `y += 78`。

| 文件 | 改了什么 |
|---|---|
| `scripts/build-site-shots.sh` | `capture-page-source-detail` 偏移 `1 1` → `79 1`；`capture-toast` `392 680 / 0 2880` → **`430 680 / 78 2880`**（浮窗标题从一行变两行，高了 38 px） |
| `site/index.html` | `capture-page` 高 2260 → **2338**、`capture-toast` 392 → **430**；两条连接线路径按新几何重算；`alt-capture-toast` 改口径（见下） |
| `site/story.html` | `story-capture` 高 2260 → **2338** |
| `site/assets/site.css` | 两个编号框的百分比、`capture-scene` 的列宽解 `58.9486% + 1.031px` → **`59.5815% + 0.425px`** |
| `scripts/site-zh-strings.mjs` | `alt-capture-toast` 中文跟着改 |
| `docs/CASE_STUDY_LEDGER.md` §7.3 | `story-capture` 尺寸行 |

⭐ 列宽那个数不是随便调的，是解方程解出来的：左图高 = 右边两张之和 + 1rem 间距，
`Wl = C·S/(R+S) + g·(1−S)/(R+S)`，其中 `R` = 左图高宽比、`S` = 两张细节图高宽比之和。
用旧数字回代能还原出旧的 `58.9486% + 1.031px`，公式是对的。

**`alt` 改了口径**：新图里浮窗标题显示的是**捕捉到的原文**（旧图显示的是批注），
批注框里是打下的那句话、**且没有光标**（拍的那一瞬间没闪）。原文写的
"the note box with its cursor ready" 已经和像素对不上，改成「写着你刚打下那句话的批注框」。

验证：Vitest **32 文件 / 352**、i18n `(none missing)`、`git diff --check` PASS、
中文生成器重跑零漂移、视觉基线 `update` 后 `check` **17 / 18 PASS**
（唯一那条红的是 §4.4 #2 记着的老毛病 `/zh/story.html @ 768`，
`changed=3524`、`max_channel_diff=55` —— 数字和记录里**一模一样**，不是本次改出来的）。

### ⚠️⚠️ 顶边框还是没在像素里 —— 因为它压根没被画在屏幕上

**§1.2 的「推断」是错的。** 那里说 S1 是拍完之后被从顶部裁过，一刀切进了浮窗。
**不是。** 新图（没裁过、带着菜单栏黑边）量出来：

- 浮窗卡片上沿在**第 78 行**，第 77 行往上**全是纯黑 (0,0,0)**，中间**没有任何过渡**；
- 第 78 行整整 640 px 宽**全是奶油色内部**，一个描边像素都没有；
  左 / 右 / 下三边的描边 `(212,205,182)` 都在，**只有上边没有**；
- 两个上圆角是**被削掉大约 2 px 的残弧**：下圆角完整弧长 14 行，上圆角只剩 ~10 行。

也就是说，**浏览器窗口和浮窗的上沿在同一行（78）**——那正是菜单栏的下沿。
浮窗被菜单栏盖掉了最上面那 2 物理像素，**刚好就是它的边框**。

根子在 `src-tauri/src/capture.rs:718`：

```rust
let m_top = mpos.y as f64 / scale;          // ← 显示器原点，主屏永远是 0
let target_y = m_top + OVERLAY_SCREEN_MARGIN as f64;   // = 20 逻辑点
```

`m_top` 取的是**显示器原点**，不是**可用区域（菜单栏以下）的顶**。主屏上 `m_top = 0`，
于是浮窗被摆在离**屏幕**顶 20 逻辑点处，而这台机器（带刘海，菜单栏 39 逻辑点 = 78 物理像素）
的菜单栏一直盖到 78。浮窗窗口本身带透明阴影边距，卡片实际上沿落在 76 物理像素 —— 正好被压掉 2 px。

⭐ **所以这件事再怎么重拍都拍不出来**，除非改产品位置或换一台没刘海的机器拍。

**网站这一侧已经好了**：黑边留着，浮窗四周都有余量，不再像被图框切掉一半。
少掉的 2 px 边框在任何一档渲染宽度下都看不出来（580 px 档相当于 0.3 px）。

**留给 Ocean 定的一件事**：要不要把 `overlay_origin` 改成按**可用区域**定位
（`NSScreen.visibleFrame`），让浮窗整个落在菜单栏下面。
⚠️ 这会让浮窗**往下挪约 19 逻辑点**，是肉眼看得见的位置变化，**没经他点头不要动**。

### ✅ 2026-08-16 Ocean 拍板：改。已改完

`src-tauri/src/capture.rs` 的 `overlay_origin` 从 `monitor.position()/size()`
改成 `monitor.work_area()`（Tauri 2.11 有这个 API，macOS 那侧就是
`NSScreen.visibleFrame` 换算成左上原点坐标，见 `tauri-runtime-wry/src/monitor/macos.rs`）。
⚠️ **没有引 objc/cocoa 依赖**，不需要自己写 NSScreen 调用。

顺带的一个副作用，**是对的不是 bug**：x 也跟着按可用区域算了，所以**当 Dock 停在屏幕右侧时**
浮窗会让开 Dock。函数注释本来就写着「bottom-right is hidden by the macOS dock」，
按可用区域定位正是它想要的。Dock 在下方（本机）时 x 一个像素都不变。

#### ⭐ 实测取证：真因不止「菜单栏盖住」，是 AppKit 把窗口按回去了

不靠推理。本机 `NSScreen`：`frame` 原点 (0,0) 1800×1169，`visibleFrame` 原点 (0,82) 1800×1049
—— 即**菜单栏 38 逻辑点**（不是 §4.5 上文估的 39）、Dock 82 点。按 Tauri 的换算
`work_area.y = 38`，所以新的 `target_y = 58`，`target_x` 仍是 1440。

然后**直接驱动了真的浮窗进程**验证落点（`spool --overlay` 单独起一个，喂一条 `notice` —— 
notice 不抢前台，不打扰人；`--overlay` 在 `main.rs` 里早于 GUI 分支返回，不碰库）：

| 喂进去的 y | 卡片上边框实际落在（物理行） | 说明 |
|---|---|---|
| **20**（旧代码算出来的） | **76** = 38 逻辑点 | ⚠️ **AppKit 把它按回可用区域顶了** |
| **58**（新代码算出来的） | **116** = 58 逻辑点 | 原样照办，边框 652px 完整 |

⭐ **所以旧代码的 20 点边距从来没生效过**：AppKit 的 `constrainFrameRect:toScreen:`
把窗口顶回菜单栏正下方，卡片和菜单栏之间**一点空隙都没有**，
菜单栏画到哪一行、就吃掉卡片的哪一行 —— S1 里丢的那 2 物理像素就是这么来的。
新位置有 20 逻辑点的净空，**菜单栏再也够不着它**。

⚠️ 还没走到的一步：**装机后没人用真手指双击 ⌥ 复核过**（合成事件在这台机器上驱动不了 Spool，
§3.2 那条限制仍然有效）。上面验的是「给定原点，窗口落在哪」+「新代码算出的原点是多少」，
两段都是实测；缺的是主进程在真捕捉时把这个原点算出来那一下。**Ocean 下次捕捉时扫一眼即可。**
真改了就得重拍 S1 并把上面整条链再跑一遍。

---

## 5. 2026-08-18：v0.5.0 发布收尾 + 双平台上官网 —— 【已完成，已上线】

### 5.1 发布（A 段）

| 项 | 结果 |
|---|---|
| tag `v0.5.0` | 打在 `55aa065`，已推；GitHub 上解引用回 `55aa065` 核过 |
| Release | 已建，**四个资产全在**（两个带版本号 + 两个官网用的固定名） |
| 固定名 URL | `releases/latest/download/Spool-macOS-arm64.dmg` 和 `…-windows-x64-setup.exe` 都是 **200** |
| ⭐ 回环复验 | 把发布出去的 dmg **下载回来**，sha256 与本地一致，`spctl` 仍是 `accepted / Notarized Developer ID` —— 验的是「用户真正拿到的那份」，不是本地那份 |
| 发布说明 | `docs/RELEASE_NOTES_v0.5.0.md`（英文，仿 v0.4.0 那份的写法） |
| 台账 | `CASE_STUDY_LEDGER.md` §1.2 追加 v0.5.0 一行 |

⚠️ **公证回执只留下一个 submission id**（`e02a0bd1-…`）。`.app` 和 dmg 各交过一次，
另一个当场没抄下来；按台账开头那条定义，**它已经找不回来了**（`notarytool` 跑完就查不到）。
结论本身不存疑（Gatekeeper 认、staple 验得过），丢的是其中一次的审计线索。
**下次发布，两个 id 都要当场抄。**

### 5.2 双平台上官网（B 段）—— 已上线

Pages run `32157489236` **success**，spoolapp.org 实测：英文 title 含 Windows、
首页有「Download for Windows」、中文站有「下载 Windows 版」，
按钮指向的 exe 回 200 且 `content-length` 与产物字节数一致。

⭐ **这一段的要点不是加按钮，是「加按钮就必须同时改掉已经变成假话的句子」**：
FAQ 那条「不支持 Windows，捕捉依赖 macOS 特有接口」在 0.5.0 之后是直接说反的。
一并改的还有 title / 三段社交卡、story 的「实际上只支持 macOS」、发布那一段、
信任卡三张、权限那一节（macOS 权限清单照旧，另起一段说 Windows 上没有需要授权的项目），
以及架构图里那个 `YOUR MAC` 标签（是 svg 文字不是图，改得动）。

**未签名这件事写在三处**（首屏细则、信任卡、FAQ）——SmartScreen 那一下最好下载之前就知道。

⭐ **视觉基线的一条方法**：18 张全红的时候，先 `git stash` 回干净 HEAD 跑一遍 `check`，
确认基线本来是绿的（17/18，唯一那条是 §4.4 记着的老毛病），**再** `update`。
不然分不清「我改红的」和「本来就红」。

### 5.3 README / 文档

- README：去掉三个中文界面词（照它们在设置里一个都找不到，因为界面默认英文）；
  **「复制使用提示」那个按钮 2026-08-15 就拆了**，README 却还写着「how-to 只住在这一个地方」；
  状态段改成 v0.5.0 双平台；MCP 工具数 18 → **19（12 读 + 7 写）**。
  ⚠️ **许可那一节没动**，仍是「尚未选择许可，保留所有权利」——
  Ocean 2026-08-18 明确：**不要 MIT，往 README 里补 MIT/许可说明是错的**。
- 归档七份进 `docs/archive/`（三份已有答案的调查 + 官网重做那一组三份 + 引擎位自评），
  引用全部跟着改；`docs/README.md` 整个重写（原来那份说 Windows 移植「未开工」）。
- `DEMO_SCRIPT.md` 按 v0.5.0 重写（旧稿写「选意图」，那三种任务类型 v0.4.0 就删了）。

### 5.4 ⏳ 还欠着的

1. **S1 重拍** —— **Ocean 2026-08-18 拍板：先放着。** 官网现在看着是好的，
   欠的只是「截图与产品当前形态一致」：卡片顶边框那 2 像素早已在产品侧修好
   （`work_area` 定位），但线上那张 S1 是旧位置拍的。
   ⚠️ 这一张**代拍不了**：要隔离演示库的构建 + 在物理屏上真按一次捕捉键 + 全屏 Retina 截图，
   而且新图必须和旧图几何对齐，否则 §4.5 那整条裁切链、CSS 列宽方程和视觉基线都要重解。
   ⭐ 真要拍时**别按双击 ⌥**：`seed-demo-library.sh` 已经把捕捉键绑成 `⌘⇧K`
   （绑定快捷键走 `RegisterEventHotKey`，不需要输入监控授权），隔离 identifier 从来没被授过权。
2. **Windows 代码签名** —— 首版故意不签（2026-08-15 决策 5）。要不要买证书是独立一件事。
3. 老账仍在：`AXFrontmost` 被拒那条路没实测走到过；多显示器 / 全屏 Space 没验过；
   新浮窗位置没人用真手指双击 ⌥ 复核过。
