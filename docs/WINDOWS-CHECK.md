# Windows 首版 —— 装机和验收（给 Ocean）

> 这份是给你在 Windows 电脑上照着做的。**一步一个动作**，
> 每条前面一个 ☐，做完打个勾；哪条不对，把那条的编号发给我就行。
>
> ⚠️ **2026-08-17 你跑过一轮了**（结果见下面「第二轮」那一节）。第一部分到第四部分
> 是那一轮的原稿,里面被推翻的几条已经就地改掉并标了日期。
> 这台机器仍然编译不了 Windows,所以**改完的东西还是只有你看得见** —— 每一条都是真的在问。
>
> ⚠️ **改动仍然只在 `windows-port` 这个分支上,没有合进 main** —— 你 Mac 上装着的 Spool
> 不会自己变。
>
> ⚠️ **但第二轮有五处是跨平台的,合进 main 的那天 Mac 也会跟着变**(前四处是你自己定的):
> ① 没装 Claude Code / Codex 时,左栏不显示「周回顾」;
> ② 右栏的「跟进内容」反过来 —— 没有本机 CLI 也照样显示;
> ③ 设置 → AI 引擎 多了说明和「手动指定 CLI 路径」;
> ④ 绑快捷键时 ⌘Z 这类会被拒;
> ⑤ AI 写入时那个「21 位大小写混排」的拦截放宽了(要带数字或 `-`/`_`)。
> **第一轮的 Windows 修复一处都没碰 Mac,那句话对第一轮仍然成立。**

---

# 第二轮（2026-08-18）—— 你上一轮报的，我改了什么

> 你上一轮的结论：**23 条通过，3 条不过**(13、20、24),外加 34 那个小插曲、
> 一个「默认快捷键能不能是双击 fn」的问题,和你自己发现的 4 件事。
> **全部认下了,全部改了**(fn 那条是答复,不是改动 —— 见最后一节)。
> ⚠️ 你写的通过清单里有个 **39**,那一条不存在,我按 **30** 记了;要是我记错了说一声。
>
> ⚠️ **先说那个编译失败的网址**：`runs/32044993155` 是**那天最早的一次**，
> 它挂在 Windows 的换行符上（Git 在 Windows 上会把文本文件的换行改掉，
> 有两个测试是逐字节比对的，就崩在这儿）。**同一天后面三次都是绿的**，
> 你装的就是绿的那一版，所以你才能往下验。这一条不用再管。
>
> **你要做的**：照第一部分重新下载最新的绿色构建，覆盖装上去（数据不会丢），
> 然后只验下面这几条。上一轮通过的那些不用重跑。

## 上一轮那四条

☐ **36.** 点右上角 ✕。**先看托盘**：那个图标**还在不在**？
（这是最关键的一问 —— 上一轮你说「完全关闭 app」，我需要知道是真的退了，
还是只是窗口没了、图标其实还在。）

☐ **37.** ✕ 之后，**左键单击一下**托盘图标 → **窗口应该回来**。
（以前左键弹的是菜单，现在左键=开窗口、**右键**=菜单。右键那份菜单还在，里面还是有「退出」。）

☐ **38.** 复制一段字，按你的捕捉快捷键 → 卡片弹出来 → **直接打字**，不用先点输入框。
⚠️ 这条上一轮我写的是「Windows 大概率不行」。**改了**：按快捷键的那一刻系统是允许我们抢焦点的，
现在卡片会自己跳到最前面。**如果还是要点一下才能打字，告诉我。**

☐ **39.** 卡片弹出来之后按 **Esc** → **关掉**（这就是上一轮的第 24 条）。

☐ **40.** 关掉卡片之后，**你原来那个软件应该还是在最前面**，接着打字就打进它里面。
（不是跳到 Spool 主窗口。）

☐ **41.** 卡片还在屏幕上的那几秒，切回你原来那个软件按 **Ctrl+Z** →
撤销的应该是**那个软件自己的东西**，不是 Spool 的捕捉。
（以前那几秒 Ctrl+Z 是被 Spool 借走的。现在卡片自己能收键盘了，就不借了。）

☐ **42.** 设置 → 快捷键 → 点「未设置」→ 按 **Ctrl+Z**。
**应该被拒绝**，并且告诉你为什么（「Ctrl+Z 是所有软件通用的『撤销』……」）。
再按 **Ctrl+Shift+Z** → 这个**应该收下**。
（这就是上一轮第 20 条你说的「什么都收」。被拒的是「单个 Ctrl + 一个所有软件都在用的键」
那一类：Z Y X C V A S F P N O W T Q。多加一个 Shift 或 Alt 就随你。）

☐ **43.** 让 AI 再写一条带 `SpaceTimeAStarPlanner` 这种**21 位大小写混排、纯字母**的内容进去
→ **不该再被拦**（上一轮第 34 条那个小插曲）。
（原因：那个检测器只看「21 位 + 有大小写」，你的类名正好长这样。现在还要求里面**带数字或 `-`/`_`**
—— 真的 id 几乎一定有，类名几乎一定没有。）

## 你自己发现的那四件

☐ **44.** 窗口打开 → **底部不再被任务栏挡住**。
（以前窗口是写死 1600×1000，那是「逻辑像素」；你的屏幕缩放到 150% 之后，屏幕能放下的高度比它小。
现在启动时会按**任务栏之外的那块可用区域**夹一下，然后居中。）

☐ **45.** 设置 → MCP → 对 Visual Studio Code 点「一键接入」→
底下会多出一行字，**告诉你还差哪几步**；再下面有一行**「Visual Studio Code 接完还要点两下 — 一步一步」**
可以点开，里面是完整步骤。
⚠️ **说实话**：最后那一下（让 VS Code 真的把 spool 这个服务跑起来）**Spool 替不了你** ——
VS Code 出于安全，不会自动去跑一个它没见过的 MCP 服务。我能做的是**不让你自己去猜**。

☐ **46.** 左栏**不再有「周回顾」**；右栏也**不再出现**那句「装了 Claude Code 或 Codex……才有东西」。
（Windows 上这条路本来就是关着的，说了也做不到。你定的：所有平台都这样 —— Mac 上没装 CLI 也一样不显示。）

☐ **47.** 右栏的**「跟进内容」现在看得见了**（以前 Windows 上整块不显示，那是错的 ——
跟进清单是 MCP 也能写的东西，不该跟着本机 CLI 一起消失）。
能「定一个」、能编辑、AI 也能通过 MCP 往里加。**「联网搜索」那个按钮不出现** —— 那一下要本机 CLI。

☐ **48.**（只有 Mac 上看得见，Windows 这一页是整页不显示的）设置 → AI 引擎 →
多了一行**「这到底要装什么、key 放在哪儿」**可以点开，
和一行**「手动指定 CLI 路径」**（装了但没搜到的时候用）。

## 你问的：默认快捷键能不能是「双击 fn」

**不能，而且不是我不想做。** fn 那个键在绝大多数 Windows 笔记本上**根本走不到操作系统**——
它在键盘固件里就被吃掉了（按住 fn 再按 F5，系统只看得见「F5」或者「音量键」）。
所以没有任何软件能知道你「双击了 fn」，Spool 也不行。

「用复制和按键的时间差来辅助判断」这一步是对的，**Mac 上双击 ⌥ 就是这么做的**（10 秒内复制过才算）。
卡住的不是这个判断，是**那个键本身送不到软件手里**。

真要在 Windows 上做双击手势，只能是**双击 Ctrl 或双击 Alt**，而且要装一个全局键盘钩子
（`WH_KEYBOARD_LL`），杀毒软件容易把它当成键盘记录器。**你 08-15 定的「首版不做」我建议继续不做**;
想做的话单独开一轮,我先把杀软那一关查清楚再动手。

---

## 第一部分：拿到安装包

**你不需要在 Windows 上装任何开发工具。** 安装包是 GitHub 上的机器编译好的，
你只要下载。

☐ **1.** 在 Windows 上打开浏览器，登录 GitHub，进这个页面：

```
https://github.com/KIM-ocean-HZ/spool/actions
```

☐ **2.** 左边一列点 **「Windows build」**。

☐ **3.** 列表最上面那一条就是最新的。看它左边的图标：

- **绿色的勾** → 编译成功，往下走。
- **黄色的圈** → 还在编译，等几分钟刷新一下（第一次大概 15–25 分钟）。
- **红色的叉** → 编译失败了。**别往下走，把这一条的网址发给我。**

☐ **4.** 点进那一条，页面拉到最底下，有一块叫 **「Artifacts」**，
里面有一个 **`spool-windows-nsis`**。点它，会下载一个 zip。

☐ **5.** 解压那个 zip，里面是一个 **`.exe`** 文件，名字大概长这样：

```
Spool_0.4.0_x64-setup.exe
```

---

## 第二部分：装上去

⚠️ **这个安装包没有签名**（是你自己 08-15 定的：首版先不签名）。
所以 Windows 一定会拦一下。**这是预期的，不是坏了。**

☐ **6.** 双击那个 `.exe`。

☐ **7.** 如果出现一个蓝色的窗口写着 **「Windows 已保护你的电脑」**
（英文是 "Windows protected your PC"）：

1. 点那句话下面的 **「更多信息」**（"More info"）；
2. 下面会多出一个按钮 **「仍要运行」**（"Run anyway"）；点它。

☐ **8.** 之后就是普通的安装向导，一路下一步。
**不需要管理员密码** —— 它装在你自己的用户目录里。

☐ **9.** 装完打开 Spool。

**如果开不起来 / 白屏 / 报 WebView2 的错**：这个安装包安装时需要联网下载一个
叫 WebView2 的微软运行时。确认网是通的，然后重装一次。如果还是不行，
把报错的截图发我。

---

## 第三部分：验收 —— 先看这四件

这四件是你 08-15 定的首版范围（**能装能跑 + 快捷键捕捉 + 打包 + MCP 接得上**）。
**其他都可以先不管，这四件不过就是没做成。**

### A. 能装能跑

☐ **10.** 窗口开起来了，不是白屏。左边能看到侧边栏。

☐ **11.** 屏幕右下角任务栏那一排小图标里（可能要点那个 `∧` 展开），
**能看见 Spool 的图标**。
⚠️ **这条我特别不确定**：那张图标在 Mac 上是一张纯黑的图，
Windows 的托盘是深色的，黑图画上去就是看不见。我改成用彩色的 app 图标了，
但没人验过。**如果那里是空的、或者是一个黑方块，告诉我。**

☐ **12.** 右键那个托盘图标，能弹出菜单，里面有「退出」。

☐ **13.** 点窗口右上角的 ✕。**窗口应该只是藏起来，app 还在托盘里活着。**
**左键单击**托盘图标，窗口回来（右键是菜单）。
⚠️ 08-17 你报了这条不行。改了两处,见第二轮第 36–37 条。

☐ **14.** 新建一个项目，写一条内容，按回车。它存下来了。

☐ **14b.** 打开首启自动建的那个「欢迎使用 Spool」项目，**看第 2 块**。
它应该说的是「先去设一个快捷键，然后 Ctrl+C 复制再按那个快捷键」。
⚠️ **如果它说的是「双击 ⌥（Option）」或者「打开输入监听权限」，告诉我** ——
那是 Mac 的说法，Windows 上照着做什么也不会发生。
（其他块里的 `⌘⇧P` 之类也应该显示成 `Ctrl+Shift+P`。）

### B. 快捷键捕捉

⚠️ **Windows 上没有「双击 ⌥」这种手势**（你 08-15 定的先不做）。
**首版也没有给默认快捷键**（你今天定的：装机时你现场试）。
所以第一次打开，捕捉是完全不能用的 —— 这是对的，不是 bug。

☐ **15.** 窗口最上面应该有一条横幅，大意是
**「想在别的 app 里复制就存，先给捕捉定一个快捷键」**，
右边有个按钮 **「设一个快捷键」**。
⚠️ **如果这条横幅没出现，捕捉这一整块就等于没有入口，告诉我。**

☐ **16.** 点「设一个快捷键」，会打开设置面板，而且**应该直接停在「快捷键」那一栏**
（因为还没绑，那是最要紧的一件事）。看到「捕捉快捷键」那一行，右边写着「未设置」。

☐ **17.** 点「未设置」那个按钮，它会变成「按键中…」，
然后**按你想要的组合键**。我建议先试 **`Ctrl + Alt + 空格`**。

☐ **18.** 按完之后，那个按钮应该显示成 **`Ctrl+Alt+Space`** 这样的字。
⚠️ **如果它显示成 `⌃⌥Space` 这种 Mac 符号，告诉我，那是我没改干净。**

☐ **19.** 如果它报错说系统拒绝了这个快捷键 —— 说明这个组合被别的软件占了。
**换一个再试**（比如 `Ctrl+Shift+空格`、`Ctrl+Alt+C`）。
**你最后用的是哪一个，告诉我，我记进文档。**
⚠️ 2026-08-18 起还多了一种拒绝:**单个 Ctrl + 一个所有软件都在用的键**(Ctrl+Z / Ctrl+C …)
会被 Spool 自己拦下来,因为全局快捷键是**从别的软件嘴里抢走**的 —— 见第二轮第 42 条。

☐ **20.** 关掉设置。上面那条横幅应该**自己消失了**。

☐ **21.** 现在去别的软件（浏览器 / 记事本 / Word 都行），
选一段字，**按 Ctrl+C 复制**，然后**按你刚才设的那个快捷键**。

☐ **22.** 屏幕右上角应该弹出一个小卡片，里面是你刚复制的那段字。
⚠️ **卡片里应该有一行「来源」，写着你刚才那个软件的窗口标题**
（比如浏览器就是那个网页的标题）。**如果来源是空的，告诉我。**

☐ **23.** 卡片弹出来**直接就能打字**，打的字按回车能存进去。
⚠️ **2026-08-18 改过**:这条原来写的是「Windows 上大概率不行,要先点一下」。
按快捷键的那一刻系统是允许我们抢焦点的,所以现在卡片会自己跳到最前面 —— 见第二轮第 38 条。

☐ **24.** 卡片按 **Esc** 能关掉；不管它的话大概 8 秒自己消失。
⚠️ 这条 08-17 你报了不行,原因就是上面那条:卡片收不到键盘,Esc 自然没反应。一起改了。

☐ **25.** 回到 Spool 主窗口，**刚才捕捉的那条在项目里**。

### C. 打包

☐ **26.** 在一个有几条内容的项目里，点顶上的 **「打包」**。

☐ **27.** 打包出来的文字里，**如果有附件，只应该出现文件名**，
比如 `lecture-03.pdf`。
⚠️ ⚠️ **绝对不应该出现 `C:\Users\你的用户名\...` 这种完整路径。**
这是要粘给别人 AI 的东西。**看见完整路径立刻告诉我，这是隐私问题。**

☐ **28.** 左边栏在工作区那一行悬停，有个箱子图标（打包整个工作区），
点它 → 选个文件夹 → 导出。**导出的文件夹能打开，里面有 `INDEX.md` 和几个 `.md`。**

### D. MCP 接得上

☐ **29.** 设置 → **MCP** 那一栏。里面列着 Claude Desktop / Cursor / VS Code /
Claude Code / Codex 这些。

☐ **30.** 你 Windows 上装了哪个，它旁边应该显示 **「未配置」**；
没装的应该显示 **「未安装」**。
⚠️ **如果你明明装了某个客户端，它却说「未安装」，告诉我是哪个** ——
说明我猜的 Windows 路径不对。

☐ **31.** 对一个你装了的客户端点 **「一键接入」**，应该变成 **「已配置」**。

☐ **32.** **把那个客户端完全退出再打开**（重要 —— 它要重新启动 Spool 的服务）。

☐ **33.** 在那个客户端里问一句：**「我在 spool 里有哪些项目？」**
它应该能列出来。

☐ **34.** ⚠️ **这一条最关键，务必要做**：再说一句
**「把『这是 windows 上的第一条测试』存进 XX 项目」**（XX 换成一个真项目名）。

**为什么这条最关键**：读得出来 ≠ 写得进去。写入要生成一个随机 ID，
那段代码在 Windows 上是全新写的、从来没跑过。**如果它报「随机源不可用」，
立刻告诉我。**

☐ **35.** 回 Spool 看一眼，那条真的进去了。

---

## 第四部分：这些我知道是不行的，不用测

- **AI 维护 / 联网跟进那几个按钮** —— Windows 上整块是关着的，右栏不会出现，
  设置里那一栏「AI 引擎」也不显示。**2026-08-18 起左栏的「周回顾」也不显示了**
  (以前它在,点进去只有一句「装了 Claude Code 或 Codex 才有东西」——在 Windows 上那是永远做不到的事)。
  原因：点「取消」在 Windows 上停不掉整棵进程树，会留下还在计费、
  还攥着数据库的后台进程。这个要另做一版。
  ⚠️ **跟进清单本身还在**（面板、右栏、MCP 里加/关条目都正常），
  关掉的只是「让本机的 claude/codex 自己跑一轮」那条路。
  ⚠️⚠️ **这句话 08-17 之前是假的**:右栏那一块当时跟着本机 CLI 一起被藏了,
  所以 Windows 上清单能写、能被 MCP 改,就是**看不见**。2026-08-18 修了 —— 第二轮第 47 条。
- **双击 ⌥ / 双击 Alt** —— 不做（你定的）。fn 为什么连做都做不了,见第二轮最后一节。
- **点卡片外面让它消失** —— 不做，用 Esc 或 ✕ 或等它自己走。
- **浏览器的「当前标签页标题」** —— Windows 上退成「窗口标题」，
  浏览器的窗口标题本来就带着标签页名字，够用。
- **设置里的「浏览器自动化权限」那一块** —— Windows 上直接不显示了（那是 Mac 的权限）。

---

## 出问题了怎么给我信息

☐ 截图 + **上面那条的编号**（比如「22 不行」）。

☐ 如果是白屏或者点了没反应：在窗口里**点右键 → 检查 / Inspect**，
切到 **Console** 那一栏，把红色的字截图给我。

☐ 如果是 MCP 那一块：告诉我**是哪个客户端**、**它的报错原话**。

---

## 我这边接下来会做的

1. 你报的问题，我修完重新推一次，GitHub 会重新编译一个新的安装包 ——
   **你重新走第一部分下载新的，直接覆盖装上去就行**（数据不会丢，
   库在 `%APPDATA%\com.oceanjin.spool` 里，安装包不碰它）。
2. 全部走通之后，再问你要不要合进 main。**现在不合。**
