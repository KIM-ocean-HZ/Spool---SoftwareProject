# 设计稿 — MCP 写入权限该干什么(角色定位 + 分流审批)

> 状态:**M1 已落地(2026-08-05)** —— `propose_blocks` + 待审面 + 溯源 A 案全部做完,
> 实机探针跑过(`archive/EVAL_M1_M2_M3.md`)。**M2 等真实使用**。
> ✅ **§4.4-bis 已落地(2026-08-06)** —— 原文块现在带来源标签
> `<客户端> · MCP — 用户原文`,权威漏洞关上了。
>
> 原状态:**§7 全部四件已拍完,本稿进入执行期 —— M1 可以开工。**
> (2026-08-05 Ocean 明示:角色定死为"回答者"、不做独立维护通道、否掉完整写入权限、
> 提案保持追加式;溯源走 A 案。**驳掉他自己那个方案的那一条,他没有坚持,采纳了本稿的理由。**)
> 2026-08-05 由 Ocean 提出的问题写成
> ——「MCP 到底是维护我数据的那个角色,还是回答问题的那个 AI?如果只是省去 compact
> 那一步,写入权限有什么用?」本稿给出答案并据此收口写入面。
>
> ⚠️ 本稿**否掉了 Ocean 当天提的另一个方案**(git merge conflict 式的完整写入权限 +
> 逐块审批 + 自动维护一份用户原始数据),理由在 §3。否的是手段,不是问题 ——
> 他要的那个安全属性成立,而且已经免费拿到了。

## 0. 一句话定位

**MCP 是回答者,不是维护者。它的写入是从"回答"那一端伸回库里的手,
只能伸回库里自己产不出来的东西。**

推论,也是本稿全部设计的来源:
- **维护是回答的副产品,不是一份独立的工作。** 不做"专门跑一趟整理你的库"这种通道。
- **写入必须让库变成更好的输入。** 否则它就该被砍掉 —— 见 §1 那个反问。

## 1. 先回答那个反问:写入权限凭什么存在

Ocean 问得比他自己以为的更狠:**如果 MCP 只是省去 compact 那一步,写入权限有什么用?**

答:**没用。** 而且这不是修辞 —— `get_pack` 的定位本来就是"用户原样粘给任何 AI",
官网白纸黑字写着「没配 MCP 也全功能」。**纯回答这件事,剪贴板已经覆盖了。**

所以:**写入权限唯一不可被剪贴板替代的价值,是改变库的状态。**
一次写入如果没让下一次的输入变好,它就是在给剪贴板做无用功。
这条是下面那道闸门的全部依据。

## 2. 那道闸门:可推导性

> **能从库里已有的东西推出来的,只进答案,不进库。**

**能推出来 → 别写。** 两处害:
1. **噪音放大。** 写一份"机器学习课整体总结"存回去,下次读 pack 时它躺在那儿,
   而 pack 的授权规则把它标成 🧩 Synthesis —— "只当框架,不当事实"。
   等于喂自己一份降级过的自己的输出。信息量没增加,长度增加了。
2. **权威洗白。** AI 的一句推测,下一次以"库里躺着的材料"的身份出现,
   看着和用户亲手存的东西一样正当。这比噪音更坏。
3. **会过期,而且看不出来。** 再来五块新的,那份总结就是错的,
   但它结构上完全合法,`check_library` 查不出来。

**推不出来 → 写。** 用户的困惑 + AI 的推理 = 那个结论,库里原先只有困惑。
这时候库是真的变厚了,不是变啰嗦了。

### 2.1 三种场景照这条闸门判

| 场景 | 判 | 为什么 |
|---|---|---|
| **结论回填** —— 用户问、AI 答、用户说"存回去" | ✅ 写 | 那句话原先不在库里,来自推理不是压缩。golden fixture 里就是这个样子:`#1` 用户块带批注「一直没懂为什么要除 sqrt(d_k)」,`#6` 是 MCP 署名结论 + `↩ cites:` 指回 `#1` |
| **分流归位** —— 用户贴一大段,横跨三个项目,AI 拆开分派 | ✅ 写 | AI 干的是**判断**不是生成。这是最有价值的一种,也是唯一需要新界面的一种(§4) |
| **总结全文** | ❌ 别写 | 可推导。**除非**推导很贵且结论很稳(两百块的项目、花了大力气才得的结论)—— 那是 `distill` 的定位,不是"总结全文" |

### 2.2 顺带说清:MCP 块和用户块的价值不同币种

不等同,但都是硬通货:
- **用户块**:第一手、带体温。💭 Personal 信号最强就是这个意思。
- **MCP 块**:补上库里缺的那一环。

所以不需要"给 AI 块单独划一块区域"。**块流按时间排,来源标签负责区分权威**,
这套四类授权规则已经在跑了,再加一层物理分区是重复造闸。

## 3. 为什么否掉 merge conflict 那套

Ocean 的方案:给 MCP 完整写入权限(含修改),用户逐块审批,并自动维护一份用户原始数据。

**三条否的理由,由轻到重:**

### 3.1 git 那个比喻在这儿没有活干

git 要三方合并,是因为**代码是被维护的对象**,两个人改同一行。
块是 append-only 的,**没有人改块**。没有同一行、没有共同祖先、没有冲突可解。
merge conflict 那整套机器搬过来是空转。

### 3.2 「自动维护一份用户原始数据」已经免费拿到了

用户的块从来不被修改 —— 所以原始版本**永远在那儿**,不需要额外存快照。
Ocean 想造的那个安全属性,是现在这个数据模型的**结构性后果**,不是要加的功能。
(⚠️ 这一条别当成"不用管数据安全",而是"这一处的安全已经由 append-only 承担,
再加一层快照是把已经硬的东西又软一遍"。)

### 3.3 最重的一条:审批疲劳,它换掉的机制比它自己强

「完整写入权限 + 用户点头」逻辑上自洽:用户批的就是用户自己的编辑,AI 只是手。
反对的理由不是逻辑,是人:

**审批闸防的是"写错的东西",防不了"用户点得太快"。** 第五十次弹窗的时候,
人一定是连看都不看就过。

而 append-only 那条承诺防的**恰恰是这个** —— 它不依赖用户每一次都清醒。
那是它作为**信任特性**的全部价值,也是「允许 AI 写入」这个开关敢让人打开的原因。
**用一个会疲劳的机制,去换一个不会疲劳的机制,不划算。**

**所以:提案永远是追加式的。** 分流可以提"这段该拆成三块进三个项目",
不能提"把你那块改一下"。

## 4. 保留下来的那一半:分流审批队列

Ocean 方案里真正有价值的是**审批队列**,但它该小得多、也只服务一个场景。

### 4.1 为什么只有分流需要界面

| | 单条结论回填 | 分流一大段 |
|---|---|---|
| 一次几块 | 1 | 三到十几 |
| 出错的后果 | 一块内容不对,看得见 | **进错抽屉** —— 块本身没错,位置错了,而位置就是 Spool 的全部结构 |
| 判断是谁做的 | AI 生成内容,用户判断内容 | AI 判断归属,用户**只能判断归属** |
| 在聊天窗里逐条念 | 可行,一句话的事 | 不可行,十几块念完用户已经不看了 |

结论:**分流是唯一"高量 + 高风险 + 机械"的场景**,而这三条正好是需要独立界面的三条。
单条结论回填继续走 `dry_run`,同步、轻量,不动。

### 4.2 时间形状:异步,不是 git 那种同步

这是最硬的一处工程约束,得先定。

git 解冲突时人在键盘前,合并停住等他。**MCP 写入不是** —— AI 调工具,几秒内必须拿到返回,
而用户可能在睡觉、在别的 app 里、四十分钟后才点。所以不能卡住等。

**`propose_blocks` 立刻返回「已提交 N 条待审」**,不等用户。三条随之而来的规矩:

1. **AI 必须把"已提交待审"和"已存进库"说成两件事。** 工具描述里写死,
   否则 AI 会对用户说"存好了",而用户的库里什么都没有。
   (⚠️ 这是本设计最可能出的事故,`human_headline` 的措辞要盯 —— 硬规则 13。)
2. **提案不进块流、不进 pack、不进 digest、不进 search。** 待审就是待审,
   `get_pack` 读不到它。否则 AI 会读到自己没落地的提案,拿它当材料。
3. **提案有保质期**(建议 7 天),过期自动作废并在审批面留一行"已过期"。
   不设期限,队列会变成第二个待办清单 —— §3.2 第 4 条那条"从 08-01 拖到今天"的教训。

### 4.3 界面:一屏,批量为默认动作

不做逐块弹窗(那正是 §3.3 说的疲劳源)。做**一个待审面**:

- 入口:主窗一个角标(「AI 提了 8 条待你过目」),**不弹窗、不打断**
  (`capture-note-first` 那条铁律:主窗永不跳前)。
- 一屏列完,每条显示:**正文预览 + 要进哪个项目 + 原文的哪一段**(见 §4.4)。
- 默认动作是**全批**和**全驳**两个大按钮;逐条勾选是次要路径。
  理由:分流的判断颗粒是"这次拆得对不对",不是"第三块对不对"。
  拆错了通常是整体拆错,一条一条改的成本比重来一次高。
- 驳回不需要理由,也不留痕(留痕会让队列变成垃圾场)。
- **驳回一条待审项 = 撤回**。所以交接 §4 第 10 条那件事**不用单独设计了**,
  也不再需要回答"时间窗多长""算不算 AI 自己写的"那一堆边界 —— 那些问题在
  "东西还没落地"的世界里不存在。

### 4.4 溯源:拆出来的块必须指得回原文

这是现在还空着的一处,也是分流跟普通 `add_block` 唯一的数据差别。

一段话拆成三块,三块各自都是原文的**片段**。如果不记原文,用户三周后看到其中一块,
没法判断它是不是被拆得断章取义了 —— 而断章取义正是分流唯一会犯的错。

✅ **Ocean 2026-08-05 拍板:走 A 案,用现成的 `ref_block_id`。** B 案封存,别动 schema。

### ✅ 4.4-bis 修订(2026-08-05 晚 Ocean 拍板选 (a);**2026-08-06 已落地**)—— 原文块**要带来源标签**

**为什么要修订**:M1 落地后的实机探针证实了一条通路 —— `source_text` 里塞什么都行,
批准之后那一块**不带任何来源标记**落库,而 pack 的授权规则把无来源块判成
**💭 Personal = 用户自己的意图,信号最高**。也就是说 AI 递过来的一段话,
能借用户的名义拿到库里权威最高的身份。唯一的防线是待审面上摊开给用户看,
但默认动作是「都存进去」(§4.3),人很可能不读就点了。

**Ocean 选的是 (a):给那一块加个来源标签。**(另外两个候选 —— 单独勾选、
按长度触发单独确认 —— 都没选。)

**这条修订推翻的是 §4.4 A 案里「用户署名的那种,因为它是用户给的」这半句。**
理由仍然成立(那段话确实是用户给的),但**它不是用户存进来的** —— 中间隔着一个 AI,
而来源标签记的正是"经谁的手进来的"。

**落地记录(2026-08-06):**

- 标签写成 `<客户端> · MCP — 用户原文`(英文侧 `— user's own passage`),
  由 [proposals.ts](../src/lib/db/proposals.ts) 的 `passageSource()` 拼,写库那一刻按当时的
  界面语言定 —— 它是**存下来的数据**,以后换语言不会跟着变(跟别的来源标签一样)。
  两半都必要:`· MCP` 那半让 `isMcpSource()` 认出它,GUI 给 🤖 图标、
  「AI 活动」面板数得到它;`用户原文` 那半是给**读 pack 的模型**看的。
- ⚠️ **一个必须知道的副作用**:带 AI 来源标签 + 长段落,按 pack 的授权规则会被读成
  **🧩 Synthesis =「别人的框架,不能当事实」**。而这段其实是用户的原话 ——
  等于从"抬得过高"换成了"压得过低"。标签里那句「用户原文」就是为了让模型看出这层,
  所以**措辞不能省**。
- 预计的四处改动都改了:`approveBatch` 里那个 `source: null`;待审面那句文案
  (现在直接把标签原样摊给用户看);`proposals.test.ts` 的断言(反过来断,
  外加一条 `isMcpSource()` 认不认得它);i18n 两侧。
- ⚠️ **实际多出第五处:`mcp.rs` 里四句"以用户自己的名义"全是错的了。**
  两句在工具描述里(`propose_blocks` 的描述 + `source_text` 参数的描述),
  一句在 `human_headline`(AI 转述给用户的那句),一句在代码注释。
  **最该改的是 `source_text` 参数那句** —— 它原先等于把这个漏洞写在说明书上
  (「存成用户自己的块」),现在改成「这里只放用户的原话,别放你自己写的」。
- ⚠️ **别顺手去动 pack 的授权表头**给它加一类 —— 那是 golden 锁步的文件(硬规则 5),
  要付三侧改动 + fixture 重生。这一处不值。

---

- **A(已批,M1 照这条做;原文块的署名见上面 4.4-bis 修订)**:分流时先把**整段原文**
  存成一块,再把拆出来的每一块用现成的 `ref_block_id` 指回它。
  零 schema 改动,`↩ cites:` 那一行渲染现成,pack 里读起来就是
  「这块出自那段原文」。代价:原文那一块会占一个项目的位置 —— 建议进用户指定的
  某一个项目,或者一个"收件箱"项目。
- **B(封存,别做)**:新增一个 `excerpt_of` 字段。更干净,但要动 schema(第 10 版),
  两侧锁步常量 + 真库迁移 + golden 重生,一整套。真需要时再升级 ——
  `ref_block_id` 到 `excerpt_of` 是加字段不是改语义,两者不互斥,今天走 A 不堵死 B。

⚠️ **A 案落地时的一处前提**:原文块和拆出来的块**可能不在同一个项目**
(原文进"收件箱",拆出来的进三个正主项目)。所以 `↩ cites:` 那一行**必须标出被引块
在哪个项目** —— 否则用户看到"出自那段原文"却在本项目里找不到它。
这正好是 §3.1 第 3 条那笔旧账(`↩ cites:` 不标项目名),**它现在是 A 案的前置依赖,
不再是可有可无的润色。**

## 5. 工具描述要加的话(那道闸门的确切措辞)

闸门写在文档里没用,得写进 AI 读得到的地方。三处:

**5.1 `add_block` 的描述,开头加一句:**

> Write only what the library cannot already produce. A conclusion that took real
> reasoning to reach, or the user's own words they asked you to keep — yes. A summary
> of blocks that are already here — no: you would be feeding yourself back a
> downgraded copy of your own output (packs mark AI-written text as 🧩 Synthesis =
> framing, not fact), and it silently goes stale as new blocks arrive.

中文那一半照 `t!` / `ts!` 的规矩一起写(硬规则 12)。

**5.2 server instructions 里,写入那一段改成角色声明:**

> You are the one who answers, not a librarian. Writing back is for what this
> conversation produced and the library lacks — not for tidying it up.

**5.3 `propose_blocks`(新)的描述里,把 §4.2 第 1 条写死:**

> This does NOT save anything. It queues proposals for the user to approve in Spool.
> Tell the user they have N items waiting for review — never that you saved them.

## 6. 分期

| 期 | 做什么 | 依赖 |
|---|---|---|
| **M0** | 只加 §5 那三处措辞。**一行代码不动接口面** | 无。⚠️ 建议**先只做这个**,见 §7 |
| **M1** | `propose_blocks` + 待审面(§4.2/4.3)+ 溯源走 A 案(§4.4,**已批**) | ✅ **已落地**(2026-08-05)。schema v10 两张新表,提案对 pack/digest/search/块流全不可见;7 天过期;批准逐块可重试 |
| **M2** | 视 M1 结果决定:写入开关是否可以默认打开 | ✅ **已落地(2026-08-13)—— 默认打开了** |

M2 那条是这套东西真正的回报:**写入开关原先默认关,一部分原因就是写下去不可逆。
有了审批闸,它有可能默认打开。** 这是产品层面的解锁,不只是修一个 bug。

**2026-08-13 兑现:** 闸门条件(2026-08-08 B3)是「`add_block` 真跑过且没出事」,而那已经发生
(08-07 ChatGPT 写进 11 块,两次报错自己恢复)。默认值翻在**两处,必须一起翻**——
`settingsStore.ts` 的默认值和 `mcp.rs` 的 `mcp_write_enabled`,因为
**这个键在有人碰过开关之前根本不在 `settings.json` 里**,两侧各自吃自己的默认值。
⚠️ 它仍然是 `mcpEnabled` 的子开关:**用户不开 MCP 服务,谁也写不进来**;
⚠️ `settings.json` 读不出来或解析不了仍然一律**不许写**(读不出的文件不算同意)。
连带改掉的三处措辞:设置页那句「默认只读」、`PRIVACY.md`、官网隐私页(中英双侧)。

## 7. ✅ 四件都拍完了(2026-08-05 Ocean)

1. ✅ **角色定死为"回答者",不做独立维护通道** —— §0。全稿的根。
2. ✅ **否掉完整写入权限,提案保持追加式** —— §3.3。
   ⚠️ 这一条是**驳 Ocean 自己的方案**,他有权坚持 —— **他没有坚持**,
   接受了"审批疲劳换掉的是一个不会疲劳的机制"这个理由。
   所以 §8 那条"AI 修改用户块,哪怕有审批"是**定论**,不是待议。
3. ✅ **M0 先单独发,M1 等第三轮报告** —— §6。M0 已于 08-05 单独走完;
   第三轮报告 08-05 由开发窗自己当客户端跑完(`archive/mcp-reviews/ROUND3_CLAUDE_CODE.md`),
   报出的两条栅栏漏洞已修完并实机验过。
4. ✅ **溯源走 A 案** —— §4.4,现成的 `ref_block_id`,不动 schema。

**结论:M1 的两个前置(批稿 + 第三轮报告)都清了,可以开工。**

**`dry_run` 那个悬着的问题,第三轮已经答了(不再是预测)。** R13 问的是"AI 会不会主动用
`dry_run`",本稿原先押的是"不会"。第三轮的答复
(`archive/mcp-reviews/ROUND3_CLAUDE_CODE.md` Z4)证实了这一押注,
并且给出了本稿原先没有的理由:**用户说"存回去"的时候,要存的那句话刚刚就在屏幕上,
他刚读过** —— 再调一次 `dry_run` 把同一句念第二遍,对用户是重复,对 AI 是多一次往返。
自然的做法是在聊天里说一句「存到〈某项目〉,批注写……,要改吗」,那已经是散文版的预演。

**所以 §4.1 的结论被使用方独立确认了:预演的战场不在单条结论,在分流。**
`dry_run` 真正有用的场合是**落点或编号拿不准**的时候(分流一大段;或者用户说
"存到那个课的项目里"而有两个项目都叫"机器学习课…")。**这条抬高了 M1 的优先级,
而不是降低。**

## 8. 明确不做的

- **AI 修改用户块**(哪怕有审批)—— §3.3。**08-05 已拍成定论**,不再是待议项。
- **给 AI 块划单独的物理区域** —— §2.2,来源标签已经承担了区分。
- **自动维护用户原始数据的快照** —— §3.2,append-only 已经免费给了。
- **"专门整理一趟"的通道** —— §0,维护是副产品。
  ⚠️ 这条和 `thread_health` 不矛盾:`thread_health` **只报不改**,它的产出是给用户看的
  发现,不是写回去的块。
- **提案进 pack / digest / search** —— §4.2 第 2 条。


---

## 9. ✅ 第一次真跑:ChatGPT 桌面版往真库写入(2026-08-07 16:47–17:09)

**这是 §6.2-ter「子进程/外部客户端的活必须真跑一次」在写入侧的第一次兑现,
也是第一次有非开发窗的 AI 客户端往 Ocean 的真库写东西。**

**怎么跑的**:Ocean 在 ChatGPT 桌面版(Codex app-server,code mode)一场**已有的**
长对话里,让它把这场对话里已经定下来的信息写进 Spool。**没有给它任何工具用法提示。**

### 9.1 它实际调了哪几个工具、什么顺序(从 `~/.codex/sessions/**/rollout-*.jsonl` 扒的)

```
list_threads → create_thread(title + summary) → add_block ×12
  → get_pack(自己回读检查) → list_threads → add_block ×2 → set_thread_summary
```

**这条链和本稿设计的路径一字不差**,而且有两处**没人教它、它自己做的**:

1. **写完一批之后 `get_pack` 回读自查** —— 本稿没预料到这个动作。
   它把刚写的东西按用户会看到的样子读了一遍,然后才继续写。
2. **`set_thread_summary` 收尾** —— 建项目时先给了一版摘要,全部写完后按最终结论**改了一次**。

### 9.2 量出来的数

| 项 | 值 |
|---|---|
| 落库块数 | **11 块**(789–1722 字,平均约 970) |
| 客户端自报的名字 | **`Codex`** → 来源标签 `Codex · MCP` |
| 走的是 `add_block` 还是 `propose_blocks` | **全走 `add_block`**,`proposals` 表始终为 0 |
| 落在哪个工作区 | 没传 `workspace_title` → 默认(第一个)工作区,现在叫〈升学〉 |
| 报错 | 2 次,都自己恢复了(见 §9.3) |

⚠️ **`source` 那个可选参数它用得比预期好** —— 每块都填了一句自己的说明,
库里长这样:`Codex · MCP — CMU 官方招生与 FAQ 页面核验(2026-08-07)`。
**每块的来源都能看出这块是从哪儿核来的**,不是一律 `Codex · MCP`。

### 9.3 两次报错,都不是设计问题

1. `ReferenceError: planThread is not defined` —— **它自己的 JS 写错了**
   (code mode 里每段 exec 是独立作用域,它跨段引用了变量)。跟 Spool 无关。
2. `缺少 content 参数。` —— 它调 `add_block` 忘了带正文。
   **Spool 的大白话拒绝语直接够用**,它下一步就补上了。
   → **这条证明:参数缺失的拒绝语不必带示例 JSON,一句人话模型就能接住。**

### 9.4 对本稿的三条结论

1. **§4.1「预演的战场不在单条结论」再次被独立确认** —— 全程 **0 次 `dry_run`**。
   它选的自查方式是**写完之后 `get_pack` 回读**,不是写之前预演。
2. **`propose_blocks` 一次没被选中。** 提示词里 Ocean 只说了「写入」,
   模型判成单条结论回填,**这正是 §4.2 第 1 条希望的分工**。
   ⚠️ **但也意味着分流那条路至今没有被外部客户端自发走过一次。**
3. ⚠️ **仍然没验的:重名拒绝、`dry_run`、`ref_block_id` 溯源、以及分流。**
   这一场全是「新项目 + 连续追加」这一种形状。

### 9.5 ⭐ 复盘时发现的第四条:GPT 想用档③,但今天没有档③可用(2026-08-08)

Ocean 在〈申请规划〉#10/#11 底下看到一行「更正」,截断、点不开、看不出指向谁,
问是不是 bug。**查完:不是 bug,是三件事撞在一起,而且每一件都值得单独记。**

**它们实际是什么**(真库读的):

| 块 | `ref_block_id` | `ref_kind` | 自己的批注(**GPT 写的**) |
|---|---|---|---|
| #10 | → **#4** | **空** | 「**更正**先前"项目相似所以不能同时申请"的误导性表述…」 |
| #11 | → **#4** | **空** | 「**更正**原先"音乐产品契合"的过度判断…」 |

而 #4 自己的批注(**也是 GPT 写的**)是「避免把 CMU 项目混为一谈:MHCI 是资格性排除…」
—— **那就是 Ocean 看到的那行字的来源**:`CitationLine` 按 `blockLabel()` 的阶梯
拿被引块的**批注**当名字,截在 40 字。

**三条结论:**

1. ⭐ **GPT 想做的正是档③「部分更正」,而它做不到。**
   `ref_kind` 今天**只有用户能写**,MCP 工具面上没有这一位。
   于是它退而求其次:`ref_block_id` 指过去 + **在批注里用中文写「更正」**。
   → **这是拍板甲(档③开给 MCP)在真实数据里的第一份证据,而且是模型自己撞出来的。**
   甲落地后这两行会渲染成「↩ **更正了其中一处:**#4 …」,今天渲染成没有动词的裸 `↩`。
2. ⭐ **拍板乙的活标本。** #4 那句批注是 GPT 写的,现在正以「用户批注」的身份
   充当 #4 的名字,出现在 #10/#11 底下。**这就是 §9.3 拍板乙要堵的那条通路,
   而它已经在真库里发生了。**
3. ⚠️ **`CitationLine` 这一层「太安静」了。**
   2026-07-12 定它的时候写死了「presentation only:一行浅色,无按钮、无跳转」
   (`CitationLine.tsx` 头部注释),那时**还没有任何 MCP 客户端真的在写引用**。
   现在真的写了,这一行的三个缺点全暴露:**不显示块号(`#4`)、CSS 截断没法展开、点不动**。
   → **建议随拍板甲一起改**:至少把块号加上。改动很小,而 Ocean 已经被它绊了一次。

### 9.6 ✅ 上面三条都处理了(2026-08-08,schema v14)

| 条 | 做了什么 |
|---|---|
| **1** | ✅ **档③ 开给 MCP 了**(拍板甲)。`propose_blocks` 的 item 多一个 `ref_kind`,只认 `"corrects"`;`"supersedes"` 当场拒绝。**#10/#11 那两行今后会渲染成「↩ 更正了其中一处:#4 …」,不再是没有动词的裸 `↩`** —— 前提是模型这次用新参数,不是在批注里写「更正」 |
| **2** | ✅ **拍板乙落地了**,而且**正是从这条实例出发改的设计**:#4 那句 GPT 写的批注,今后在 pack 里是 `ai note:`,并且**不再给 #4 当名字** |
| **3** | ✅ **`CitationLine` 加上块号了** —— `#4` 显示在时间前面(`seq` 为空的 v9 前老行没有号,就不显示)。⚠️ **只加了块号**:「截断能展开」和「点得动」**没做**,本稿只推荐了块号,那两件没人拍过板 |

⚠️ **一条给下一窗的提醒**:第 2 条修的是**今后**渲染的样子。
**真库里 #10/#11 现有的那两行不会自己变成档③** —— 它们的 `ref_kind` 仍然是空,
批注里仍然写着中文「更正」。**那是数据,不是渲染**,只有 Ocean 手动改或者 AI 重提一次才会变。
