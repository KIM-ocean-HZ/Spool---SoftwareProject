# 工作计划 —— 2026-08-20

> **这一份是现在唯一的开工面。** 它合并了三份东西，那三份到此为止只作存档，不用再翻：
>
> | 并进来的 | 原来是什么 | 现在在哪一节 |
> |---|---|---|
> | `docs/BACKLOG-2026-08-19.md` | 商业化四件（看板 / 定价 / 宣传 / 未来） | §1、§3、§4 |
> | `docs/Spool-strategy-2026-08-19.md` | 方向判断、七条接口裁决、渠道计划、AI 压缩形态 | §2、§3、§4、§6 |
> | `docs/Spool-header-analysis-2026-8-19.md` | 表头的八条修法 | §1.4（七条已做）、§2.6（剩一条） |
> | `docs/HANDOFF-2026-08-19.md` §1 | 五件工程欠账 | §5 |
>
> **规矩没变**：§7 那些红线只能由你**明说**推翻，不能作为做别的事的副作用被顺手推倒。
>
> **产品状态**：v0.6.1 已发布并装机，macOS 签名+公证，Windows 未签名（**08-20 Windows 装机已验，通过**）；
> 官网 spoolapp.org 已上线；本体零出网完好。
>
> 🔄 **2026-08-20 有两条红线被你明确反转**（API 引擎、宣传渲染图），**先看 §7 开头那个框**再动手。
>
> ## 👉 下一窗只看 §9
>
> §9 是照着做的执行清单，~~12 步~~ **10 步**，分五批。
>
> ✅ **2026-08-20 又做掉四件**：第一批 **1 / 2 / 3 全做完**（漏斗补齐），加上第三批的
> **表头第八条**。**下一件是第二批第 4 步**：`compress_pack` 跑通形态 C sidecar。
>
> ⚠️⚠️ **动手之前先读 §0 那条更正**：
> **看板上那 32 次下载全部是 Ocean 自测的，真实外部用户 = 0。**
> §9 的顺序完全建立在这一条上——**先让第一个真人能进来并留下，而不是先加功能。**
>
> ⏸ **两件已降级为留档，都不在 §9 上**：
>
> | 留档的 | 什么时候定的 | 一句话 | 去哪读 |
> |---|---|---|---|
> | **收费 / 定价** | 08-20 | 「不急，功能齐全再说」 | §6.1 |
> | 🆕 **宣发物料** | **08-20 当天晚些** | 第一批海报和视频**整批舍弃**，**宣发就地暂停** | **`PROMO-VERDICT-2026-08-20.md`** |
>
> 🆕 ⛔ **同时新增一条红线**：**「95% 用户粘贴 / 5% AI 写入」这个数字作废，任何对外场合都不许再用**
> ——测试初期的数据。⚠️ **它现在还挂在官网 story 页上**，等你拍板怎么处理（那份定论的 §5）。

---

## 0. 这一窗做完了什么（可以验收）

| # | 做了什么 | 怎么验 |
|---|---|---|
| 1 | **下载量看板**，按你定的四条口径全做了 | `node scripts/metrics-dashboard.mjs && open docs/metrics/dashboard.html` |
| 2 | **每天自动存一个点**，launchd 定时任务已装并实跑通过 | `launchctl print gui/$(id -u)/org.spoolapp.metrics.daily` → `last exit code = 0` |
| 3 | **反馈三条路全开**，一行 app 代码没动 | 官网 `#feedback` 一节；GitHub 的 issue 模板 |
| 4 | **表头八条修法做了七条** | `npx vitest run src/lib/pack` → 72 passed |
| 5 | 补了一个**表头双份一致性测试**（以前没有，两边会悄悄漂） | 同上，`byte-identical` 那条 |

**起点数字：2026-08-20，累计 32 次下载**（macOS 27 / Windows 5，最老的 v0.3.0 占 19）。
这是第一个快照，在这之前没有历史。

> ⚠️⚠️ **2026-08-20 Ocean 更正：这 32 次全部是他自己测试时下载的。**
>
> **真实外部下载 = 0。** 这条更正推翻了好几个基于「32」推出来的结论
> （包括我原来那条「macOS 占 84%，所以先在 macOS 收钱」的建议，已作废，见 §6.1.2）。
>
> **看板本身没白做**，但它现在的用途是**纯前瞻的**：
> **32 是噪声底，不是起点。** 从今天起**超出他自测的那部分**才是信号。
> 在那之前，任何拿这些数字算比例、算平台分布、算「老用户有多少」的推理**都是在算他自己**。

### 0.1 三条实测结论（都改变了做法，不是描述做完的东西）

1. **`~/Desktop` 是 TCC 保护目录，launchd 任务碰一下就永久卡死。**
   三个探针实测：脚本放 Desktop 里 → 卡死；`WorkingDirectory` 设在 Desktop → 卡死；
   两样都挪出去 → 退出码 0。**没有报错、没有日志**，`launchctl` 只显示 `state = running`。
   这个仓库正好在 Desktop 下，所以数据存到了 `~/Library/Application Support/spool-metrics/`。
   全稿在 `docs/METRICS.md` §4。
2. **`gh` 在定时任务里同样卡死** —— token 在钥匙串里，后台任务等一个没人能点的授权框。
   改成不带鉴权直接打公开 API（发布信息本来就公开）。**别「优化」回 `gh`。**
3. **「四个数相加会双算」这个担心不成立。** 一个人只会点四个链接里的一个，
   相加算的是链接点击次数，不是人头。你定的口径是对的。

---

## 1. 已经做完的四件事，细节留档

### 1.1 下载量看板

你定的四条口径，逐条落地：

| 你的回答 | 怎么实现的 |
|---|---|
| 「看板给谁看？**我自己看**」 | 本地一个 HTML 文件，不部署、不上线。没有任何东西挂到公网 |
| 「想要**每天新增多少**，这个窗口就做」 | 每天 09:30 自动存一个点；柱状图画的就是每天新增 |
| 「口径**四个数相加**」 | 所有发布的所有资产全部相加 = 32（⚠️ 全是自测，见 §0） |
| 「反馈**三个都做，不动 app**」 | 见 §1.2 |

**漏一天不会丢数据**：GitHub 那个计数是累计的，下次读数把这段并进来，
柱子标成 `+7 / 2d` 说明它盖了几天。笔记本合盖过夜也会在唤醒后补跑。

⚠️ **看板回答不了「有多少人在用」**。它数的是下载动作：同一个人换台机器再下算两次，
下了不装也算一次。想知道真实使用量要另外的东西，**而那件事会撞零出网红线**（见 §7）。

### 1.2 反馈三条路 —— 全在官网长出来的，app 一行没动

按你的话「扩反馈渠道优先从这里长，别从 app 里长」，
也守住 `email-collection-website-only`（2026-07-31）那条决策：

| 路 | 在哪 | 需要账号吗 |
|---|---|---|
| **邮件** | 官网 `#feedback` 直接给邮箱；privacy 页原来那个也还在 | 不需要 |
| **官网表单** | `#feedback` 一节，三个字段（发生了什么 / 版本系统 / 回信邮箱） | 不需要 |
| **GitHub Issue** | 两个模板：「Something is broken」「It should do something it does not」 | 需要 |

**表单没有引入任何第三方。** 它是一个 `mailto:` 组装器——按钮把你填的内容拼成一封写好的信，
打开你自己的邮件应用，发不发由你决定。这是唯一一种「静态站上能收表单、又不让第三方
收集访客」的做法。页面上原话就是这么写的：「这个页面没有服务器，什么都不收集。」

> ⚠️ **要不要换成真正的托管表单，是你的决定，不是我的。**
> 真表单（Formspree / Tally / 腾讯问卷之类）能让没有邮件客户端的人也提交，
> 但那要引第三方，`email-collection-website-only` 那条写着**引第三方依赖需要你单独批准**。
> 现在这版不需要你批就能用。要换的话见 §2.5。

### 1.3 顺带做的：发布订阅

官网那行还加了「盯着发布页」的链接（GitHub 的 Watch → Releases）。
**这是零成本的「订阅新版本」**：不需要邮件列表、不需要第三方、不需要你维护。
真要做邮箱订阅列表得引第三方，同 §2.5。

### 1.4 表头：八条修法做了七条

`Spool-header-analysis-2026-8-19.md` 那八条，**七条是纯文字修改、零行为改动，已经全做了**，
两份拷贝（`src/lib/pack/templates.ts` 和 `src-tauri/src/mcp.rs`）同步改的：

| # | 原来的毛病 | 改成了什么 |
|---|---|---|
| 1 | **两条规则相乘出错**：Reference 是不可反驳的真相 + Personal 要直接挑错 ⇒ 八个月前的官网页面会让 AI 主动、不留情面地告诉你你错了，**而你是对的** | Reference 的优先**只在同等时效下成立**；较晚的块说它变了，就把冲突和两个日期一起摆出来，不许静默选边，也不许光凭旧 Reference 说用户错了 |
| 2 | Synthesis 和 Process 的判据缝里，正好掉着**最常见的那种块**（AI 的一段长回答，没标题也不是对话）。判错一边信息保留率差一个量级 | 加了默认值：AI 来源、不明显是对话轨迹的，**按 Synthesis 处理** |
| 3 | **有注解层没有任务层** —— 粘进去不打字直接回车，不同模型猜得不一样：有的总结全项目，有的审计你的笔记，有的干等。**字节是确定的，行为不是** | 末尾加了一节 `## What This Is`：这是上下文不是任务；还没提问就给一份简短的再入场简报（现在在哪、什么还开着、最近变了什么），然后停下等 |
| 4 | `unsafe` 是过度声明，会让模型整体变谨慎、加免责、绕圈子 | 换成「会产生自信的错误答案」 |
| 5 | Personal 只被用来挑错，**没被用来省事** —— 你已经写对的它还要再讲一遍，正好抵消 pack 想省的十分钟 | 加了：你已经正确写下的内容，不要再解释一遍 |
| 6 | `↩ replaces` 写的「It is history: do not use it」太绝，连「这条路你已经排除过」都不说了 | 改成：不作为现行事实使用，但**可以说你考虑过并排除了** —— 此路不通本身有价值 |
| 7 | `[... ...]` 只是被动披露被裁掉了什么，没要求做任何事 | 加了：被裁掉的部分若可能关系到你问的事，**先说出来再回答** |

**第八条没做**（要改渲染器，见 §2.6）。

⚠️ **压缩提示词也同步改了**：`mcp.rs` 里中英两版压缩规则原本列着「哪些整节要一字不改照抄」，
新加的 `## What This Is` 不在那张单子上会被压掉，已经补进去。

### 1.5 补的那个测试

表头在 TS 和 Rust 里**各写了一遍**，以前**没有任何东西保证两份一致**。
两边各自编译得过、各自的测试也绿，唯一症状是「app 里复制的 pack」和「MCP 读到的 pack」
悄悄开始不一样——这正是这个项目最怕的那类沉默故障。
现在 `assemble.test.ts` 里有一条 `byte-identical`，实测能抓到漂移（故意改坏 Rust 那份，它红了）。

---

## 2. 现在就能接着做、不需要你拍板的

按「先补漏斗、再开渠道」排（渠道修好了只会把更多人送进同一个漏斗）。

### 2.1 ✅ Windows 0.6.1 装机验证 —— **2026-08-20 你已验过，通过。发布阻塞解除**

~~0.6.0 你验过、通过。**0.6.1 的包已经发布，但没有任何人在 Windows 上装过。**~~

**2026-08-20：Ocean 报告 0.6.1 在 Windows 上装机验证完成，没问题。**
程序原文留在 `docs/WINDOWS-CHECK.md`，下次发版照旧走一遍。

> 🎉 **这意味着 §9 那条「唯一的发布阻塞项」没有了。**
> 现在挡在渠道前面的只剩 §2.2 / §2.3 / §2.4 三件补漏斗的，
> 而那三件**都不需要你拍板**。

### 2.2 修首次运行的信任冲突

一个主张「永不发出网络请求」的软件，在第一分钟弹**键盘监听**权限请求。
**改的不是架构，是顺序**：零出网这句话要说在权限弹窗**之前**，而不是等用户点进设置页才看到。

### 2.3 载入示例项目

那七张截图来自一个隔离的演示库，**那个库已经在硬盘上了**（`scripts/seed-demo-library.sh`）。
做成「载入示例项目」，让新用户第一个动作就是在一个真实项目上按 ⌘⇧P，30 秒内看到 pack 长什么样。
**这是唯一一个不用积累就能展示积累的办法**，素材现成。

### 2.4 零安装试用

官网直接挂一份真实的 pack `.md`：「复制这段，粘进你的 AI，看它能不能立刻接住这个项目。」
pack 就是文本，**别人做在线 demo 要写后端，这里只需要挂一个文件**。

### 2.5 把 provenance graph 浮上来 —— 产品线里优先级最高

`cites` / `supersedes` / `corrects` **已经在数据里**，却几乎没有被呈现出来——
最强的资产目前只活在 pack 的静态表头里。v0.6.1 的纠正折叠已经开了个头。
推到「这个结论踩在哪几块上、其中哪一块已经被纠正过」，MCP 加一个只读工具暴露引用关系。

⚠️ 加 MCP 工具的两条硬规矩（都栽过）：
新工具**必须同时出现在 `OPENERS` 或 `INSTRUCTION_BODY` 里**，否则第三方客户端永远不会调用它；
**必须声明 `annotations`**，少一个在本地毫无症状，但 codex 上那个工具直接调不动。

### 2.6 ✅ 表头第八条 —— 你说没看懂，这里重写一遍（2026-08-20 已批：做）

原文写得太术语了，是我的问题。用真的 pack 字节讲一遍。

**现在 pack 里，一个块长这样**（这是真的，出自 golden fixture）：

```
#5 [2025-06-15 15:06] 我的想法:先复现 baseline 再谈改进,别一上来就魔改
#6 [2025-06-15 15:16 · from Claude · MCP] 结论:除以 sqrt(d_k) 是为了控制点积方差
```

看出 #5 和 #6 的区别了吗？**#5 没有 `· from XXX` 那一截，#6 有。**

表头开头那一大段（「How to Read This Context」）教了 AI 一条规则：
**没有来源的 = 💭 Personal = 用户自己敲的，信号最强。**

所以 AI 读到 #5 的时候，要做这么一件事：

> 「唔，#5 这一行……我找找 `from`……没有 `from`……
>   我回想一下三千字之前那个表头是怎么说的来着……哦，没有来源的算 Personal。」

**这就是第八条要干掉的东西。** 三个毛病：

1. **它靠「找不到东西」来判断。** 「有 `from Claude`」是看见了才算数，
   「没有 from」是**看了一圈没看见**才算数。后者天然比前者弱。
2. **它靠记住表头。** 读到第 80 个块的时候，三千字之前那段规则的注意力已经掉下去了。
3. **它是唯一一个还要靠记忆的判定。** —— 这条最关键，见下。

**第八条要做的事，一句话：把 💭 直接印在那一行上。**

```
💭 #5 [2025-06-15 15:06] 我的想法:先复现 baseline 再谈改进,别一上来就魔改
   #6 [2025-06-15 15:16 · from Claude · MCP] 结论:除以 sqrt(d_k) 是为了控制点积方差
```

就这么多。**AI 不用回想表头了，也不用靠「没找到 from」推理了，字就写在眼前。**

**为什么只做两带、不做四带？**

四个带里，判定方式不一样：

| 带 | 怎么判的 | 渲染器能不能自己打？ |
|---|---|---|
| 💭 Personal | 没有 `source` 字段 | ✅ **能**，纯查字段 |
| 💭 Personal | 是 `note:` 那一行 | ✅ **能**，纯查字段 |
| 📖 Reference | 来源是不是机构/邮件/附件 | ❌ 要看内容，会判错 |
| 🧩 Synthesis / 🔄 Process | AI 来源，再看**内容形状**像文章还是像对话 | ❌ 要读内容，只有 AI 判得了 |

**下面两行是纯查字段，上面两行要读内容。** 能确定的那两行就别让 AI 猜——
四带里正好有两带是白送的。

**这条为什么值得做（真正的理由）：**

pack 里已经有一堆**印在行内**的记号：`📌` 置顶、`⚠️` 后面被纠正过、`↩` 引用了谁。
这些记号有个共同点：**它们出现在需要它的那个块上，所以就算表头已经被忘了，它们照样生效。**

而四带分类是**唯一一个**不这样、只写在表头里的东西。
也就是说，**它是整份 pack 里最先失效的那部分**。第八条把其中一半挪到行内，
让它和 `📌`、`⚠️` 一样抗遗忘。

⚠️ **动手时的两个坑**（这条改的是 pack 的输出字节，用户看得见）：

1. 要同时改 `src/lib/pack/templates.ts` 和 `src-tauri/src/mcp.rs`，
   还要更新全部 golden。§1.5 那条 `byte-identical` 测试会替你抓到只改一边。
2. ⛔ **两个渲染器在「文件可读性」上是故意不一致的**（§7 那条）。
   改行内标记的时候顺手「把两边对齐一下」，就会把 `mcp.rs` 里
   **没有** `|| include_in_pack` 那个洞重新开出来。**只改带标记，别碰文件那一段。**

📌 **和 §6.4.1 的联动**：压缩稿一旦能进 pack，读的人更需要一眼分清
「这是用户自己写的」和「这是 AI 压出来的」。所以第八条落地时，
**顺手给压缩稿也留一个行内记号**（比如 `🗜`），两件事一次做完，只动一遍渲染器和 golden。

### 2.7 ~~五件~~ 三件工程欠账（08-20：第 4、5 两件你已验完，划掉）

| # | 什么 | 备注 |
|---|---|---|
| 1 | 环境卫生：`codex-marketplace` 和 `Spool-old-040.app` 还没删 | 命令在 `HANDOFF-2026-08-19.md` §1.1，**桌面上其余几样照旧文档删会删错，以那张表为准** |
| 2 | 多显示器下的 `frontmost_window_owner_pid()` | 缺硬件，插上第二块屏才能验 |
| 3 | `register_undo_shortcut()` 那一步 | 优先级最低，失败它自己会打日志，不会是静默的 |
| ~~4~~ | ✅ **v0.6.1 那三样改动，2026-08-20 你已用真手指验过，通过** | 四步全过（`HANDOFF-2026-08-19.md` §1.4）。⌘Q 完全退出再开这条也照做了 |
| ~~5~~ | ✅ **Windows 0.6.1 装机，2026-08-20 已验，通过** | 见 §2.1 |

---

## 3. 渠道计划（漏斗补完再开）

**核心认知转换：现在找到 Spool 的不一定是人，可能是 Agent。**

| 优先级 | 渠道 | 做什么 | 受众 | 成本 |
|---|---|---|---|---|
| 1 | **官方 MCP Registry** | 用 `mcp-publisher` CLI 提交 | 已装 MCP 客户端的人——**不需要被说服「上下文丢失」这个问题存在** | 一次提交 |
| 2 | **skills.sh / `npx skills`** | 见下 | 70+ 种 Agent 的用户 | 一个仓库 |
| 3 | **Show HN** | 本地优先 / 零出网 / 确定性，HN 正中靶心 | 英文开发者 | **一次性机会，放在 §2 全部做完之后** |
| 4 | **V2EX → 少数派 → 知乎长文** | 见 §3.2 | 中文开发者、效率工具人群 | 时间 |
| 5 | **小红书 / 留学申请社群** | 以「申请者本人」的身份写工作流 | 跑多条申请工作流的学生 | 时间 |

**19 个 MCP 工具 + 7 个 prompt 在整个生态里是少见的完整度，却不在任何目录上。**

### 3.1 skills.sh 的打法要变

**不要发一个「Spool skill」**——没装软件的人拿它没用。
**发那套四带权威格式本身**：任何人装上就能立刻用来组织自己的上下文，
而它天然说明「这个格式 Spool 会自动生成、并且在每条写入路径上强制执行」。

> **送格式，卖生成器。** 护城河是**写入路径上的强制执行**，不是那段表头文字；
> 表头本来就印在每一份 pack 上，送出去不亏一分钱。

### 3.2 最好的宣传素材不是产品介绍，是测量记录

已经写在 `docs/CASE_STUDY_LEDGER.md` 里、**零新增工作量、换个包装就能发**：

- 免费 AI 额度实测比设计假设**少 75 倍**，且随后整个免费层被关掉
- ~~一个真实项目的 pack 里 **95% 体积来自人粘贴，AI 写入只占 5%**，块大小相差 11 倍~~
  ⛔ **2026-08-20 Ocean 作废**：那是**测试初期**的数据，和真实使用完全不一样，**不许再对外用**。
  见 `PROMO-VERDICT-2026-08-20.md` §3.1
- 外部 AI 第一次通过 MCP 写入，自己造了 **11 个块**、走了一条没人教过的工具链、两次出错都自我恢复
- **43 个失败连同修复**；「活着的数据库被清空」那一次
- **新增候选（今天测的）**：`~/Desktop` 的 TCC 陷阱——定时任务卡死、无报错无日志，三个探针定位

> **人们不会因为你说产品好而下载它。他们会因为读到一篇「我把免费 AI 额度测了一遍，
> 发现它比所有人以为的少 75 倍」而记住你是谁，然后才顺手点那个下载链接。**

### 3.3 小红书这条别漏

你就是目标用户本人——一个同时跑多条申请工作流的研究生申请者。
这是唯一一个**受众密度高 + 有第一手可信度 + 现有工具完全不在场**的渠道。
「当前版本更适合找工作找学校」不是短板，是唯一一个你能自己站台的市场。

⚠️ **文案铁律：大白话**（你说过「你写的我没看懂」）。
⚠️ **截图铁律：多场景，且必须是真实界面**（2026-08-11：「不要渲染，渲染的图示和真实不一样」）。

### 3.4 🆕 素材库 + 渲染图规矩（2026-08-20 你批的两件）

#### 3.4.1 渲染效果图：解禁，但只解禁一格

**你的原话**：「这条过了，可以加渲染图……真实产品截图会放在它后面做事实依据。」

**所以规矩改成这样，一格一格写清楚，免得下次含糊：**

| 场合 | 能不能用渲染图 |
|---|---|
| 宣传海报、宣传视频、社媒配图 | ✅ **能** —— 这是你反转的那一格 |
| **紧跟其后的事实依据** | ⛔ **必须是真截图**，你自己指定的 |
| 官网的功能说明区 | ⛔ 真截图（那里是在说明产品**怎么用**，不是吸引注意） |
| 文档 / README / 案例账本 | ⛔ 真截图 |
| 应用内的任何地方 | ⛔ 真截图 |

**分界线是一句话**：**渲染图负责「让人停下来看」，真截图负责「让人相信」。**
海报可以好看，但**海报后面那一屏必须是真的**，而且要能被人对着 app 一一核对上。
这条不是自我限制，是 §3.2 那套打法的前提——
一个靠「测量记录」建立信任的产品，被抓到一次图是假的，前面攒的全归零。

#### 3.4.2 素材库：把已经付过代价的东西收起来

**你的原话**：「需要把这些类似测量记录，开发记录，比较能够抓人眼球的素材收集起来。」

**这些东西 100% 已经写完了**，在 `docs/CASE_STUDY_LEDGER.md` 里
（43 条失败+修复、2 节测量记录）。**零新增工作量，缺的只是「挑出来、翻译成人话」。**

**按「能不能抓住不认识你的人」排序，前七条：**

| # | 素材 | 一句话钩子（这就是标题） | 出处 | 平台 |
|---|---|---|---|---|
| 1 | 免费 AI 额度实测 | **「官方说 1500 次/天，我实测 20 次。差 75 倍。」** | 账本 §2.5 | Reddit / V2EX / 知乎 |
| ~~2~~ | ~~谁在撑爆你的上下文~~ | ⛔ **已作废，不许发** —— 测试初期数据，见 `PROMO-VERDICT-2026-08-20.md` §3.1 | ~~账本 §2.1~~ | — |
| 3 | 活库被清空那天 | **「我把自己的数据库清空了。这是完整的事故报告。」** | 账本 §3.1 | Reddit（HN 也吃这套） |
| 4 | 外部 AI 第一次写入 | **「我没教它怎么用。它自己造了 11 个块，错了两次，自己爬起来了。」** | 账本 §2.3 | Reddit / 小红书 |
| 5 | 🆕 桌面文件夹陷阱 | **「我的定时任务永远卡死。没有报错，没有日志。原因是它碰了 ~/Desktop。」** | `METRICS.md` §4 | Reddit（技术向最爱） |
| 6 | 一个字段让两个工具消失 | **「工具写好了、测试绿了、本地能调。换个客户端，它根本不存在。」** | 账本 §3.9 | Reddit |
| 7 | 43 次失败 | **「一个人做了 12 周，这是全部 43 次搞砸的记录。」** | 账本 §3 全节 | 知乎长文 / 少数派 |

**三条写的时候的规矩**：

1. ⚠️ **大白话。** 上面每一条钩子都不带术语，这是标准，不是示例。
   写完念一遍，有一个词你自己要停顿，就换掉。
2. ⚠️ **数字必须能被读者自己复算。** 账本里每条测量都附了重算命令，
   **把命令一起发出去** —— 这是这套素材和普通营销文的唯一区别，别省掉。
3. ⛔ **不许在素材里新造数字。** 账本 §3.13/§3.14 记着三次「凭空造数字」的事故，
   宣传稿是最容易再犯一次的地方。**没量过就别写，写了就附命令。**

⚠️ **一条必须先做的事**：素材 #4 要发的话，
账本原文写着**「Publishing requires redaction」**——那条记录里的项目名和块内容是真材料。
**发之前必须先脱敏。** 别忘了这句。

#### 3.4.3 ⛔ ~~已经做出来的物料~~ → **2026-08-20 当天全部作废，宣发暂停**

**Ocean 当天看完就整批否掉了，10 张海报 + 2 段视频一张都不发。**
**宣发工作就地暂停，和收费同一档：留档，不排期，不动手。**

| | |
|---|---|
| 定论全文（原话七条 + 硬约束 + 还没定的设计决策） | **`docs/PROMO-VERDICT-2026-08-20.md`** |
| 作废的物料挪到了 | `docs/archive/promo-2026-08-20-discarded/` |

**一句话诊断**：**观众搞错了。** 这批把「43 次失败」「95% 粘贴」「缓存命中 30×」这些
**开发过程**做成了海报 —— 那是 §3.2 给**开发者渠道长文**准备的料，
**不是 app 的门面**。海报是给**用户**看的，讲「它能为你做什么」。

⛔ **下次重开工前必读那份定论的 §3（三条硬约束）和 §4（三件设计稿）**，别再从头吵一遍。


---

## 4. 定位：不做工作版（已定，别再翻）

**结论：不做「工作版」，做「和办公软件并排装的项目记忆层」。**

一句话定位：**给 AI Agent 用的项目记忆层，装在你公司的办公软件旁边**——而不是「一款办公软件」。

**2026-08-18 企业微信 5.0.10 对所有企业开放 CLI 与 MCP**（11 类办公能力，取消人数门槛）。
这件事的三层含义：

1. **「给企业微信做中转」这个位置，腾讯自己占了。** 何况那条路本来就走不通——
   2022-06 起新建自建应用必须配可信 IP 白名单，动态 IP 上的本地桌面应用拿不到入场券。
2. **但腾讯放出来的是「动作能力」，不是「跨天记忆」。** wecom-cli 能读文档、建日程、拉纪要，
   它不回答「这个项目三周前为什么否掉了方案 A」。
3. **那一层是空的，而 Spool 正好是那个形状。**

### 4.1 一个应该前置的定位论据

**零出网在中国企业环境里不是包袱，是最强的过审论据。**
很多公司明令禁止内部数据外传，Spool 是唯一一个「装了也不会往外发一个字节」的东西。
配合「一次性买断 + 离线签名许可证」，恰好是采购最容易签字的组合。
反倒是工作版——要账号、要同步、要上传——最难过审。

> ⚠️ 这条论据与 §6 的 API 引擎有直接张力。选了形态 C，这句话要精确化，见 §6.3。

### 4.1.5 🆕 你问的：企业微信开放了 CLI 和 MCP，Spool 是不是能接进去？

**能。而且要做的工程量是零——今天就能用。**
但「让企业微信读取 Spool」这个说法要掰一下，因为方向反了，而**方向反了这件事恰好是好消息**。

**先看这两样东西各是什么角色：**

| | 是什么角色 | 谁主动 |
|---|---|---|
| **Spool** | MCP **服务端**（`spool --mcp`，等着被连） | 被动 |
| **企业微信** | MCP **服务端** + 一个 CLI（`@wecom/cli`） | 被动 |

**两个都是「被连的一方」，所以它们俩永远不会互相说话。**
「企业微信读取 Spool」这件事，字面上不会发生——企业微信不会主动伸手过来。

**真正发生的是这个：**

```
          你的 AI 客户端（Codex / DeepSeek Harness / Kimi Work…）
                        ↙                    ↘
              连 Spool（MCP）          连企业微信（MCP 或 CLI）
                   ↓                            ↓
            读你的项目记忆              读文档/纪要/日程/待办
                        ↘                    ↙
                    AI 在中间把两边搬来搬去
```

**搬运工是 AI，不是 Spool，也不是企业微信。**
所以「企业微信写进 Spool」的真实形态是：
**你对 Codex 说一句「把刚才那个会议纪要归到 Spool 的 X 项目里」，它就做了。**

**这为什么是好消息 —— 它绕开了 §4.2 第 1 条那个死结。**

§4.2 说读日程/云盘走不通，理由是**「自建应用必须配可信 IP 白名单，动态 IP 上的桌面应用拿不到入场券」**。
那条判断针对的是「**Spool 自己去做一个企业微信应用**」。

**而现在这条路上，Spool 根本不需要成为企业微信应用。**
凭证在 `wecom-cli` 手里（`wecom-cli auth init` 扫码登录，是用户自己的身份），
Spool 从头到尾不知道企业微信的存在。**没有应用，就没有可信 IP 这回事。**

> **一句话：那个死结锁的是「Spool 连企业微信」这条路，
> 而现在走的是「AI 同时连两边」这条路，锁根本不在这条路上。**

**要做什么？—— 一件事，而且是最便宜的那种：写字。**

Spool 的 routing text（`OPENERS` / `INSTRUCTION_BODY`）里加几句，让 AI 知道
「读完企业微信的东西之后，该归到哪个项目里去」。**一行代码逻辑都不用改。**
这正是 §4.2 第 7 条已经裁决过的做法（「降级为 routing text」），
只是当时没意识到它同时也解决了第 1 条和第 2 条。

⚠️ **两个必须提醒的现实：**

1. **企业里得管理员点头。** 企业微信那侧有「人员与 AI 操作权限独立配置、
   关键动作人工审批、AI 授权会过期、全程审计」。这是**好事**（正是 §4.1 说的过审论据），
   但意味着**你自己一个人试不出真实企业场景**——你没有一个真企业。
2. ⛔ **别把它做成一个功能去宣传。** 现在能说的是「Spool 和企业微信可以在同一个 AI 里并用」，
   **不能说「Spool 支持企业微信」**——后者暗示有对接、有维护、有保证，
   而实际上中间那一段完全不由你控制，企业微信改一次 API 你就变成撒谎的人。

📌 **一个顺带捡到的东西，比上面整段都值钱**：
企业微信自己的分发方式是 **`npx skills add WecomTeam/wecom-unified -y -g`**
—— **就是 §3.1 里那条 skills.sh 渠道。**
腾讯在用这条渠道发东西，说明这条渠道是活的、有人走的。
**§3.1 那个「发四带格式当 skill」的计划，因此从「猜测」变成了「有先例」。**

### 4.2 七条办公软件接口的裁决

| # | 原设想 | 裁决 | 理由 |
|---|---|---|---|
| 1 | 日程/待办承接 | ⚠️ **08-20 修正：改为「不由 Spool 做，由 AI 做」** | 原裁决（可信 IP + 只能读自己建的日历）**只对「Spool 当企业微信应用」成立**。走 §4.1.5 那条路，凭证在 `wecom-cli` 手里，这两个限制都不适用。**Spool 侧要做的仍是零**：routing text |
| 2 | 公司云盘 | **不做对接** | 已有附件抽取 + 每文件 `ai_access`。该做的是让 Agent 把云盘文件摘录存进 Spool——是 routing text 的事，不是新管道 |
| 3 | 智能文档承接 | **做** | 唯一零红线成本的一条。pack 已是纯字符串组装，加一层输出模板即可 |
| 4 | 公司 API 是主战场 | **做，见 §6** | 免费额度撑不起维护引擎（20 请求/天 vs 设计假设 1500，差 75×） |
| 5 | 分享共创 | **不做** | 要账号、服务器、同步，一次性反转零出网/无账号/MCP 唯一通道 |
| 6 | 蓝白色调专业办公 UI | **不做** | 收益不可验证，且与「经典版 = 发布版本身」冲突。办公场景认的是审计痕迹，这些已经有了，**缺的是用企业听得懂的话讲出来** |
| 7 | 语音会议纪要中转 | **降级为 routing text** ✅ 08-20 确认，并升为**这七条里唯一要动手的** | wecom-cli 已能读纪要。Spool 不需要碰语音，只需让 Agent 知道「纪要读完归档到哪个项目」。§4.1.5 说明这一条同时覆盖了第 1、2、7 条 |

⚠️ **一个必须分清的坑**：**DeepSeek Harness 不走引擎槽。** 它是本地部署、浏览器访问的 Web 应用，
原生 skills，MCP 需改配置启用，**目前不支持 CLI**。所以 dsh 接 Spool 走的是 **MCP 方向**
（它当 client 连 stdio server），不是 §6 的引擎槽。**搞混会白做一轮。**

### 4.3 明确不做

**Linux、移动端、E2EE 同步。** 在有第二个用户之前是纯投机；
而且 Linux 的捕获触发与焦点处理是**重写不是移植**。

### 4.4 先说瓶颈的真名

「不知道往哪强化」不是想象力不够，是**手上没有回答这个问题所需的信息**。
功能过滤器现在正常工作，它告诉你的是：**没有东西可以再为「你这一个用户」建了**。
一个用户被深度服务十三周，是合格的 v1 方法、不合格的 v2 方法。

**所以宣传不是第二件事，是解锁第一件事的前提。**
顺序搞反，就会在零外部信号下继续凭直觉加功能。
**§1.1 那个看板，是第一个外部信号源** —— ⚠️ **但它现在读数是 0**：
那 32 次是 Ocean 自测的（§0）。**也就是说 §4.4 这一节说的「手上没有信息」不是修辞，是字面意义上的零。**
这让 §9 的顺序更硬了：**先补漏斗、再开渠道，因为在有第一个真实用户之前，任何功能判断都没有输入。**

---

## 5. 按自己测出来的数据调头

~~一个真实项目的 pack 里，**95% 的体积来自人粘贴文档，AI 写入只占 5%，且块大小小 11 倍**。~~

> ⛔ **2026-08-20 Ocean 作废了这个数字**：**测试初期测的，和真实使用完全不一样，不许对外发布。**
>
> ⚠️ **但下面这两个推论仍然成立** —— 它们当时**已经改过做法并且做完了**（去重降级、更正折叠上线，
> 而且上线之后被实际使用验证过）。**作废的是那个可以引用的数字，不是那次调头本身。**
> 要再拿这条当依据、或者要对外说，**必须重新测一遍当前真库**。

**两个推论**：

1. 「AI 写入会淹没库」**已被证伪** —— 该**扩大** AI 写入面，而不是收缩。
2. 真正的容量敌人是**大段粘贴**，所以抽取、范围选择、folder pack 才是打在实处的。

**红线成本：零。**

---

## 6. ~~⏸ 等你拍板才能动的~~ → **2026-08-20 全部拍完，一条不剩**

> 按规矩，红线只能由你**明确**反转。**08-20 你把这一节清空了**：
>
> | 节 | 结果 |
> |---|---|
> | §6.1 收费 | ⏸ **定调「不急，功能齐全再说」** → 降级为留档，**不进 §9** |
> | §6.2 AI 压缩形态 | ✅ **选形态 C**，同时明确反转两条红线 |
> | §6.3 对外说法 | ✅ 跟着 C 改，写法已给 |
> | §6.4 其余四条 | ✅ 四条全部有裁决（压缩进 pack / skill / 表单选问卷星 / 表头第八条） |
>
> **§9 那张表上没有任何一项还在等你。**

### 6.1 ✅ 收费 —— 2026-08-20 定调：**不急，前置工作先做完**

> ## 一句话结论
>
> **收费不是下一窗的事，也不是下下窗的事。**
> 你的原话：「**收费不急，先把前置工作做好，功能齐全再说**」，
> 以及「**等 app 的收费功能做强了再说**」。
>
> **所以这一节从「待决」降级为「留档」** —— 下面记的是已经定的方向和已经查清的做法，
> **不进 §9 的开工顺序**。等到功能齐全那天，直接翻这一节，不用重新想一遍。

#### 6.1.1 已经定下来的六条

| # | 问题 | 你的决定 |
|---|---|---|
| 1 | 什么时候收 | **不急。功能齐全、付费功能做强之后再说** |
| 2 | 卖什么 | **整个 app**；到时候分**免费版 + 付费版** |
| 3 | Win / Mac 分开还是统一 | **统一**。不接受「先只在 macOS 收钱」 |
| 4 | Windows 没签名会不会被当骗子 | **现在先不考虑这个问题**（见 §6.1.3 的时序提醒） |
| 5 | 源码继续公开吗 | ⛔ **不行。收费之后必须限制** —— 公开等于可以直接下载自己编译 |
| 6 | 给朋友的兑换码 | **全免费**（永久、全功能） |

⚠️ **我原来那条「先只在 macOS 收钱，因为 macOS 占 84%」的建议已作废**，
理由是 §0 那条更正：**32 次下载全是你自测的，那个 84% 算的是你自己。**
拿自测数据推平台策略是彻头彻尾的错误，这条建议从根上就不成立。

#### 6.1.2 「源码公开 + 收费」这条你否掉了 —— 那要做的事和时机

你说得对：**仓库现在是 PUBLIC，任何人 `git clone` 完自己 `cargo build` 就有一份完整的软件。**
再严的许可证也拦不住这件事——法律上不许，技术上毫无障碍。

**要做的动作只有一个：收费前把仓库转私有。** 但**时机很讲究**，有两件事会被它砸到：

| 会被砸到的 | 怎么回事 | 怎么办 |
|---|---|---|
| **§3.2 的素材库** | 你最好的宣传素材是 `CASE_STUDY_LEDGER.md`（43 次失败）和 `METRICS.md`。仓库一私有，这些**全部从公网消失**，而它们正是建立信任的东西 | **把要公开的那几份文档单独挪到一个公开仓库**，或直接发到官网。**转私有之前先挪** |
| **§3 全部渠道** | MCP Registry、skills.sh、Show HN 都会指向 GitHub。链接会 404 | 转私有**必须排在渠道开完、且收费真要上线的时候**，不能提前 |

> 📌 **所以顺序是死的：先开渠道 → 再做付费功能 → 挪走公开文档 → 最后转私有。**
> **反过来做会把已经攒到的曝光全部作废。** 这条写在这里就是防止将来某天顺手把仓库转私有。

#### 6.1.3 Windows 签名：你说先不考虑，我记一条时序提醒就不再提

你选了「Win / Mac 统一收费」+「先不考虑用户担心」。**照办，这一节不再问。**

但有一条**纯时序**的提醒，不是重新征求意见：

> ⚠️ **「统一收费」+「Windows 不签名」这两条同时成立的那一天，
> 就是一个收费软件在 Windows 上弹「无法识别的应用」的那一天。**
>
> 现在不构成问题，因为**收费还早**（§6.1.1 第 1 条）。
> 但它会在**真要上线收费的前一个月**变成必须处理的事，而那时候：
> - Azure Artifact Signing 是 **$9.99/月**，但**个人身份目前只对美国/加拿大开放**（2026-08-20 复核）；
> - 走 EV 证书是 **$400+/年**，还要等硬件 U 盾寄送。
>
> **两条都不是当天能办完的。** 所以这条提醒的全部作用是：
> **别等到要上线那一周才发现签名办不下来。** 到那时再决定就行，现在不用动。

#### 6.1.4 防不住分享 —— 你问有没有现成方法，有，三种

**先说清楚：没有任何方法能在「不联网」的前提下真正防住分享。** 这是数学问题，不是工程问题。
所以下面三种都是**提高分享成本**，不是**杜绝分享**。按「对老实用户的伤害」从小到大排：

**① 人名水印（Sublime Text 就是这么干的）** ⭐ 推荐

许可证里写死买家的名字/邮箱，app 里**始终显示一行「已授权给 XXX」**。
Sublime Text 的条款原文就是这个路子：**按人授权、可以装在自己的多台机器上、不需要激活服务器、
但转卖或散布会被吊销**。

- **对老实用户的伤害：0。** 他什么都不用做。
- **防的是什么**：把带自己真名的许可证发到网上，这件事本身就有社交成本。
- **工程量**：几乎为零，签证的时候多写一个字段，界面上多印一行。

**② 吊销名单随版本下发** ⭐ 推荐，和 ① 配对

**每个新版本的二进制里，编进一份已吊销的许可证编号清单。**
泄露的那张证在用户升级到下一版之前一直能用，升级之后失效。

- **完全离线**：不需要联网查，名单是构建时烤进去的。
- **对老实用户的伤害：0。**
- **工程量**：一个常量数组 + 校验时比对一下。
- ⚠️ **代价**：吊销要等下一次发版才生效。对你这个量级完全够用。

**③ 机器绑定 + 离线激活（node-locked，行业标准做法）** ⚠️ 暂不推荐

app 显示一串机器指纹（CPU/主板/磁盘序列号的哈希）→ 用户贴到你网站上 →
拿回一张**只对那台机器有效**的激活文件 → 之后运行时全程离线。
Keygen / LicenseSpring / Cryptolens 都提供这套「离线 / 气隙激活」。

- **防分享效果最好**：钥匙和机器绑死，发给朋友也用不了。
- ⛔ **但代价全部落在老实用户身上**：换电脑、重装系统、换硬盘 → 每一次都要重新激活，
  而重新激活**要你本人在回路里**。
- **在你有几百个付费用户之前，这套东西制造的客服量比它挡住的盗版还多。**

> **建议：①+② 一起做，跳过 ③。**
> 加起来大概一天的工作量，**对老实用户零摩擦**，而且 ③ 随时可以后加 ——
> 它是在同一张许可证上多绑一个字段，不是推翻重来。

#### 6.1.5 技术形态（查清了，留档，现在不做）

一张离线签名许可证，**同一个格式同时表达订阅和买断**：

```
买给谁    : ocean@example.com     ← ①的人名水印就印这个
类型      : perpetual / subscription
有效期至  : (空) 或 2027-08-20     ← 填了就是订阅
版本上限  : 0.x 或 (空)            ← 填了就是买断到某版本
许可证编号: SP-0001                ← ②的吊销名单比对这个
签名      : <你的私钥签的一串>
```

app 只做两件事，**全在本机，零网络请求**：验签名（内置公钥）→ 看日期/版本/吊销名单。

**订阅和买断的差别只是上面填哪一栏。** 一套代码，两种填法。
**扣款发生在你网站上，不在 app 里** —— 所以「本体零出网」这条红线**从头到尾不用动**。

⭐ **兑换码不需要做系统**：你决定「全免费」，那就是签一张两栏都留空的证发给对方。
十个朋友以内，手签比任何发码系统都快。

#### 6.1.6 那条许可证红线，答案仍然是「不用动」

**加 MIT 会让你收不了费** —— MIT 明确允许任何人复制、修改、再发布、**甚至拿去卖**。
收费需要的是**销售条款 / EULA**，而那是**官网上一个页面**，不是仓库里一个文件。

**现在「没有 LICENSE」= 保留全部权利 = 收费最有利的状态。**
✅ `no-license-file` 这条红线**不用动**，我也不会去加。

### 6.2 ✅ AI 压缩重启 + 国产 API 接入 —— **2026-08-20 已拍板：形态 C**

> **决定（Ocean，2026-08-20，明说）：**
> ① **选形态 C**（随 Spool 分发的 sidecar 子进程）。
> ② **明确反转**「No API key is stored, entered or needed」这一条红线。
> ③ 「MCP 是唯一 AI 通道、内置 AI 全部剔除」（2026-07-09）**同时被明确反转**，
>    改成：**MCP 是唯一的「别人家 AI 进来」的通道；Spool 自己出去调 API 走 sidecar，且默认关闭。**
>
> 两条反转都是**主动、单独作出的**，不是实现压缩时的副作用——这正是 §6.2 原本要求的形式。
> 下面的三形态对照表保留作存档，说明为什么不是 A 也不是 B。

**还没被反转、依然生效的**（别顺手带走）：

- **主进程零出网仍然成立**，而且是**架构性的**：CSP 一个字不改，出网只发生在 sidecar 进程里。
- **形态 B 并行保留**：已经装了 Qwen Code / claude / codex 的人，走引擎槽，**一分钱不花、一个 key 不填**。
  形态 C 是给「不想装 CLI 的人」的第二条路，不是替代品。
- §4.1 那条过审论据**不作废，但要精确化**，改法见 §6.3。默认关闭是这条论据能活下来的前提。

三种形态，**选定之前不动代码**（已选 C，存档）：

| 形态 | 得到 | 代价 | 判断 |
|---|---|---|---|
| **A** 主进程直接调 API | 体验最顺，一个设置项就能用 | CSP 要开口子；「永不发出网络请求」从**架构性质**降级为**意图承诺**；网站/README/案例研究全要改；§4.1 那条最强的过审论据直接作废 | **不建议**，代价远大于收益 |
| **B** 引擎槽加一个 OpenAI 兼容 CLI | **零红线成本** | 用户要先自己装一个 CLI，转化差；DeepSeek 官方目前没有 CLI | **并行保留**。Qwen Code 支持 MCP/Skills/子智能体，凭证它自己管，与 claude/codex/gemini 完全同构，**不需要反转任何红线** |
| **C** 随 Spool 分发的 sidecar 子进程 | 出网发生在子进程里，**主进程 CSP 不动**；detection / streaming / cost parsing / sandboxing 全部复用 | **只需反转一条**：「不存储、不输入、不需要 API key」 | ⭐ **推荐** |

~~**要你明确点头的两件**~~ —— **两件都已点头，见本节开头的决定框。**

**无论选哪种，五条设计约束都得守**（第 1 条已按 §6.4 改写，其余四条原样生效）：

1. ~~压缩产物是额外输出，永不替换确定性 pack。~~
   **→ 已改**：压缩产物**可以**进 pack，但只能经「你核对 → 存成新块」这条路进去。
   详见 §6.4 #1。**「原版始终一键可达」这半句仍然是硬的**，一个字不让。
2. **压缩产物必须自报家门**（`DeepSeek · API` 之类），客户端不可设置。
3. **压缩必须说出自己压掉了什么。** 「每一次删减都在原地说明」是这个产品最好的性质之一，
   **而 AI 压缩天然会静默摘要——这是重启这个功能最容易破坏的东西。**
   顶部必须写明原始块数、压缩后块数、被合并/省略了哪些。
4. **失败必须可见。** 超时、余额不足、限流都要在界面上说出来，不能退回未压缩版而不告诉用户。
   （「唯一症状是沉默的失败」是这个项目最怕的一类 bug。）
5. **默认关闭。** 与 MCP 一样，第二个独立开关。

**实施顺序**：先做 `compress_pack` **一个动作**验证形态，不要一次上齐周回顾/项目总结；
跑通后再迁其余；最后往案例账本追一行「压缩成本实测 vs 估算」——
**这个项目的传统是用测量推翻假设，别在这里破例。**

#### 为什么现在做是对的（成本模型）

**DeepSeek 新价目（2026-08-17 生效），元/百万 tokens，缓存命中 / 未命中 / 输出：**

| 模型 | 空闲时段 | 高峰时段 |
|---|---|---|
| V4-Flash | 0.05 / 1.5 / 4.5 | 0.10 / 3 / 9 |
| V4-Pro | 0.15 / 4.5 / 13.5 | 0.30 / 9 / 27 |

高峰 = 北京时间 9:00–12:00、14:00–18:00，闲时是高峰的一半。V4 支持百万级上下文，
**同时兼容 OpenAI 与 Anthropic 两种调用格式**（接入成本因此很低）。

一次满额 pack 压缩（上限 50,000 字符 ≈ 30,000 tokens 输入 + 2,000 输出）：
V4-Flash 高峰未命中 ≈ **0.11 元**，闲时 ≈ **0.055 元**。
对照实测的「一次实时 web 运行 ≈ $0.45（约 3.2 元）」，**差约 30 倍**。

**两个别人做不了的功能**：

- **确定性 × 缓存。** 缓存命中的输入价是未命中的 **1/30**（不是 1/50，见下面那条更正），
  而 **pack 是确定性的**——同一项目同一天 pack 出同样的字节，
  **前缀天然稳定，表头和旧块几乎必然命中缓存**。
  确定性在这里直接换成了钱。**竞品做不到，因为他们的 pack 每次都不一样。**
- **错峰运行。** 高峰时段正好覆盖上班时间，周回顾/项目总结本来就可批处理，
  加一个「闲时运行」开关直接省一半。**别人的任务不可延迟，所以也做不了。**

#### ⚠️ 更正：是 30 倍，不是 50 倍 —— 这条准备上宣传物料，必须是对的

2026-08-20 回查 DeepSeek 官方价目，**命中/未命中的比值全表都是整齐的 30 倍**，四个格子无一例外：

| | 命中 | 未命中 | 比值 |
|---|---|---|---|
| Flash 高峰 | 0.10 | 3 | **30×** |
| Flash 闲时 | 0.05 | 1.5 | **30×** |
| Pro 高峰 | 0.30 | 9 | **30×** |
| Pro 闲时 | 0.15 | 4.5 | **30×** |

**而且 30 倍只属于「输入」这一项，输出 token 不打折。** 按整次调用算，账是这样的
（30,000 输入 + 2,000 输出，V4-Flash）：

| 情形 | 算式 | 一次多少钱 |
|---|---|---|
| 高峰、全未命中（最贵） | 30000×3 + 2000×9 | **0.108 元** |
| 高峰、九成命中（真实重复压缩） | 27000×0.1 + 3000×3 + 2000×9 | **0.030 元** |
| 闲时、九成命中（开了闲时开关） | 减半 | **0.015 元** |
| 闲时、全命中（理论下限） | 30000×0.05 + 2000×4.5 | **0.0105 元** |

**对外能说的真话是这三句**，都不用打折扣：

1. **一次压缩一到三分钱。**（0.011–0.030 元，量纲对得上，谁都能自己复算）
2. **重复压缩同一个项目，最贵到最省差 10 倍**（0.108 → 0.0105）。
3. **省下来的那部分，来源是「pack 每次都吐同样的字节」**——这句才是护城河，
   前面两个数字只是它的后果。

⚠️ **别再写「50 倍」。** 这个项目的招牌是「用实测推翻假设」，
在一张海报上把厂商价目表写错，是对这块招牌伤害最大的一种错法。
真实的 30 倍已经足够好看，不需要加价。

⚠️ **「几乎必然命中」也要收一收。** 你会去重复压缩，通常正因为**加了新块**；
新的那一截必然未命中。命中的是**表头 + 旧块**，也就是上表「九成命中」那一行——
它已经是真实场景，不需要吹成全命中。

### 6.3 选了形态 C 之后，对外说法要同步改

从「永不联网」精确化为：

> **Spool 主进程不发出任何网络请求。当你打开 API 引擎时，请求由 Spool 启动的本地子进程发出，
> 用你自己的 key 和额度，内容到达你选择的那家模型厂商。**

**不要因为不好讲就不讲** —— 官网那一节「deliberately not claimed」的存在本身就是可信度来源。

### 6.4 ✅ 其余四条 —— 2026-08-20 全部拍完

| # | 事情 | 裁决 |
|---|---|---|
| 1 | 压缩产物是否进入 pack 默认路径 | ✅ **进**，但只能走「核对后存成新块」这条路。全文见 §6.4.1 |
| 2 | 四带权威格式作为独立 skill 公开发布 | ✅ **做**。见 §3.1 |
| 3 | 官网表单换成托管表单（引第三方） | ✅ **做，选定问卷星**。见 §6.4.2 |
| 4 | 表头第八条（行内带标记） | ✅ **做**。原文没讲清楚，重写在 §2.6 |

#### 6.4.1 压缩进 pack：你说得对，我原来那条建议是错的

**你的原话**：「如果压缩不进 pack，那它节省上下文就没有意义了。」

这句是对的，我原来那条「建议否」站不住：压缩的**唯一目的**就是让 pack 变小，
产物不进 pack，就等于花钱买了一份没人会读的副本。**采纳你的。**

**但「核对无误后直接替换原文本」这个动作，要换一种做法实现**，理由只有一条：

> **这个库里唯一不可再生的东西，是你当初粘进去的那段原文。**
> 模型可以再跑一次，网页可以再抓一次，唯独「2026 年 3 月你从招生页上复制的那一段」，
> 删了就没了。压缩必然丢信息——这是它的定义，不是它的 bug。
> 用一个必然有损的产物去覆盖一个不可再生的原件，只要错一次就找不回来。

**做法：把它接到你已经有的那套机制上，一行新概念都不用发明。**

库里本来就有 `supersedes` / `corrects` / `cites`（§2.5 说它们「已经在数据里」）。
压缩就是 `supersedes` 的一个新用例：

```
你点「压缩」
  → sidecar 跑，回来一份压缩稿
  → 界面并排放：左边原文，右边压缩稿，中间标出压掉了哪些句子
  → 你逐条核对（可以改字，可以整段驳回）
  → 你点「用这一份」
  → 压缩稿存成一个新块，标 supersedes 原块
  → 原块留在库里，从 pack 默认路径里退下来，pack 里印一行
     「↩ 这里原本是 #7，压缩后长这样，原文一键可取」
```

**这样四件事同时成立**，一件都不用牺牲：

| 你要的 | 怎么拿到 |
|---|---|
| pack 真的变小 | 默认路径里走的是压缩稿，体积立刻降下来 |
| 你核对过才生效 | 不点「用这一份」，库里什么都不变 |
| 原文没丢 | 原块还在，`supersedes` 指着它，一键展开 |
| **pack 还是确定性的** | 关键在这 —— 见下面那段 |

⚠️ **「pack 还是确定性的」这一条别当成细节，它是钱。**

区别在于**压缩发生在哪一步**：

- ❌ **压缩在 pack 的热路径上**（每次生成 pack 时现调 AI）：
  同一个项目每次吐的字节都不一样 → **前缀缓存永远命中不了** →
  §6.2 那个 30 倍、那一到三分钱，**全部作废**。而且你会失去「pack 可复现」这个卖点。
- ✅ **压缩在写入路径上**（你核对后存成块，pack 照旧纯字符串组装）：
  pack 仍然是「给定块集合 → 唯一确定的字节」。**AI 不在热路径上，缓存照常命中。**

**换句话说：你要的「压缩进 pack」，和我要的「pack 不许有 AI」，本来就不冲突——
冲突的是「压缩在读的时候发生」还是「在写的时候发生」。放在写入路径上，两边全要到。**

**你提的提示词调教也接在这里**：压缩比例做成一个显式档位
（比如「只删重复」/「保留结论和数字」/「压到最短」），
默认最保守那档。**这比在提示词里写「请少删一点」可靠得多**——
后者是求模型自觉，前者是给它一个能被核对的目标。
每一档都要在压缩稿顶部写清：原始 N 块 → 压缩后 M 块，删掉了哪几类东西。

**第一步动哪个文件**：`compress_pack` 一个动作先跑通形态 C，
产物先只做「并排核对」界面，**暂不接 `supersedes` 写入**——
先确认压缩质量你认可，再开写入那一段。质量不认可的话，后面那半段就不用做了。

#### 6.4.2 ✅ 官网表单：**选定问卷星（wjx.cn）**

你给的三条标准：**能在中国大陆收集 · 免费 · 使用量最多**。按这三条查下来只有一个答案。

| | 问卷星 | 金数据 | 腾讯问卷 |
|---|---|---|---|
| 大陆可用 | ✅ 本土 | ✅ 本土 | ✅ 本土 |
| **使用量** | ⭐ **市占率第一**，深耕近 20 年 | 第三方 | 第三方 |
| **免费额度** | ⭐ **永久免费，单份问卷 10 万份回收，每天可建 10 份** | 每月 1000 份 | 单问卷 1000 份 |
| 免费版有没有套路 | ⭐ **无时间限制** | 有额度墙 | 有额度墙 |

**问卷星在你这三条上全部第一，不用纠结。** 免费额度大到对你现在的量级（真实外部访问 = 0）
完全构不成约束——**这一条实际上不是选型因素，是「随便哪家都够」。**
真正决定的是**使用量最多**这一条，而问卷星在这条上没有对手。

**⚠️ 落地时的三件事，一件都不能漏：**

1. ⛔ **官网那句话必须跟着改。** 现在 `#feedback` 那一节的原话是
   「**这个页面没有服务器，什么都不收集**」——嵌了问卷星之后**这句话就变成假话了**。
   改成「表单由问卷星处理，你填的内容会到他们那里」这种直白的说法。
   **这个产品的信任全靠「说的和做的一致」，为了少改一句话把它变成谎言，代价远超收益。**
2. ⚠️ **privacy 页同步改**，别只改表单不改说明。
3. ⭐ **`mailto:` 那条路保留，两条并存。** 问卷星挂了、被限流了、外国用户打不开，
   用户还有一条路能找到你。反馈本来就已经是三条路并存（§1.2），**这不是过度设计，是一贯做法**。

**⚠️ 英文站单独判断一次。** 问卷星是中文界面、国内服务器，
Reddit / HN 过来的人打开它会慢、会看不懂。
**建议：中文站用问卷星，英文站保持 `mailto:` + GitHub Issue 不动。**
这一条我按建议做，你觉得不对再说。

---

## 7. 🚧 红线速查 —— 都是你自己定的，不许顺手推翻

> ### 🔄 2026-08-20：两条被明确反转
>
> 两条都是 Ocean **主动、单独、明说**反转的，符合「只能由你明说推翻」这条规矩：
>
> | 反转的 | 原来 | 现在 | 全文 |
> |---|---|---|---|
> | **1** | MCP 是唯一 AI 通道，内置 AI 全部剔除（07-09） | MCP 仍是唯一「进来」的通道；**出去**走 sidecar，默认关闭 | §6.2 |
> | **2** | 不要渲染效果图，要真实装机截图（08-11） | **宣传物料**可以用渲染图，真截图跟在后面作依据；**产品内和文档仍只许真截图** | §3.4 |
>
> ⚠️ **第 2 条当天晚些又出了状况**：解禁之后做出来的第一批物料**被他整批否掉了**（§3.4.3）。
> **「可以用渲染图」这条解禁本身没有被收回** —— 被否的是**内容做给了错的观众**。
> 宣发**已暂停**，重开工前先读 `PROMO-VERDICT-2026-08-20.md`。
>
> **顺带明确「没有被反转」的**（这几条最容易被顺手带走）：
>
> - ✅ **主进程零出网仍然成立，而且仍然是架构性的**：CSP 一个字不改。
>   出网只发生在 sidecar 里，**且默认关闭**。
> - ✅ **Windows 不签名**、✅ **不要 LICENSE 文件**、✅ **不上 Mac App Store** —— 三条全部原样。
>   08-20 问到的三个问题，答案都是「不用动这条红线」。
> - ✅ **文案大白话**、✅ **Spool 不许挤掉别的工具**、✅ **两个渲染器故意不一致** —— 原样。


| 红线 | 定的时间 | 对上面这些意味着什么 |
|---|---|---|
| **不上 Mac App Store**，走 Developer ID 公证直发 | 2026-07-06 | §6.1 用不了 App Store 的订阅/兑换码基建 |
| **本体零出网**；出网只发生在用户自己的 CLI 子进程里 | 2026-07-09 | §6.2 三种形态里只有 C 不破坏它 |
| ~~**MCP 是唯一 AI 通道**，内置 AI 全部剔除~~ **🔄 2026-08-20 你明确反转** | 2026-07-09 → **08-20 改写** | **新版本**：MCP 是唯一「别人家 AI 进来」的通道；**Spool 自己出去调 API，走 sidecar 子进程，默认关闭**。见 §6.2 |
| **邮箱收集只放官网**，app 内零出网叙事不动 | 2026-07-31 | §1.2 已遵守；反馈**优先从官网长**。⚠️ 08-20 批了托管表单（§6.4.2），那是**官网第一次引第三方**——app 侧不受影响 |
| **不要 LICENSE 文件**（问过三次都是否） ✅ **仍然成立，且不用动** | 反复 | 08-20 已答：见 §6.1.6。**加 MIT 反而会让你收不了费**；「没有 LICENSE」本来就是收费最有利的状态。要加的是**官网一页销售条款**，不是仓库里一个文件 |
| **Windows 不签名**，「这条不要再问」 ✅ **仍然成立** | 2026-08-15 | 08-20 你自己提了，也自己收了：**收费要 Win/Mac 统一，且先不考虑用户担心**。⚠️ 我原来那条「先只在 macOS 收钱（占 84%）」**已作废** —— 那 84% 算的是你自测的 32 次。§6.1.3 只留一条时序提醒，**不再问** |
| **文案要大白话** | 反复 | §3 全部适用 |
| ~~**不要渲染效果图，要真实装机截图**~~ **🔄 2026-08-20 你明确反转（只对宣传物料）** | 2026-08-11 → **08-20 收窄** | **新版本**：宣传海报/视频**可以**用渲染图；**但真实截图必须跟在后面作事实依据**。⛔ **产品内、官网功能说明、文档里仍然只许真截图** —— 反转的只是「宣传物料」这一格 |
| **Spool 不许挤掉别的工具**：不许让用户关插件，也不许给用户发明记号 | 2026-08-11 | 规矩加在 AI 身上，不加在用户身上 |
| 文件可读**只认 `ai_access`**，`mcp.rs` 里没有 `\|\| include_in_pack`，**别「补回去」** | 2026-08-19 | §2.6 动渲染器时最容易误伤这条 |
| 两个 pack 渲染器**故意不一致**（golden 对的是渲染函数，分歧在喂进去的那行数据） | 2026-08-19 | 「对齐」它们就会把 AI 那侧的洞重新开出来 |
| 🆕 **收费之后源码不许继续公开** —— 公开等于可以直接下载自己编译 | **2026-08-20** | ⛔ 但**转私有的时机是死的**：先开渠道 → 再做付费功能 → **把要公开的文档挪走** → 最后转私有。提前转会把攒到的曝光全部作废。见 §6.1.2 |
| 🆕 **收费要 Win / Mac 统一**，不接受只在一个平台收 | **2026-08-20** | §6.1.1 第 3 条。⚠️ 这条让 Windows 签名从「可以不管」变成「上线前必须处理」，时序提醒在 §6.1.3 |
| 🆕 **看板上那 32 次下载全是自测，真实外部用户 = 0** | **2026-08-20** | ⛔ **别拿这批数字算平台分布、算比例、算老用户数** —— 那都是在算 Ocean 自己。见 §0 |
| 🆕 ⛔ **「95% 用户粘贴 / 5% AI 写入」作废，任何对外场合都不许再用** | **2026-08-20（当天晚些）** | **测试初期的数据，和真实使用完全不一样。** 海报/视频/社媒/官网/README 全禁；内部文档留档但要标「已作废」。⚠️ **它现在还挂在官网 story 页上**（中英各一处），处理办法见 `PROMO-VERDICT-2026-08-20.md` §5 |
| 🆕 ⏸ **宣发物料暂停，和收费同一档** | **2026-08-20（当天晚些）** | 8-20 那批海报和视频**整批舍弃**。**海报是给用户看的门面，不是开发日志** —— 观众分界线和三条硬约束写在 `PROMO-VERDICT-2026-08-20.md`。⛔ **重开工要他明说**，别顺手往下做 |

> 工程层面的红线（高亮正则、选区偏移映射、更正折叠、休息 5 分钟写死、经典版 = 发布版、
> `--mcp` 子进程一个都不要杀、构建日志重定向……）全稿仍在 `HANDOFF-2026-08-19.md` §2，
> **动手前扫一眼**，那一节没有被这份取代。

---

## 8. 事实基准

> 外部事实核实于 **2026-08-19**；本机实测事实为 **2026-08-20**。过期后只更新这一节。

| 事实 | 出处 |
|---|---|
| 企业微信 5.0.10 开放 CLI 与 MCP，11 类办公能力，取消人数/资质门槛 | IT之家 2026-08-18；`work.weixin.qq.com/nl/index/aicli` |
| `wecom-cli` 覆盖消息/邮件/文档/表格/待办/日程/会议/微盘/通讯录，可读会议纪要与转写原文 | `github.com/WecomTeam/wecom-cli` |
| >1000 人企业长连接接入成员默认为空，需管理员开通 | 企业微信管理后台文档 |
| 企业微信「获取日历下的日程列表」仅可获取应用自己创建的日历下的日程 | 企业微信开发者中心 |
| 2022-06-20 起新建自建应用必须配置可信 IP | 企业微信「开发前必读」 |
| DeepSeek 新价目 2026-08-17 生效；峰谷 9:00–12:00 / 14:00–18:00；兼容 OpenAI 与 Anthropic 格式 | DeepSeek 开放平台 2026-08-13 公告 |
| **缓存命中 : 未命中 = 1 : 30**（四个价格格子全部一致），且**只作用于输入，输出不打折** | 2026-08-20 回查 DeepSeek 官方价目页。⚠️ 更正了旧稿里的「50 倍」 |
| **企业微信同时开放 CLI 与 MCP**，支持 DeepSeek Harness / Codex / Kimi Work / MiniMax Code 等；分发走 `npx skills add WecomTeam/wecom-unified` | 2026-08-20 复核；`work.weixin.qq.com/nl/index/aicli` |
| `wecom-cli` 凭证走 `auth init` 扫码登录（用户自己的身份），**不是自建应用**，因此不受可信 IP 限制 | 2026-08-20 复核 `WecomTeam/wecom-cli` README |
| **Azure Artifact Signing（原 Trusted Signing）$9.99/月起，个人身份目前只对美国/加拿大开放** | 2026-08-20 复核 Microsoft 官方定价与资格页 |
| **v0.6.1 的 Windows 装机 + 三样改动的真手指验证，Ocean 已完成，通过** | 2026-08-20 Ocean 本人报告 |
| DeepSeek Harness：本地部署浏览器应用，原生 skills，MCP 需改配置，**暂不支持 CLI** | dsh 实测报告 2026-08-14 |
| Qwen Code 支持 MCP / Skills / 子智能体，可用百炼 Token Plan API Key | 阿里云帮助文档 |
| 官方 MCP Registry 提交用 `mcp-publisher`；面向子注册表程序化消费 | `modelcontextprotocol/registry` |
| skills CLI 支持 70+ Agent；`gh skill publish`（GitHub CLI 2.90+，public preview） | Vercel changelog |
| ~~累计下载 32 次~~ → **32 次全部是 Ocean 自测；真实外部下载 = 0** | 2026-08-20 Ocean 本人更正。数字本身仍在 `docs/metrics/downloads.csv`，但**只能当噪声底用** |
| **`~/Desktop` 的 TCC 会让 launchd 任务永久卡死**，无报错无日志 | 本机三探针实测 2026-08-20，`docs/METRICS.md` §4 |
| **`gh` 的 token 在钥匙串里，后台任务访问会卡死** | 同上，`docs/METRICS.md` §5 |

---

## 9. ✅ 开工顺序 —— 2026-08-20 定稿，下一窗照着做

> **计划全部定完了。这一节是唯一要看的执行清单。**
> **没有任何一项还在等你拍板** —— §6.1 收费已降级为留档，不在这张表上。
>
> ⚠️ **一条贯穿全表的前提**：真实外部用户 = **0**（§0 那条更正）。
> 所以顺序的逻辑是 **「先让第一个真人能进来并且留下」**，而不是「先加功能」。
> **在有第一个真实用户之前，任何功能判断都没有输入。**

### 第一批：补漏斗（渠道开之前必须做完）

| # | 做什么 | 在哪一节 | 动哪个文件 | 完成的标志 |
|---|---|---|---|---|
| ✅ **1** | **修首次运行的信任冲突** | §2.2 | `PermissionBanner.tsx` + i18n | **2026-08-20 已做**：点「打开捕捉」先出一张卡说清楚（这权限用来干什么、打的字一个都不存、Spool 不联网），按「继续」才调系统弹窗。⚠️ 卡上三句话对着 `double_tap.rs` 核过，改文案要重核 |
| ✅ **2** | **载入示例项目** | §2.3 | 🆕 `src/lib/db/demoSeed.ts` + `client.ts` + 设置→通用 | **2026-08-20 已做**：新装机自动带一条 Machine learning course（四带齐全），已有库走设置里的「载入」。⚠️ 内容和 `seed-demo-library.sh` 逐块一致，改一处要改两处 |
| ✅ **3** | **零安装试用** | §2.4 | 🆕 `scripts/build-sample-pack.ts` → `site/assets/sample-pack{,.zh}.md` | **2026-08-20 已做并上线**：`spoolapp.org` 首页「Try it」下面，复制/查看两个按钮。⚠️ 文件是**渲染器生成的**，不是手写的；改 pack 格式之后要重跑那个脚本 |

**为什么这三件排最前**：渠道修好了只会把更多人送进同一个漏斗。
**这三件都不需要你拍板，且都不动核心逻辑。**

### 第二批：拿数据的那件事

| # | 做什么 | 在哪一节 | 注意 |
|---|---|---|---|
| **4** | **`compress_pack` 一个动作**跑通形态 C sidecar + **并排核对界面** | §6.2 / §6.4.1 | ⛔ **先不接 `supersedes` 写入**。先确认压缩质量你认可 |
| **5** | 跑通后往案例账本追一行**「压缩成本实测 vs 估算」** | §6.2 | 这个项目的传统是用实测推翻假设，**别在这里破例** |

> ⚠️ **第 4 步的先后不能颠倒**：质量不认可的话，§6.4.1 后半段（写入 + supersedes）
> **一行都不用写**。别一次做完再看效果。

### 第三批：一次动完渲染器

| # | 做什么 | 在哪一节 | 注意 |
|---|---|---|---|
| ✅ **6a** | **表头第八条**（`💭` 打进行内） | §2.6 | **2026-08-20 已做**。没等 `🗜` 一起做：`🗜` 要等第 4 步的压缩跑通，而第八条挡着第 3 步（官网那份 pack 要印对）。代价是 golden 改两遍，认了 |
| **6b** | 压缩稿的 **`🗜` 记号** | §6.4.1 | 跟着第 4 步走。⛔ 两个渲染器**故意不一致**，只改带标记，**别碰文件那一段** |

**合成一步做**，因为它们动的是同一批文件（`templates.ts` + `mcp.rs` + 全部 golden），
分两次做等于把 golden 改两遍。§1.5 那条 `byte-identical` 测试会替你抓只改一边。

### 第四批：官网

| # | 做什么 | 在哪一节 | 注意 |
|---|---|---|---|
| **7** | **换问卷星表单**（中文站），英文站保持 `mailto:` | §6.4.2 | ⛔ **必须同时改掉「什么都不收集」那句话**，否则它变成假话 |

### 第五批：开渠道（前面全做完才开）

| # | 渠道 | 在哪一节 | 注意 |
|---|---|---|---|
| **8** | **官方 MCP Registry**（`mcp-publisher`） | §3 | 最便宜的一条，受众不需要被说服「上下文丢失」这个问题存在 |
| **9** | **四带格式发成 skill**（skills.sh） | §3.1 | 送格式，卖生成器。腾讯自己就走这条路（§4.1.5），有先例 |
| ~~**10**~~ | ~~**素材库**~~ | — | ⏸ **2026-08-20 摘出这张表**：宣发暂停，见下方 |
| ~~**11**~~ | ~~**V2EX / 小红书**~~ | — | ⏸ 同上 |
| **12** | ⛔ **Show HN** | §3 | **一次性机会，放最后**。前面全部做完再开 |

### 排在这张表之外的

| 事情 | 为什么不在表上 |
|---|---|
| **收费 / 定价 / 许可证 / 仓库转私有** | §6.1：**不急，功能齐全再说**。到时候翻 §6.1 就行，不用重想 |
| ⏸ 🆕 **宣发物料（海报 / 视频 / 素材库 / 社媒）** | **2026-08-20 暂停，和收费同一档。** 第一批做出来的整批舍弃了 —— 观众做错了。定论、三条硬约束、三件还没定的设计稿全在 `PROMO-VERDICT-2026-08-20.md`。⛔ **要他明说才重开** |
| **Windows 代码签名** | 跟着收费走。§6.1.3 只留了一条时序提醒，**现在不用动** |
| **provenance graph 浮上来**（§2.5） | 产品线里优先级最高，但它是**给已有用户的深化**，而现在用户是 0。**排在第一个真实用户出现之后** |
| **多显示器 pid / `register_undo_shortcut`** | §2.7 剩下的两件，缺硬件 / 优先级最低 |
| **Linux、移动端、E2EE 同步** | §4.3 明确不做 |

> ⚠️ 留给下一窗一句：**§4 那类「未来往哪走」最容易写成一堆漂亮的空话。**
> 要的是「有野心」，不是「宏大」——每条想法都要能回答**「第一步动哪个文件」**。
