# 交接文档 — 2026-08-02 深夜(给下一个窗口)

> 先读 CLAUDE.md 与 memory(`next-stage-goals-website-portfolio`、`write-plainly-for-ocean`、
> `no-license-file`、`spool-db-wipe-incident`、`isolated-verify-workflow`、
> `distribution-route-notarized-dmg`、`mcp-first-pivot`、`ui-language-follows-system`)。
> 完成后删除本文件。

---

## 0. 一句话状态

**本地八个提交,一个都没推**(Ocean 明示「先不推送,等我审查」)。
基线全绿:`npx tsc -b` / `npx vitest run`(152)。真库全程没碰。

本窗做完:**官网按 Ocean 的四条批复改完了**,并且把「已签名公证」实测坐实了。
`webimproveadvice.txt` 里剩下的争议点已全部有结论(见 §2)。

**👉 下一窗按 §1 的顺序做。动手前先看 §3 —— 那里有两笔会拖后腿的欠账。**

---

## 1. 下一窗要做的三件事(按顺序)

1. **首启头 60 秒改造** —— `docs/DESIGN_FIRST_RUN.md`,**五个拍板点 Ocean 已全批,还没动手**。
   见 §4。这是现在唯一一件「已批准 + 未开工」的事,优先级最高。
2. **app 内术语统一 project** —— 本窗只做了用户看得见的文档层,app 内那一层还在。见 §5。
3. **中文版重新表达** —— 英文终版已经定了(本窗改完),这一轮可以开始了。见 §6。

顺序建议:1 和 2 都动代码,可以合并规划;3 是纯文案,随时可插。

---

## 2. `webimproveadvice.txt` 的争议点——**全部已结,不用再问 Ocean**

上一版交接列了 7 处冲突。Ocean 2026-08-02 已逐条批复,现记录最终结论:

| # | 原争议 | Ocean 的结论 | 状态 |
|---|---|---|---|
| 1 | 首页只写给 AI 重度用户/开发者? | **认可,但要加上学生和学术工作者**(上课记笔记、复习很好用)。找工作/签证这类**短期任务不是主攻对象**。全站框定在「目的是长期做一个任务」 | ✅ 已落地 |
| 2 | Hero 主 CTA | 换回下载(上一窗已做)。**内嵌 15 秒演示视频这半条仍开着——视频没录之前这一屏保持现状** | ⏸ 待录视频 |
| 3 | 删「第一个下午就能用上」 | **删**。理由是它和「重点应该放在吸引下载」抢位置 | ✅ 已删 |
| 4 | 删「没有东西要学」四条 | **删整段**。Ocean 原话:「简单易学应该留给 app 内部实现」 | ✅ 已删 |
| 5 | 「每周变强」并进新屏 | 真截图保留,并且现在它是全站唯一能证明「长期积累」的证据,分量比以前更重 | ✅ 保留 |
| 6 | 术语统一 project/thread | **统一成 project** | ⚠️ 文档层已做,**app 层没做**,见 §5 |
| 7 | license 措辞 | 不加 LICENSE(memory `no-license-file`),改措辞 | ✅ 已改成「公开的是构建记录;源码目前没有开放」 |

### 2.1 ✅「已签名公证」——实测过了,是真的,可以放心写

上一版交接说这条「在核实之前绝对不许写」。**本窗核实了,结论是可以写。**
把 Releases 上那份 dmg 完整拉下来(7,540,829 字节,与 GitHub API 报的一致)后:

```
xcrun stapler validate  → The validate action worked!
spctl -a -vv -t install → accepted
                          source=Notarized Developer ID
                          origin=Developer ID Application: Hanze JIN (Q5Y5JRXZ58)
```

dmg **和里面的 Spool.app 都钉了公证票**。README 第 15、42 行那两句「signed, notarized」
**不是假话,不用改**。官网现在也写了(hero chip + 信任块 + FAQ)。

> 上一窗测到 `/Applications/Spool.app` 是 `Unnotarized Developer ID`——那个是本地换装的
> 产物,和 Releases 里的发布件不是同一个东西,别被它误导。

### 2.2 ⚠️「macOS 12+」仍然是 advice 编的,**别写**

实测两个 bundle 的 `LSMinimumSystemVersion` 都是 **10.13**,`tauri.conf.json` 里
从来没设过最低版本。官网现在只写 **Apple Silicon**(这条是真的,dmg 只有 arm64)。
要写系统要求,得先真的测出下限,别把 12+ 当事实印上去。

### 2.3 其余事实性问题

- 「170 项自动化测试」是错的,实际 **152 vitest + 16 cargo**。官网没写数字,回避掉了。
- story 页那句「无任何用户数据受影响」**没写**——按 memory `spool-db-wipe-incident`,
  5/29 清空的是 Ocean 自己的真库。要写就写准确的,别为了好看说滑。
- 「grid-3 塞 4 张卡会掉行」不是 bug(`auto-fit` + `minmax`),本窗信任块正是 4 张卡,
  1440 下一行四张,窄屏自动堆叠,实拍验过。

---

## 3. ⚠️ 两笔会拖后腿的欠账(下一窗最好先解决)

### 3.1 🚨 截图和演示脚本讲的还是「找工作」——和新定位打架

这是本窗改动**自己造出来的**问题,必须说清楚:

- 首页现在白纸黑字写着「找工作这类短期事务不是主攻对象」,
- 但 **step 02 那张主截图、day1/week6 那对增长图、OG 分享卡、以及整个交互演示
  (`site/assets/demo.js` 的 EN+ZH 两套脚本)讲的都是 "Job search / 找工作"**。

严格说不算说谎(那个 job search 项目确实横跨六周、确实在积累),但**文案和图片在
互相拆台**,滚一遍就走的访客只会看到「嘴上说长期,图上全是找工作」。

**建议怎么修**(没做,因为要重建隔离构建 + 重写演示脚本,是独立一轮的工作量):
演示库里现成就有 `Machine learning course`(Study 工作区,2 blocks)和
`Portfolio site`——把主截图和增长对照换成「机器学习课」那条线,天然贴「上课/学一个
领域」这个新受众。脚本 `scripts/seed-growth-demo.sh day1|week6` 现在写死的是找工作
的内容,要改。隔离 app 还在 `~/Desktop/Spool-Demo/Spool.app`。

### 3.2 🚨 本地凭据文件待处置

本窗发现签名/公证用的那个本地凭据文件之前**没被 `.gitignore` 盖住**——
`git add -A` 一把梭就会把它提交进历史。

**已做**:加进 `.gitignore`(提交 809ff26)。已核实**它从未进过任何一次提交,
相关内容在全部 git 历史里也搜不到,目前是干净的**。

**Ocean 还需要决定**:硬规则 8 是「密钥永不落盘」,而文件本身仍在工作区。
建议撤销重发、改成只在环境变量里用(菜谱见 `docs/RELEASE.md`),
或至少挪出仓库目录。**这条需要他本人拍板,别自作主张删他的文件。**
细节不写在这份公开文档里,当面问他。

---

## 4. 首启改造:五个拍板点 Ocean 已全批(**未开工**)

方案与证据在 `docs/DESIGN_FIRST_RUN.md`(含实拍首启截图
`docs/screenshots/first-run-2026-08-02.png`)。**Ocean 2026-08-02 批复:四条全过,
并且「拍板点 5 和前四点一起加进本轮」。** 所以本轮要做全五条:

1. 首启开在教程脉络「Welcome to Spool」,不开在空的 Unsorted。
2. 空状态文案按授权状态分叉 —— **未授权时绝不叫用户去双击 ⌥ 从别的 app 捕捉**。
3. **把两个系统授权弹窗推迟到用户主动要开捕捉时**(要改 `src-tauri/src/double_tap.rs`
   的 `run_tap()`)。
4. 权限横幅改写成「还差一步 + 在那之前你能干什么」。
5. **第一条捕捉成功后的一次性收口**(原稿建议缓做,Ocean 要求本轮一起上)。

⚠️ 第 5 条做的时候盯住这三点:
- 它需要一个**一次性状态**(「这个用户已经见过收口了」)。落在 `settings.json`,
  **绝不要落进数据库、更不要进种子** —— 种子只在空库重建路径跑(5/29 红线)。
- 存量用户(包括 Ocean 自己的库)**不该**突然看到这句话。判据写成「首次捕捉且没见过」,
  而不是「库里只有一条块」。
- 测试要覆盖「见过之后不再出现」。

第 3 条是唯一动 Rust 的,**必须隔离构建实机回归**(全新 identifier,
memory `isolated-verify-workflow` §14:装了正式版之后合成事件测不了手势)。
验收标准逐条写在设计稿 §5,**不许只看代码**。

💡 顺路的事:**教程种子里的 MCP 说明还停在「一键接入 Claude Desktop / Cursor」,
实际支持六个**。首启第 1 条要把用户直接送进教程脉络,送进去他读到的就是这句过时的话。
Ocean 说这句可以「预留到以后和其他教程修订一起做」,但既然本轮要把人送进去,建议顺手改掉。

---

## 5. app 内术语统一(**文档层已完成,app 层未做**)

本窗做完的是「读站 → 看 README → 看隐私」这条路径:官网、README、隐私页、幕后页
全部改口 project(提交 7a36585)。

**还剩 app 内这一层**,规模实测:`src/` **78 行 / 21 个文件**,`src-tauri/` **115 行**。
它跨了两条硬规则,所以刻意单独留一轮:

- **硬规则 4「中文即键」**:app 的 i18n 是拿中文原文当 key 的
  (`t('新建脉络')` → `'New thread'`)。把「脉络」改成「项目」,等于要动**每一处
  `t()` 调用点**,不是改一个 map 就完事。
- **硬规则 5**:`src-tauri` 那 115 行里有 **pack 输出文案和 MCP 工具描述**。
  动了就必须 `GOLDEN_WRITE=1` 重生 golden,并与 `mcp.rs` 锁步。

**刻意不动的三样(重要,别顺手改了)**:
1. **MCP 工具名** `list_threads` / `create_thread` / `set_thread_summary` —— 这是对外
   接口契约,v0.3.0 已经有 10 次下载、有人配好在用,改名等于把别人跑通的配置弄坏。
2. 源码目录名 `src/components/ThreadView/`(纯内部,改它只是 churn)。
3. 「a spool viewed from above, its thread pulling free」—— 线轴上那根线的本义,
   不是产品术语。

---

## 6. 中文版重新表达(**英文终版已定,可以开始了**)

**Ocean 原话:「目前的网站中文版是英文直译,真正的中文用户阅读不习惯。
等网站内容终版确认后修改中文版的语言表达。」** 英文终版本窗已经定了。

- 本窗新写的中文(hero 品类句、这是给谁用的三张卡、信任块、权限说明、FAQ 八条、
  中段 CTA)是**直接用中文写的,不是从英文译的**,应该比旧文案好一截。但**仍在这一轮
  的范围内**,别默认「刚写的就不用改」。
- 旧的那些(那两分钟 / 怎么用三步 / 每周变强 / MCP 段 / 阵容段)译文腔更重,是重点。
- 判据(比「读着顺」可操作):念出来不像翻译腔;没有「它的」「们」「被」的堆叠;
  长定语拆短句;英文的破折号插入语在中文里改成独立句。

---

## 7. 环境与现状

### 7.1 隔离演示环境(留着没删)

- **隔离 app**:`~/Desktop/Spool-Demo/Spool.app`(identifier `com.oceanjin.spool.verify`)。
  **`tauri.conf.json` 的 identifier 已复位成 `com.oceanjin.spool`**,git 里干净 ——
  下次要重建隔离构建,记得改完**立刻**建、建完**立刻**改回来。
- **演示库脚本**:`scripts/seed-demo-library.sh`(8 个项目)、
  `scripts/seed-growth-demo.sh day1|week6`。两个都**只写 verify 数据目录**,真库不碰。
- 拍图的坑都在 memory `isolated-verify-workflow`;另外两个:
  **① 截图前把鼠标挪开**(悬停会把块的工具条勾出来);
  **② 每次重启后窗口位置会变,必须重新取 bounds**。

### 7.2 官网现在的骨架(本窗改后)

开头(含信任 chip)→ 那两分钟 → demo → **这是给谁用的(长期做一件事·三张卡)** →
怎么用三步 → **中段下载 CTA** → 它每周都在变强 → MCP + 客户端阵容 →
**你装的到底是什么(权限说明 + 签名公证 + 单文件 + 不追踪 + 一个人做的)** →
**FAQ 八条** → 标志 → 下载。

已删:「你需要学的东西:没有」「三个承诺」「应用本身——安静是刻意的」
(精华并进信任块;那张 app 主截图移到了「怎么用」第 02 步,替掉原来手搓的 mock,
是升级不是丢失)。

**advice 明确表扬、别在后续改版里弄丢的三样**:alt 文本质量、截图是真实界面不是
渲染稿、主动声明「截图用的是演示库,无个人内容」。

### 7.3 网页工程债(还没做的)

- **中文独立 URL**(`/zh/` + hreflang):是对的,但**牵动 Pages 部署结构**,单独一轮。
- 图片 `srcset`:本窗补了真实 `height`(CLS 已解决),但 2000+px 原图直出没换成多尺寸。
- 中文用户点「中文」再进 story 页会看到一句英文道歉——要么翻译,要么该页隐藏切换。

### 7.4 对外动作(全部需 Ocean 单独明示,一件都没做)

1. **MCP 官方注册表挂号**(<https://registry.modelcontextprotocol.io>)—— 投入产出比最高。
2. demo 链接单独短地址。
3. Show HN / Product Hunt —— 只有一次机会,等页面定稿之后(dmg 公证这条已确认,不再是卡点)。
4. ❌ 刷好评、假装用户安利:不做。

### 7.5 商标结论(动官网/README 提到客户端名字时必看)

2026-08-02 逐家查过官方页面:**六家没有一家可以直接把 logo 摆上我们官网**。
Visual Studio Code **明文禁止**用图标标识/推广自己的产品,且**禁止 `VS Code` 这类简写**;
Anthropic / OpenAI 要**事先书面批准**;Windsurf 要先问;Cursor 最宽松但也没明确许可。
**文字如实说「支持 Cursor」安全(指名性使用),贴 logo 不安全,把 logo 改成单色也不安全。**
完整来源清单在 `docs/DESIGN_MCP_ECOSYSTEM.md` §8,**会过期,下次动这块前重查**。

### 7.6 还欠的一笔小账

**app 设置页那处文案改动没有实机截图**(纯文案,tsc + 152 用例都过)。
下次任何一次隔离验证顺手看一眼折叠段即可。

---

## 8. 硬规则(违反即事故)

1. git/代码/文档**绝不出现 AI 署名**。提交后自检:
   `git log -1 --pretty=%B | grep -iE 'co-authored-by|🤖|generated with|noreply@'` 必须为空。
   (⚠️ **别 grep `claude` / `anthropic`** —— 第三方品牌名属于产品内容,必然误报。)
2. 绝不添加 LICENSE(Ocean 未定);新依赖需 Ocean 批准。
3. 真库动前备份;实机验证走隔离 identifier 流程;每次合成输入前重新定位窗口边界。
   ⚠️ `npm run tauri dev` 走真库路径,别为了看一眼文案就跑它。
4. i18n:**中文即键**;新 GUI 文案同步补 EN。**官网文案要大白话,中文是重写不是翻译**(§6)。
5. 改 `assemble.ts`/`templates.ts` 输出必须 GOLDEN_WRITE=1 重生 golden 并同步 mcp.rs;
   动 schema 必须迁移注册表 + 双侧锁步常量 + 真库备份。
6. 每任务独立提交;**设计类任务先出方案交 Ocean 批复再动手**。
7. 换装/清数据/迁移等破坏性操作前核对证据链,且需 Ocean 明示。
   **对外动作(发 Release、推公开站点、去第三方注册表挂号)同样需要明示。**
   ⚠️ 推 main 会触发 `pages.yml` 自动部署官网 —— **现在有八个提交待推,Ocean 说等他审完。**
8. **密钥永不落盘**:Apple 专用密码之类只当环境变量用,不写进任何文件、不进 git。
   ⚠️ 见 §3.2 —— `docs/ID.txt` 目前违反这条,已挡住 git,但文件还在。
9. ⚠️ 别用 `git add -A` 一把梭,提交前先 `git status --short` 看一眼。
