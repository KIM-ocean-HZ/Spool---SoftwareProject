# 交接文档 — 2026-08-10 深夜(给下一个窗口:**A5 代码写完了,先请他装机 + 看一眼**)

> 先读 CLAUDE.md 与 memory(`isolated-verify-workflow`、`next-stage-goals-website-portfolio`、
> `write-plainly-for-ocean`、`no-license-file`、`spool-db-wipe-incident`、
> `distribution-route-notarized-dmg`、`mcp-first-pivot`、`ui-language-follows-system`、
> `double-tap-exclusivity`、`capture-note-first`、`email-collection-website-only`、
> `follow-up-decision`、`claude-code-effort-unavailable`、`chatgpt-mcp-forensics`、
> `mcp-tool-annotations-required`、`mcp-tool-routing-required`、`gemini-free-tier-closed`)。
> 完成后删除本文件。
> ⚠️ **日期标签的一处漂移,别再传下去。**
> 文中所有 **08-11 / 08-12 / 08-13** 都是上几窗自己写的标签,**真实落库日期是 08-08 或 08-09**。
> **08-10 决定仍然不逐条回改** —— 逐条改要靠猜,把「已知错、但顶上有警告」换成
> 「可能错、而且没警告」更糟。
> ✅ **要解码就用 schema 版本号锚定**(每次 schema 变更只对应一个提交,查得准):
> `git log --format='%h %ad %s' --date=short | grep 'schema v'`。
> 已核对的两个:**v15 / v17 都在 `7e6ee68`,日期是 2026-08-09**(文中标成「08-12 / 08-13」)。
> **08-10 以后的日期都是真的**(`644fba3` / `63f2824` / `e5163b8` / `33d9802` 全部 2026-08-10)。
> ⭐⭐ **A5(首日价值二期)的代码 08-10 深夜写完了** —— 全稿 `DESIGN_FIRST_DAY_VALUE.md`,
> 落地记录在该稿 **§6**(含三处和稿子不一样的地方)。**⏳ 还没提交、还没装机,两件都要他点头。**
> ⚠️ **改写这份交接时,§4 的长期计划清单必须原样带上** —— 08-02 那次改写把 MCP 新增接口和
> Windows 版整段弄丢了,Ocean 08-03 才发现。
> ⚠️ **⭐ 失败与修复不要只写在这里。** 这份文件规定要删,而它是 case-study 最值钱的素材来源。
> **新出的每一条,顺手往 `CASE_STUDY_LEDGER.md` §3 追加一条** ——那份台账只增不改
> (规矩在 `DESIGN_CASE_STUDY` §6.3)。

---

## 0. ⭐⭐⭐ 下一窗开工:**A5 已写完,先装机再往下走**

**A5(首日价值二期)四件全做完了**,全稿 `DESIGN_FIRST_DAY_VALUE.md`,**落地记录在该稿 §6**。
四条基线全绿:`tsc` 干净 / **vitest 332** / `cargo` 68 / i18n `(none missing)`。

✅ **08-10 22:45 已提交(`87b6ab7`)并装机**,Ocean 当窗点的头。四条旁证全过(§0.5)。
⚠️⚠️ **没推送** —— 他选的是「提交 + 装机」,**没选推**。`origin/main` 仍在 `eda795d`,
**现在有 1 个未推提交**。推之前照样要单独问他。

⏳ **只剩一件:请他看那张卡长得对不对** —— 线轴在左边栏最上面,
**空轴/绕线/满轴三种样子只有他能判**。量具只证明了那一小块 SVG(九档逐档看过),
**证明不了装进那一屏之后挤不挤**(§6.2-sexies 的边界)。

**A5 之后按 §0.1 往下走:A1/A2 截图 + 视频 → A0 官网整页 → A3 case-study 四五六期。**
⚠️ **拍截图之前先装 A5** —— 它改的就是左边栏那一屏(§5.1 最后一条已经改过了)。

---

**08-10 晚一共四窗。** 前三窗:推送到 `eda795d`、首日价值一期装机、
⭐ 更正那一摊的界面返工 + schema v21、圆圈徽章返工三轮、A5 开稿并问完四个拍板。
**本窗(第四窗)只做了一件:把 A5 写出来**(§1-quinquies)。

1. ⭐ **A5 四件全落地**:捕捉数(分出 MCP)/ 个人字数 / 线轴小组件 / 总线轴数;
   一期那三条正文预览按稿子撤掉了,`TodayCard.tsx` 删掉换成 `SpoolCard.tsx`。
2. ⭐ **两处「同一条规矩写了两遍」的坑,当场躲掉了一个、被测试抓到一个** —— 台账 §3.25。
3. ✅ **提交 `87b6ab7` + 装机(22:45)都做了**,⚠️ **但没推送**(他没选推那一项)。
4. ✅ **顺手清了 15 份旧 app**(他点头:只清 app,备份一份都别动)—— 留了最新那一份
   `Spool.app.previous-20260810-224509`(它就是 A5 之前那一版,回退用),腾出 ~261MB。

⭐⭐ **app 代码到此仍然全清**,而且 **08-10 晚 Ocean 重新定了顺序:A5 首日价值二期 →
截图 + 视频 → 官网整页重做 → case-study 四五六期。**
⚠️⚠️ **上一版交接写的「先做 case-study」是错的,别再照抄** —— 见 §0.1 那条核查。

| 摊 | 代码 | 装到 `/Applications` 了吗 |
|---|---|---|
| 上上窗四件(文件库三期 / AI 可读开关 / 分流 / 读改 brief) | ✅ 已提交 `0af4c3e` | ✅ 装了(08-09 15:57) |
| 上一窗五件(A/B/C/D/E 意图路由) | ✅ 已提交 `0578622` | ✅ 装了(08-09 23:44) |
| F(出处三字段,schema v20) | ✅ `644fba3`,✅ 已推送 | ✅ 装了(08-10 16:11) |
| 首日价值三小项 | ✅ `e5163b8`,✅ **已推送** | ✅ **装了(08-10 17:18)** |
| **更正的界面返工 + 出处原话(schema v21)** | ✅ `7146914`,✅ **已推送** | ✅ **装了(08-10 17:57,真迁移 v20 → v21)** |
| **圆圈徽章第二轮(居中 + 尺寸)** | ✅ `7f85f59`,✅ **已推送** | ✅ 装了(08-10 21:25)—— ⚠️ **这一版还是偏的** |
| ⭐ **圆圈第三轮(WebKit 量出来的 1.5px 补偿)** | ✅ `cf6395a`,✅ **已推送** | ✅ **装了(08-10 21:52,纯前端不迁移)** |
| ⭐⭐ **A5 首日价值二期(线轴 + 三个计数)** | ✅ `87b6ab7`,⚠️ **没推送** | ✅ **装了(08-10 22:45,纯前端不迁移)** |
| ⭐ **A5 返工:20 档 + 排版重做**(他看完提的) | ⏳ 见下 | ⏳ 见下 |

⚠️ **`origin/main` 曾经骗过一次。** 本地那个 ref 停在 `95b5eb4`(SSH 拉不动),
`git log origin/main..HEAD` 报了 **7** 个未推提交,**实际只有 2 个**。
**下次算未推提交之前,先按 §6.6 那条 HTTPS 的 `git fetch` 刷 ref。**
✅ 08-10 晚照做了两次,刷完的数和实际一致(第二次刷完报 5 个,推完归零)。

### 0.0 ⭐⭐ 开工顺序(照这个走)

| 序 | 做什么 | 要不要 Ocean 点头 |
|---|---|---|
| **1** | ⚠️⚠️ **第一句话就提醒他:完全退出再打开 AI 客户端**。真库 08-10 晚已升 **v21**,他那边还连着的老 `--mcp` 子进程是 v20 的 —— 不重开,**每次工具调用都报错**(原话见 §0.3 第 3 条)。⚠️ **A5 这次装机不需要重开** —— 这一条说的是 v21 那次,他要是从那以后一直没重开过就还欠着 | 不用,直接说 |
| ~~**2**~~ | ~~问他要不要提交 + 装 A5~~ ✅ **08-10 22:45 都做了**(`87b6ab7`),⚠️ **他没让推** | — |
| **3** | ⭐ **请他看两样**:①左边栏最上面那张卡 / 线轴(A5,只有他能判)②块头那个圆圈(第三轮,数字正没正) | 不用,直接说 |
| **4** | §0.2 那张「等你点头」的清单,凑一次问完,别一件件挤 | — |
| **5** | 他点头之后接着走 §0.1:**A1/A2 截图 + 视频** | **要** |

### 0.1 ⭐⭐ 后续工作(2026-08-10 晚重排过,**这一版才是对的**)

⚠️⚠️ **先读这条核查,别照抄旧顺序。** 上一版交接写着「A3 case-study 四五六期闸门已开,
先做这个」。**08-10 晚去核 `DESIGN_CASE_STUDY.md` §4/§7.4,那是错的** ——
四、五、六期各自卡着别的东西:

| 期 | 要什么 | 真实状态 |
|---|---|---|
| 四 | 架构图 + **截图** + 公证回执 | **只有架构图能做**,截图排在后面 |
| 五 | 两处上线:台账 + **官网那一页** | ❌ 官网要整页重做 |
| 六 | 改仓库简介 | ❌ Ocean 自己的条件是「等代码最终版落地」 |

**稿子 §7.4 自己就写着**:「case-study 这一摊现在没有任何可以推进的活,不是没做,是到点了。」
⚠️ **教训**:那条「闸门已开」是几窗前照抄下来的,**没有人回去核它到底依赖什么**。
**下次写「这个能开工」之前,去把它的稿子翻到依赖那一节。**

⭐ **Ocean 08-10 晚看完这份核查,重排了顺序**(真实依赖链,从源头往下):

| 序 | 事情 | 为什么排这里 | 全稿 / 位置 |
|---|---|---|---|
| ~~**A5**~~ | ✅ **首日价值二期,08-10 深夜代码全清** | 它是整条链的源头 —— 界面定稿了,后面才能拍 | ⭐ `DESIGN_FIRST_DAY_VALUE.md`(**落地记录 §6**)⏳ 只欠提交 + 装机 |
| **A1/A2** | ⭐ **截图全套重建 + 演示视频(现在轮到它了)** | ⚠️ **先把 A5 装上去再拍** —— 它改的就是左边栏那一屏,拍在装机之前等于拍两遍 | §5.1 + memory(**多场景铁律**) |
| **A0** | **官网整页重做** | ⚠️ **Ocean 08-10:「官网现在的内容已经落后最新版本软件很多了,需要重新设计功能展示和介绍,甚至交互小组件也需要重新设计。」** 是整页重做,不是换几张图 | memory `next-stage-goals-website-portfolio`(08-10 那一节) |
| **A3** | **case-study 四、五、六期(放最后)** | 它要的截图和官网页,前面三条正好产出 | `DESIGN_CASE_STUDY.md` §4 |

**B 类 —— 只欠一次真跑,几分钟的事**

| 序 | 事情 | 卡在哪 |
|---|---|---|
| ~~**B1**~~ | ~~§7 第 5 句(`corrects`)~~ | ✅ **08-10 晚 Ocean 自己在 ChatGPT 里跑完了,过了** —— 真库里第一条 `ref_kind='corrects'` 落库。他当场提的四条界面问题就是 08-10 晚做的那一摊(§1-ter) |
| **B2** | **§7 第 6 句(出处三字段)** | 白话:**这一句现在只差他在 ChatGPT 里发一句话**。功能 08-10 16:11 就装好了,没有任何前提卡着 |
| **B4** | ➕ **第 5 句重跑一次(带原话)** | v21 让模型可以多交一个 `corrected_quote`。⚠️ **和第 5 句是两回事**:那次验的是「会不会声明更正」,这次验的是「会不会说清更正的是哪句话」。大白话版在 §7.1 |
| **B3** | **Claude Desktop 的写入侧** | 至今一次没真跑过(§6.2-ter 末尾) |

**C 类 —— 仍然卡着别的东西,别硬上**

| 序 | 事情 | 卡在哪 |
|---|---|---|
| **C1** | **M4 跟进定时** | 两个前提都还在(§4-8):M3 要先被证明有用,**而且要先有一个「往外拿」的动作** —— A1/A2/A0/A3 正好是那个动作 |
| **C2** | **Windows 版** | 未开工,移植前先读 §4-3 那一整条(那里记着五处 macOS 专属通路) |
| **C3** | **对外动作**(MCP 注册表 / Show HN / Product Hunt) | 每一件都要 Ocean 单独明示 |
| **C4** | **装 Antigravity 实测** | 要 `curl \| bash` 装到他机器上,需他明示 |

### 0.2 ⭐⭐ 等 Ocean 点头的事(**一次问完,别一件一件挤牙膏**)

⚠️ **问法见 §6.7**:每个选项都要写清**好处 / 代价**,不要只报选项名。这条已验证八次。

| # | 要他定什么 | 现状 / 取舍 |
|---|---|---|
| ~~⭐ **1**~~ | ~~A5 提不提交、装不装机~~ | ✅ **08-10 22:45 他选了「提交 + 装机」,两件都做完了**(`87b6ab7`)。⚠️ **他没选推送那一项** —— 现在有 1 个未推提交 |
| ~~**2**~~ | ~~推不推(08-10 那七个提交)~~ | ✅ **08-10 晚全推完了**,`origin/main` 到 `eda795d`,零未推。⚠️ **A5 是新的一笔,要单独问** |
| ⭐ **3** | **B 类那两句真跑(第 6 句出处 / 第 7 句原话)** | ✅ **08-10 他定了:等 A5 装完一起弄** —— A5 已经装了(22:45),**所以现在就是那个「一起弄」的时候**。大白话版在 §7.1。⚠️ **两句都要先完全退出重开 ChatGPT** |
| ~~**4**~~ | ~~库目录里那批备份能不能清~~ | ✅ **08-10 他明确说了:备份留着,别清**(只清了 app)。⚠️ **这条现在是禁令,别再自作主张清** |
| ~~**5**~~ | ~~旧 app 能不能删~~ | ✅ **08-10 22:45 他点头「只清旧 app、备份留着」,清完了** —— 15 份删掉,留最新那一份回退用。⚠️ **备份一份没动**(他明确要留) |

⭐ **只剩第 3 条(B 类真跑)**,而且他已经说了「等 A5 装完一起弄」—— **现在就是那个时候**。
➕ **新增一条要问的:A5 推不推**(他 08-10 只批了提交 + 装机,没批推送)。

### 0.3 ✅ 迁移装机的留档(F 那次 v19→v20;08-10 晚 v20→v21 照同一个流程)

⚠️ **08-10 晚这一窗又走了一遍同样的流程(v20 → v21)。下面这四步是通用的,别再重新发明。**
⚠️ **首日价值那次(17:18)不算** —— 那是纯前端,schema 没动,不迁移、也不用重开客户端。

08-10 16:11 的实际过程,四步:

1. 先备份真库到 `~/Desktop/spool-snapshot-20260810-pre-v20-install.db`(量到 v19 / 49 块 / ok)。
2. 装完启动,app 自己把真库从 **v19 迁到 v20**,迁移前还写了一份
   `spool.pre-migration-v19-2026-08-10T08-11-22-533Z.db`。
   **结果:`user_version` = 20、块数 49 一个没少、`integrity_check` ok、
   `tools/list` 仍 18(12 读 + 6 写,`annotations` 一个不缺,prompts 5 个)。**
3. ⭐ **旧二进制对着新库会说什么,这次当场实测了**(拿 `Spool.app.previous-*` 的二进制
   喂一个 v20 的库):
   > Spool 的数据库是 v20,比这个 MCP 服务(v19)还新 — 客户端连的是旧版程序。
   > 请把客户端配置指向新版 Spool.app 里的执行文件,并重启客户端。
   ⚠️ **这句话的后半截是误导的**:配置本来就指着对的路径(`/Applications/Spool.app/…`),
   真正要做的**只有重开客户端**,没有任何东西要改。**下次动 `mcp.rs` 那段文案时顺手改掉。**
4. ⚠️ **退 app 的时候只杀主进程和 `--overlay`,按 pid 发 `kill -TERM`。**
   那 6 个 `--mcp` 一个都不能碰(§6.2-quinquies 第 4 步)—— 这次照做了,它们全程活着。

### 0.4 ✅ 不迁移的那种装机长什么样(08-10 21:25,圆圈第二轮)

**先判是哪一种**:`client.ts` 的 `SCHEMA_VERSION` 是 **21**,真库 `PRAGMA user_version` 也是
**21** → **不迁移**。不迁移的装机**不用重开 AI 客户端**,`--mcp` 那几个子进程照常连着就行。

装完的实测(下次照这四条核):

- **CPU**:两次采样都是 **0.0%**(没白屏、没打转)。
- ⭐ **库确实被打开了**:`lsof -p <主进程 pid> | grep -c spool.db` = **16**(§6.2-bis 那条,
  别再用 `-wal`/`-shm` 判,会假阴性)。
- **库没动**:`user_version` 仍 21、**50 块一个没少**、`integrity_check` ok。
- ⭐ **没有新的 `spool.pre-migration-*.db`**(最新那份还是 17:57 v20 那次的)——
  **这正是「没迁移」的正面证据**,不迁移的装机就该一份都不多。
- ➕ **多核一条**:`shasum -a256` 比对 `/Applications/Spool.app` 里的二进制和刚构建出来的
  那个,**两份哈希必须一样** —— `cp -R` 有没有真的换掉,这是唯一不靠猜的判据。
- **`--mcp` 那 4 个子进程(18290 / 32964 / 83832 / 92790)全程活着**,只杀了主进程和 overlay。

⚠️ **仍然没验的**:「长得对不对」。三个旁证只排除白屏和数据损坏,**圆圈正不正只有 Ocean 能看**。

### 0.5 ✅ A5 的装机留档(08-10 22:45,又一次「不迁移」的装法)

**先判是哪一种**:`client.ts` 的 `SCHEMA_VERSION` = **21**,真库 `PRAGMA user_version` 也是
**21** → **不迁移**,**不用重开 AI 客户端**。照 §0.4 那四条核完,全过:

- **CPU**:两次采样都是 **0.0%**(没白屏、没打转)。
- **库确实被打开了**:`lsof -p 71313 | grep -c spool.db` = **22**。
- **库没动**:`user_version` 仍 21、**50 块一个没少**、`integrity_check` ok。
- **没有新的 `spool.pre-migration-*.db`**(最新那份还是 09:57 v21 那次的)——
  **这就是「没迁移」的正面证据**。
- ⭐ **`shasum -a256` 两份哈希一样**(`/Applications` 里的 vs 刚构建的,`fb73fe41…`)——
  `cp -R` 有没有真换掉,这是唯一不靠猜的判据。
- **5 个 `--mcp` 子进程全程活着**(18290 / 32964 / 54391 / 83832 / 92790),只杀了主进程
  45160 和 overlay 45170。

⚠️ **仍然没验的还是那一句**:「长得对不对」只有 Ocean 能看。

### 0.6 ✅ 返工版的装机留档(08-10 23:21,20 档 + 新排版)

**同一套流程,同样是不迁移那一种。** 四条旁证全过:CPU 两次 **0.0%**、
`lsof` 数到 **22** 个 `spool.db`、`user_version` 21 / `integrity_check` ok、
**没有新的 `spool.pre-migration-*.db`**、`shasum` 两份一样(`703b456c…`)。
5 个 `--mcp` 子进程全程活着,只杀了主进程 71313 和 overlay 71339。

⚠️⚠️ **块数从 50 涨到 91 了** —— 这不是装机弄的,是 **22:45 到 23:10 之间他自己在用**
(§6.9 那条「真库是活的,要准数就现场量」当场又应验一次)。
现量的三个数:**捕捉 34 / MCP 32 / 自写 2973 字**。

⚠️ **旧 app 现在只留一份 `...-20260810-224509`** —— 它是 **A5 之前**那一版,
是有意义的那个回退点;中间那份 A5 第一版(他说排版不好看的那个)删掉了。

---

## 1. ⭐⭐ 这一窗做了什么 —— **A5 首日价值二期**(全稿 `DESIGN_FIRST_DAY_VALUE.md`,落地记录该稿 §6)

**四件全做完,schema 一列没动。** 卡从「今天读了哪三条」改成「我攒了多少」:
一期那三条正文预览**撤掉了**(Ocean 原话第一句就是「不添加具体内容」),
`TodayCard.tsx` 删掉,换成 `SpoolCard.tsx` + `SpoolMeter.tsx`。

| 稿子要的 | 落地成了什么 |
|---|---|
| ①捕捉数,分出 MCP 那一份 | `你攒了 N 条` / `AI 写回 M 条`(`countCaptures` + 现成的 `countMcpBlocks`) |
| ②个人 note 字数 | `我写了 X 字` —— 新的 `countUserWrittenChars()`,口径乙(批注 + 无 source 的块正文) |
| ③⭐ 线轴小组件 | `SpoolMeter.tsx`,inline SVG,**40×36 的小线轴**,**20 档**(§1-septies 两轮返工之后的样子)|
| ④总线轴数 | 卡上四个格子之一 `已缠满 N 轴`;满轴那一下写 `这一轴缠满了` + 线轴闪一下 |

⚠️⚠️ **三处和稿子不完全一样,理由在稿子 §6.2,别当成疏忽改回去**:

1. **教程过滤从 TS 挪进了 SQL,但没有抄第二份。** `countCaptures` 换成 `COUNT(*)` 之后
   TS 那侧就没有行可过滤了 —— 改成把 `TUTORIAL_SOURCES` 的**值**绑成参数。
   **名单仍然只有一处出处**(`client.ts` 新导出的 `tutorialSourceLabels()`)。
2. **字数也排除教程种子**(稿子没写)。**教程块是带批注的** —— 不排除的话新装第一眼是
   「我写了 700 字」,和 §2.1 拦掉的「今天读了 6 条」是同一个谎。
3. **线轴数的是「你捕的」那个数,不含 MCP** —— §2.3 说「谓词复用 §2.1 那一处定义」,
   而那处定义是不含 MCP 的。**这样线轴上的线和头一行的数字永远对得上。**
   ⚠️ 他要是想让线轴把 MCP 也算上,改 `SpoolCard` 里 `spoolState()` 的入参一行,
   **但头一行的数字要跟着一起改**。

### 1-quinquies ⭐⭐ 这一摊真正值钱的两条(台账 §3.25)

1. ⭐ **「便宜」是关于调用方的判断,不是关于这个查询的判断。**
   `countCaptures()` 原来是 `listCapturesSince(0).length` —— **把全库每一行捕捉读成对象再数长度**。
   那在当初是对的:唯一的调用方是一次性提示,**而且只在库还很小的时候才会问**(注释里写着)。
   线轴是常显的,同一个函数就变成了每次重绘扫全表。**复用一个函数就是继承它的前提,
   而前提往往写在注释里、不在签名里。**
2. ⚠️⚠️ **同一条规矩翻译成 SQL 会静默错,因为 SQL 有三个真值。**
   `countUserWrittenChars` 要用 `annotationIsAi()` 那条规矩(`annotation_by` 说了算,
   NULL 就回退看 source)。照字面翻成 SQL:`annotation_by = 'ai'` 在 NULL 行上**不是 false 是 NULL**,
   `NOT (NULL)` 还是 NULL,`CASE WHEN` 直接落空 ——
   **被漏掉的全是 v14 之前的行,也就是真库里最常见的那种行。**
   ✅ 两个 `COALESCE` 就是为这个加的(写的时候躲掉的,不是线上炸出来的),
   **测试里专门放了两行 v14 之前的行按住它**。
   ⭐ 通用解药:**要在两种语言里表达同一条规矩,搬值,别搬字**(教程名单就是这么办的);
   搬不了值的(比如 NULL 那条),就写一条**拿同一批行同时喂两种表达**的测试(§3.17 同款)。

### 1-septies ⭐⭐ 装机之后 Ocean 提的两条(**返工已落地**,全稿 `DESIGN_FIRST_DAY_VALUE` §7)

**他看完真 app 里那张卡说了三句**:一句确认、两句要改。

| 他说的 | 做了什么 |
|---|---|
| ✅「确认不含 AI 写回」 | **线轴数的仍然是「你捕的」** —— §1 第 3 条那个取舍他认了,一行没动 |
| 「价值面板排版不好看,右边全空,不平衡」 | 线轴改成**横着占满整张卡**,四个数字排成 **2×2 两列** |
| 「线轴的状态增加,变成 20 个」 | `SPOOL_STEPS` 8 → **20**,一格 **5 条** |

⭐⭐ **这一条最值得记:加档位这件事,照原画法做等于做废。**
原来一格 = **线缠粗一点**(半径 4.5px → 14px)。**分成 20 格,一格 0.5 个 CSS 像素** ——
2 倍屏上 1 个物理像素,**等于看不见**。「加档位」本来就是为了让用户更常看见变化,
**照原样加满 20 档反而每一格都看不见。**
✅ **换成「一格 = 一整圈线」**:轴上 20 个绕线位,每 5 条多一道线,新增那一圈从轴里长出来。
⭐ **顺带**:20 圈线**需要横向的地方**,所以「排版不平衡」和「20 档」**是同一个解** ——
线轴占满整张卡的宽度。

⭐ **真库现量的数(08-10 深夜)**:捕捉 **34** / MCP **32** / 自写 **2973 字**。
**MCP 那一份几乎和捕捉一样多** —— §2.1 要分开这两个数,在他的库上确实有意义。

### 1-octies ⭐⭐⭐ 第二次返工:面板退回小线轴,只改文字(**这一条是最值钱的一条**)

**§1-septies 那一版他当场否了**(全稿 `DESIGN_FIRST_DAY_VALUE` §8)。原话:

> 面板完全不行,回到上一版本,上一版本的线轴没有问题(这版太大个了),只是文字的布局和选择
> 不太对,导致右边什么都没有,而左边很紧凑,另外 logo 太小了,被面板抢占了注意力,增大一点,
> 面板和 logo 增加距离。

⭐⭐ **教训写在这里,下一窗别重犯**:上一轮他说「右边全空,不平衡」,
**我把它读成「面板需要更多地方」** —— 于是把线轴放大到占满一整行。
**他要的是「字排得不对」。** 这一次他自己把话说明白了:「**只是**文字的布局和选择不太对」。
**同一句抱怨,两种读法,做出来差了一整版,而且贵的那一版是我自己加的。**
⚠️ **下次收到「不好看」这类反馈,先问「是尺寸还是文字」,别自己挑一个就动手。**

| 他说的 | 做了什么 |
|---|---|
| 「回到上一版本」「线轴没有问题(这版太大个了)」 | 线轴退回 **40×36**,整行 banner 撤掉 |
| 「文字的布局和选择不太对」 | **左标签 / 右数字**,数字右对齐成一列(`tabular-nums`) |
| 「logo 太小了,被面板抢占了注意力」 | 标题 `text-2xl → text-3xl`、「思簿」`text-base → text-lg` |
| 「面板和 logo 增加距离」 | 头部 `pb-3 → pb-6` |

⚠️ **别把文案改回整句**(「你攒了 34 条」)——**整句必须在某处结束,那个结束点就是
「左边很紧凑、右边什么都没有」。**
⚠️ **20 档留着,代价照说**:小线轴上一格约 0.5 个 CSS 像素,**单独一格看不出来,
四五格才看得出来**。**两版他都看过,选的是「线轴小」这一侧。**

### 1-sexies ⭐ 怎么验的(还是 §6.2-sexies 那把量具,**没用 Chrome**)

- 第一版:九档(0…8)+ 空轴 + 满轴 + 中英文两版,**用 `scripts/wk-snapshot.m` 逐档截图看过**。
- 返工版(20 档 + 新排版):空轴 / 5 条 / 47 条 / 99 条 / 满轴,**外加侧边栏拉到最窄(200px)
  那一档** —— 窄的时候第二列会截字,四位数以内的数字不会被截。
- ⭐ **第二次返工(§1-octies)**:这次**把侧边栏整块画出来了**(标题 + 面板 + 下面两行项目),
  260px 和 200px 两个宽度都看过,中英文都看过。
  ⚠️ **教训**:前两次只渲了那张卡,**而他这次的意见有一半是关于卡以外的东西**
  (标题大小、标题和卡的距离)—— **量一个组件的时候,把它旁边的东西也放进去。**
- ⭐ **看图当场改了一处**:空轴原来是「细轴 + 两块端板」,在 40×36 上**读起来像大写 H**。
  轴厚从 6px 提到 9px 才像线轴。⚠️ **新装用户第一眼就是空轴那一档**(拍板 3 之后卡是常显的),
  所以这一档比别的档更值得盯。
- ❌ **没验的**:装进真 app 之后那一屏挤不挤 —— 量具只渲这一小块(§6.2-sexies 自己的边界)。
- ❌ **进格那一下的动画**:静态图只能证明 `transform` 生效(九档长得不一样),**动画本身要装机才看得见**。

---

## 1-old. 上一窗做了什么 —— 首日价值三小项(`DESIGN_NEXT_STAGE` §4.5,已归档)

**「产品下一程」那条(§4-7)到此三件全清。** 那份稿子只有三行字,三个岔路口是 Ocean 08-10
当场拍的,**其中两个不是推荐项** —— 第八次印证 §6.7 那条「把代价写清楚,他会选得比你准」。

| 他选了什么 | 他没选的那个 | 他接下的代价 |
|---|---|---|
| **日卡只是界面,不写库** | 每天自动存一块 | 卡明天就没了,以后翻不回来 |
| **那行提示从第一条挪到第三条** | 第一条留着、第三条再加一行 | **第一条捕捉完再没有任何收尾**,用户不知道手势成没成 |
| **日卡放左边栏顶上** | 块流顶上(和日期提醒条并排) | 左边栏是「项目列表」,这是里面第一个不是项目的东西 |

落地的三件:

1. **`listCapturesSince` / `countCaptures`**(`lib/db/blocks.ts`)——「什么算一条捕捉」只有
   这一处定义:**有 source、不是 MCP、不是教程种子**。composer 写的(source 为 null)不算,
   AI 写的不算,教程播下的更不算(否则新装第一眼就是「今天读了 6 条」)。
   ⚠️ **手打的来源标签算** —— 他在徽章上敲「Chrome」就是因为他真在那儿读的。
   ⚠️⚠️ **教程那一半当时是在 TS 里过滤的 —— A5 之后不是了。**
   现在两个函数共用 `captureClause()`,教程名单**当参数绑进 SQL**,
   出处仍然只有 `client.ts` 一处(§1 第 1 条)。**别照着这段老话去找 TS 过滤器。**
   ⚠️ **`ORDER BY created_at DESC, rowid DESC`** —— `created_at` 是毫秒、**不唯一**,
   连着捕三条会同毫秒,不加 `rowid` 那张卡的「最近三条」和打包目标是随机的。
   (这条是测试当场抓到的,不是想出来的。)
2. **`Sidebar/TodayCard.tsx`** —— 纯读,零写。没捕捉的日子**整个不渲染**。
   跨午夜靠 `window focus` 重查(`startOfToday()` 每次现算),否则开着过夜会把昨天的三条
   顶在「今天」下面。
   ⚠️⚠️ **这个文件 A5 之后不存在了** —— 换成了 `SpoolCard.tsx`,而且「没捕捉就不渲染」
   这条规矩**只剩「今天」那一行还守着**,卡本身是常显的(拍板 3)。
3. **那行提示挪到第三条**(`captureStore` 的 `PACK_HINT_AFTER = 3`)。
   ⚠️ **settings.json 里那个键仍然叫 `firstCaptureHintPending`,故意没改** ——
   改一个已落盘的键除了一次迁移什么也买不到。运行期那个字段改名成 `packHintBlockId` 了。

**外加第三小项(稿子里的第三条)**:README 和教程种子把「MCP 没配也全功能」讲透了。
⚠️ **改教程种子有个已知代价**:老用户的教程块和新种子文本不再逐字相同,
**他们切换语言时教程就不再跟着翻译了**(`client.ts` 的 `retranslateTutorial` 是逐字比对的)。
这不是新账 —— `4631c25`(08-04)改教程时就是同一个代价,那次也接受了。
⚠️ **没有把客户端名字写回教程** —— `4631c25` 的标题就是「教程种子不再点名两个客户端」。

### 1-ter ⭐⭐ 08-10 晚的主活:更正那一摊的界面返工 + 出处原话(schema v21)

**起因是一次真跑。** Ocean 08-10 晚在 ChatGPT 里跑完 §7 第 5 句(`corrects`),真库里第一条
`ref_kind='corrects'` 落了库 —— 然后他去界面上看,当场提了四条。**这一摊全部是他提的,
不是设计稿里排的。**

他看到的那一行长这样(原样):

```
更正了其中一处：#1 8/7 16:50 # 申请人定位与不可变约束（2026-08-07） **目标。** Fall 2…
```

| 他说的 | 做了什么 |
|---|---|
| 「点击更正源无法跳转」 | 整行可点,跳过去并闪一下。⚠️ **这是推翻原设计** —— 那一行当初被明确写成「纯展示,不给按钮、不给跳转」(§2.5 安静)。理由记在 `CitationLine.tsx` 里:`cites` 是可以不理的脚注,`corrects` 是一条关于**另一块**的断言,你非去读不可 |
| 「block 标记和时间放在一起过于混乱」+「`#` 看起来太凌乱」 | 编号改成**圆圈徽章**(`SeqBadge.tsx`),时间和正文用 `·` 和颜色分开。⚠️ **Ocean 拍板「两处一起改」**,所以块头那个 `#1` 也一起换了 |
| 「正文是 md 文档形式,看起来太混乱」 | 预览走 `plainText()` 去掉 md 符号。⚠️ **复用的是渲染器自己的取词器,不是另写一套正则** —— 「什么算符号」只有一处定义 |
| 「展开也不知道到底是哪里被修改了,应该要高亮标记出被更正的文字」 | 见下面两条 |

**外加一条他没提、但是同一个病根的**:⚠️ **§7.1 那份验收稿子叫他「去旧块底下看有没有一行
『有一点被更正了』」—— 那一行只存在于 pack 里,Spool 界面上根本没有。** 所以他从旧块那一侧
看是完全无迹可寻的。现在补上了(`CorrectedByLine.tsx`),可点跳回新块。台账 §3.21。

**schema v21 —— 标到「哪句话」**(Ocean 在「标到这一块 / 标到哪句话 / 手动圈」三档里选了最贵的那档):

```sql
ALTER TABLE blocks    ADD COLUMN corrected_quote TEXT;
ALTER TABLE proposals ADD COLUMN corrected_quote TEXT;
```

- **写侧**:`add_block` 和 `propose_blocks` 各开一个 `corrected_quote`,共用
  `parse_corrected_quote`(两条路不许分叉,和 F 的 `parse_provenance` 同一条规矩)。上限 200 字,
  **理由是「引了整块等于什么都没引」**,不是安全余量。
- ⚠️⚠️ **抄错当场拒**(`check_quote_occurs`):写入时就拿去被更正的那一块正文里比对,找不到就报错。
  **模型是唯一还能回去重抄的人,而且只有此刻还在** —— 存下来几个月后画不出来是静默失败。
- ⚠️ **存原话,不存位置。** 位置在用户编辑那一块之后会**悄悄指错**;原话找不到就安静不画,
  块级那行 ⚠️ 照样在。这条取舍写进 schema.sql 注释了。
- **界面**:`contentRuns` 加了一个独立的 `corrected` 属性(和 `highlight`/`hit` 平级,能叠加),
  被更正那句话用**暖色底 + 虚线**画出来。⚠️ **不加删除线** —— `corrects` 只说一处不成立,
  整块作不作废是用户的判断(§3.1 «谁能用»)。
- **兑现口两个**(§4-1 铁律):pack 那行 `⚠️ one point … — see #N` 带上原话、
  `get_blocks` 把 `corrected_quote` 回读出去。
- `INSTRUCTION_BODY`、`OPENERS`、pack 的 `## Notation` 三段路由/契约文本都改了。
  ⚠️ **`OPENERS` 在这之前压根没有一条「更正」的开场白** —— 第 5 句能跑通是工具描述撑住的,
  不是路由撑住的。现在补了一行。工具面**仍是 18**(只加参数,没加工具)。

### 1-quater ⭐ 圆圈徽章第二轮:数字居中 + 圆圈缩到和旁边的字一样大(08-10 晚上一窗的代码活)

**Ocean 看完上一轮的成品又提了两条**:「数字标记里面的数字没有居中」「圆圈的大小需要和边上的
时间和文字一致」。**两条是同一个病根:这个徽章是用 `em` 量的。**

改的只有一个文件:`SeqBadge.tsx`,一行 class。
`h-[1.5em] / text-[0.9em] / px-[0.32em]` → **`h-[13px] / min-w-[13px] / text-[9px] / px-[3px]`**。

- ⚠️⚠️ **`em` 会乘起来**:0.9em 的字装在 1.5em 的盒子里,在更正那一行(11px)就是
  **9.9px 的数字装在 14.85px 的圆里** —— 两个都不是整像素。居中要分的那点余量、
  以及数字自己的栅格化,**会朝着这一行恰好落在哪半个像素上四舍五入**。
  实测(2 倍屏)**数字偏上 1.5px,而且行一动它就变**。换成整像素之后剩 **0.5px**,
  那 0.5 是**字体自己的 overshoot**(圆头的 `2` 会冒过 cap 线),不是排版能修的。
- **圆圈变大也是同一个乘法**:14.85px 的圈,旁边 11px 的字其实只有 7.8px 高。
  **13px 这个数是对着「旁边的字」挑的,不是对着字号挑的** —— 它下沿和文字的下伸部齐,
  上沿比 cap 线高约 2px,所以读起来是这一行上的东西,不是浮在上面的泡泡。
- ⚠️ **现在是「两处共用一个写死的尺寸」,不再跟着行的字号走。** 两个调用点是 10px(块头)
  和 11px(更正那一行),13px 在中间。**不要为此加一个 size 参数** —— 加了小数就回来了;
  真有第三个调用点,就在这个文件里再写一个整像素的值。

⭐⭐ **怎么验的(这一条对下一窗最有用,配方在 §6.2-sexies)**:
**四条基线一条都看不见这种 bug** —— `tsc` 干净、324 个测试全过、两侧渲染器的 golden 平价
测试更是压根不看界面,**改之前改之后一模一样**。而 §6.2-bis 说的「app 截图这条路在这台机器上
是断的」也还断着。**办法是把这一小块 markup 单独拿去渲染**(真字体 + 真色板),
**圆圈画红色、数字画蓝色,再用 Python 量两个墨迹包围盒相减**,上下差多少个物理像素当场是个数。
⚠️ **它只证明这一小块,不证明装起来的那一屏** —— 「长得对不对」仍然只有 Ocean 能看。
台账 §3.22 / §3.23。

**⚠️⚠️ 第二轮(同一天更晚):Ocean 说「数字有点偏上,没有对准圆心」—— 他是对的,
而且第一轮我用错了量具。**

| | Chrome 量的 | **WKWebView 量的(app 真正跑的)** |
|---|---|---|
| 第一轮改完 | 偏上 0.5px「看着修好了」 | **偏上 1.5px —— 根本没修好** |

**两个内核把基线放在行盒里的位置不一样**,拿 Chrome 量这个 app 等于量另一个程序。
换成 WKWebView 重量之后:

- **补偿方式**:`pt-[1.5px]`。flex 居中会把余量对半分,所以顶上垫 p,数字就往下走 p/2 ——
  1.5px 正好抵消 WebKit 抬上去的那 1.5px。**这个数是量出来的,不是估的。**
- ⭐ **剩下的 0.5px 修不掉,是奇偶数**:数字墨迹 **13 个物理像素**高,圆圈里面是偶数格,
  13 除不开 2。**我把这一格留在了下面** —— 偏下他不提,偏上他一眼就看见(这次就是)。
- **结果**:最坏 **3~4 个物理像素 → 0~1 个**;两位数(`#12` 这种,真库里最常见)是 **0**。
- ⚠️ **代价写清楚**:Chrome 里现在反过来偏下 0.5px。**这是对的取舍,用户不跑 Chrome。**
- ⚠️ **别照抄那个 1.5px**:WebKit 升级可能把基线又挪一点,**发现不对就重量**(§6.2-sexies)。

⭐ **量具本身进仓库了:`scripts/wk-snapshot.m`**(ObjC + WKWebView 截图;
这台机器 Swift SDK 坏了,ObjC 这条路是通的)。台账 §3.23。

### 1-bis F(出处三字段,schema v20)—— 已装机,细节看 §8.3

全稿 `DESIGN_MCP_INTENT_ROUTING.md` §4.6,落地记录 **§8.3**(比这里详细,含四处和稿子不一样的地方)。

```sql
ALTER TABLE blocks    ADD COLUMN source_url TEXT / retrieved_at INTEGER / recheck_after INTEGER;
ALTER TABLE proposals ADD COLUMN source_url TEXT / retrieved_at INTEGER / recheck_after INTEGER;
```

⚠️ **`proposals` 那三列是稿子里没有的。** `propose_blocks` 也开了这三个参数,而
**待审队列正是它们最容易丢的地方** —— 用户下周才点批准,那时调用方早走了,谁也补不出一个网址。

- **写侧**:`add_block` 和 `propose_blocks` 的 item 各开三个参数,共用一个 `parse_provenance`
  (两条路不许对「什么算网址」分叉)。⚠️ **`source_url` 只收 `http(s)://`,本地路径当场拒** ——
  pack 是唯一被设计成要离开这台机器的东西,一个 `/Users/hzjin/…` 进去就等于把账号名和
  目录结构写进他今后拷给任何人的每一份简报。
- **用户界面一个输入框都没加**(稿子明说的):这三个字段的用户是「出去查过官网的那个模型」。
- **两侧渲染器一起动**,块头**正下方**一行:
  `↗ https://… · retrieved 2026-08-09 · recheck after 2027-08-01`;
  过期了换成 `⚠️ may be out of date — was to be rechecked after <date>`。
  ⚠️ **说的是「可能」,而且块照样整条渲染** —— 作废是用户的判断,这一行只是提个醒。
- **兑现口两个都补了**:`get_project_overview.needs_attention.due_for_recheck`(只数没作废的)、
  pack 里过期那条的标注。⚠️ **另外自己加了第三个:`get_blocks` 把三个字段回读出去** ——
  没有它这三列就是只写不读,那正是 §5-3 那条毒。
- `source` 的描述改成「只留短标签」,URL 从此进 `source_url`。真库里那 20 个长标签**不回改**。
- `INSTRUCTION_BODY` 和 `OPENERS` 都改了(硬规矩,见 §4-1 那条铁律)。工具面**仍是 18**。

### 1.1 ⚠️⚠️ 三个日期上的坑,下一窗别踩回去

1. **`retrieved_at` / `recheck_after` 存的是 UTC 零点,渲染走 `format_utc_date` /
   `formatUtcDate`** —— **不走**库里其它时间戳那条本地时区的路。
   它们是**日子不是时刻**:「查于 2026-08-09」在时区两侧要读成同一天。
2. **`now` 现在要传进两侧渲染器**。「过没过期」必须和 pack 表头日期是同一瞬,
   而且**不能让 golden 变成定时炸弹** —— 测试把 `now` 钉死。
3. ⭐ **golden 平价测试挡不住这一类 bug,这是这一窗真正留下来的东西**:
   它比对前会把两边的 `YYYY-MM-DD` 全换成 `<DATE>`(本地时区渲染的字节在别的机器上不一样)。
   所以**「一侧走 UTC、另一侧走本地」它照样全绿**。
   两侧各补了钉死日期字面量的断言。**测试排除掉的东西,也是它规格的一部分。**
   全文台账 §3.18。

---

## 2. ⚠️ 还没验的那一层

**四条基线 + stdio 全绿一条也答不了「模型会不会真的走过去」**(§6.2-ter)。

1. ✅ **§7 前四句 08-10 跑完了,四句全过** —— 取证在 §7.2。
2. ✅ **§7 第 5 句 08-10 晚 Ocean 自己跑完了,过了。** 真库里第一条 `ref_kind='corrects'`
   落库。⭐ **而这一句真正的产出不是「过了」,是他看完界面提的四条**(§1-ter)——
   **机制对、界面读不懂,和 08-09 那次「机制对、路由没接上」是同一形状的第二例。**
3. ⏳ **§7 第 6 句(出处三字段)还没跑** —— 装机在 08-10 16:11,前提早就没了,随时能跑。
4. ⏳ **v21 的原话那一半也没在真客户端上跑过**(§7 第 5 句的加强版,B4)。
   隔离库上写入校验、pack、`get_blocks` 都通了,**但「模型会不会真的填那个字段」只有真跑能答**
   (§6.2-ter)。
5. ⏳ 上上窗那四件的「好不好看」仍然没人验过(装了但没请他看)。
6. ⏳ ⭐ **A5 装了(08-10 22:45)但没人看过** —— 九档 SVG 单独截图看过(§1-sexies),
   **「装进那一屏之后长什么样」仍然一个字都没验**,进格那一下的动画也只有他能看见。
   **这是现在最该请他看的一件。**

### 2-bis ✅ 那张卡现在是常显的(A5 之后规矩变了)

⚠️⚠️ **一期那条「没捕捉的日子整张卡不出现」已经作废了**(拍板 3,`DESIGN_FIRST_DAY_VALUE` §4)。
现在**线轴 + 三个计数永远在**,只有「今天读了 N 条」那一行还是当天有捕捉才出现。
**新装用户第一眼就会看到一个空线轴 —— 那是设计,不是 bug**(该稿 §4 第 3 条推论)。

⚠️ **那行「现在够打一个包了」他这辈子都看不到** —— `firstCaptureHintPending` 只在
**创建数据库的那一次启动**才会被点亮,他的库是 7 月建的。那一行只有新装的人能看见,
**验它只能靠隔离库**(把 settings.json 里那个键设成 true,再捕三条)。

## 3. ⚠️ 给 Ocean 的两句话(不是代码活)

1. **Flux 里那两块「我会这样写…你同意吗」可以直接删,右键就有。**
   模型上一场告诉他「Spool 不支持直接改删」——**那是假的**,来源是 `add_block` 的描述
   (08-09 已经改掉了)。那两块是他自己在旧的运行卡片上点「存回项目」存进去的。
2. ⚠️⚠️ **v21 已经装上去了(08-10 晚),他必须完全退出再打开 AI 客户端。**
   不重开,ChatGPT 那边**每一次**调 Spool 都会回「数据库比这个 MCP 服务还新」。
   **完全退出再打开就好,配置一个字都不用改**(§0.3 第 3 条)。
3. ⭐⭐ **A5 写完了,问他要不要装** —— 装完请他看**左边栏最上面那张卡**:
   线轴(空/绕线/满轴)、三个数字、以及「今天读了 N 条」那一行。
   ⚠️ **这两样只有他能判**:①线轴在那个尺寸下认不认得出是线轴 ②那张卡在侧边栏顶上挤不挤。
   ⚠️ **纯前端,不用重开 AI 客户端。**
4. ⭐ **请他去看一眼更正那一摊现在长什么样**(§1-ter):〈申请规划〉里 08-10 那条更正块,
   以及**被它更正的那一块**(旧块底下现在挂着一行,而且被更正的那句话会画出来 ——
   ⚠️ 前提是模型填了 `corrected_quote`,08-10 那次是 v21 之前写的,**没有原话**,
   所以他只会看到块级那一行。要看见画线得等 B4 真跑一次)。
5. ⭐⭐ **圆圈返工了三轮,最后一轮 08-10 21:52 装上去了**(§1-quater)。
   ⚠️ **请他看一眼**:随便打开一个项目,**看块头那个圆圈** —— 数字在不在正中。
   ⚠️ **量到的残差是 0.5px 且偏下**,他要还说偏,**先重量再动手**(§6.2-sexies),
   **别凭感觉调** —— 这一摊已经因为「凭一个来自错引擎的数字」返工过一次了。
   ⚠️ **这是纯前端,不用重开 AI 客户端**(和 v21 那次不一样)。

## 4. 长期计划(⚠️ 改写交接时必须原样带上)

> 08-02 那次改写把第 1、3 条整段弄丢了,Ocean 08-03 才发现。**这一节只增不减。**

1. **MCP 新增接口面**(超出现有工具面的部分)
   - ~~`propose_blocks` + 待审面~~ —— ✅ 已落地(2026-08-05)
   - 溯源:**A 案已批并落地** —— 用现成的 `ref_block_id`,没动 schema 的块结构
   - ~~分流的原文块带来源标签~~ —— ✅ 已落地(2026-08-06)
   - ~~`get_blocks` 的历史过滤位~~ —— ✅ 已落地(2026-08-07,v13 的 `stale`)
   - ~~档③「部分更正」开给 MCP,走提案~~ —— ✅ 已落地(2026-08-08,schema v14)。
     `propose_blocks` 的 item 多一个 `ref_kind`,**只认 `"corrects"`**;
     传 `"supersedes"` 当场拒绝。⚠️ 只开 ③,`stale_at` 那一列 AI 永远碰不到
   - ~~**MCP 读 follow up brief + 建议改** —— 决定 5~~ —— ✅ **已落地(2026-08-09,schema v19)**。
     ⚠️ 写那一半走的是新的「过目」闸(建议停在 `follow_up_brief_suggested`),
     **直写是注入提权链**(网页注入 → 改 brief → 改变下次搜索方向)—— 这条理由要留住
   - ~~**`request_file_access`** —— 项目文件库三期~~ —— ✅ **已落地(2026-08-09,schema v18)**。
     ⚠️ 参数只认 `attachment_id`,**永远不接受路径**,而且必须属于所申请的那个项目
   - ~~M2:待审闸跑过一段真实使用后,评估写入开关能否默认打开~~ ——
     ✅ **已落地(2026-08-13),默认打开了**
   - ➕ **2026-08-11 新增铁律**:**每个工具都必须写 `annotations`**。少一个,
     在本地一点声音都没有,但第三方客户端会拒绝调用。已有测试钉住
   - ➕ **2026-08-09 晚新增铁律(同一类的第二条)**:**加工具的同时必须改
     `OPENERS` 和 `INSTRUCTION_BODY` 这两段路由文本**。
     ✅ **2026-08-09 已落地,并且钉成测试了**(`every_tool_is_reachable_from_the_routing_text`)。
     全稿 + 取证 + 六件设计:`DESIGN_MCP_INTENT_ROUTING.md`(**三题已拍板,都选乙**)。
     ✅ **A 门把手 / B 路由 / C `list_threads` 三字段 / D `add_block` 开 `corrects` /
     E `get_project_overview`(工具面 17 → 18)全部落地(2026-08-09,§8)**;
     ✅ **F 出处三字段(schema v20)也落地了(2026-08-10,§8.3),已推送、已装机。**
     ✅ **§7 前四句 2026-08-10 真跑过了,四句全过(§7.2)** —— 这一稿到此
     **代码全清、路由那一半也验过了**;只欠第 5 句(`corrects`)和第 6 句(出处字段)
   - ➕ **2026-08-10 晚新增:`corrected_quote`(schema v21)** —— 更正现在能说清「更正的是
     旧块里的哪一句」,`add_block` / `propose_blocks` 各开一个参数。全稿见 §1-ter。
     ⚠️ **两条新规矩从这里出来的**:
     ① **写入时就去被引的那一块里比对,抄错当场拒** —— 模型是唯一还能重抄的人,
     而且只有那一刻还在;存下来几个月后画不出来是静默失败。
     ② **存原话不存位置** —— 位置在用户编辑之后会悄悄指错,原话找不到就安静不画。
   - ➕ **2026-08-10 新增(F 带出来的)**:**加了会被写的列,就要在同一次改动里给它读回来的口**。
     `recheck_after` 有三个(pack 标注 / `due_for_recheck` / `get_blocks` 回读),
     理由是 §5-3 那条「v13 给了两把刀但没人提醒你去用」。台账 §3.18
2. **Claude Code 引擎位**(目标 v0.4.0)—— ✅ M1/M2/M3 全部落地。
   ✅ 引擎位泛化成预设也落地了(2026-08-06)。
   ✅ **v0.4.0 已发布**(2026-08-10)
3. **Windows 版** —— 未开工。⚠️ 现在这一版有 macOS 专属通路(双击 ⌥ 走 HID tap、
   AXFrontmost 抢焦点),移植前先读 memory `double-tap-exclusivity` 和 `capture-note-first`,
   那两条记着哪些路是死路。⚠️ M2 的取消走 `setpgid` + `killpg`(Unix 专属),
   移植时这一段要重写。⚠️ `run_env()` 里的 `USER` 在 Windows 上是 `USERNAME`,别照抄。
   ⚠️ 引擎检测的候选路径表(`candidate_paths`)整个是 macOS/Unix 形状
   (`~/.nvm/…`、`/opt/homebrew/bin`),Windows 上要另写一份。
   ⚠️ `stream_with_timeout` 的两个读取线程本身是跨平台的,
   但**杀进程组那一段仍然是 Unix 专属**,跟 M2 的取消是同一处。
   ⚠️ `focus_mcp_client`(一键问 AI 用来把客户端调到前台的)整个是 `open -a`,
   **Windows 上要另写**。
   ➕ **2026-08-10**:`format_utc_date` / `parse_iso_date` 走的是 `libc::gmtime_r` /
   `libc::timegm`,**Windows 上这两个名字不一样**(`_mkgmtime`),移植时要换
4. **分发**:公证直发 `.dmg`,**不上 MAS**(memory `distribution-route-notarized-dmg`,
   沙盒冲突清单在里面)
5. **LICENSE 仍未定** —— ⚠️ 绝不擅自加(memory `no-license-file`)。
   ✅ **2026-08-08 Ocean 明确答复「不加」** —— 这现在是一条禁令,不只是暂缓。
   后果(公开仓库无许可 = 保留所有权利)要在 case-study 页面主动答一句,
   措辞已写进 `CASE_STUDY_LEDGER.md` §4
6. **对外动作**:MCP 注册表挂号 / Show HN / Product Hunt —— 每件都要 Ocean 单独明示
7. **产品下一程剩下的三条**(原 `DESIGN_NEXT_STAGE.md` §4.3–§4.5,那份稿子已归档)
   - ~~**AI 活动面**~~ —— ✅ 已落地(M3)。⚠️ 正确形态是右侧栏;
     **08-07:那个折叠区已经删掉并入右侧栏了(R2)**
   - ~~**「我的思考」凸显**~~ —— ✅ **两半都落地了**:W7(批注当标题)08-07,
     **「只看我写的」过滤 2026-08-13**
   - ~~**首日价值一期**:捕捉满三条 → 一行安静提示;「今天读了什么」日卡~~ ——
     ✅ **2026-08-10 落地(§1),连稿子里第三条「README + 教程讲透 MCP 没配也全功能」一起**。
     ✅ 已提交 `e5163b8`、✅ 已推送、✅ 已装机(08-10 17:18)。
   - ➕ ⚠️ **2026-08-10 晚,这一条被 Ocean 重新打开了:首日价值二期**。
     原话:「首日价值面板内容没什么价值,不添加具体内容」—— 日卡从「列出你今天读了什么」
     改成「显示你攒了多少」:**捕捉数(含 MCP 那一份)/ 个人 note 字数 / ⭐ 线轴小组件
     (100 条缠满一轴,中间多做几档,满了提醒并清空)/ 总线轴数**。
     ⭐ **全稿 `DESIGN_FIRST_DAY_VALUE.md`。✅ 该稿 §4 那四个拍板 2026-08-10 晚全答完了,
     开工不用再问。** ⚠️ **一期那三条正文预览是要撤掉的,不是在旁边加东西。**
     ➕ ⭐⭐ **2026-08-10 更晚,它被排到了第一位**(§0.1):它动的是界面,而截图 / 官网 /
     case-study 全在下游 —— **它是整条链的源头,先做它后面才不用返工两次。**
     ➕ ✅ **2026-08-10 深夜:代码全清**(四件 + 撤掉一期那三条预览),落地记录
     `DESIGN_FIRST_DAY_VALUE.md` §6。**schema 没动、Rust 没动、vitest 324 → 332。**
     ⏳ **还没提交、还没装机。** ⚠️ **三处和稿子不一样的地方在该稿 §6.2**,别当疏忽改回去。
8. **Follow up(联网跟进)** —— 全稿 `DESIGN_FOLLOW_UP.md`。四期:
   ~~M1 引擎泛化~~ ✅ / ~~M2 brief + 手动跟进 + 进待审面(schema v11)~~ ✅ /
   ~~M3 没新东西就静默~~ ✅(08-07)/ **M4 定时(仍然只在 M3 被证明有用之后)**
   ⚠️ **硬约束**:`DESIGN_CONTEXT_HYGIENE` §9.5 —— **M4 上线前应当先有一个「往外拿」的动作**。
   定时 = 自动往项目里灌块而另一头没有出口,按真库数字**每周一次跟进约 14 周撞满预算**,
   而那时去重帮不上任何忙
   ⚠️ **2026-08-11:跟进现在是右侧栏唯一的动作了**(§5-C),它的分量比以前重
   ➕ **2026-08-09:brief 现在能被 MCP 读到、也能被它提议改** ——
   M4 真做的时候,「谁能改 brief」这条线已经画好了,别推翻它
   ➕ **2026-08-09 晚**:`list_threads` 现在报 `following_up` 了,
   `applyBriefSuggestion` 也开始动 `updated_at` —— M4 做定时调度时这两个都是现成的输入
   ➕ **2026-08-10**:`recheck_after` 是**第二个天然的定时触发源**,而且它比 brief 精确
   (是块自己说的日子)。M4 想找「什么时候该去看一眼」,先看这一列
9. ~~**引擎档位问题**~~ —— ⚠️ **2026-08-11 重新打开,而且更糟**:
   第三个档(Gemini CLI)虽然落地了,但**它的免费入口 06-18 已被 Google 关掉**,
   只剩 API key(~20 次/天)。全文 `DESIGN_AI_ENGINE` §7.9。
   **「稳定免费又能跑全部四个动作」的档仍然不存在,而且缺口变大了。**
10. ⚠️ **工作台** —— 全稿 `DESIGN_WORKBENCH.md`。
    一~六期 + §7 notes 当标题(W7)全部 ✅。
    ✅ **§11.1(周回顾归位)+ §11.3(压缩撤、去重换本地角标)2026-08-11 落地。**
    ✅ **§11.4(C / D / E)2026-08-12 落地。**
    ✅ **§10.1(块正文 MD 渲染)+ §12(排版按标准 MD 返工)2026-08-12 落地。**
    ✅ **§13(标题字号退回 + 折叠改成一屏)2026-08-13 落地。**
    ✅ **§10.2 三个键全删完了**,动作条 10 → 7 个键。
    **这一摊已经返工过五轮 —— 下一轮反馈大概率还在这里。**
11. ⚠️ **上下文卫生** —— 全稿 `DESIGN_CONTEXT_HYGIENE.md`。
    五件里 **1/2/3/4 已落地**(08-07 深夜,见 §8),**拍板甲 + 乙已落地**(08-08,§9.3.1),
    **两处反转已落地**(08-09,§1.1-ter)。
    第 5 件(AI 一句话标签)**按稿子自己的判断口径先不做**,缺口记在 §8.6。
    ⚠️ §2 那一节的调研**会过期**(2026-08 查的),下次动这摊之前重查。
    ➕ **§9 是实测账,动这摊之前先读它**
    ➕ **§9.5.1 是 2026-08-09 的落地记录(整场对话分流走选项 A)**
    ➕ **2026-08-09 晚**:§9.3 那条「被更正的旧块还在以有效结论的样子渲染」的毒,
    `add_block` 这一侧的解药已经给了(`ref_kind`)—— 但**还没在真客户端上验过**
    ➕ **2026-08-10**:§9.6 那条「两把刀没人提醒你去用」现在有了第一个反例 ——
    `recheck_after` 是**带自己提醒机制的第一列**(见 §4-1 那条新铁律)
12. ➕ **三份计划稿(2026-08-08 Ocean 提)**
    - ~~**`DESIGN_PROJECT_FILES.md`**~~ —— ✅ **一期 + 二期 2026-08-12(v15)、
      三期 2026-08-09(v18)全部落地**,记录在该稿 §7 / §8
    - ~~**`DESIGN_WORKBENCH.md` §10.1 / §12 / §13**~~ —— ✅ 全部落地
    - **`DESIGN_CASE_STUDY.md`** —— 给研究生申请用的公开 case-study。
      ✅ 第一期(台账)、第三期(八栏正文)已落地。
      **剩四、五、六期**,卡在「代码全做完之后」这个时机。
      ➕ **台账新增:§3.9/§3.10/§3.11(08-11)、§3.12(08-12)、§3.13/§3.14(08-13)、
      ➕ §3.15(08-09:为一扇已经开着的门做锁)、§3.16(08-09:三个工具上线即隐身)、
      ➕ §3.17(08-09:同一条规矩破第二次之后被钉成断言)、
      ➕ §3.18(08-10:没人读的列 + 那条看不见这个 bug 的测试)、
      ➕ **§3.19(08-10:路由那一修,在当初失败的那个客户端上量出来了 —— 12→0 / 0→1)**、
      ➕ **§3.20(08-10:一次静默指错程序的验证 —— 按 pid 过滤被无声忽略)**、
      ➕ **§3.21 / §3.22 / §3.23 / §3.24(08-10)**、
      ➕ ⭐ **§3.25(08-10 深夜,A5 带出来的:同一条规矩写两遍 —— 一次性查询被常显组件继承,
      以及 TS 谓词翻成 SQL 之后被三值逻辑静默吃掉最常见的那种行)**
      ➕ ✅ **台账 §5 的工具面计数已经改对了(2026-08-09,Ocean 当窗点头破例)**:
      10 读 / 4 写 → **12 读 / 6 写,共 18**,旧数字作为趋势线留了一行。
      ⚠️ **F 没有加工具,那个数字仍然是 18,不用动。**
      ⚠️ **仍然是破例不是废规矩** —— 下次数字变了还是加新行
13. ~~➕ **「把整场对话分流进项目」** —— 决定 4,走选项 A~~ ——
    ✅ **已落地(2026-08-09)**,记录在 `DESIGN_CONTEXT_HYGIENE` §9.5.1。
    ⚠️ **那条预算警告仍然成立,别丢**:原文块的 `source_text` 如果照搬整场对话就是**文档级**的,
    三到四次分流撞满一个项目的预算。落地的做法是**只把用户的提问序列存成原文块**,
    并且**不设长度上限**、改成把字数报给模型和用户

---

## 5. 还没还的旧账

1. ~~写之前先给用户看一眼~~ —— ✅ 分流(待审面)+ 运行卡片,两半都做完了
2. ~~AI 到底往我库里写了什么~~ —— ✅ **08-07 搬进右侧栏**(R2)
3. ~~**块正文里的截止日期没人管**~~ —— ✅ **2026-08-13 落地**(schema v17)。
   ⚠️ 形状是「不弹窗,项目顶上挂一条」,时刻表 `NOTICE_STAGES = [60, 30, 7]`,
   ✕ 只压当前这一档
   ⚠️ **同形的第二例仍然没人管**(`DESIGN_CONTEXT_HYGIENE` §9.6):v13 给了作废/更正
   这两把刀,**但没有任何东西会提醒用户去用**。
   ➕ **2026-08-09:AI 这一侧的入口已经补齐了**(`ref_kind` 现在 `add_block` 也能带),
   **但「提醒用户去用」这一半仍然没人做**
   ➕ ✅ **2026-08-10 晚,「提醒用户去用」这一半在 `corrects` 上补齐了**:被更正的旧块
   **在界面上**终于挂出了一行(`CorrectedByLine.tsx`),v21 还能把被更正的那句话画出来。
   ⚠️ **但只补了「更正」这一把刀。「作废」(`stale_at`)那一把仍然没有任何东西提醒用户去用。**
   ➕ ⚠️ **2026-08-10 新增一条,和上面那条是同形的**:`recheck_after` 过期了,
   **pack 里会标、`get_project_overview` 会数,但 Spool 界面上一个字都不显示**。
   ⏸ 这是 F 自己**明确推迟**的(稿子 §4.6 兑现口 3):项目顶上那条日期提醒(v17,
   `NOTICE_STAGES`)是**另一套机制**(它从正文里认日期),两套合并要单独想,别顺手做
4. ~~**重复块:用户想清但清不动**~~ —— ✅ **2026-08-11 换了个方向解决**:
   去重按钮撤了,改成**项目管理每行一个免费的本地角标**「⚠️ N 块重复」。
   ⚠️ **只认逐字节相同**,不做模糊相似度
5. ~~**摘要没有写作时间**~~ —— ✅ **2026-08-13 落地**(schema v16)。记进库,UI 不显示
6. **一件事被拆成两个项目** —— ✅ **Ocean:「不管,这种情况几乎没有」。** 留档,因为它仍然是
   「用对话标题自动建项目」被否的理由

### 5-B ~~写入开关默认打开~~ —— ✅ 2026-08-13 落地

⚠️⚠️ **那条附带的判断已经被证伪了,别再传下去**:原话是「Ocean 日常用法结构上永远落在
`add_block` 那侧,分流基本不会自发发生」。**08-09 晚上他亲口说了一句标准分流句**
(「flux 相关的存 flux,其他存原来的升学规划」)。**触发场景是真的,是模型没接住** ——
那一场 `propose_blocks` **调用 0 次**、`add_block` **12 次**(真库取证)。
➕ **2026-08-09 已修**:`OPENERS` 里现在有两行专门钓这种句式,并且明说了
**不许用两次 `add_block` 代替**。
⚠️ **但这只是提示词。真跑之前不算数**(§6.2-ter)—— §7 第 3 句就是验它的。

### 5-C ⚠️ 引擎位现在只剩两个动作(2026-08-11 Ocean 拍板)

| 动作 | 在 Spool 里 | 在别的 AI 客户端里(MCP) |
|---|---|---|
| 压缩 distill | ❌ **撤了** | ✅ 还在 |
| 去重 thread_health | ❌ **撤了**(换成免费本地角标) | ✅ 还在 |
| **周回顾 weekly_review** | ✅ **自己的一屏** | ✅ |
| **跟进 follow_up** | ✅ 右侧栏唯一的动作 | —(不是 MCP prompt) |

⚠️⚠️ **MCP 的 `distill` / `thread_health` 工具和提示词一个字没动。**
它们服务的是聊天客户端 —— **那边真的有人在**。
坏掉的从来不是这两个动作,是**在没人的情况下跑它们**。

⚠️ 连带删掉的:`useAutoMaintain` 的自动压缩分支、`SETTLE_MS`/`COOLDOWN_MS`、
`threadsDueForMaintenance`、`lastSuccessfulRunAt`、每个项目的「自动维护这个项目」开关。
总开关搬到周回顾那一屏,改名**「每周自动回顾一次」**。
⚠️ **`threads.auto_maintain` 这一列留着但没人读了** —— 删列要走 schema 迁移,不值。

### 5.1 ⭐ 截图与演示(Ocean 已批时机:app 代码全部做完之后 —— **这个闸门 08-10 打开了**)

- **截图全套重建**:现在官网/README 用的是旧图,**块流(W7)、右侧栏、项目管理、
  左侧边栏、新的周回顾一屏** 都换了样子。
  要求见 memory `next-stage-goals-website-portfolio`(**多场景铁律**)
- **演示视频**:录完才动 Hero 那一屏
- 顺序是 Ocean 定的:**代码 → 截图 + 视频一起 → 官网那两屏**
- ⚠️ **`RELEASE.md` §3 的验收清单里那一条现在是不合格的**,已标注
- ➕ **2026-08-13 变了三处**:标题字号、长块基本不折了、项目顶上可能挂着日期提醒条、
  块流右上角多一个 ✎
- ➕ **2026-08-09 又变了两处**:**右侧栏「项目文件」每个文件多一行 ✓**、
  **待审面多了两种卡片**(拍待审面那一屏时值得让文件申请卡入镜 —— 它是这一版最有故事的一屏)
- ➕ **2026-08-10**:pack 里可能多出一行 `↗ …`,**但那是拷出去的文本,不是界面** ——
  界面上 F 一个像素都没改,截图不受影响
- ➕ ⚠️ **2026-08-10 晚又变了三处(更正那一摊)**:**块头的 `#1` 全部变成了圆圈徽章**
  (每一块都变,每一屏都入镜);更正块底下那一行重排了;**被更正的旧块底下多一行 ⚠️**。
  ⚠️ **圆圈那一处是全 app 的视觉改动,旧截图全部作废** —— 这是拍摄前必须知道的。
  ➕ ⚠️ **圆圈又改过一轮(§1-quater,08-10 21:25 已装机)**:现在是 **13px 的圈 / 9px 的数字**,
  比第一轮小了一圈,数字也正了。**拍之前先确认装的是 `7f85f59` 之后的版本**,
  否则拍出来的是偏上、偏大的旧圈。
- ➕ ⚠️ **官网这一摊 Ocean 08-10 定了新调子**:不是换图,是**整页重新设计功能展示和介绍,
  交互小组件也要重做**。截图要拍成什么样,得等官网设计定了才知道 —— **别先拍**。
- ➕ ⚠️ **2026-08-10 晚,首日价值动了界面,拍之前先读这条**:
  **左边栏顶上多一张「今天读了 N 条」的卡**,⚠️ **它只在当天有捕捉时才出现** ——
  拍左边栏那一屏之前**先当场捕两三条**,否则拍出来的是没有它的旧样子。
  这张卡值得入镜:它是「首日价值」这条唯一看得见的东西
- ➕ ⭐⭐ **2026-08-10 深夜,A5 把上面那一条整个改掉了,以这条为准**:
  那张卡现在是**「我攒了多少」面板 + 一个线轴**,而且**常显**(没捕捉的日子也在)。
  ⚠️ **拍之前先把 A5 装上去**(§0.0 第 2 步)—— 没装的话拍到的是一期那张列三条预览的旧卡。
  ⚠️ **线轴上有多少线是会入镜的**:真库现在 50 块、捕捉数远不到 100,拍出来是**半空的轴**。
  **想拍好看的状态就得先想清楚拍哪一档**(九档,每 12.5 条一格),这一条 Ocean 可能有意见。
  ⚠️ **一期那三条正文预览没了** —— 旧截图里那张卡全部作废

### 5.2 其余待办

| # | 事情 | 状态 |
|---|---|---|
| **M4** | Follow up 定时 | ⚠️ 仍然要等 M3 被证明有用,⚠️ 并且要先有一个「往外拿」的动作(§4-8) |
| ~~**J**~~ | ✅ **工具面数字 14 → 18 全部改对了(2026-08-10,Ocean 明示)** —— **四处**:`README.md` 三处(总数 + 读 10→12 + 写 4→6,并补上漏记的四个工具)、`docs/CASE_STUDY_PAGE.md` 两处(架构图 + 正文「fourteen tools」)、`site/story.html` 一处。⚠️ **官网那处改了源码不等于上线**,spoolapp.org 要单独部署。⚠️ 台账 §5 和 §2 的开头**没动**——那是历史记录,只增不改 |

**要等别的事先完成的**:

| # | 事情 | 卡在哪 |
|---|---|---|
| F | 截图 + 演示脚本整体重建 | Ocean 已批:排在 app 代码全部做完之后 |
| G | Hero 内嵌 15 秒演示视频 | 视频没录之前这一屏保持现状 |
| H | 对外动作 | 每一件都需 Ocean 单独明示 |
| I | 装 Antigravity 实测 | 要 `curl \| bash` 装到他机器上,**需 Ocean 明示** |

---

## 6. 干活须知(踩过的坑)

### 6.1 基线与验证

```
npx tsc --noEmit                                  # 干净
npx vitest run                                    # 332 通过(2026-08-10 深夜,A5 之后)
cargo test --manifest-path src-tauri/Cargo.toml   # 68 通过(2026-08-10 晚)
node scripts/i18n-check.mjs                       # (none missing)
```

⚠️ **vitest 314 → 318 → 320 → 324**:318 那一档是 F(出处三件 + 待审携带出处一条);
320 是首日价值两条(`listCapturesSince` 的取舍一条、日界一条);
**324 是 v21 四条**(pack 带原话一条、原话失配退回一条、取词器 `corrected` 属性两条)。
`captureStore.test.ts` **改了没加** —— 断言从「第一条」改成了「前两条不响、第三条才响」。
cargo **65 → 67 → 68**:日期往返一条、`add_block` 记出处并被读回来一条、
**v21 那条一次跑完写入校验 + pack + `get_blocks` 三处**。
➕ **A5 加了 8 条:线轴算术 6 条(`spoolProgress.test.ts`,九档边界 + 恰好 100 + 删块降数)、
字数口径 2 条(`blocks.test.ts`,其中一条专门放 v14 之前的 NULL 行钉那两个 `COALESCE`)。**
⚠️ **`countCaptures` 换成 `COUNT(*)` 之后没加新测试** —— 原有那两条断言(`toBe(3)` / `toBe(2)`)
本来就在数它,换实现之后它们**恰好变成了「两种写法必须同意」的钉子**,这是想要的。
➕ **圆圈第二轮(§1-quater)一条测试都没加,当时数字是 324** —— 不是偷懒:
它改的是一行 class 里的四个长度值,**能写出来的断言只会是把那四个值抄一遍**(改样式必红,
而红了不说明任何事)。**这一类只能靠量像素,配方在 §6.2-sexies。**

改任何 pack 渲染都要跑满前三条 —— 两侧渲染器有 golden 平价测试盯着。
⚠️ **改官网(`site/*.html`、`scripts/site-zh-*.html` 或中文串)之后要跑
`node scripts/build-site-zh.mjs`**。

⚠️ **`cargo test` 必须带 `--manifest-path` 或先 `cd src-tauri`。**

⚠️ **`engine.rs` 里有三个测试真的会 fork 子进程**,它们**共用一个 `STREAM_TESTS` 互斥锁**。

⚠️ **有一条既有的 `dead_code` 警告**(`ThreadMeta.updated_at` 没人读),**不是这几窗弄出来的**,
按 CLAUDE.md §3 没动它。

### 6.1-bis ⚠️ 漏译检查是仓库里的脚本

`node scripts/i18n-check.mjs`(加 `--dead` 还会列出没人用的字典条目)。
⚠️ 它只看**字面量**。`t(SOME_CONST)` 这种它看不见。
⚠️ **`--dead` 报的绝大多数是既有的** —— 按 CLAUDE.md §3 没删。
⚠️ **判断哪条是自己弄出来的**:`git grep -F "<串>" HEAD -- 'src/**' ':!src/lib/i18n/index.ts'`

### 6.2 实机验 MCP(stdio 喂 JSON-RPC)

完整手法在 memory `isolated-verify-workflow`。要点:

- 二进制在 `src-tauri/target/release/spool`,跑 `spool --mcp`
- ⚠️ **`SPOOL_DATA_DIR` 要指到装着 `spool.db` 和 `settings.json` 的那一层**
- 要先发 `initialize` + `notifications/initialized`,才能 `tools/call`
- **写侧探针请在副本上做**。⚠️ 推荐照抄:
  `sqlite3 <真库> ".backup <副本>"`
- ➕ **更省的做法,推荐(08-09 / 08-10 三窗都用了)**:**当场建一个新库**:
  `sed 's/--.*$//' src/lib/db/schema.sql | sqlite3 lab/spool.db`,再
  `PRAGMA user_version = 20` + 手写几行 seed + 一个
  `{"mcpEnabled":true,"mcpWriteEnabled":true,"language":"zh"}` 的 settings.json。
  **三十秒,而且想造什么状态就造什么状态。**
  ➕ **08-10 顺手证实了一件事**:把那个库的 `user_version` 手动改成 19 再喂新二进制,
  **schema 版本守卫真的会拦下来并说人话** —— 想验「版本不匹配长什么样」,这是最快的路
- ⚠️ **返回文本前面可能有一行「人话头条」**(`human_headline`),
  解析 JSON 之前要先跳到第一个 `{` 或 `[`,别直接 `json.loads`
- ⚠️ **改完 Rust、重新构建之后,已经连上的客户端不会换二进制** —— 必须完全退出重开
- ⚠️ **`SPOOL_DATA_DIR` 对 GUI 无效**,只管 MCP 那一侧

### 6.2-bis ⚠️ 装完新版,一定要**看一眼窗口**

08-05 出过一次:`tsc` 干净、测试全绿、构建签名全过,装上去**主窗白屏**。
**没有任何一条自动化会打开那个窗口。**

⚠️ 通用的一条:**`selectAllThreadsFlat` 只能 imperative 用**,**绝不能当 hook selector**。
组件要这张表就订阅 `threadsByWorkspace` 再 `useMemo` 摊平。
➕ 同一条规矩:右侧栏附件那几个 selector 全部写成
`s.attachmentsByThread[id] ?? EMPTY_ATTACHMENTS`(常量兜底),**绝不在 selector 里现造数组**。

隔离验证配方:

1. `tauri.conf.json` 的 identifier 临时改一个没用过的(用过 `.wb` / `.e3`)
2. `npm run tauri build -- --bundles app`
3. 库和 settings 预置在 `~/Library/Application Support/<新id>/` 的**根上**
4. `open -n <app> --stdout /tmp/x.out --stderr /tmp/x.err` 起来抓日志
5. ⚠️⚠️ **截图这一步 08-10 整个失败了,别再按老办法硬试**:
   `screencapture -x` 抓的是**另一个 Space**;
   **`swift` 走 `CGWindowList` 也不通 —— 这台机器的 Swift SDK 坏了**。
   ✅ **改用三个旁证**:**CPU 稳定 0.0%**、**库确实被打开了**、stderr 无报错。
   ⚠️ **但这只排除白屏,排除不了"长得不对"**

   ➕ **2026-08-10 晚,这一步修正了三处,下一窗直接用**:
   - ⭐ **「库被打开了没有」用 `lsof -p <pid> | grep spool.db`,别用 wal/shm。**
     旧办法有**假阴性**:`sqlite3` 现建的库是 `journal_mode=delete`,app 打开它**不会**
     生成 `-wal`/`-shm`。08-10 就照这个误判过一次「app 没开库」,`lsof` 一看是 4 个 fd。
   - ⚠️ **`System Events` 不是「报 0」,是时通时不通。** 这次拿到了窗口(1360×840),
     但紧接着 `every application process whose name is "spool"` 就 `AppleEvent timed out`。
     **能拿到就当赠品,拿不到别耗**。
   - ⚠️⚠️⚠️ **最凶的一条:`whose unix id is <pid>` 会静默返回错的进程。**
     隔离构建和正式版**进程名都是小写 `spool``**,按 pid 过滤那句**被无声忽略**,
     返回的是**正式版**那个 —— 而它 dump 出来的是真库里的真项目,
     **长得完全像一次通过的验证**。
     ✅ **解药:先让目标自报家门** —— `{name, unix id, file}`,拿 `file` 那个 bundle 路径
     和你启动的那个对一遍。08-10 就是这么抓到的。全文台账 §3.20。
6. ⚠️ 想看某个项目的块流,就把测试库的 `is_capture_target` 改到那个项目上再起
7. ⚠️⚠️ **收尾当场做**:按**全路径**杀进程(**绝不用模糊 `pkill -f spool`,正式版一直在跑**)、
   复位 identifier、删掉测试库

⚠️ **合成鼠标点击驱动不了这个 webview。** 「点开之后长什么样」这一层**永远验不到**。

### 6.2-sexies ⭐⭐ 单块 UI 能自己看了:WKWebView 截图 + 量墨迹(2026-08-10 晚新增)

**上面那一节说的是「整个 app 截不了图」,这条说的是「一小块界面可以」。** 圆圈徽章那两轮
(§1-quater)就是靠它定的,**它把「看着不居中」变成了一个数**。

⚠️⚠️⚠️ **先读这一条,别用 Chrome。** 第一轮我就是用 headless Chrome 量的,报「偏上 0.5px」,
当成修好了 —— **Ocean 一眼就看出来还是偏的**。拿 WKWebView 重量:**偏上 1.5px,差三倍**。
**两个内核把基线放在行盒里的位置本来就不一样**,拿 Chrome 量这个 app,量的是另一个程序。
⚠️ **Swift SDK 在这台机器上是坏的(§6.2-bis),但 ObjC + clang 是通的** —— 工具已经进仓库:

```
cc -fobjc-arc -framework Cocoa -framework WebKit -o /tmp/wk-snapshot scripts/wk-snapshot.m
/tmp/wk-snapshot page.html out.png 120 200      # 后两个是 CSS 宽高
```

**出来的 png 就是物理像素**(这台机器 2 倍屏,120×200 会写出 240×400)—— 半像素问题只活在
这个单位里。

配方(四步,别偷懒):

1. **写一个小 html,把要看的那几行 markup 抄进去** —— ⚠️ **字体和色板必须是真的**:
   `@font-face` 直接指 `src/assets/fonts/Geist[wght].ttf` / `GeistMono[wght].ttf`,
   颜色抄 `src/styles/tokens.css`。**字体不对,量出来的居中就是假的**(centering 是字体
   的 ascent/cap/descent 决定的)。
2. **把父容器也照抄**(块头是 `flex items-center text-[10px]`,更正那一行是
   `flex items-baseline text-[11px]`)—— 只测一行会漏。
3. ⭐ **要量就上色**:把边框画 `red`、文字画 `blue`,截图后用 numpy 按通道分开,
   各求墨迹包围盒,**上边距减下边距**就是偏移量(单位是物理像素,÷2 才是 CSS 像素)。
   ⚠️ **多试几个数字**:`1`/`7` 是平头平脚的,`2`/`8`/`0` 圆头会**冒过 cap 线**(overshoot),
   量出来天生差 1 个物理像素 —— **那是字体设计,不是排版错**,别去修它。
4. **要看就放大**:同一份 html 里再放一份 `zoom:6` 的,自己 Read 那张 png 用眼睛看一遍。
   **量出来的数和眼睛看到的要对上**,对不上先信眼睛,是量法错了。

⭐ **一条量出来的规律,下次直接用**:**偏差有个下限,是奇偶数决定的**。9px 字的墨迹高
**13 个物理像素**(奇数),圆圈里面是偶数格,**13 除不开 2** —— 所以最好也只能差 1 个物理像素
(0.5 CSS px)。**要么接受它,要么换一个让墨迹变成偶数的字号**(10px 字 → 14 个物理像素,
能整除)。⚠️ **换字号就要把补偿值重新量一遍**,两者是绑在一起的。

⚠️ ⚠️ **两条边界,别拿它当验收**:
- **它只渲这一小块。** 组件装到真界面上会不会挤、会不会和别的东西打架,它一个字都答不了 ——
  那仍然只有 Ocean 能看(§6.2-bis 最后一句)。
- **它也不保证下一版 WebKit 还这样。** 系统升级可能把基线又挪一点,
  **发现不对就重量,别照抄这里的 1.5px。**

### 6.2-quinquies ⚠️ 给 Ocean 重装正式版(照这个来)

**和 §6.2-bis 的隔离验证是两回事** —— 这是**装到他真的在用的那一份上**,读的是**真库**。

1. **先备份真库**:`sqlite3 <真库> ".backup ~/Desktop/spool-snapshot-<日期>-pre-reinstall.db"`。
   顺手核一眼 `PRAGMA user_version` 和代码里的 `SCHEMA_VERSION`(memory `spool-db-wipe-incident`)。
2. 四条基线全绿(§6.1)。
3. `npm run tauri build -- --bundles app` —— 签名走 `Spool Dev`,**没有公证**。
4. ⚠️ **退出正在跑的那一个**:`osascript -e 'quit app "Spool"'` **对它无效**(08-12 实测)。
   **按 pid 发 `kill -TERM`**,主进程和 `--overlay` 一起。
   ⚠️⚠️ **`--mcp` 那一堆子进程绝对不能杀** —— 那是别的 AI 客户端连着的服务端。
5. `mv /Applications/Spool.app /Applications/Spool.app.previous-<时间戳>`,再 `cp -R` 新的过去。
6. `open /Applications/Spool.app`,然后看三个旁证:CPU 稳定、`spool.db-shm` 当场重建、
   库里块数没变。**「长得对不对」还是只有 Ocean 能看。**
7. ⚠️⚠️ **会迁移的那种装机(F 的 v19→v20、08-10 晚的 v20→v21)另外看 §0.3。**
   判据很简单:**`client.ts` 的 `SCHEMA_VERSION` 变了没有。** 变了就会迁移 ——
   `user_version` 装完要跟着变、块数不变、app 自己先写一份 `spool.pre-migration-v*.db`,
   **而且不重开 AI 客户端会直接报错**,不是少个工具。
   没变(比如首日价值那次)就是纯前端,什么都不用做。

### 6.2-ter ⚠️ 子进程 / 外部客户端的活,必须真跑一次

**已被七次独立事件证实**(全部收进 `CASE_STUDY_LEDGER.md` §3.4/§3.5/§3.6/§3.8/§3.9/§3.10/§3.16):
`CLAUDE_CODE_EFFORT_LEVEL`、跟进的 URL 规则、Codex 免费档、
Gemini 免费额度(稿子 1500/天,实测 20/天,差 75 倍)、
**少一个 `annotations` 导致两个工具在 codex 上调不动**、
**gemini 把错误信封打在 stderr 上**、
➕ **三个工具描述写得很好、annotations 齐、stdio 跑通,但两段路由文本没改,
真跑起来三件里两件模型根本没走到**。

**⚠️ 提示词里写了规则 ≠ 规则生效。稿子里写了数字 ≠ 数字是真的。
工具描述写得再对 ≠ 第三方客户端调得动。➕ 工具存在 ≠ 模型找得到它。**

**怎么真跑(固定下来):**

1. `scripts/seed-workbench-lab.sh` 建隔离库,或 §6.2 那个「三十秒新建一个库」的做法;
2. **argv 从 Rust 里打印出来,别手抄** —— 临时加一个 `#[test]` 打印,拿完**立刻删掉**;
3. 提示词从 **MCP 的 `prompts/get`** 拿。⚠️ 例外:`follow_up` **不是 MCP prompt**;
4. `env -i PATH=… HOME=… USER=…` 起,`< /dev/null`;
5. ⚠️ **一次跟进要跑五到十分钟,前台会被 10 分钟超时打断 —— 放后台跑。**
6. **验 MCP 工具能不能被真的调用**,直接喂一句「call tool X, then tool Y, report each
   verbatim」,然后从 `--json` 事件流里筛 `item.completed` + `mcp_tool_call`,
   看 `status` / `error`。**比读模型的总结可靠得多。**

⚠️ **仍然没真跑过的**:**Claude Desktop 的写入**、**Antigravity 的一切**、
➕ **08-09 那一窗的三个新工具在 ChatGPT 里跑过了(结论就是上一窗修的东西)**,
➕ ✅ **A–E 里的四件 2026-08-10 在 ChatGPT(Codex)上真跑过了,四句全过**(§7.2)。
   ⭐ **这是这条规矩第一次给出"修好了"的正面读数,不只是"坏了"** ——
   而且它量到了一个数(12 次 `add_block` → 0 次;0 次 `propose_blocks` → 1 次)。
➕ ⏳ **E 那一件里的 `corrects`(§7 第 5 句)仍然零次**,
➕ ⏳ **F 也一样:隔离库上 stdio 全通,但真客户端零次**(§7 第 6 句)。
➕ ⚠️ **08-10 新增的一条,和这一节同源**:验证工具本身也会骗人 ——
   `System Events` 的 `whose unix id is <pid>` **静默返回了另一个进程**,
   而它 dump 出来的东西**长得完全像一次通过的验证**(§6.2-bis 第 5 条 / 台账 §3.20)。
   **判据本身要先被验一遍。**

### 6.2-quater ⚠️ 探子进程可以不花模型额度(但有边界)

- **`--strict-config`(codex)**:把「要试的键」和「一个肯定不存在的键」一起传。
  ⚠️ 它验的是**键**,**不验值**。
- **拿包装脚本当探针**;**翻二进制里的字符串**能证明「这个词在里面」,**不能证明「它现在还有效」**。
- **直接打 REST API 是最省的探针**,⚠️ **但它会真的消耗额度**。
- **`ListModels` 返回的名字不等于能用的名字**。
- **翻 CLI 自己的 bundle 找配置键名,零成本且准**。
- **验第三方 CLI 的认证,用「假 HOME + symlink 凭据」**,别去动用户真的 `~/.gemini`。
- ⚠️ **提示词里写了规则,连「模型会不会照做」都答不了。** 只有真跑能答。

### 6.3 ⚠️ 环境坑

1. **`cargo build --release` 必须 `cd src-tauri`(或带 `--manifest-path`)。**
2. **开测第一件事:`tools/list` 数一下工具个数。** ➕ **2026-08-09 起是 18 个**
   (12 读 + 6 写;写的那 6 个里有两个其实什么都不存 —— 它们只是排队等用户点头)。
   ➕ **F 没有加工具,仍然 18。**
   ➕ **顺便看一眼每个工具都有 `annotations`** —— 有测试钉着了。
   ➕ **prompts 是 5 个**。
3. ✅ **seed 脚本都从 `client.ts` 读 schema 版本**。
4. ⚠️ **`codex exec` 的 stdin 必须给 `/dev/null`**。claude 和 gemini 也一样。
5. ⚠️ **schema 版本有三处要一起动**:`client.ts` 的 `SCHEMA_VERSION`、
   `mcp.rs` 的 `EXPECTED_SCHEMA_VERSION`、`client.test.ts` 里那一堆 `toBe(n)`。
   **现在是 v21**。⚠️ **`client.test.ts` 里那串 `downgradeToVn` 是链式的**,
   每加一版就要在**链条最前面**加一个新的、并让原来的头一个先调它
   (现在的头是 **`downgradeToV20`**,它被 `downgradeToV19` 调)。
   ➕ **还有两处容易忘**:那份测试里有几处拿 `SELECT * FROM threads` / `SELECT * FROM blocks`
   前后对比,新增的列要有一个 strip helper 把它摘掉,否则会红。
   现在是三个:`stripBriefSuggestion`(v19 的三列)/ `stripProvenance`(v20 的三列)/
   **`stripCorrectedQuote`(v21 的一列)**。
   ⚠️ **别往老 helper 里塞新列** —— 名字对不上它摘的东西,下一个人就会漏掉。
6. ⚠️ `mcp.rs` 的 `INSTRUCTION_HEADER` 是 `r##"…"##`,**不是 `r#"…"#`**。
   ➕ ⚠️ **别把它和 `INSTRUCTION_BODY` 搞混**:`INSTRUCTION_HEADER` 是 **pack 的授权表头**;
   `INSTRUCTION_BODY` 是 **`initialize` instructions 的正文**,
   和 `OPENERS` 一起构成**路由文本**。加工具要改的是后两个。
7. ⚠️ **版本号三处要一起动**:`package.json` / `src-tauri/tauri.conf.json` /
   `src-tauri/Cargo.toml`(⚠️ `Cargo.lock` 也会变,一起提交)。
8. **加引擎要动的地方**:`engine.rs` 的 `EngineKind`(枚举 / `ALL` / `as_str` / `parse` /
   `models`)、参数适配、输出解析、`run_env`、`run_action` 的 match、
   `lib.rs` 的 `open_mcp_client_page`、TS 侧 `engineStore.ts` 的 `EngineKind` + `ENGINE_LABEL`、
   `settingsStore.ts` 的 `aiEngine` 联合类型(+ 新模型键要在**四处**登记)、
   `EngineBar.tsx` 的 `ENGINE_MODELS`、`EngineConfig.tsx` 的安装入口、i18n。
   ⚠️ **`EngineKind::parse` 有一条测试断言「某个不认识的名字返回 None」**。
9. **写会毁东西的迁移**:顺序必须是「**先补齐新列 → 再删行 → 最后删列**」,
   结尾放一条断言,不满足就**抛错、不盖版本号**。
   ⚠️ **SQLite 不能给已有列加 NOT NULL 或外键**。
   ➕ **v18/v19/v20 都不属于这一类**:一张新表 / 三个可空列 / 六个可空列,跑一半和跑完没区别。
10. **默认值在 TS 和 Rust 各有一份的,翻一处等于没翻**(`mcpWriteEnabled`)。
11. ➕ **加新工具时,`every_tool_declares_its_read_write_annotation`
    那条测试里写死了「哪几个是写工具」的名单和顺序** —— 加**写**工具就要改那一行。
    ⚠️ 加**读**工具不用改(08-09 加 `get_project_overview` 时它一声没吭)。
12. ➕ **加工具还必须给 `OPENERS` 加一行**,
    否则 `every_tool_is_reachable_from_the_routing_text` 会红。**这条红是对的,别绕过它** ——
    绕过去的代价见台账 §3.16。
    ⚠️ **只加参数不加工具的话它不会红**,但该说的话还是要说(F 就是这么办的:
    `INSTRUCTION_BODY` 加了一段,`OPENERS` 加了一行「这些还准吗」)。
13. ➕ **2026-08-10 新增:块的两个日期列不走本地时区。**
    `retrieved_at` / `recheck_after` 是**日子**,存 UTC 零点,
    渲染必须走 `format_utc_date`(Rust)/ `formatUtcDate`(TS)。
    其余所有时间戳(`created_at`、`stale_at` …)仍然走本地时区那条路,**别混**。

### 6.4 语言双侧(硬规则 12)与它的例外

用户能读到的文案走 `t!`/`ts!`,中文那一半在前。⚠️ **例外**:工具名、工具描述、
`initialize` instructions、pack 的权威表头 **和 `## Notation` 那一节** ——
这些是**给模型读的契约,任何 locale 下都保持英文**。`ai note:` 也属于这一侧,**不翻**。
➕ **pack 里的块级标记全都属于这一侧**:`note:` / `↩ cites:` / `⚠️ one point…`,
➕ **以及 08-10 新增的 `↗ … · retrieved … · recheck after …`**。
⚠️ **引擎名(Claude Code / Codex / Gemini CLI)是产品名,也不翻。**
➕ **提示词正文(`prompts/get` 那几份)是双语的**,和工具描述不一样。
➕ ⚠️ **`OPENERS` 里的触发词是中英并列的**,散文部分仍然全英文。
理由:那些不是界面文案,是**用户可能说出口的原话样例**,而用户说中文。**箭头右边永远不翻。**

⚠️ **第三个受众**:**case-study 是对外材料,整份英文**。

⚠️ **一条通用判据(Ocean N6 给的)**:回答「只有做这个东西的人才会问的问题」
= **开发者提示,不该出现在界面上**。

### 6.5 golden fixture 重生(硬规则 5)

⚠️ **重生前必须 `TZ=Europe/London`。**

```
TZ=Europe/London GOLDEN_WRITE=1 npx vitest run src/lib/pack/assemble.test.ts
```

⚠️ **能合并成一次重生就合并。**
⚠️ **反过来的一条**:**动 pack 之前先想一遍"输出真的变了吗"**,别条件反射重生。
✅ **08-13、08-09 三窗都一次没重生**;**08-10(F)重生了一次,那是设计里写好的**。
⚠️ **fixture 现在覆盖 v13/v14/v15/v20/v21**:一个被作废的**置顶**块、一条 `supersedes`、
一条 `corrects`,标签阶梯的两处,**三个项目级文件**,
➕ **出处三块(b10 全三项且已过期 / b11 只有查证日期 / b12 全三项且还没到期)**,
➕ **b11 带 `correctedQuote`(引的是 b2 正文里的「占总分 40%」)**。别删掉这几块。
⚠️ **b11 那个 quote 必须在 b2 正文里逐字存在** —— 渲染器找不到就不画,fixture 会静静地
少一段而不是报错。
⚠️ **fixture 里的 attachment 带 `threadId`,不带 `blockId`** —— Rust 那侧
`fixture_rows()` 按同一个键读,两边一起改。
⚠️⚠️ **golden 比对前会把两边的 `YYYY-MM-DD` 全换成 `<DATE>`** ——
所以它**看不见日期本身对不对**(「一侧 UTC、一侧本地」它照样全绿)。
钉日期的断言在 `assemble.test.ts` 的 `provenance (§4.6)` 和 mcp.rs 的
`a_retrieval_date_survives_the_round_trip_as_the_same_day`。台账 §3.18。

### 6.6 提交与推送

⚠️⚠️ **现在有 1 个未推提交:`87b6ab7`(A5)。** `origin/main` 仍是 `eda795d` ——
**08-10 22:45 Ocean 只批了「提交 + 装机」,没批推送**,别自作主张推。
⚠️ **算未推之前照样先按下面那条 HTTPS 的 `git fetch` 刷 ref**(本地 ref 骗过一次)。

**08-10 这一天的提交(时间顺序,七个,全部已推)**:

| 提交 | 内容 |
|---|---|
| `644fba3` | F(出处三字段,schema v20) |
| `63f2824` | 交接同步 |
| `e5163b8` | 首日价值一期三小项 + README 数字订正 |
| `33d9802` | 台账两条 + 对外材料数字订正 + 交接重排 |
| `7146914` | 更正那一摊的界面返工 + `corrected_quote`(schema v21) |
| `7f85f59` | 圆圈第二轮(改成整像素)+ 台账 §3.22 |
| `b7aea58` | 第二轮装机留档 |
| `cf6395a` | ⭐ 圆圈第三轮(WebKit 量出来的补偿)+ `scripts/wk-snapshot.m` + 台账 §3.23 |
| `eda795d` | 第三轮装机留档 ← **`origin/main` 到此** |
| ⭐ `87b6ab7` | **A5 首日价值二期(线轴 + 三个计数)+ 台账 §3.25 + 交接** ⚠️ **未推送** |

⚠️ **推送要单独问 Ocean。** 而且 ⚠️⚠️ **推到 GitHub ≠ 官网上线** —— `site/story.html` 早就改了,
spoolapp.org 是单独部署的。**08-10 Ocean 明确说了「先不上线」**,而且他说官网内容已经
落后软件很多、要整页重做(§0.1 的 A0)。

⚠️⚠️ **08-10 撞到一次:`docs/HANDOFF.md` 在工作区被改回了「F 还没提交」** ——
看着像 IDE 里的一次撤销,把 `63f2824` 对这个文件的改动整段退掉了,而那时 `644fba3`
早就提交、也推送了。**已按 HEAD 复原。** 提交前扫 `git status --short` 这条规矩就是干这个的,
⚠️ **不认识的改动先 `git diff` 看内容,别看文件名就放过。**

- ⚠️ **提交、推送、装机三件都要单独问 Ocean,每次都要。** 他明示的是**那一次**的授权。
- ⚠️ **SSH 推不了(08-07 实测)**:`~/.ssh/id_ed25519` 带口令、agent 空、沙盒弹不出 askpass。
  **走 HTTPS**:`git push https://github.com/KIM-ocean-HZ/spool.git main`
  ⚠️ `git fetch origin` 同理会失败,刷新本地 ref 要
  `git fetch https://github.com/KIM-ocean-HZ/spool.git main:refs/remotes/origin/main`。
  **别去改 remote。**
- ⚠️ **绝不写自己的署名进 git 历史** —— 硬规则见 CLAUDE.md §5。每次提交后自检:
  `git log -1 --pretty=full | grep -iE 'claude|anthropic|co-authored|🤖|generated with'`
  ⚠️ **这个自检会误报**:「Claude Code 引擎位」是功能名、「claude 2.0.50」是 CLI 名、
  「Claude · MCP」是产品自己写的来源标签 —— 这三类是**产品内容**,CLAUDE.md §5 明确允许。
  **判断标准看 author/committer 和 trailer**,不是看正文有没有这个词。
- `docs/ID.txt` 是凭据文件,`.gitignore` 挡着,**别提交**
- ⚠️ **`git add -A` 会把 Ocean 在 IDE 里的顺手改动一起带走。** 提交前扫一眼
  `git status --short`,不认识的改动先看 diff。**逐个文件 `git add`。**

### 6.7 给 Ocean 写东西

大白话、一步一个动作,别堆术语(memory `write-plainly-for-ocean`)。
他说过「你写的我没看懂」。凡是"等 Ocean 明示"的,**问的时候要把取舍讲清楚,
不要只报选项名**。

⚠️ **验证有效(八次)**:08-07 晚四个选择题、每个选项写清「好处 / 代价」+ ASCII 草图 → 四题秒选。
08-07 深夜「会不会过载」先给结论、再给真实数字表 → 当场拍两块板。
08-10 把「我需要你给的东西」列成 4 条 → 一次全答完。
08-11 三个选择题全答,**都不是默认答案**。08-12 两题都答。
➕ **08-13 最值钱**:日期提醒三个选择题全答,**第一题选的不是推荐项** ——
他在读到代价(「你不进那个项目就永远看不见」)之后仍然这么选。
**把代价写清楚,他会做出比你推荐的更精确的决定。**
➕ **08-09**:「先提交还是先开工」三个选项各写清好处/代价 → 秒选,而且选的是
**比推荐项更彻底的那个**(连推送一起)。同一条规律第七次成立。
➕ ⭐ **08-10 第八次,而且这次是三题里两题都不选推荐项**:首日价值那三个岔路口
(日卡形态 / 提示放哪 / 卡放哪),每个选项都写了好处 + 代价 + ASCII 草图 → 三题一次答完。
**他挑的那两个都是「代价更大但更符合他要的效果」的那一个** ——
日卡放左边栏(接受「在项目列表里开个新口子」)、提示挪到第三条(接受「第一条没有收尾」)。
**再次证明:代价写清楚,他就会替你做那个你不敢替他做的取舍。**

### 6.8 测试库怎么用

```
scripts/seed-workbench-lab.sh                     重建(会先清空)
scripts/seed-workbench-lab.sh --argv distill 选哪个向量库   打印怎么手动跑
```

库在 `~/Library/Application Support/com.oceanjin.spool.wb/`。
⚠️ 它和 `seed-mcp-lab.sh` 是**两个库**,互不干扰。
⚠️ **它们的 seed SQL 里如果写了 `attachments`,v15 之后要改成 `thread_id`。**

### 6.9 ⚠️ 真库现在长什么样

**08-10 17:57 装完 v21 现场量的**:`PRAGMA user_version = 21`,**50 块**,
`integrity_check` ok,`blocks` 和 `proposals` 都有了 `corrected_quote` 列。

⚠️⚠️ **下次要准数一定现场量,别抄任何写下来的数字 —— 08-10 晚就被这个坑了一次**:
17:15 量到 54 块,17:55 再量是 **50**,中间 Ocean 自己删了 4 块(交接 §3 建议他删的
Flux 那两块就在其中)。**真库是活的,他随时在用它。**

**块数的轨迹**:08-09 是 46 → 08-10 验收第 3 句分流批准后 49 → 当天晚些 54 →
Ocean 删了几块之后 **50**。
⭐ ⚠️ **`ref_kind` 不再全是 NULL 了** —— 08-10 晚第 5 句真跑之后,真库里**第一条
`ref_kind='corrects'` 落库了**(§5-3 那条毒的第一个反例)。
⚠️ **`stale_at` 仍然一条都没有** —— 作废那把刀在真库上还是一次都没被用过。
⚠️ **那条更正是 v21 之前写的,所以 `corrected_quote` 是空的** —— 想看见画线得等 B4 真跑一次。

➕ **08-10 晚又多两份**:`~/Desktop/spool-snapshot-20260810-pre-seqbadge-install.db`(21:25)
和 `~/Desktop/spool-snapshot-20260810-pre-seqbadge2-install.db`(21:52),两次都量到 v21 / 50 块 / ok。
➕ **A5 那次又多一份**:`~/Desktop/spool-snapshot-20260810-pre-spoolcard-install.db`(22:45,
v21 / 50 块 / ok);**返工那次再一份**
`~/Desktop/spool-snapshot-20260810-pre-spoolcard2-install.db`(23:21,v21 / **91 块** / ok)。
⚠️⚠️ **50 → 91 是他自己在用**,不是装机弄的 —— **真库是活的,别抄任何写下来的块数。**
⚠️⚠️ **`Spool.app.previous-*` 现在只剩 1 份** —— **08-10 22:45 Ocean 点头清掉了 15 份**,
留下的 `...-20260810-224509` **就是 A5 之前那一版**(要回退就是它)。
⚠️⚠️ **他同一次明确说了「备份留着」** —— 桌面那 16 份 `spool-snapshot-*.db` 和库目录里
14 份 `spool.pre-migration-*.db` **一份都没动,以后也别自己动**。

⚠️ **备份文件 08-10 一天多了三份**(**都是用户数据,别自己删**):
`~/Desktop/spool-snapshot-20260810-pre-v20-install.db`、
`~/Desktop/spool-snapshot-20260810-pre-firstday-install.db`、
库目录里的 `spool.pre-migration-v19-2026-08-10T08-11-22-533Z.db`,
**外加 08-10 晚装 v21 时 app 自己写的那份 `spool.pre-migration-v20-*.db`**。
⚠️ **`/Applications/Spool.app.previous-*` 已经积到 13 份**,最新两份是
`...-20260810-161112`(v19 二进制)和 `...-20260810-171853`(v20 二进制)。
Ocean 确认新版用着没问题之后可以清,**但要问他**。

⚠️ **「7 个项目」这个说法上几窗一直在传,它数的是行数** —— `list_threads` 只报 **2 个**,
因为另外 5 个是软删的。下次跟他说项目数之前先看 `deleted_at IS NULL`。

⚠️ **库目录里有一批备份文件,占空间。** Ocean 确认没问题之后可以清掉老的
—— ⚠️ **但那是用户数据,清之前必须问他,一份都别自己删。**
最近两份:`~/Desktop/spool-snapshot-20260809-234225-pre-routing.db` 和库目录里
app 自己写的 `spool.pre-migration-v17-2026-08-09T07-57-53-212Z.db`。

---

## 7. ⭐ 验收:六句话,每句钉一个工具

**✅ 第 1–4 句 2026-08-10 跑完了,四句全过**(取证见 §7.2)。
**✅ 第 5 句 08-10 晚 Ocean 自己跑完了,过了** —— ⭐ 它真正的产出是他看完界面提的四条(§1-ter)。
⏳ **第 6 句还没跑(功能 16:11 就装好了,只差他发一句话)。**
⏳ ➕ **新增第 5-bis 句:v21 的原话**(下面第 7 条)。

⚠️⚠️ **每次跑之前都要先让 Ocean 完全退出再打开 AI 客户端。**
⚠️⚠️ **跑第 1 句之前,先让他把那个 PDF 的 ✓ 点掉**:真库里唯一那个附件
`ai_access` 现在又是 1 了(08-10 他批了那次申请),不点掉的话模型直接就读到了,验的是假阳性。
⚠️⚠️ **判据是 `mcp_tool_call` 事件流里的工具名,不是模型说得漂不漂亮。**

> 1. 「看看〈申请规划〉里有哪些文件,里面写了什么?」
>    → 必须出现 **`request_file_access`**。**不出现就是没修好。**
> 2. 「跟进一下〈申请规划〉。」
>    → 必须**先**出现 **`get_follow_up_brief`**,并且**先把 brief 念回来**,再问或再查。
> 3. 「把这几条分别存进 Flux 和申请规划。」
>    → 必须出现 **`propose_blocks`**,**不能是两次 `add_block`**。
> 4. 「〈申请规划〉现在什么情况?」
>    → 必须出现 **`get_project_overview`**,**一次**,
>    而不是 `list_threads` + `get_pack` + `get_follow_up_brief` 三连。
> 5. 「库里有一条结论已经不对了,帮我记一下正确的。」
>    → 写回去的那一次必须带 **`ref_kind: "corrects"` + `ref_block_id`**,
>    **不能只是正文开头写「更正」**。
> 6. (**F,装机之后**)「去查一下 CMU 那个项目今年的截止日期,查到了存进〈申请规划〉。」
>    → 那次 `add_block` 必须带 **`source_url` + `retrieved_at`**,
>    ⭐ 而且**最好自己带上 `recheck_after`** —— 截止日期是典型的会过期的事实。
>    **只写正文不带这三个字段,就是提示词没写到位。**
> 7. (**v21,装机之后**)「〈申请规划〉里有条结论不对了,正确的是 XXX,帮我记一下。」
>    → 和第 5 句同一句话,但这次判据更严:那次写入除了 `ref_kind` + `ref_block_id`,
>    **还必须带 `corrected_quote`,而且内容是旧块正文里逐字抄下来的一句**。
>    ⭐ 判据在库里:`SELECT corrected_quote FROM blocks WHERE ref_kind='corrects'`。
>    ⚠️ **抄错会被当场拒绝并报错**,所以「模型收到报错之后会不会自己改对」也是这一句要看的。

⚠️ **两个客户端都要跑**:ChatGPT(Codex)归 Ocean;
**Claude Desktop 的写入侧至今一次没真跑过**(§6.2-ter 末尾)。

### 7.1 给 Ocean 的大白话版(可以直接复制给他)

⭐ **下一窗只要请他跑第 6 句和第 7 句** —— 1–5 句 08-10 都跑完并全过。
下面整份仍然留着:要重跑、或换到 Claude Desktop 上再验一遍时,直接整段复制。
⚠️ **无论跑哪一句,那两件小事都要先做**(重开客户端;跑第 1 句才需要点 PDF 的 ✓)。

> **先做两件小事,不然第 1 句白跑:**
> ① **把 ChatGPT 完全退出再打开一次**(不然它连的还是旧的,新工具不会出现);
> ② 打开 Spool →〈申请规划〉→ 右边「项目文件」→ 把那个 PDF 下面的 **✓ 点掉**
> (你 08-09 已经批过它了,不点掉的话 AI 直接就能读,第 1 句就试不出东西来)。
>
> 然后一句一句发过去,每句发完看它**做了什么**,不是看它说了什么:
>
> 1. 「看看我 Spool 里〈申请规划〉有哪些文件,里面写了什么?」
>    —— 它应该**主动向你申请**读那个文件。回 Spool 待审面能看到那张卡。
>    ❌ 如果它又叫你「把 PDF 传给我」,就是没修好。
> 2. 「跟进一下〈申请规划〉。」
>    —— 它应该**先把你现在盯的那几条念给你听**,再去查。
>    ❌ 如果它自己编了个目标就开始查,没修好。
> 3. 「把这几条分别存进 Flux 和申请规划。」
>    —— 它应该说「Spool 里有 N 条待你过目」。
>    ❌ 如果它说「已经存好了」,没修好。
> 4. 「〈申请规划〉现在什么情况?」
>    —— 它应该**一次**就说全:摘要、在盯什么、有几个文件、最近几条。
> 5. 「〈申请规划〉里有条结论不对了,正确的是 XXX,帮我记一下。」
>    —— 存完之后回 Spool 看那条**旧块底下有没有挂出一行「有一点被更正了」**。
>    ❌ 只是新块正文开头写着「更正」两个字,不算。
>
> **下面这两句都要先把 ChatGPT 完全退出再打开**(新版装上去了,不重开它连的还是旧的):
>
> 6. 「去查一下 CMU 那个项目今年的截止日期,查到了存进〈申请规划〉。」
>    —— 存完回 Spool 打开〈申请规划〉,**把这个项目打个包拷出来看一眼**:
>    那一块底下应该多一行,写着它是从**哪个网址**查的、**哪天**查的、**什么时候该再查一次**。
>    ❌ 如果只有正文、没有那一行,就是模型没填 —— 那是提示词的问题,不是功能坏了。
>
> 7. 「〈申请规划〉里有条结论不对了,正确的是 XXX,帮我记一下。」(和上次那句一样)
>    —— 这次回 Spool 去看**被更正的那一块**:那句不成立的话应该**被暖色画出来**,
>    底下还挂着一行说是被哪一块更正的。
>    ❌ 如果只有底下那一行、正文里什么都没画,就是模型没照抄原话。
>    ⚠️ **它抄错的话会当场收到报错**,那是对的 —— 看它会不会自己改对再存一次。

### 7.2 ✅ 2026-08-10 的取证(第 1–4 句,四句全过)

**怎么取的**(下次照抄,memory `chatgpt-mcp-forensics`):
`~/.codex/sessions/2026/08/10/*.jsonl` 里筛 `mcp_tool_call_end`,看工具名和 `status`。
**不读模型的总结。**

| 句 | 该出现的 | 实际调用序列 | 结论 |
|---|---|---|---|
| 1 文件 | `request_file_access` | `list_threads` → `get_blocks` → **`request_file_access`** ok | ✅ |
| 2 跟进 | `get_follow_up_brief` **先** | **`get_follow_up_brief`** 是第一个,而且**原样念回了 brief** 才去查 | ✅ |
| 3 分流 | `propose_blocks`,不能两次 `add_block` | `list_threads` → **`propose_blocks`** ×1,`add_block` **0 次** | ✅ |
| 4 现状 | `get_project_overview` 一次 | **`get_project_overview`** ×1,没有三连 | ✅ |

⭐ **第 3 句是这次最值钱的数字**:08-09 晚同一种句式是 **`propose_blocks` 0 次 /
`add_block` 12 次**(§5-B 那条被证伪的判断);修完之后是 **1 次 / 0 次**。
**§5-B 那条「只是提示词,真跑之前不算数」现在算数了。**

⚠️ **第 1 句里模型的第一反应不是 Spool。** 它先 `exec_command` 跑 `rg` 去本机文件系统
找叫「申请规划」的文件,翻完 `ALL_TOOLS` 才找到那三个工具。
**路由文本管的是「开始找之后走哪扇门」,管不了「第一步要不要去找」。**
(codex 是 code mode,工具是 `ALL_TOOLS.filter(...)` 现查的,那张表一次 dump 是 44k tokens。)

⚠️ **PDF 那个 ✓:第 1 句跑的时候是关着的**(工具当场回「一个字都没读到…已排进待审」),
**跑完他在待审面批了,现在 `ai_access` 又是 1**。下次跑第 1 句要重新点掉。
