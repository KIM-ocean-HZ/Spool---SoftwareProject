# MCP 接口评审 — 三轮记录

三轮下来一共报出并修完 **20 条**。这里是原始记录,留着是为了**别把修过的东西再修一遍**,
也为了下次开评审时知道哪些问题「已经问过了」。

| 文件 | 是什么 | 谁测的 |
|---|---|---|
| `ROUND2_RAW.md` | 第二轮的原始回信,三方并行 | ChatGPT 桌面版 / Claude Desktop |
| `ROUND3_CLAUDE_CODE.md` | 第三轮报告(R1–R17 回归 + 新问题) | Claude Code(开发窗自己当客户端跑) |

第一轮的十一条没有单独存档 —— 它的结论已经全部并进第二轮的回归表里了。

## 三轮各自解决了什么

- **第一轮**:十一条,基础可用性(参数、报错、翻页)。
- **第二轮**:三方并行评审。八条当场修完,六条「设计级」的(规矩只写在给 AI 的话里、
  没写进数据结构)留到下一窗修完。
  ⚠️ 三个客户端**同时**跑同一个实验室,互相看得见对方写的块 —— 所以那一轮的**数字要打折**。
- **第三轮**(2026-08-05):R1–R16 全部修对、零回归;R17 那条(材料栅栏)修对了主路,
  但漏了两处 —— `get_digest` 整个工具没有栅栏,`distill` 的 Block IDs 表未净化。
  两处都在同一天修完(提交 `9615d1a`),根因是**净化放错了层**。

## 下次开评审前必看

1. **一个客户端测完,重播一次实验室,再测下一个。** 并行会污染数字(第二轮的教训)。
2. **改完 Rust 要重新构建,而且客户端必须完全退出重开** —— 已经跑着的服务器进程不会
   因为你重新编译就换掉。第三轮开测时就撞到了这个:真实连接跑的是几小时前的旧二进制。
   开测第一件事:调 `list_threads`,看新字段在不在。
3. 实验室重播:`./scripts/seed-mcp-lab.sh`,十秒钟。提示词在 `docs/MCP_LAB_PROMPT.md`。
