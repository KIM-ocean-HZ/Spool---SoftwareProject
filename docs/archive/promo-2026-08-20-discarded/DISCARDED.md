# ⛔ 这一批全部作废（2026-08-20）

**一张都不许发。** 否掉的理由和重做时的约束在 `docs/PROMO-VERDICT-2026-08-20.md`。

留着只为两件事：ffmpeg `zoompan` 那条踩坑记录（见原 `README.md` 末尾），
以及「无头 Chrome 出 2 倍图」这条无依赖流水线。

⚠️ 挪进存档后目录深度变了，`poster.css` 里 `../../site/assets/fonts/` 已经失效（要写成 `../../../`）。
**不用修** —— 重做时是换设计，不是接着改这批。
